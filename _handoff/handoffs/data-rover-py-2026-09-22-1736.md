# Handoff: write plan 6 of sub-project B (the forked store)

## Mission
Write plan 6 of 6 for sub-project B of the client-engine program in /home/mdp/workspace/data-rover-py. Plan 6 is the forked store: staged edits live in the engine replica instead of the legacy store, and the five read surfaces (already served by the replica) show them. Deliverable: `docs/superpowers/plans/<date>-forked-store.md`, written in the same form as plan 5, ready for `/build-plan`. Planning only: no production code.

## Orient First
1. `CLAUDE.md` — especially "Engine package" (`src/working/`, `src/service/`), "Sandbox", "Shell" (sync, surfaces, shadow, the store), "Backend session & the delta protocol", "Check-out/commit + locking".
2. `docs/superpowers/specs/2026-09-19-replica-and-frontend-seam-design.md` — §8 "The forked store" (line 269) is plan 6's scope. §7 (seam), §9 (tests, incl. e2e "a staged edit visible in the tree"), §11 (plans), §12 (done when) and "Known limits" bind it too. The spec is the authority.
3. `docs/superpowers/plans/2026-09-22-transport-swap.md` — plan 5, the template for form: "What planning found" (facts checked by reading or a throwaway probe), Decisions (owner's vs the plan's), Global Constraints, File Structure, Mechanisms, Tasks with the test cases each asserts, Known limits, After this plan. Read its "After this plan" (line ~480) — it is plan 6's inheritance list.
4. `docs/superpowers/plans/2026-09-21-sandbox-and-shell.md` — plan 4; its Decisions and Mechanisms M7–M11 describe the sync that plan 6 builds on.
5. `architecture/README.md`, then `architecture/program.md` (B's row, MR-1..MR-4), `contracts.md` (CT-2, CT-4, CT-5), `constraints.md` (CN-3, CN-5, CN-9, CN-14..CN-17), `decisions.md` (AD-10, AD-16, AD-22, AD-25..AD-28), `system.md`.
6. `frontend/README.md` — "State model" (the legacy staged-edit buffers plan 6 replaces), "Replica (engine shell)", "Auth, projects & routing".
7. `BACKLOG-ENGINE.md` — open items K-29, K-32, K-35, K-36, K-38, K-39, K-40, C-20, C-21; `BACKLOG.md` K-33, K-34.
8. The code plan 6 changes, read before planning: `frontend/src/lib/state/` (checkout, model, stage/ops files, replica.svelte.ts, view.svelte.ts), `frontend/src/lib/engine/{sync,seam,surfaces,shadow}.ts`, `frontend/src/lib/api/engine-route.ts`, `engine/src/working/`, `engine/src/service/service.ts`.

Confirm state: `git status --short`, `git log --oneline -5`, `pixi run dr-test` (and `pixi run frontend-test-e2e` if a baseline is needed; stop any stale preview on :5174 and `pixi run sandbox-build` first).

Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` (plan 5 fast-forwarded in; `feat/transport-swap` deleted). HEAD `8482178`.
- Uncommitted: clean.
- Tests: `dr-test` green as of 2026-09-22 ~17:00 (core 2565, frontend 2775, engine 461, sandbox 14). e2e: 56 passed, 2 failed as of 2026-09-22. The 2 failures are pre-existing: `script-embedding.spec.ts:91` and `snippet-flow.spec.ts:69/70`. Zero `[shadow]` lines.
- No remote configured for `engine-migration`; nothing pushed. `docs/` and `benchmarks/` are git-ignored — the plan file stays uncommitted by design (never `git add -f`).
- `main` is untouched; never touch it.

## Standing Constraints
- **Plan style (owner's):** "direction with specifics" — interfaces, signatures, the test cases and what each asserts, order, and the mechanisms that are easy to get wrong. No full code. A "What planning found" section lists only facts actually checked, by reading or by a throwaway probe in the session scratch dir. Anything not checked goes under Known limits → "Not checked while planning".
- **Owner decides, plan proposes:** split Decisions into "taken with the owner" and "taken by this plan — each reversible at review". Ask the owner the real forks before finalizing (plan 5 took D1–D8 with the owner).
- **One commit per task** (the owner's choice for this program). Commit subjects are one imperative capitalized sentence with no prefix or trailing period, ending with the session's Co-Authored-By line. `architecture/`, `CLAUDE.md`, `frontend/README.md` and `BACKLOG-ENGINE.md` change in the same commit as the code they describe (RC-10).
- **Execution route:** the owner runs plans with `/build-plan <plan path>`, which drives superpowers:subagent-driven-development with implementer/critical-implementer/task-reviewer/branch-reviewer agents. Tag each task `implementer` or `critical-implementer` so the builder routes it.
- **Carried from plan 5, not to relitigate:**
  - Nothing under `engine/src/` changed in plan 5, and reads never mock the engine. Tests use `connectInProcess()` / `fakeProject()` / `syncOver()`, with no fake timers, and dispose every link.
  - The shell never re-serializes model content for the engine (AD-26).
  - Engine results pass the same zod schema as the server's.
  - `lib/api` imports no `lib/engine`/`lib/state`, and `lib/engine` has no runes. `$engine`/`$sandbox` are type-only in production code.
  - No plan, spec or decision ids in code comments.
- **Owner accepted a CN-3 miss:** the browser benchmark bounds the longest slice from outside at 51 ms (27 ms after parse began) against 16 ms. The owner said to accept it and continue without optimizing. Numbers are in `architecture/program.md` B's row. Don't make plan 6 about it.
- **Shadow's "only while nothing is staged" rule is plan 6's** (plan 5 D21): once edits are staged in the engine, shadow must not compare staged reads against the server's committed answers.
- **`staging` joins `surfaces.ts`** and forces the five reads to `engine` (spec §7, plan 5 "After this plan").
- **Retry/overlay copy promises "Your uncommitted edits are kept."** In plan 5 that is true of the legacy store. Plan 6 must keep it true for the engine's staged batches (`run.held`, `adoptStaged`, `retry()` already carry them), and must not lie for a frozen replica with staged edits (AD-27).

## Done
- Plans 1–5 of sub-project B are built and merged into `engine-migration`. Plan 5 (transport swap) delivered:
  - sandbox `frame-ancestors`
  - `engine-bench-browser`
  - the server `reset` feed event (K-37 done)
  - `sync.call` with the read barrier (AD-28)
  - view placements
  - `retry` / `feedReset` / waking from `off` / tail retry
  - the injected seam and per-surface switches, all defaulting to `engine`
  - shadow comparison, dev-only, and on in every e2e spec through `frontend/e2e/fixtures.ts`
  - the wait-for-ready gate with a progress bar
  - the fallback notice and the "Model out of sync" retry overlay
  - `engine-mode.spec.ts`
- The whole-branch review was clean after one fix wave, which included re-reading the structure after an own rebind.

## Known Issues, Not Yet Fixed
- **K-39** (`BACKLOG-ENGINE.md`): a peer's rebind never bumps the structure rev (`onReloadRebind` in `frontend/src/routes/p/[projectId]/+page.svelte`), so the tree stays stale after a peer's rebind plus structural commit. Read from code, not reproduced. It is pre-existing on the server path too.
- **K-40**: a successful `retryReplica()` does not re-read the structure. Read from code.
- `known` (the read barrier's target) has no ceiling. The final review judged it safe, because every source is server-authoritative and a frame from another project is impossible. Optional hardening: clamp to `applyTail`'s rev.
- The e2e fixture asserts `[shadow]` lines right after the test body. A comparison still in its re-test at a test's last reads goes unreported. This is never a false failure; it only weakens the gate.
- Flaky Python test, pre-existing: `tests/model/test_search_index.py::test_string_properties_indexed_non_strings_ignored` (a UUIDv7 id can contain the tested digit substring).
- A ~300 ms main-thread long task appears when snapshot bytes arrive in the real app under `vite dev`. The cause is unknown. It was measured in plan 5 Task 2 and not investigated.

## Deferred — Do Not Do
- Optimizing the replica's slices or cold open. The owner accepted the benchmark as is on 2026-09-22.
- Measuring slices from inside the worker. It was offered to the owner, who declined it for plan 5. It would be an engine/sandbox change.
- Fixing K-39/K-40 inside plan 5. They were deferred to the backlog; plan 6 may absorb them if its store rework touches those paths — the owner's call.

## Plan
1. Read the Orient First list. Start with spec §8 and plan 5's "After this plan".
2. Invoke `superpowers:writing-plans`. If the scope has real forks the spec leaves open, first invoke `superpowers:brainstorming` with the owner. Candidate forks: how the legacy staged buffers retire (all at once or per family: model / artifact / view / metametamodel); whether artifact, view and metamodel staging stay in the legacy store; how commit and preview build their batch from the engine's `staged()`; what `stagedDiff` feeds; how conflicts (parked batches) surface in the UI; `flight.settle`'s `batchIds`; and whether K-39/K-40 fold in.
3. Probe what matters with throwaway scripts in the session scratch dir, and record the results as "What planning found" facts with file:line.
4. Write `docs/superpowers/plans/<YYYY-MM-DD>-forked-store.md` in plan 5's form, with tasks tagged for the builder. Close with Known limits and a "When B is done" note, since plan 6 finishes sub-project B (spec §12).
5. Hand the plan path to the owner, who builds it with `/build-plan`.

## Open Questions
- Which staged families move to the engine in plan 6: only model ops (the engine's `applyBatch` covers the six model ops), or artifact/view/metamodel as well? Ask the owner.
- Should K-39/K-40 be fixed in plan 6? Ask the owner.
