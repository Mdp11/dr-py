# Handoff: Data Rover client-engine program — WRITE plan 3 of sub-project B ("Engine service")

## Mission
Data Rover (`/home/mdp/workspace/data-rover-py`) is moving computation into the browser against a full model replica (TypeScript engine in `engine/`), with the Python core as the oracle. Sub-project B has six plans; plans 1 and 2 are built and merged into `engine-migration`. This session WRITES plan 3, the engine service (spec §3 and §4: CT-4 dispatcher, scheduler, sliced index build and digest check, the reads), as `docs/superpowers/plans/2026-09-19-engine-service.md` (git-ignored), in the form plan 2 has, then self-reviews it and gets the owner's approval. "Done" = the plan is approved. Building it is a later session.

## Orient First
1. `docs/superpowers/specs/2026-09-19-replica-and-frontend-seam-design.md`: §3 (line ~130, `engine/src/service/`) and §4 (line ~176, `engine/src/read/`) are plan 3's scope. Also read §9 (tests, ~296), §10 (`architecture/` + backlog edits, ~317), §11 (plan list, ~333) and Known limits (~355).
2. `docs/superpowers/plans/2026-09-19-snapshots-and-replica-routes.md` is the model to copy: the "What planning found", Decisions, Global Constraints, Mechanisms (M1–M5) and per-task Interfaces/Steps structure. Its "After this plan" section (end of file) lists what plan 3 inherits.
3. `architecture/README.md`, then `architecture/contracts.md` (CT-1 to CT-4; CT-4 is the dispatcher), `architecture/constraints.md` (CN-3 budgets, CN-11, CN-12), `architecture/system.md` rule 4 (16 ms slices), `architecture/program.md` (B's row and section), `architecture/conventions.md` (RC-n).
4. `BACKLOG-ENGINE.md`: `K-32` belongs to plan 3 (slice `rebuildIndexes()` and `verifyDigest()`; the `ord` re-sort is watched). Also `K-29`, `K-35`, `K-36`, `C-20`, `C-21`.
5. `CLAUDE.md`, "Engine package" section: the engine's current modules (`src/value`, `metamodel`, `model`, `ops`, `working`, `snapshot`, `debug`) and the golden-fixture pipeline. The "Replica routes" group describes what plan 2 built.
6. Code plan 3 builds on: `engine/src/working/` (`WorkingCopy`), `engine/src/snapshot/` (`openSnapshot`, `verifyDigest` via `WorkingCopy`), `engine/src/model/` (`IndexSet`, `rebuildIndexes`), `engine/bench/run.ts`, and the server read routes the reads must match, `src/data_rover/api/routes/read.py` (frozen by MR-3 from the start of plan 3).
7. `docs/superpowers/plans/2026-09-19-exact-server-state.md`, end of file: plan 1's "After this plan".

Confirm state: `git status --short` (expect only ` M BACKLOG-ENGINE.md`, see State), `git branch --show-current` (`engine-migration`), `git log --oneline -1` (`18058bb`).
Commands: `pixi run dr-test` (core + frontend + engine); `pixi run engine-test`; `pixi run engine-check`; `pixi run engine-bench` (needs `benchmarks/large.model.json` → `pixi run engine-bench-data` once); lint `pixi run dr-tidy true`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` at `18058bb`, with plan 2's six commits fast-forwarded in (`2d54921` → `18058bb`). `feat/replica-routes` and `feat/exact-server-state` still exist (merged, not deleted). `main` is untouched.
- Uncommitted: `BACKLOG-ENGINE.md` holds new items `K-35`, `K-36`, `C-21` and an updated `R-3` status, logged at the owner's request after plan 2. It is NOT committed because commit approval covered plan 2's tasks only. See Open Questions.
- Tests (2026-09-19 ~17:05, on `18058bb`): core 2,551 passed / 34 deselected; frontend 2,493 in 252 files; engine 272 in 39 files; `dr-tidy true` clean; `golden-fixtures` unchanged; alembic 7 passed.
- Alembic head is `0016` (`snapshots` header columns).
- No servers and no background jobs. There is no Docker in this WSL distro and no Postgres on :5432.
- `~/.claude/handoffs/data-rover-py-plan3-extract.py` belongs to an OLDER plan (engine-ops). It is unrelated to this plan 3; ignore it.

## Standing Constraints
- **The owner maintains two logs; keep both.** Any new finding that needs a decision or a fix goes into `BACKLOG-ENGINE.md` (the client-engine program's backlog; ids are unique across it and `BACKLOG.md`). Anything that loses you time mid-session gets appended to `~/code/papercuts.md` as `date · symptom · fix · project`. Check that file first when tooling fails mysteriously.
- **Plan form (owner):** direction with specifics: interfaces, signatures, the test cases and what each asserts, the order, and the mechanisms that are easy to get wrong spelled out. No full code. A "What planning found" section lists only facts checked against the code or by a throwaway probe, each saying how it was checked.
- **Only cheap probes while planning.** Building or test-running the plan while writing it was deferred by the owner. Probe scripts go in the scratchpad, never the repo.
- **The owner answers design questions fastest as ONE multiple-choice question at a time**, recommended option first.
- **Freeze rule MR-3:** `core/model`, `core/metamodel` and the op applier are frozen, and so are `routes/read.py`'s route functions from the start of plan 3 (spec §4). The Python core is the oracle: fix the engine, never a fixture. Fixtures change only via `pixi run golden-fixtures`.
- **Engine `src/` rules:** no DOM, no Node dependency, erasable syntax only, `.ts` import specifiers (see CLAUDE.md "Engine package").
- **Git conventions (RC-11):** subjects are one imperative capitalized sentence, no prefix, no period, and the message ends with the session's `Co-Authored-By` line. Never `git add -f` `docs/` or `benchmarks/`. `architecture/`, `CLAUDE.md` and the backlogs change in the same commit as their code (RC-10). Commit only with the owner's go-ahead for that specific work.
- `dr-tidy` lints neither `tests/`, `scripts/` nor `alembic/`: run ruff check and format on those by hand. Pre-existing unformatted test files are not yours to fix; format only your own lines. Known cases: `test_content.py`, `test_alembic.py`, `test_hydration.py`, `test_commit_states.py`, and `test_feed_hub.py:12` `UP037`.
- **API test trap:** `create_app()` installs its own snapshot store, so a test double must be set AFTER `TestClient(create_app())`. Temp ids must start with `tmp_`.
- **Ids:** the next free are `K-37`, `C-22`, `AD-26`.
- **Things plan 3 can rely on from plan 2:**
  - A tail delta is exactly `feed.commit_event`'s dict, `type: "commit"` included. Rebinds never appear in a tail.
  - `head_rev` is the session's `model_rev`, the same number the feed's `snapshot` event reports.
  - The descriptor's `state_digest` is the digest AT the snapshot's `rev`, not the live digest.
  - The descriptor's `url` is a path.
  - The blob route serves the gzip bytes with no `Content-Encoding`.

## Done
- Plan 1 (exact server state), built and merged earlier.
- Plan 2 (snapshots and replica routes), built and merged at `18058bb`. It has six commits, one per task, and all suites are green. It was reported to the owner.
  - Differences from the plan's expectations: `test_a_fresh_session_pays_one_pass…` clears the digest by hand (the upload itself writes the first snapshot). The test store must be set after `create_app()`. The by-hand Postgres check (Task 6 Step 4) was skipped (no dev stack) and is logged as `K-36`.
  - The tail-equals-feed test matched on its first run: the feed and the journal did not disagree on any value.

## Known Issues, Not Yet Fixed
- `K-35` (`BACKLOG-ENGINE.md`, uncommitted): `src/data_rover/api/snapshot_codec.py:116` `_decode_v2` accepts a line holding `{…},{…}`; the engine refuses it.
- `K-36` (uncommitted): the SQL test for "no entity states" (`IS NULL` or `CAST(... AS VARCHAR) = 'null'`) is verified on SQLite only.
- `C-21` (uncommitted): stale "apply-cr baseline reset" comments at `src/data_rover/api/routes/commits.py:785` and `:923`. Diagnosed.
- `K-33` (`BACKLOG.md`): `POST /model/validate`'s staged branch never calls `invalidate_derived_caches()`.
- `K-34` (`BACKLOG.md`): v2 decode costs about twice v1. The owner said: watch it, do not fix.
- `K-29`, `K-32`, `C-20` are open in `BACKLOG-ENGINE.md`.
- There is no CI. `tests/golden/test_fixtures_current.py` is the only guard against fixture drift.

## Deferred — Do Not Do
- Optimizing `_decode_v2` or the v2 encode (owner, 2026-09-19, `K-34`: watch only).
- Reading the descriptor fields from the blob's header line, or a single JSON `header` column (owner chose typed columns, 2026-09-19).
- Building or test-running plans 3–6 while writing them (owner: cheap probes only).
- Landing a scratch build as an implementation, or replacing a plan with a short decisions document (owner, plan-1 era).
- Everything under "Deferred — Do Not Do" in `~/.claude/handoffs/data-rover-py-2026-09-19-1207.md`: neighborhoods, hybrid warm-up, resumable transitions, optimizing the `ord` re-sort, feed events from `/model/ops` and `/model/undo`, and the older list.
- Refactoring `create_commit`'s scope computation onto `replica.scope_of_ops` (C-12: `commits.py` is not reshaped in passing; the tail-equals-feed test holds the two together).

## Plan
1. Verify state with the three git commands. Invoke `superpowers:writing-plans`. Read Orient First items 1–4.
2. Resolve the Open Question below before writing, because it decides what the working tree carries.
3. Survey the ground with cheap probes only:
   - `engine/src/` as it stands;
   - `routes/read.py`, for the read surfaces and their exact answers (paging, fuzzy search, incident relationships, containment tree, summary counts);
   - K-32's two slices, using the `engine-bench` numbers.
   Record each fact under "What planning found", saying how it was checked.
4. Take design choices to the owner ONE multiple-choice question at a time, recommended option first. Record the answers as Decisions.
5. Write `docs/superpowers/plans/2026-09-19-engine-service.md` in plan 2's form: Goal, Architecture, What planning found, Decisions, Global Constraints, File Structure, Mechanisms, Tasks (Files / Interfaces / Steps with expected red), Known limits, After this plan. Self-review it against spec §3, §4, §9 and §10.
6. Present it to the owner for approval. Then hand off for execution: a new session builds it with `superpowers:executing-plans`, inline, one commit per task on a branch cut from `engine-migration`.

## Open Questions
- Should the uncommitted `BACKLOG-ENGINE.md` edits (`K-35`, `K-36`, `C-21`, `R-3` status) be committed now, on `engine-migration`, as their own commit (e.g. "Log what landing the replica routes left open")? Or should they ride with plan 3's first commit? Ask the owner before committing.
