# Handoff: build sub-project C plan 1 (artifacts, navigation, criteria search)

## Mission
Execute plan 1 of 8 of sub-project C (Evaluation) of the client-engine program in
`/home/mdp/workspace/data-rover-py`: the engine holds the project's artifacts (committed
payloads from the shell + staged entries mirrored from the frontend buffer) and answers
`evaluateNavigation` and `searchModel` over the working copy, behind two new surface switches
(`navigation`, `criteria`) that default to the engine once shadow comparison is clean. Done =
the plan's 8 tasks built, one commit each, `dr-test` / `dr-tidy` / checks / e2e green, final
whole-branch review done, and (with the owner's go-ahead) `engine-migration` fast-forwarded.
Run it with `/build-plan docs/superpowers/plans/2026-09-24-eval-artifacts-navigation-search.md`.

## Orient First
1. `docs/superpowers/plans/2026-09-24-eval-artifacts-navigation-search.md` — THE plan: "What planning found" (probe-verified facts), Decisions D1–D10, Global Constraints, Review Focus, Mechanisms M1–M9, Tasks 1–8.
2. `docs/superpowers/specs/2026-09-24-evaluation-design.md` — the approved C spec the plan argues from (binding authority on conflicts).
3. `CLAUDE.md` — map, commands, cross-cutting rules (pixi only; `127.0.0.1:5173`; stale sandbox preview gotcha).
4. `architecture/contracts.md` (CT-4, CT-5, CT-7), `architecture/program.md` (MR-1…MR-4), `architecture/conventions.md` (RC-6, RC-9…RC-13).
5. `engine/README.md` and `frontend/src/lib/engine/README.md` — current engine/shell behaviour the plan extends.
6. B's executed plans for idiom: `docs/superpowers/plans/2026-09-19-engine-service.md`, `2026-09-22-transport-swap.md`.
Confirm state: `git status --short && git branch --show-current && git log --oneline -3`;
tests: `pixi run engine-test`, `pixi run core-test`, `pixi run frontend-test`, `pixi run sandbox-test`; lint: `pixi run dr-tidy true`, `pixi run engine-check`, `pixi run frontend-check`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `feat/eval-navigation`, cut from `engine-migration` at `4fd6cf6`; no commits on it yet.
- Uncommitted: clean. (`docs/` is git-ignored; spec and plan live there, never `git add -f`.)
- Tests (as of 2026-09-24 09:41–09:46, at `4fd6cf6`): engine 461 in 59 files; core 2,565 passed / 34 deselected; frontend 2,897 in 281 files; sandbox 14 — all green.
- SDD workspace for this plan exists: `.superpowers/sdd/2026-09-24-eval-artifacts-navigation-search/` (only `plan-path`; NO `progress.md` ledger yet — start it fresh, first line `# SDD ledger — plan: docs/superpowers/plans/2026-09-24-eval-artifacts-navigation-search.md`). Other dirs under `.superpowers/sdd/` belong to other plans — leave them.
- Next free ids: `AD-30`, `K-49`, `C-22`.

## Standing Constraints
- Commits pre-approved by the owner (2026-09-24): one local commit per task on `feat/eval-navigation`, no further asking; ASK before fast-forwarding `engine-migration`; never push, never touch `main`.
- Owner's C decisions (spec "Decisions"): frontend owns the staged artifact buffer, engine mirrors it; before sub-project D a call that reaches a script routes whole to the server (engine 501 → `route()` falls back); one live engine issue store (plan 2); rules reach the engine as server-parsed JSON (plan 3); own xlsx writer + `fflate` sync API (plan 5); history range diff is a server route (plan 8); compare/apply-CR read the working copy, download reads committed (plan 6); eight plans in the spec's order.
- MR-3: `core/model`, `core/metamodel`, the model-op applier frozen; from Task 1 `core/search`, `core/navigation`, `api/search.py`, `search_model` and `evaluate_navigation` frozen too — the ONE allowed Python behaviour change is D2's `_hop_property` fix (dict in an element-typed list crashed with TypeError), landing with a fixture. Python core is the oracle: fix the engine, never a fixture.
- Quirks are mirrored, not fixed (D2): top-level `artifact_id` unknown → 422 unquoted id; `KeyError` caught as 422 `unknown navigation artifact 'x'`; logged as C-22 in Task 5.
- C-20 closes in Task 8 (unobservable: `core/metamodel/check.py:124-135` refuses a redeclared property); only the comment at `core/metamodel/schema.py:203` changes.
- Criteria coercion is Python `str()`/`float()` and `re.search`, not JavaScript (probe-verified; see plan "What planning found" 1–3). Regex translator answers `ok`/`invalid`/`unsupported`; unsupported → 501 to server, never a guessed match.
- The owner chose inline ("Native") execution with a final fresh whole-branch review, as for B.
- The JWT-leeway flake fix (`~/.claude/handoffs/data-rover-py-2026-09-23-1815-record/flake-invalid-session.md`) is the owner's separate call, its own branch — not part of this work.

## Done
- Brainstorm of sub-project C; spec written and approved: `docs/superpowers/specs/2026-09-24-evaluation-design.md`.
- Plan 1 written and approved: `docs/superpowers/plans/2026-09-24-eval-artifacts-navigation-search.md` (baseline filled in).
- Branch `feat/eval-navigation` cut; the empty `chore/evaluation-spec` branch deleted.

## Known Issues, Not Yet Fixed
- Diagnosed (probe): `core/navigation/evaluate.py:388` `set(candidates)` raises TypeError when an element-typed property list holds a dict → fixed in plan Task 5 (both sides).
- Diagnosed (probe): `api/routes/artifacts.py` `evaluate_navigation`'s `except LookupError` also catches the evaluator's `KeyError` (unknown `row_element_id` behind filter/property step → 422 "unknown navigation artifact 'ghost'"); mirrored, to be logged as C-22.
- Open from B, untouched by C: K-29, K-32, K-35, K-36, K-38, K-41, K-42, K-45…K-48, C-21 (`BACKLOG-ENGINE.md`).

## Deferred — Do Not Do
- Snippet `lint`/`format`, `previewTransform`, `fetchScriptErrors`, `runSnippet` stay server calls (spec non-goals; scripts are sub-project D).
- No change to server-side strict enforcement or `validation_error_count` in C (owner: F's).
- No `rules document` in `/artifacts/payloads` and no `POST /rules/parse` in plan 1 (plan 3).
- No streaming save-format reader unless plan 6 measures compare at M over the heap budget.
- No caching of navigation/search results between pages (the server keeps none).

## Plan
1. Run `/build-plan docs/superpowers/plans/2026-09-24-eval-artifacts-navigation-search.md` on branch `feat/eval-navigation` (commits pre-approved; skip Task 1 Step 1's "ask" — already answered; the branch is already cut).
2. Task 1 first: write `tests/golden/coerce_tables.py` and `tests/golden/scenarios/py_coerce.py`, the failing `engine/test/value/coerce{,.golden}.test.ts`, run `pixi run golden-fixtures` then `pixi run engine-test` (expect only the two new files red), then implement `engine/src/value/coerce.ts` + `pyReprValue` per M1.
3. Continue Tasks 2–8 in order; one commit per task; ledger each.
4. After Task 8: final whole-branch review; fix Critical/Important with RED→GREEN tests; then ask the owner before fast-forwarding `engine-migration`.
5. Then plan 2 of C (validation core) gets its own writing-plans cycle from the spec.

## Open Questions
- None blocking. At the end: owner's go-ahead for the fast-forward of `engine-migration`.
