# Handoff: Brainstorm next slice — Phase 3 (import/export closure) vs Phase 4 (metamodel lease + diff)

## Mission
The `create_commit` refactor (the pre-Phase-3 prerequisite) is **done, merged, and pushed**
(`main` == `origin/main` == `707f6d6`) in `/home/mdp/workspace/data-rover-py`. This
session's job: **brainstorm the next slice** — Phase 3 (import/export closure) or Phase 4
(metamodel lease + diff, independent, may go first) of the artefacts revamp — with the
user, then produce a spec/plan and execute via the established SDD process. "Done" for the
brainstorm step = a chosen phase + a written plan file the user has agreed to.

## Orient First
1. `CLAUDE.md` — read fully (architecture, invariants, toolchain; all still current).
2. `docs/superpowers/specs/2026-07-29-artefacts-revamp-design.md` — parent spec; the
   Phase 3 and Phase 4 definitions are the brainstorm's raw material.
3. `src/data_rover/api/artifact_kinds.py` — `ArtifactKindSpec.extract_deps`/`rewrite_refs`
   are reserved seams for Phase 3; read their docstrings before proposing the closure design.
4. `src/data_rover/api/routes/commits.py` — post-refactor shape: `_CommitUnwind` ledger
   (~line 317) + `create_commit`/`revert_commit` rewired onto it. Phase 3/4 work will build
   on this; the ledger docstring carries the unwind-order invariants.
5. `src/data_rover/api/routes/metamodel_swap.py` + the rebind handling in
   `create_commit`'s staleness branch (`commits.py`, "baseline-or-rebind" 409) — Phase 4's
   metamodel-lease work touches this surface.
6. `docs/superpowers/plans/2026-08-08-create-commit-unwind-refactor.md` — last session's
   plan (gitignored), kept as the record of the refactor's requirements.

Confirm state:
```
git status --short                  # expect clean
git branch --show-current           # expect main
git log --oneline -1                # expect 707f6d6, and main == origin/main
pixi run dr-test                    # core pytest + frontend vitest
cd frontend && pixi run -e frontend npm run lint   # the REAL lint gate
```
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main`, clean tree, head `707f6d6` (merge of `refactor/commit-unwind`),
  **pushed** — `origin/main` is at the same commit (user pushed 2026-08-08 ~18:10; the
  long-standing "56 commits unpushed" question is resolved).
- Tests, run 2026-08-08 ~18:05 on the merged tree: backend **1638 passed, 26 deselected**;
  frontend **181 files, 1763 passed**. `dr-tidy` and `backend-lint` (ruff+mypy+pyright)
  clean. Playwright NOT run this session (no frontend changes).
- Alembic head remains `0009`. No migrations this session.
- `.superpowers/sdd/2026-08-08-create-commit-unwind-refactor/` deleted after merge.

## Standing Constraints
- **Leases are the concurrency control, not OCC** — no `view_rev`/`artifact_rev` op
  preconditions, ever (CLAUDE.md "Lease rule").
- **`_CommitUnwind` unwind order is load-bearing**: `model_rev -= 1` BEFORE
  `invalidate_derived_caches()` (it re-stamps the cell cache to the CURRENT rev); no
  `db.rollback()` on pre-staging paths; no `rollback_view` after `apply_view_ops_atomic`'s
  self-rollback. The ledger docstring in `commits.py` is the authority — extend it, don't
  contradict it.
- **`routes/ops.py::undo` was deliberately NOT migrated to the ledger** — its unwind
  re-PUSHES a popped op_log batch (different contract). It is the natural next adopter
  only when the undo/legacy-ops path itself gets reworked.
- **Preserve the dense invariant comments**; final-review breadcrumbs (Finding 1/A/B/C,
  rounds 2/3, A1/I1) must stay traceable. When retiring things, sweep ALL of `src/`,
  `tests/`, `frontend/e2e`, and README/CLAUDE prose for stale present-tense references.
- **The real frontend lint gate is `cd frontend && pixi run -e frontend npm run lint`**
  (prettier+eslint); `pixi run frontend-lint` is eslint-only.
- **Never send an empty commit batch** — the backend's empty-batch early return skips lock
  release and orphans leases until TTL.
- **Process precedent (4th session running):** brainstorm (for new design work) → plan
  file → subagent-driven development (fresh implementer per task, task review, scoped
  re-reviews) → final whole-branch review on the most capable model → ONE fix wave
  (scoped re-review per round; last session's wave needed 2 rounds because the first fix
  garbled an adjacent comment line — re-reviews earn their cost) → merge `--no-ff` to
  local `main`, delete branch. Push now happens (user pushed), but still only on ask.
- **Characterization-pin-first for refactors**: last session added the missing
  persist-failure route test BEFORE moving code; keep that discipline.

## Done
- **`create_commit` unwind refactor** (merged `707f6d6`, 5 commits): persist-failure
  characterization pin (`tests/api/test_commits_view_ops.py::
  test_persist_failure_rolls_back_all_halves_and_keeps_leases`); `_CommitUnwind` ledger
  replacing 7 unwind sites in `create_commit` + 2 in `revert_commit`; two comment-accuracy
  fix commits. Final review verified all 9 sites statement-identical to the deleted
  blocks; behavior-preserving confirmed by full gates on the merged tree.
- Everything from prior sessions (Phases 1–2 + follow-ups) — see git history.

## Known Issues, Not Yet Fixed
- **Stale cross-refs in `routes/ops.py` (~:707, :714)**: comments still say "create_commit's
  own `created_view`" — that local no longer exists (now `unwind.created_view`). Triaged
  stays-deferred by the final review; fix on next `ops.py` touch.
- **`commits.py:~688` orphan short comment line** ("is fine and does NOT") — parked by
  final-review adjudication: cosmetic, reflow on next touch of that block.
- **b/b3 preamble comments** in `commits.py` reference bare `created_view` (now a ledger
  field) — triaged stays-deferred, referent obvious in context.
- **`unwind()`'s `assert session.view is not None`** is stripped under `python -O`
  (degrades to AttributeError) — faithful to the old inline asserts; final review flagged
  explicit-raise hardening as a future candidate, NOT this-branch work.
- **Pre-existing e2e auth/session flake family** (`{"detail":"invalid session"}` during
  fixture load; `frontend/e2e/helpers/auth.ts:33`) — still undiagnosed; open question 3.
- **`routes/locks.py::expand_targets` degrades against a cold `session.view`** (~`:84`) —
  false-409-only failure mode; judged safely deferrable by two reviewers.
- **`view → realtime → artifacts → view` import cycle** worked around by
  `setTimeout(…,0)` in `view.svelte.ts`; suggested real fix: cycle-free `getView` leaf.
- **Pre-existing unit flake**: `tests/model/test_search_index.py::
  test_string_properties_indexed_non_strings_ignored` (~0.8% of runs). Re-run and move on.
- Older review-ledgered minors from the Phase-2 sessions (excluded-pool transient
  injection, `_seed_view` conftest duplication across four backend test files, discard
  banner not cleared on in-SPA project switch, client `TEMP_ID_PREFIX` guard, etc.)
  remain open and unchanged.

## Deferred — Do Not Do
- **No OCC on view/artifact ops** — declined at planning; the lease is the control.
- **No per-entry revert for the view journal** — unsound for an order-dependent journal.
- **No revert-across-view-ops UI** — backend 409 is the designed boundary.
- **No view renaming / multiple named views** — no op exists; no UX ask.
- **Do not migrate `routes/ops.py::undo` onto `_CommitUnwind`** as a drive-by — explicitly
  scoped out of the refactor (different op_log contract); belongs to the eventual
  legacy-path migration slice.
- **Do not resurrect `PUT /view/snapshot` / `DELETE /view`** — the e2e loader's
  commit-flow seeding is the supported path.
- **HistoryDrawer consuming `GET /commits/{rev}/diff`** — separate slice, still unclaimed.
- **`_seed_view` conftest consolidation** — only when next touching those four test files.

## Plan
1. **Brainstorm the next slice with the user** (superpowers:brainstorming): Phase 3
   (import/export closure — `extract_deps`/`rewrite_refs` in `artifact_kinds.py`, spec §
   in `docs/superpowers/specs/2026-07-29-artefacts-revamp-design.md`) vs Phase 4
   (metamodel lease + diff — independent, may go first). Read the spec's two phase
   definitions FIRST, come with a recommendation and its tradeoffs (e.g. Phase 4 touches
   the rebind/staleness surface `create_commit` just cleaned up; Phase 3 depends on the
   kind registry seams). The choice is the user's — open question 1.
2. Write the spec/plan file for the chosen phase (superpowers:writing-plans), then SDD per
   precedent: branch off `main`, fresh implementer per task, final review on the most
   capable model, ONE fix wave, merge `--no-ff`, delete branch.
3. Push only if asked.

## Open Questions
1. **Which phase next** — Phase 3 (import/export closure) or Phase 4 (metamodel lease +
   diff)? Both are brainstorm-first; user has not yet stated a preference.
2. **The e2e auth/session flake family** — tolerate (re-run in isolation) or spend a
   session diagnosing it (likely test-parallelism vs. session cookie)?
