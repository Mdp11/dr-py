# Handoff: P-10 top bar restructure + U-1 — execute the plan

## Mission
Implement the approved top bar restructure (BACKLOG P-10.1–P-10.5) plus the Issues
per-validator filter (U-1) in `/home/mdp/workspace/data-rover-py`. Spec and plan are
written and owner-approved; nothing is implemented yet. "Done" = all 8 plan tasks
landed on a feature branch, `pixi run dr-tidy` clean and `pixi run dr-test` green,
BACKLOG/README/CLAUDE.md updated (plan Task 8).

## Orient First
1. `docs/superpowers/plans/2026-08-18-top-bar-restructure.md` — the implementation plan; execute it task-by-task.
2. `docs/superpowers/specs/2026-08-18-top-bar-restructure-design.md` — the approved design the plan argues from.
3. `CLAUDE.md` — toolchain (everything through pixi), conventions, architecture.
4. `frontend/src/lib/state/workspace.svelte.ts` — the tab-strip state module Task 1 rewrites.
5. `frontend/src/lib/components/TopBar.svelte` + `Workspace.svelte` — the two components at the center of the wave.

Confirm state: `git status --short` (expect only `M BACKLOG.md`), `pixi run frontend-test`,
`pixi run core-test`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main` at `4c26c8e`. No feature branch created yet — create one before Task 1
  (repo convention: `feat/...`; see `superpowers:using-git-worktrees` if isolating).
- Uncommitted: `M BACKLOG.md` — a pre-existing owner edit from before this session
  (the 2026-08-18 notes batch). Do NOT revert it; it rides along until plan Task 8 edits
  the same file.
- Tests: not run this session. Last known baseline (BACKLOG header, 2026-08-16):
  core 1835 passed, frontend 2078 passed, dr-tidy clean.
- Execution mode chosen by user: **subagent-driven** — use
  `superpowers:subagent-driven-development` (fresh subagent per task, review between tasks).

## Standing Constraints
- Owner decisions baked into the spec (do not relitigate): placeholder empty workspace
  (no auto-opened tab); U-1 filters by validator identity, which REQUIRES the backend
  `check` field; all 8 top-bar controls flat icon+text (no grouping menus, no icon-only);
  only Cmd+S/Cmd+E shortcuts survive.
- Never commit anything under `docs/superpowers/` — gitignored by repo convention
  (BACKLOG §10). The brainstorming skill's "commit the spec" step was deliberately skipped.
- Spec correction discovered during planning: view warnings are built SERVER-side
  (`src/data_rover/core/view/validation.py`, six `Issue(` sites) — they get `check="view"`
  there, not client-side as the spec's §7 wording implies. Plan Task 6 is the authority.
- `check` stamping is central in `ValidationPipeline.validate` via per-validator
  `check_name` class attrs — NOT at the ~21 `Issue(` construction sites (DRY; new
  validators get it free).
- Right side of the TopBar (validation chip, Undo, Validate, Commit, Strict, changes
  counter) is explicitly untouched, same order — owner note in P-10.3.
- Frontend single-file test invocation: `pixi run frontend-test -- <path>` (pixi task sets
  cwd=frontend; a bare `pixi run -e frontend npm test` fails from repo root).
- Trigger audit for palette deletion already done (spec §5): every palette `action:*` has a
  surviving trigger; `artifactDialogsHosted` machinery has zero readers post-palette and is
  deleted in Task 4. Don't re-audit.

## Done
- Brainstorm complete (4 owner decisions recorded above).
- Spec written + self-reviewed + owner-approved: `docs/superpowers/specs/2026-08-18-top-bar-restructure-design.md`.
- Plan written + self-reviewed: `docs/superpowers/plans/2026-08-18-top-bar-restructure.md`
  (8 tasks; 1→5 sequential, 6 independent, 7 needs 6, 8 last).
- No code changed yet.

## Known Issues, Not Yet Fixed
- None from this session. Adjacent diagnosed items live in BACKLOG (U-9 DiffDrawer
  overflow shares files with this wave but is out of scope per spec §9).

## Deferred — Do Not Do
- U-9 (commit panel overflow), P-19/P-20 (sidebar/staging), e2e coverage (T-7) — all
  explicitly out of scope in spec §9, by owner approval of that spec.
- Client-only issue filter (severity/category) — rejected by owner in favor of the
  backend `check` field.
- Grouping menus or icon-only top-bar controls — rejected by owner; flat icon+text.
- Rebinding a shortcut for Issues (e.g. Cmd+I) — declined; only Cmd+S/Cmd+E survive.

## Plan
1. Invoke `superpowers:subagent-driven-development`; create the feature branch
   (e.g. `feat/top-bar-restructure`) off `main`.
2. Dispatch plan Task 1 (workspace state: issues tab kind, nullable active tab,
   neighbor-focus close) to a fresh subagent; review its diff against the plan.
3. Continue task-by-task through Task 8 per the plan's checkboxes; Tasks 6 can run
   parallel to 2–5 if convenient.
4. Final gate: `pixi run dr-tidy` + `pixi run dr-test`; then
   `superpowers:finishing-a-development-branch` (note: repo never pushes to origin —
   BACKLOG §10 "Process").

## Open Questions
None.
