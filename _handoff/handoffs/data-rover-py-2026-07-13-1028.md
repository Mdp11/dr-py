# Handoff: Table column editing & inline navigation definitions

## Mission
Implement two frontend improvements to the Stage 2 Table system in `/home/mdp/workspace/data-rover-py`: (1) property columns editable after creation (searchable picker + free text), (2) navigation columns AND navigation/chains row sources can carry an inline navigation definition edited with the real navigation-builder components (embedded in the column panel). Done = all 7 tasks of the implementation plan executed, vitest + svelte-check + the table e2e green, work committed per task.

## Orient First
1. `docs/superpowers/plans/2026-07-13-table-column-editing-and-inline-navigations.md` — the complete implementation plan (7 TDD tasks with full code); this is the work queue.
2. `docs/superpowers/specs/2026-07-13-table-column-editing-and-inline-navigations-design.md` — the user-approved design the plan implements.
3. `CLAUDE.md` — toolchain (pixi), frontend command gotcha, conventions.
4. `frontend/src/lib/state/navigation-editor.svelte.ts` — the store that will host embedded drafts; read its header comment.
5. `frontend/src/lib/components/Table/ColumnManager.svelte`, `NavigationColumnEditor.svelte`, `RowSourceEditor.svelte` — the components being changed.

Confirm state: `git status --short` (expect clean), `pixi run -e frontend bash -c 'cd frontend && npm test'`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main` at `f756dc3` (Stage 2 tables merged)
- Uncommitted: clean (as of 2026-07-13 10:28; spec + plan live in gitignored `docs/superpowers/`)
- Tests: not run this session — plan was written from code reading only
- No implementation work has started. No servers running.

## Standing Constraints
- **Execution mode: use the `superpowers:subagent-driven-development` skill** — dispatch a fresh subagent per plan task, review between tasks (user instructed this at handoff).
- Reuse mechanism is **Approach A: ephemeral embedded drafts** (`navemb:<uuid>` ids) hosted in the existing navigation-editor store — user rejected refactoring NavigationNode/PathCard to controlled components (regression risk to the working Stage 1 builder).
- Inline nav editing is **embedded in the column panel**, not a modal (user choice).
- Both navigation columns **and** row sources get inline definitions (user choice); row sources use `rowContext: false` (Scope start only — no RowStart).
- Property name editing = **searchable PropertyPicker + free-text fallback** (user choice); "+ Property column" becomes add-then-edit (quick-add input removed).
- **Frontend-only feature.** Backend already has everything: `RowStart` sentinel (`core/navigation/schema.py:124`), inline-definition resolution (`core/table/resolve.py`), `row_element_id` on `POST /navigations/evaluate` (`src/data_rover/api/routes/artifacts.py:232`). Do not touch Python.
- `runPreview`'s no-row skip must be quiet (no eval-error flag): StatusChip's "no row to preview against" hint derives from the same predicate — a red "failed" would be wrong.
- The mirror `$effect`s in the editors rely on **reference equality** of the definition object; ColumnManager's `structuredClone(defn)` clones before splicing the new column in, so the reference survives by construction. Do not "fix" either side.
- In `chainColumns`/PathCard, the `'row'` start kind must be checked **before** `readElementStart` (which reads `scope.types` and would throw on a RowStart).
- Frontend npm scripts must run from inside `frontend/`: `pixi run -e frontend bash -c 'cd frontend && npm test'`.
- Component tests follow the repo's `mount`/`flushSync` + real-stores + `vi.spyOn(artifactsApi, …)` convention (see `frontend/src/lib/components/Navigation/__tests__/status-chip.test.ts`).
- Commits end with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

## Done
- Brainstorming complete; design approved by user (all option choices recorded above).
- Spec written: `docs/superpowers/specs/2026-07-13-table-column-editing-and-inline-navigations-design.md` (user reviewed and approved).
- Implementation plan written and self-reviewed: `docs/superpowers/plans/2026-07-13-table-column-editing-and-inline-navigations.md`.
- No plan tasks executed yet.

## Known Issues, Not Yet Fixed
None.

## Deferred — Do Not Do
- Promoting an inline definition to a saved navigation artifact (spec "Out of scope").
- Column-level stable identity (columns stay index-addressed; embedded drafts are ephemeral so index shifts are harmless) — spec "Out of scope".
- Refactoring NavigationNode/PathCard/CombineFrame to `definition`+`onChange` controlled components — proposed as Approach B, rejected by user.
- Modal-hosted navigation builder for columns — proposed, rejected in favor of in-panel embedding.
- Committing spec/plan docs to git — `docs/superpowers/` is gitignored by repo convention.

## Plan
1. Invoke the `superpowers:subagent-driven-development` skill and execute `docs/superpowers/plans/2026-07-13-table-column-editing-and-inline-navigations.md` task-by-task (fresh subagent per task, review between tasks). Start with **Task 1: `RowStart` in frontend types + tree helpers** — failing tests first in `frontend/src/lib/navigation/__tests__/tree.test.ts`, then `frontend/src/lib/api/types.ts` + `frontend/src/lib/navigation/tree.ts`, run, commit.
2. Tasks 2–6 in order (each has complete code + test code in the plan; each ends in a commit).
3. Task 7: update `frontend/e2e/table.spec.ts` (property section + new inline-nav test), then full verification: `npm test`, `npm run check`, e2e `table.spec.ts`, `pixi run tidy`.
4. When all tasks pass, use `superpowers:finishing-a-development-branch` to decide merge/PR (work is currently on `main` — consider branching first, e.g. `feat/table-column-editing`, before Task 1).

## Open Questions
None — user approved spec, plan, and (implicitly, via handoff instruction) subagent-driven execution.
