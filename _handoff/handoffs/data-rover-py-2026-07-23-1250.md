# Handoff: snippet correctness & error visibility (session 1 of fix batch)

## Mission
Execute the approved implementation plan for session 1 of a 5-session fix batch in
`/home/mdp/workspace/data-rover-py`: make navigation script steps work inside tables, fix
the `step()` return contract, add a per-table script-error recap with jump-to-cell, and
close two read-only regression-test gaps. Done = all 8 plan tasks executed, committed
task-by-task, full verification sweep green (Task 8 step 3).

## Orient First
1. `docs/superpowers/plans/2026-07-23-snippet-correctness.md` — the approved plan; execute it task-by-task. It is self-contained (complete code per step).
2. `docs/superpowers/specs/2026-07-23-snippet-correctness-design.md` — the approved spec behind the plan (rationale, risks).
3. `CLAUDE.md` — toolchain (everything via pixi), test/lint commands, architecture.
4. `src/data_rover/core/script/README.md` — ground truth for the script facade / embedded evaluation (`step()` contract, cell cache, sweep).

Confirm state: `git status --short` (expect clean), `pixi run core-test`,
`pixi run core-lint`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main` @ 087ba81, clean tree (as of 2026-07-23 12:50)
- Uncommitted: none — spec + plan files live in `docs/superpowers/` which is gitignored
- Tests: not run this session (only read-only exploration happened)
- No servers running; no worktree created yet

## Standing Constraints
- **Execution mode: superpowers:subagent-driven-development** — user chose option 1 (fresh subagent per task, review between tasks). Consider superpowers:using-git-worktrees before starting.
- **Root causes are diagnosed, not suspected** (two independent Explore agents, user-confirmed symptoms):
  - Table path: `core/table/evaluate.py:93,112,268,305` call navigation `evaluate()` without `script=` → `_hop_script` prunes silently at `core/navigation/evaluate.py:348-349`. Fix = plan Task 3.
  - Preview path: `step()` returning a single `Element` dies with `KeyError: 0` (guest serializer iterates via `__getitem__`, `facade_src.py` ~741); user's `el.children()[0]` case is this same bug. Fix = plan Task 1.
  - `step(el) -> [el]` empty is **by design** (visited/cycle guard) — user confirmed understanding; only add a warning (plan Task 2), do NOT change guard semantics.
- **`script=None` keeps pruning silently** (spec §1 deviation, user-approved): there is no warnings channel without a context; the guarantee is structural (`table_has_script()` forces a context). Don't add a side-channel for it.
- **Recap data source: dedicated cache-only backend endpoint** (user chose over client-side aggregation and sweep-accumulated) — client only sees a window of a virtualized table, and sweeps skip cached cells, so both alternatives are incomplete.
- **Recap scope: per-table panel + jump** (user chose over project-wide console).
- **Read-only enforcement in src/ is correct — do not change it**; only test coverage is missing (plan Task 7). If the `/snippets/run` entry-mapping test FAILS, that's a live security bug: stop and report.
- Plan tests contain "mirror the neighboring file's fixtures" instructions (e.g. `tests/api/test_tables_script_status.py`) — wire-shape details there are deliberate pointers, the asserted behaviors are the contract.

## Done
- The full fix list (13 items) was batched into 5 approved sessions:
  1. **this one** (snippet correctness & error visibility);
  2. view editing via snippet facade `dr.view.*` (user chose facade over REST; needs view ops in the op/commit protocol — today view is snapshot-replace only, `routes/view.py`);
  3. artifact locking (extend Phase 4 leases);
  4. OR-able property filters everywhere + scope-column edit bug;
  5. polish pack (editor drag-resize, clone column, collapse snippets, row numbers, details back/forward, Excel autofit/borders/filters).
  Order: 1 → 2 (view work builds on snippet plumbing), then 3/4/5 any order.
- Spec written and user-approved: `docs/superpowers/specs/2026-07-23-snippet-correctness-design.md`.
- Plan written, self-reviewed, user-approved: `docs/superpowers/plans/2026-07-23-snippet-correctness.md` (8 tasks).
- No implementation has started — zero code changes on disk.

## Known Issues, Not Yet Fixed
- All four spec problems (diagnosed, see Standing Constraints for path:line): table context drop; hostile `step()` contract; silent visited-drop; no error recap. These ARE the work — fixed by executing the plan.

## Deferred — Do Not Do
- **Project-wide script-error console** — user explicitly picked per-table scope; don't build aggregation across tables/projects.
- **Per-step `allow_revisit` toggle / changing cycle-guard semantics** — superseded when the user confirmed the guard behaved as designed; only the warning was kept.
- **One-shotting all 13 fix items in one plan** — user approved the 5-session split; sessions 2–5 are out of scope here.
- **Warning channel for `script=None` navigation evaluation** — I proposed it, then withdrew it in the spec (no channel exists without a context); user approved the spec with the structural-guarantee stance instead.

## Plan
1. Invoke `superpowers:subagent-driven-development` and execute
   `docs/superpowers/plans/2026-07-23-snippet-correctness.md` starting at **Task 1**
   (`step()` serializer fix in `src/data_rover/core/script/facade_src.py`, tests in
   `tests/script/test_session.py`). Consider a worktree first (superpowers:using-git-worktrees).
2. Tasks in order 1→8; each has its own TDD cycle and commit message (with Claude Fable 5 trailer).
3. After Task 8's verification sweep, use superpowers:finishing-a-development-branch.

## Open Questions
None.
