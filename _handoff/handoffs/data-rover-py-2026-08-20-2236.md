# Handoff: Exporter v2 Phase 3 — Automation Surface (execution)

## Mission
Execute the **approved** implementation plan for Exporter v2 Phase 3 in
`/home/mdp/workspace/data-rover-py`:
`docs/superpowers/plans/2026-08-20-exporter-v2-phase3-automation-surface.md`.
Use **superpowers:subagent-driven-development** (user's explicit choice): fresh
implementer subagent per task, review after each. Done = all 7 plan tasks landed
green (`dr-tidy` + `core-test` + `frontend-test`) on branch
`feat/exporter-v2-phase3`, finished via superpowers:finishing-a-development-branch
(merge to `main` with `--no-ff`, repo convention).

## Orient First
1. `docs/superpowers/plans/2026-08-20-exporter-v2-phase3-automation-surface.md` — THE plan; user-approved 2026-08-20, execute as written. Its Global Constraints block applies to every task.
2. `docs/superpowers/specs/2026-08-19-custom-export-v2-design.md` §9, §11, §13, §16 — the binding spec the plan argues from (draft runs 9.1, run-by-name 9.2, picker/button 11).
3. `CLAUDE.md` — toolchain (everything through pixi) + architecture; "Table export formats" bullet describes the current 4-format world.
4. `src/data_rover/api/routes/exports.py` — the route Tasks 2–3 refactor (`run_export` → `_execute_export` extraction).
5. `frontend/src/lib/components/Export/ExporterTab.svelte` — Tasks 4–5 surface (Export gating at :179, add-table `<select>` at :404).
6. `frontend/README.md` — read before touching `frontend/src/lib/state/`.

Confirm state: `git status --short` (clean), `git log --oneline -1` (`3b31492`), then `pixi run dr-test` for a fresh baseline before Task 1.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main` (Task 1 Step 1 creates `feat/exporter-v2-phase3`)
- Uncommitted: clean
- Tests: `pixi run dr-test` green on `main` — core 1914 passed / 30 deselected, frontend 231 files / 2192 tests (as of 2026-08-20 ~21:00 at `3b31492`; re-verify before starting)
- HEAD: `3b31492`, **pushed** — `main` is level with `origin/main` (pushed this session, user-approved)
- No servers running; docker services stopped.

## Standing Constraints
- **The plan is approved — execute it, don't redesign it.** Deviations only if a task's premise turns out factually wrong; then stop and tell the user.
- **Phase ordering is the spec owner's (§16).** Phase 3 only; `transform` hook is Phase 4, bundle-draft export is Phase 5 (Phase 5 needs re-confirmation before starting).
- **Never block Save** — all strictness is run-time 422/404/409 naming the offender. Task 6 *removes* a Save gate; nothing may add one.
- **Draft runs are render-only client input** (spec §9.1): same validation as committed payloads, referenced tables always evaluate from committed definitions. `/exports/run` stays viewer-callable.
- **RENDER ONLY boundary** — nothing here touches evaluation/cache keys.
- **Run `pixi run core-lint` before every Python commit** — pytest can't catch annotation-only breaks under `from __future__ import annotations` (bit Phase 2 Task 1).
- Everything through **pixi**; frontend tasks set `cwd=frontend` (a bare `pixi run -e frontend npm test` fails). `core-lint` does NOT run ruff format; only `dr-tidy` is the full gate; `tests/` is linted by nothing.
- **Comments/docstrings are load-bearing and treated as contract** — Phase 2's final review made comment accuracy merge-blocking. The plan's Task 2 says to move `routes/exports.py`'s long comments verbatim with the code.
- Commit trailers name the actual authoring model; merge with `--no-ff`.
- Process that worked in Phase 2 (repeat it): fresh implementer subagent per task, task review after each, cheap models for transcription-grade work, final whole-branch review before merge.
- Frontend testids referenced by existing tests must keep working or be updated in the same task (plan Tasks 5–6 name the greps: `add-table-select`, `splitTemplateInvalid`/`toBeDisabled`).

## Done
- Exporter v2 Phases 1–2 shipped and merged (`3b31492`); `main` pushed to origin this session.
- Phase 3 **planning**: four open questions resolved with the user (see Deferred/plan), plan written, self-reviewed (one fix applied: TS narrowing instead of non-null assertion in Task 4's `runExport`), and **approved by the user 2026-08-20**.
- Code survey already baked into the plan — do not re-derive: most §11 controls shipped in Phases 1–2; Phase 3 frontend is only picker + Export button + F-16. `content.find_artifact` raises on duplicates (why Task 3 adds `find_artifacts_by_name`). `/exports/run` already in `authz._READ_ONLY_POST_SUFFIXES`. `tests/api/test_exports_route.py` helpers `_mk_table`/`_mk_export`/`_run`/`_names` exist; duplicate-name rows for the 409 test must be inserted at the content layer (`from data_rover.api.db import db_session` — established pattern, e.g. `tests/api/test_issues_route.py`).

## Known Issues, Not Yet Fixed
- **`BACKLOG.md` K-11** — CSV formula-injection posture (keep unsanitized, documented in `core/table/csv_export.py`) was **confirmed accepted by the owner 2026-08-20**. No code change; Task 7 records the closure in BACKLOG.md.
- **`BACKLOG.md` F-16** — EntryLayoutDialog Save gate on invalid split template: fix IS Task 6 of the plan (owner approved fixing it in Phase 3).
- Coverage nit from Phase 2 (no test for `key_column` into an honored group bucket) — traced safe, still open, not Phase 3 scope.

## Deferred — Do Not Do
- **Phase 4 (`transform` hook) and Phase 5 (bundle-draft export)** — spec §16 orders them after; owner has not re-confirmed Phase 5.
- **Sanitizing CSV formula output** — owner confirmed the keep-as-documented posture 2026-08-20. Do not add `'`-prefixing.
- **Object-shape JSON preview** (`POST /tables/json-preview` still renders array shape) — deliberately deferred in Phase 2; only on user request.
- **Always sending `definition` from the Export button** — rejected in plan self-review: a clean committed draft runs by `artifact_id` so the manifest carries the real artifact id (plan "Deliberate interpretations" #2).
- **Filtering the picker against already-added tables** — F-11 decided duplicates are legal and useful; the picker is deliberately unfiltered.

## Plan
1. Invoke **superpowers:subagent-driven-development** for the plan file above.
2. Baseline: `git status --short`, `pixi run dr-test` on `main`.
3. Execute Tasks 1–7 in order (Task 1 creates the branch as its Step 1): cleanups → draft runs → run-by-name → ungated Export button → P-15.1 picker → F-16 → docs+verification. Fresh implementer per task; review each before the next.
4. Final whole-branch review (Phase 2 precedent: comment accuracy is merge-blocking), then superpowers:finishing-a-development-branch → merge `--no-ff` into `main`.
5. Ask the user whether to push `main` after the merge (they approved pushing the *planning-time* state; a new merge is a new ask).

## Open Questions
None.
