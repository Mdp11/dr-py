# Handoff: Artefacts Phase 3 frontend — execute the approved plan

## Mission
Execute the already-written, already-approved implementation plan for the Phase 3
import/export **frontend** in `/home/mdp/workspace/data-rover-py`: TopBar Artifacts menu,
export dialog (live closure preview + name filter), plan→confirm import dialog, workspace
tab export button, wizard artifacts slot + one small backend field. Brainstorming, spec,
and plan are DONE and user-approved this session — do not redo them. "Done" = all 9 plan
tasks green, whole-branch review + one fix wave, merged `--no-ff` to local `main`, branch
deleted. Push only if asked.

## Orient First
1. `docs/superpowers/plans/2026-08-09-artefacts-phase-3-frontend.md` — THE plan. 9 tasks
   with code, tests, and commands. This is what you execute, task by task.
2. `docs/superpowers/specs/2026-08-09-artefacts-phase-3-frontend-design.md` — the approved
   spec behind it (behavior contracts, decision matrix, out-of-scope list).
3. `CLAUDE.md` — "Import/export (artefacts revamp Phase 3)" bullet: the frozen backend
   contract.
4. `frontend/README.md` — mandatory before touching `frontend/src/lib/state/`.
5. `src/data_rover/api/artifact_bundle.py:404` (`build_import_ops`) — the legal-decision
   matrix the import dialog mirrors; `routes/artifact_bundle.py` for the four routes.

Confirm state:
```
git status --short                       # expect clean
git branch --show-current                # expect main (plan Task 0 creates the branch)
git log --oneline -1                     # expect b25e7d7
pixi run core-test                       # 1689 passed, 26 deselected expected
pixi run frontend-test                   # last known good 1763 passed (2026-08-08)
```
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main`, clean tree, head `b25e7d7` (as of 2026-08-09 19:45). No feature branch yet.
- `main` is 11 commits ahead of `origin/main` — nothing pushed; user has not asked for a push.
- Tests NOT re-run this session (session was design/planning only, zero code written).
- Spec + plan live in gitignored `docs/superpowers/` — do not try to commit them.
- Task tracker: brainstorm/spec/plan tasks all completed; nothing in flight.

## Standing Constraints
- **User chose subagent-driven execution** (option 1): use
  `superpowers:subagent-driven-development` — fresh implementer subagent per plan task,
  review after each, then final whole-branch review on the most capable model, ONE fix
  wave, merge `--no-ff`, delete branch. This is also the 7-session process precedent.
- **Backend contract is frozen.** The ONLY sanctioned backend change is plan Task 1
  (`ProjectOut.skipped_artifacts`). If a route looks wrong, raise it, don't change it.
- **User-decided UX (do not relitigate):** a NEW growable toolbar on the TopBar logo row —
  this feature contributes ONE dropdown button ("Artifacts ▾" → Export…/Import…); export
  dialog = live debounced closure preview with dependency badges + client-side name
  filter; import dialog = single-dialog plan table with per-row legal-action selects and
  only-when-edited copy renames; wizard gets the fourth slot + skipped warning; the
  per-tab export button opens the export dialog PRE-SELECTED (user rejected direct
  download).
- Import never places artifacts in the view — bundle carries no placement data; user
  declined placement twice in the backend sessions.
- Viewer gating is hide-not-disable (`canEdit()`), matching `ArtifactsSection.svelte`;
  export/preview are viewer-allowed, plan/import are not.
- A 409 from `POST /artifacts/import` with `body.plan` must re-render from that fresh
  plan (never the submitted one — infinite loop otherwise); a `{detail, model_rev}` 409
  without `plan` re-plans from the held bundle. Both shapes are typed in plan Task 2.
- No clean-staged-buffer gate on import — import creates fresh ids only; the name-clash
  edge is a recoverable later 422 (decided during design, don't add a gate).
- Real frontend lint gate: `pixi run -e frontend bash -c 'cd frontend && npm run lint'`
  (`dr-tidy` misses things). Frontend npm scripts need `cd frontend` — pixi runs from
  repo root.
- `rev: null` in the import confirm response is a SUCCESSFUL no-op, render it as such.

## Done
- Brainstorm (all UX questions answered by user), spec written + user-approved, plan
  written + self-reviewed. Zero implementation code exists — grep for `artifacts/export`
  in `frontend/src` is empty.
- Phase 3 backend: merged at `b25e7d7`, green, reviewed (previous session).

## Known Issues, Not Yet Fixed
Carried from the Phase 3 backend handoff, none blocking (all diagnosed there):
- Untrusted import path skips unregistered kinds (`diagram`) — deliberate, documented.
- `importer._landable_artifacts` vs `derive_plan_ex` order-of-checks reason-string
  mismatch — cosmetic.
- Ship-as-is minors from the backend final review: inert `ack_errors` in the 422→409
  remap; unpluralized "Imported 1 artifacts" default message (test-pinned); unbounded
  bundle sizes.
- Pre-existing flakes: e2e auth family (`frontend/e2e/helpers/auth.ts:33`),
  `tests/model/test_search_index.py::test_string_properties_indexed_non_strings_ignored`
  (~0.8%). Re-run and move on.
- Plan-authoring caveats: Task 5/8 test code says "adapt to the file's existing harness"
  — implementers must read the named test files first; Task 6 may need a TopBar.test.ts
  tolerance tweak for the new nav region (called out in the plan).

## Deferred — Do Not Do
- **Phase 4 (metamodel lease + structural metamodel diff)** — user re-confirmed Phase 3
  frontend over it this session. Still open, still independent.
- Auto-placement of imported artifacts in the view; bundle folder placements — user
  declined twice.
- Stateful/staged import with server token — rejected; stateless re-send is the design.
- Global store for the dialogs (approach B) and route-based flows (approach C) — user
  picked thin-client + component-local dialogs (approach A).
- HistoryDrawer consuming `GET /commits/{rev}/diff`; `_seed_view` conftest consolidation;
  `routes/ops.py::undo` migration onto `_CommitUnwind` — all still parked from earlier
  sessions.
- Committing anything under `docs/superpowers/` — gitignored by convention.

## Plan
1. Invoke `superpowers:subagent-driven-development` with the plan file
   `docs/superpowers/plans/2026-08-09-artefacts-phase-3-frontend.md`.
2. Task 0: `git checkout -b feature/artefacts-phase-3-frontend main`, then dispatch
   Task 1 (backend `ProjectOut.skipped_artifacts`) to a fresh implementer; review; continue
   through Task 9 in order (2→3 are prerequisites of 4→7; 8 needs 1).
3. After Task 9's full gates: final whole-branch review on the most capable model, ONE
   fix wave, re-review the fixes.
4. Merge `--no-ff` into local `main`, delete the branch. Do not push.

## Open Questions
None.
