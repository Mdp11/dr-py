# Handoff: plan 3 of sub-project C (rules), the planning session

## Mission
Write plan 3 of 8 of sub-project C (Evaluation), **Rules**, for `/home/mdp/workspace/data-rover-py`. It ports the Python rule engine to the TypeScript engine: compile, reach (`expand_scope`), evaluation, `POST /rules/parse`, and `document` in `GET /artifacts/payloads`. Rules plug into plan 2's live issue store as a seventh validator.
Done means: `docs/superpowers/plans/<date>-eval-rules.md` is researched, probe-verified, self-reviewed, and approved by the owner. It is written in the same shape as plans 1 and 2 (a "What planning found" section of facts checked against code or probes; Decisions `D-n`; Global Constraints; Review Focus; File Structure; Mechanisms `M-n`; tasks tagged `implementer` / `critical-implementer`).
This session plans only; building is a later `/build-plan` session.

## Orient First
1. `docs/superpowers/specs/2026-09-24-evaluation-design.md` is the binding spec. Plan 3 covers §3 `### Rules` (from line 268), §7's plan-3 rows, §8's plan-3 freeze row and §11 item 3. Read §2 "Shadow" and §3 "Origins" too.
2. `docs/superpowers/plans/2026-09-24-eval-validation-core.md` is plan 2, now built. It is the template to follow. Its "After this plan" section (end of file) lists what plan 3 inherits.
3. `docs/superpowers/plans/2026-09-24-eval-artifacts-navigation-search.md` is plan 1, built. It holds the artifact-family idioms that rules artifacts reuse.
4. `CLAUDE.md` is the map, the commands and the cross-cutting rules. Also read `architecture/README.md`, then `architecture/program.md` (MR-1…MR-4, C's status), `architecture/decisions.md` (AD-31, AD-32), `architecture/contracts.md` (CT-4).
5. Code to trace for the port:
   - Python rules: `src/data_rover/core/validation/rules/` (the validator, compile, reach), `src/data_rover/api/rules.py` (`session_pipeline`, `attributable_issues`), and the lint route. Read `src/data_rover/core/README.md` for the rules part of the pipeline.
   - Engine: `engine/src/validation/` (`live.ts`, `pipeline.ts`, `dirty.ts`, `bodies.ts`), `engine/src/service/service.ts` (the `reaches validation rules` 501 and `moveArtifacts`), `engine/src/artifacts/artifact-set.ts` (`resolvesKind`), and `engine/README.md`.
   - Shell: `frontend/src/lib/engine/README.md` (surfaces, the `issues` gate, fallbacks, shadow); `frontend/src/lib/state/replica.svelte.ts` (the gate, which waits for the artifact follower).
6. `BACKLOG-ENGINE.md`: R-3 (the freeze text), C-23, and K-49…K-63.

Confirm state with `git status --short && git branch --show-current && git log --oneline -3`.
- Tests: `pixi run dr-test`.
- Lint: `pixi run dr-tidy true`, `pixi run engine-check`, `pixi run frontend-check`.
- e2e: `pixi run sandbox-build`, then `pixi run frontend-test-e2e`.

Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration`, at `2cca78a` (plan 2 fast-forwarded in; `feat/eval-validation` deleted). `main` is untouched. Nothing is pushed; `engine-migration` has no upstream.
- Uncommitted: clean.
- Tests on `2cca78a`, 2026-09-25:
  - `pixi run dr-test` is green: core 2,575 passed / 34 deselected; frontend 3,008 in 283 files; engine 1,214 in 78 files; sandbox 14.
  - e2e: 63 passed, 2 failed; the failures are the pre-existing T-8 and T-9. No `[shadow]` lines.
  - `pixi run engine-parity-large`: equal over 7,708 injected issues.
- Plan 2's SDD workspace was deleted. Its rulings live in the commit history and in `architecture/` and `BACKLOG-ENGINE.md`. The plan-2 plan file is in `docs/`, which is git-ignored.
- Engine bench data is in `benchmarks/`: `large.model.json`, `large.snapshot.v2*`, `large.violations.ops.json`, `large.issues.json`.
- Next free ids (grep before use):
  - `AD-33` (architecture/);
  - `C-24`;
  - `K-65` (K ids are unique across `BACKLOG.md` and `BACKLOG-ENGINE.md`);
  - `T-11`;
  - `U-11` (U-9 was reopened).

## Standing Constraints
- **Freeze (MR-3 / R-3).**
  - No file under `src/data_rover/` changes, unless a Python bug found while porting lands on both sides with a fixture step (D16). Plan 2 did this twice:
    - `value_conforms` crashed on an unhashable float value;
    - key-relationship dirty hooks, `6b3cdb6`.
  - `core/validation` minus `rules/`, `api/validation_sweep.py` and the preview's conformance half stay frozen. Until F, a FEATURE there also lands on both sides with a fixture, because the server still decides strict commits and answers every fallback (ruling R18).
  - Plan 3 is where `rules/` joins the freeze. Say so in the plan.
- **The Python core is the oracle.** Fix the engine, never a fixture; fixtures change only through `pixi run golden-fixtures`.
- **Owner decisions, carried from plan 2:**
  - Strict enforcement and `validation_error_count` stay server-side until F.
  - The artifact and view half of the commit preview stays a server call until F.
  - A staged `metamodel.rebind` sends the whole preview to the server until plan 7.
- **What plan 3 inherits** (plan 2's "After this plan", and built this way):
  - `LiveIssues` takes a seventh validator.
  - `expand_scope` joins the dirty rules.
  - The origin-probe cache key `(rev, staged_version)` must widen with an `artifacts_version`, which doesn't exist yet.
  - The 501 `reaches validation rules` (engine `service.ts`, shell `FALLBACKS` → `'rules'`) is deleted.
  - `rules_status` in the bodies is hard-coded `{total: 0, skipped: [], eval_errors: {}}` today.
- **The dirty rules as built** (AD-32). Keep them when adding rule reach:
  - stage fires the Python hooks from the applier;
  - a rebase takes the neighbourhood of every id it may touch, before and after, and carries the ends of every relationship it touches on both sides;
  - a coalesced edit takes its trial run's hooks plus the rebase rule over the batches the merge replays.
  - Plan 2's first premise ("every input a rebase can change is an entity it touches") was wrong and only seeded invariants caught it. Plan 3 must state its reach premise and hold it with seeded invariants, not only fixtures.
- **The `issues` gate as built** (`replica.svelte.ts`). It opens with engine staging, AND the replica's FIRST sweep complete, AND the artifact follower `loaded()`.
  - A re-sweep does not close it. It closes when the replica leaves `ready` or a new replica starts. A frozen replica keeps it.
  - The engine posts a bare `changed` with a moved `issues_version` when `resolvesKind('validation_rules')` flips. Plan 3 must replace that flip with real rule-set changes moving `issues_version`.
  - Engine 409s (`stale staged batches`, `stale base_rev`, `replica is not ready`) fall back to the server with the whole request.
  - An unstaged `validateModel` is never shadow-compared (C-23).
- **Plan-writing lessons (plans 1–2):**
  - Spell out the startup and commit windows.
  - Say how a same-run read sees a staged change.
  - Gate reads that come before the data has loaded to the server; for rules, artifacts can arrive after the sweep (the final-review bug of plan 2).
  - The 501 fallback never guesses.
  - Probe message texts against Python.
  - Parity at M must inject violations; a 0-vs-0 comparison proved nothing.
- **Process.**
  - Build with `/build-plan` (agent types `implementer`, `critical-implementer`, `task-reviewer`, `branch-reviewer`, `chores`; no `model` parameter).
  - One commit per task, on a branch cut from `engine-migration`.
  - Ask the owner before any fast-forward; never push; never touch `main`.
- **Benchmarks.** Report numbers to the owner before optimizing anything.
- **Local files.** `docs/` and `benchmarks/` are git-ignored: never `git add -f`.

## Done
- Plans 1 and 2 of C are built and merged into `engine-migration`.
- Plan 2 delivered:
  - six validators and a scoped pipeline;
  - dirty sets and the issue store;
  - `LiveIssues` (the sweep, revalidation, the origin probe);
  - the service methods `getModelIssues`, `validateModel` and `previewCommit` (model half);
  - `issues_version` and `progress sweep`;
  - the `issues` surface, defaulting to `engine`;
  - e2e `eval-issues.spec.ts`;
  - AD-32 and CT-4 updates;
  - `pixi run engine-parity-large`.
- The spec's local edits for plan 2 (§3 Event / Surface / Origins) are made.

## Known Issues, Not Yet Fixed
- **C-23:** the server's full `validate` names one cycle representative; the engine's scoped runs name every element on the chain.
- **K-58:** `change_request_dirty_ids` (`src/data_rover/core/validation/dirty.py`) is dead outside tests and shares the key-relationship gap. Fix it or delete it.
- **K-59 to K-63** (performance and races):
  - K-59: the sweep's first step, 13 ms at M, and a 21 ms browser slice.
  - K-60: `uniqGroupOf` is O(group).
  - K-61: a probe runs on every refetch.
  - K-62: a server refetch can overwrite the engine answer.
  - K-63: past the 5,000 cap, staged issues are not listed.
- **K-64** (`BACKLOG.md`): the migration CLI's `migration/legacy.py:332` `_coerce_scalar` raises TypeError on a dict or nested-list float.
- **T-10:** failure and race paths are untested, including a probe replay failure → diverge, and `recheck` against a real engine.
- **U-9, reopened:** the DiffDrawer's `Dialog.Content` grid has no `min-width: 0`. A long unbroken staged value pushes the Commit button off-screen. The e2e works around it with focus + Enter.
- **Test noise:** `frontend/src/lib/state/__tests__/replica.svelte.test.ts` logs MSW unhandled `GET /model/issues` in about three tests. Fix: serve `/model/issues` in `realReplica`'s defaults.
- **Duplicate id:** R-3 exists in both `BACKLOG.md` and `BACKLOG-ENGINE.md`. This predates plan 2.
- K-49…K-57 (plan 1) remain open.

## Deferred — Do Not Do
- **A full-run (`Scope.all`) port.** Deferred to plan 7 by plan 2 (D1): the candidate validation decides it.
- **Driving the open journey's progress bar from the engine sweep.** Rejected in plan 2 (D12): the sweep feeds the gate only.
- **Optimizing K-59, K-60 and K-61.** Deferred to the owner's call (report-before-optimizing rule). Only `SWEEP_STEP` was tunable, and it stays 512.
- **K-50 and K-51.** Not folded into plan 2 (D14). Leave them unless plan 3 needs them.
- **Server-side strict enforcement and `validation_error_count`, the artifact/view preview half, and rebind previews.** These stay on the server (owner decisions above).
- **The JWT-leeway flake fix.** The owner's separate call, on its own branch.
- **Fixing U-9's CSS.** Recorded, not scheduled. It is out of C's scope unless the owner says otherwise.

## Plan
1. Orient (the list above). Then trace the Python rules stack end to end:
   - the parser and caps;
   - `RuleSet` normalization;
   - compile (drift check, `applies_types`, `rules_by_type`);
   - `derive_paths` / `expand_scope`;
   - every semantic of the validator;
   - `rules_status` and `eval_errors`;
   - how `session_pipeline` and `attributable_issues` use rules.

   Write the traces to this session's scratchpad. Probe message texts and edge semantics with throwaway Python through `pixi run -e core-dev`: `_eq`'s `True ≠ 1`, missing values, list matching, `contains`, relationship atoms over descendants with `to` / `where`, dangling far ends.
2. Trace what plan 3 changes in the engine and the shell:
   - `LiveIssues` / pipeline / dirty (the seventh validator and reach joining the dirty sets);
   - `artifacts_version` and the probe cache key;
   - `issues_version` on rule-set changes;
   - deleting the 501 and the `'rules'` fallback;
   - `rules_status` in the bodies;
   - `/rules/parse` routing;
   - the payloads' `document`;
   - shadow for rules;
   - the gate's interaction with artifacts loading after the sweep.
3. Write the plan in plan 2's shape:
   - golden families for rules;
   - seeded invariants for reach;
   - parity at M with injected rule violations;
   - the freeze row for `rules/`.
4. Self-review against the spec. Then present it to the owner for approval, and ask whether commits are pre-approved for the build.
5. On approval, write a handoff for the `/build-plan` session.

## Open Questions
- **Commits:** are they pre-approved for plan 3's build? Ask at plan approval.
- **Performance:** does the owner want K-59, K-60 or K-61 addressed before or inside plan 3? A rules sweep adds work to the same steps.
- **U-9:** should it be scheduled anywhere?
