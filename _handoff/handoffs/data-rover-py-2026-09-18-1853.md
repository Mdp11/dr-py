# Handoff: Data Rover client-engine program — execute plan 2 (engine store) inline

## Mission
Data Rover (`/home/mdp/workspace/data-rover-py`) is being re-architected so computation runs in the browser against a full model replica (TypeScript engine in `engine/`), with the Python core as the oracle. Plan 1 (package, value layer, golden pipeline) has landed; plan 2 (Python snapshot v2 + state digest, metamodel, record-graph store, indexes, mutation boundary) is written, pre-verified and approved by the user. This session's job: **execute plan 2 inline** with `superpowers:executing-plans`, Tasks 0 → 8 in order, one commit per task (Task 0 makes two). "Done" = every task committed on `feat/engine-store`, `pixi run dr-test` and `pixi run dr-tidy` green, the branch fast-forward merged back into `engine-migration`.

## Orient First
1. `docs/superpowers/plans/2026-09-18-engine-store.md` — THE plan (5,034 lines, git-ignored). Read lines 1–90 first (header, provenance, "What the oracle taught this plan" :15, Global Constraints :29, File Structure :45); then each task as you reach it (Task 0 at :92, "After this plan" at :5026).
2. `~/.claude/handoffs/data-rover-py-plan2-extract.py` — writes a task's code blocks from the plan ON DISK (see the unicode trap below). Read its docstring.
3. `docs/superpowers/specs/2026-09-18-engine-foundation-design.md` §3 (:110), §4, §7, §8 — the spec, already updated this session to match what the oracle forced.
4. `architecture/README.md`, `architecture/contracts.md` (CT-1, CT-3, CT-7), `architecture/program.md` (MR-3 freeze rule), `architecture/conventions.md`.
5. `CLAUDE.md` — "Engine package (`engine/`)" and the toolchain commands.

Confirm state: `git status --short` (expect only `?? spikes/client_engine/`), `git branch --show-current` (expect `engine-migration`), `git log --oneline -1` (expect `1b31a31`). Tests: `pixi run dr-test`; lint: `pixi run dr-tidy`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` at `1b31a31`. `feat/engine-store` does NOT exist yet — plan Task 0 Step 1 cuts it.
- Uncommitted: clean except untracked `spikes/client_engine/` (24 files, ≈ 200 KB, no binaries; Task 0 Step 4 commits it).
- Tests on the real tree: not re-run this session. Last known (2026-09-18 ~17:50): `dr-test` green — core pytest 2,414 passed / 34 deselected, frontend vitest 2,492, engine vitest 19.
- Verified in scratch this session (2026-09-18 ~18:30–18:50): plan end state = 135 engine tests / 25 files, 2,429 pytest passed / 34 deselected, `tsc` ×2 + eslint + prettier + ruff + mypy + pyright clean; formatter-only copy of `HEAD` = exactly 8 files reformatted, 2,414 passed.
- Git-ignored files written this session: the plan (above) and edits to the spec (§3, §4, §8 wording).
- Scratch (MAY VANISH, not needed to execute): `/tmp/claude-1000/-home-mdp-workspace-data-rover-py/a1d82f09-c592-43fc-a136-246ba4511982/scratchpad/` — `wt/` = the verified final tree over a copy of `HEAD`; `replay.sh` + `replay2.log` = the task-by-task replay and its observed outputs; `plan.template.md` + `make_plan.py` + `variants/` = the plan's generator (re-render with `pixi run -e core-dev python make_plan.py` if the plan must change); `px.sh` = run the repo's pixi env with another cwd.
- `engine/node_modules` is installed. No servers running.

## Standing Constraints
- **Copy the plan's code, do not retype or improve it.** Every block is verbatim from files that passed; the plan's expected outputs are observed ones. If a step's result differs, suspect transcription or environment before the code.
- **Unicode-escape trap (tooling):** the Write/Edit/Bash tool layer decodes 4-digit `\uXXXX` typed into a tool call into literal characters (8-digit `\U0001f600` and `\u{1F600}` survive). Strings stay equal but U+E000/U+FFFF/U+2028 become invisible and the plan's ASCII check fails. So create files with the extractor, which reads the plan from disk: `pixi run -e core-dev python ~/.claude/handoffs/data-rover-py-plan2-extract.py <task> [--only TEXT]... [--skip TEXT]... [--partials] [--dry-run]`. It was verified to reproduce every file of the verified tree byte for byte, the partial edits included (Task 2: test file + `serialize.py`; Task 7: `model.ts`). Filters are path substrings — use them to keep TDD order: scenarios `--only tests/golden`, engine tests `--only engine/test`, implementation `--only engine/src`; Python `--only tests/api` then `--only src/data_rover`. With `--partials` the same filters apply. `engine/src/index.ts`, `tests/golden/scenarios/__init__.py` and `model.ts` appear in several tasks on purpose; the extractor takes the current task's version.
- **User decisions (2026-09-18, this session):** (1) reformat the 8 drifted `src/` files and commit them separately → plan Task 0; (2) branch `feat/engine-store` cut from `engine-migration`, merged back THERE, never into `main`; (3) commit `spikes/client_engine/` as the evidence `architecture/constraints.md` cites → Task 0; (4) inline execution.
- **The Python core is the oracle; MR-3 freeze starts now** for `core/model`, `core/metamodel`, the model-op applier: no behaviour change. A failing golden test means the engine is wrong. Never hand-edit a fixture; regenerate with `pixi run golden-fixtures`. An oracle bug found mid-port is raised with the user, not fixed on one side.
- **Do not relitigate the plan's nine findings** (plan :15–27); each mirrors observed oracle behaviour or a measured need: ancestor's property definition wins on a name clash (`_effective_props` keeps first-seen root → leaf; its comment is wrong); engine never mints ids (`createElement(typeName, id)`); loader stricter than Python only for cross-kind duplicate ids and array-index keys at any depth; `outAt`/`inAt` on `RelRec` keep edge removal O(1); uniqueness buckets `Map<hash, ElementRec | Set>` confirmed by exact `pyKey` text; `verifyConsistent` rebuilds in place because adjacency lives on the records; fixtures carry per-step digest + fingerprint and periodic full dumps (full dumps every step ≈ 1 MB); `decode_snapshot` sniffs v2 by byte prefix because a compact v1 "first line" is the whole document; `Multiplicity.parse` follows Python `int()` except non-ASCII digits.
- **Server writers keep emitting v1.** `encode_snapshot_v2` is called by nothing but tests and the `snapshot_v2` scenario until sub-project B.
- **Engine source rules:** no DOM/Node in `engine/src` (`lib: ["ES2023"]`, `types: []`), erasable syntax only, `.ts` specifiers, no `any` in exported signatures, no `Date.now`/`Math.random`/`Intl` in `src/`; anything observable sorts with `cmpCodePoint`; read data-named properties with `getProp`/`Object.hasOwn`.
- **Use pixi's Node and Python, never system ones.** Format the engine with `pixi run engine-tidy` (prettier 3.9.8 there).
- **`architecture/` is tracked; `docs/` is git-ignored — never `git add -f`.** The plan carries its own `architecture/` edits (CT-1 in Task 2, CT-1 + CT-7 in Task 7, `program.md` status in Task 8) — same commit as the code (RC-10).
- **Git (RC-11):** subject = one imperative sentence, capitalized, no prefix, no period; end each commit with the session's `Co-Authored-By` line. Commit messages are given in the plan.
- **Sub-project A is four plans, written just-in-time.** Plans 3–4 are written only after plan 2 lands.
- **Performance is plan 4's job.** Measured at model M: load ≈ 0.45 s + rebuild ≈ 0.6 s, which with the 1.49 s line-routed parse leaves ≈ 0.4 s of CN-3's 3 s. Do not optimize during plan 2; the two known costs (one `pyKey` text per element ≈ 0.23 s; the deep array-index scan in `readProps`) are recorded in "After this plan".
- Benchmark hygiene (CN-5): timings drift 1.5–1.9× between sessions; compare only within one pass.

## Done
- Plan 1 executed and committed (`e8abbf3`..`1b31a31`); `engine-migration` holds it.
- Plan 2 written to `docs/superpowers/plans/2026-09-18-engine-store.md`, built first in a scratch copy against the real Python core, replayed task by task in a second clean copy (red → green at every step, final tree identical), and cross-checked: rebuilding every file from the plan's code blocks alone reproduces the verified tree.
- Spec updated where the oracle contradicted it (§3 effective properties, §4 record fields / ids / loader / `verifyConsistent`, §8 recorder protocol).
- User reviewed the plan summary and answered the three open questions (see Standing Constraints).
- Extractor helper written and verified (`~/.claude/handoffs/data-rover-py-plan2-extract.py`).

## Known Issues, Not Yet Fixed
- **`K-29` (to be logged by Task 8):** `src/data_rover/api/routes/_snapshot.py` guards keep one `seen_ids` per kind, so `build_model_from_dicts` loads an element and a relationship sharing an id; the mutation boundary and the CT-3 digest treat ids as one namespace; the engine loader refuses it. Verified by reading the guards and by the engine unit test. Log only — not fixed in plan 2.
- **`C-20` (to be logged by Task 8):** `src/data_rover/core/metamodel/schema.py:198-211` — comment says closer types win, code keeps the ancestor's definition. Verified by running the oracle. Frozen under MR-3; log only.
- `Multiplicity.parse`: Python `int()` reads non-ASCII digits, the engine refuses them (documented gap, no fixture).
- No CI in the repo; `tests/golden/test_fixtures_current.py` inside `core-test` is the only drift enforcement. `dr-tidy` does not lint `tests/` (BACKLOG `O-3`) — the plan runs ruff on `tests/golden` by hand.

## Deferred — Do Not Do
- **Rust/wasm engine** — user chose TypeScript after the cost table; revisit only with profiling evidence.
- **Python core under Pyodide as the engine; orjson in Pyodide; Pyodide memory snapshots** — benchmarked dead ends (CN-22).
- **Server-side strict-mode verification / periodic headless audit** — user chose client-enforced.
- **Merging into `main`** — user asked only for the `engine-migration` integration branch.
- **`pyEquals`, `isPlainObject`, `engine-bench`, ambient `TextDecoder` declaration** — cut from plan 1 as YAGNI; they belong to C / plan 4.
- **Writing plans 3–4 now** — just-in-time after plan 2 lands (user's process choice).
- **Subagent-driven execution** — offered, user chose inline.

## Plan
1. Verify state with the three git commands above; invoke `superpowers:executing-plans`; read plan lines 1–90.
2. **Task 0** (plan :92): `git switch engine-migration && git switch -c feat/engine-store`; `pixi run core-format` (expect 3 files reformatted) and `pixi run backend-format` (expect 5); `pixi run core-test` (expect 2,414 passed / 34 deselected); commit `Apply the formatter to the files that drifted`; then `git add spikes/client_engine` and commit `Track the client-engine spike behind the measured constraints`.
3. **Tasks 1 → 7** in order, following each task's steps literally: write tests (extractor with `--only`), run and see the stated failure, write the implementation (extractor with `--skip`/`--only`, `--partials` for Task 2 and Task 7 Step 5), run and see the stated pass, tidy, commit with the plan's message. Generated fixtures come from `pixi run golden-fixtures`, never from the plan. Do the `architecture/contracts.md` edits of Task 2 Step 6 and Task 7 Step 7 by hand (prose, no escapes).
4. **Task 8**: `pixi run dr-test` (expect core 2,429 / 34 deselected, frontend 2,492, engine 135 in 25 files), `pixi run dr-tidy` (expect no changes), then the `CLAUDE.md`, `BACKLOG.md` (R-3 text, new `K-29`, new `C-20` row) and `architecture/program.md` edits as written; commit.
5. Report to the user, then fast-forward merge: `git switch engine-migration && git merge --ff-only feat/engine-store` (approved by the user; do not touch `main`).
6. Write the next handoff: plan 3 (op applier + working copy) is written with `superpowers:writing-plans`, starting from the plan's "After this plan" section (:5026).

## Open Questions
None.
