# Handoff: Data Rover client-engine program — write plan 2 of sub-project B ("Snapshots and replica routes")

## Mission
Data Rover (`/home/mdp/workspace/data-rover-py`) is moving computation into the browser against a full model replica (TypeScript engine in `engine/`), with the Python core as the oracle. Sub-project B (replica and frontend seam) is six plans; **plan 1 (exact server state) is built and merged**. This session's job is to **WRITE plan 2** — spec §2: v2 snapshot writers, `Snapshot.format`, the `GET /replica/snapshot` descriptor, `GET /replica/snapshots/{rev}` blob, `GET /replica/tail`, `X-Metamodel-Id` — **as a plan, not a build**, and get the owner's approval. "Done" = plan 2 written to `docs/superpowers/plans/`, reviewed, approved by the owner. Nothing is implemented in this session.

## Orient First
1. `docs/superpowers/specs/2026-09-19-replica-and-frontend-seam-design.md` (git-ignored) — B's approved spec. §2 (lines ~104-128) is plan 2's whole scope; §9 its tests, §10 its `architecture/`/backlog edits, §11 the plan list (its "each pre-verified in a scratch clone" is SUPERSEDED — see Standing Constraints).
2. `docs/superpowers/plans/2026-09-19-exact-server-state.md` — plan 1, the house format to follow (header, Global Constraints, File Structure, task/step layout). Its "After this plan" section (end of file) lists exactly what plan 2 inherits.
3. `architecture/contracts.md` — CT-1 (snapshot v2 format), CT-2 (delta + tail shape), CT-3 (digest), CN-11 (no `Content-Encoding`). `architecture/program.md` (B), `architecture/conventions.md`.
4. Server code plan 2 touches: `src/data_rover/api/snapshot_codec.py` (`encode_snapshot_v2`, `decode_snapshot`), `src/data_rover/api/hydration.py` (`write_snapshot` — the one funnel of every snapshot writer), `src/data_rover/api/db_models.py` (`Snapshot`, `Commit.state_digest`, `Commit.entity_states`), `src/data_rover/api/commit_states.py` (`load_entity_states`, `recreated` key), `src/data_rover/api/session.py` (`Session.state_digest()` — call under `write_mutex`), `src/data_rover/api/routes/metamodel.py` (`GET /metamodel`), `src/data_rover/api/snapshot_job.py`, `src/data_rover/api/importer.py`, `alembic/versions/0015_commit_state_digest.py` (latest revision; plan 2's migration is 0016).
5. `~/.claude/handoffs/data-rover-py-2026-09-19-1207-server-map.md` — server code map (line numbers as of `0dd5c3d`, stale-ish).
6. `CLAUDE.md` — commands; the "Durable persistence" section describes snapshots/journal as they are today.
7. `BACKLOG-ENGINE.md`, `BACKLOG.md` (`K-33`) — open items.

Confirm state: `git status --short` (expect clean), `git branch --show-current` (expect `engine-migration`), `git log --oneline -1` (expect `1cd75f9`).
Tests: `pixi run dr-test`; lint `pixi run dr-tidy true` (check-only; NOTE `dr-tidy check_only=true` is rejected by this pixi — the arg is positional).
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` at `1cd75f9` (plan 1's four commits `ce66161`, `50f1d76`, `1d94e1b`, `1cd75f9` fast-forwarded on top of `d086ff7`). `main` untouched. `feat/exact-server-state` still exists, merged, not deleted.
- Uncommitted: clean (verified 2026-09-19 14:35).
- Tests (as of 2026-09-19 ~14:25, on `1cd75f9`): core 2,465 passed / 34 deselected; frontend 2,493 tests in 252 files; engine 272 tests in 39 files; `dr-tidy true` clean; golden fixtures current; `tests/api/test_alembic.py` 6 passed.
- `benchmarks/large.model.json`, `benchmarks/large.snapshot.v2` exist (git-ignored). `engine/node_modules` installed. No servers, no background jobs.

## Standing Constraints
- **Planning is planning (owner, 2026-09-19).** A plan gives implementation direction with specifics — interfaces, signatures, test cases, order, the tricky mechanism spelled out — but NOT full code of everything, NOT a build of the implementation, and NO running of the implementation's tests. Reason: building while planning cost hours before the owner saw anything and made decisions in code before they were approved. Where `superpowers:writing-plans` demands complete code in every step, the owner's instruction wins.
- **Cheap probes ARE allowed while planning (owner, 2026-09-19 14:35)** — e.g. a measurement the spec demands before approval, or checking a fact the plan rests on (a column, a call site, a timing). Not a build, not the implementation's test suite.
- Plan 1 ran inline with `superpowers:executing-plans` (owner's choice over subagent-driven); the owner gave a blanket go-ahead to commit per task + ff-merge for plan 1 only — ask again for plan 2's execution.
- The owner answers design questions fastest as ONE multiple-choice question at a time, recommended option first.
- Measured and reported, owner raised no objection — **do not optimize, ask first if a number looks wrong**: order repair after a re-inserting rollback ≈ 70 ms / 106 ms at M; rollback of 1,000 updates 5.7 ms; first digest after hydration / `set_model` / `touch_model` = one 286 ms pass under `write_mutex`.
- `Model.settle_order()` REPLACES the entity dicts; never keep a reference to `model.elements` / `model.relationships` across it (read routes iterate them without `write_mutex`).
- `Session.state_digest()` must be read under `write_mutex`; it costs one full pass when `state_digest_value` is None (fresh/hydrated session, after `set_model`/`touch_model`).
- `Commit.state_digest` is NULL on rows older than migration 0015 and on baseline markers; `entity_states` lacks the `recreated` key on older rows (loads as none named) and is NULL over `ENTITY_STATES_MAX` — the tail route must answer `complete: false` for any of these (spec §2).
- `prev_rev` is NOT stored on `Commit` — the tail derives it from the row before. `recreated_*` are their own lists, never repeated in `deleted_*`. `/model/ops` and `/model/undo` stay silent on the feed.
- Still in force: MR-3 freeze on `core/model`, `core/metamodel`, the op applier; the Python core is the oracle, fixtures change only via `pixi run golden-fixtures`; `engine/src/` has no DOM/Node built-ins, erasable syntax, `.ts` specifiers, no `Date.now`/`Math.random`/`Intl`; `dr-tidy` lints neither `tests/`, `scripts/` nor `alembic/` — plans must say to run ruff on those by hand; RC-11 git (work on `feat/<topic>` from `engine-migration`, `git merge --ff-only`, never touch `main`; subjects one imperative capitalized sentence, no prefix, no period; message ends with the session's `Co-Authored-By` line); commit only when the owner asks; never `git add -f` `docs/` or `benchmarks/`; `architecture/`, `CLAUDE.md`, backlogs change in the same commit as the code (RC-10).
- Ids: next K is `K-34`, next C is `C-21`, next AD is `AD-26`.
- Plan-writing lessons from plan 1's execution (bake into plan 2): a "see it fail" step should expect ALL tests that go red (a shared test helper touching a missing field fails many more than the new tests); pre-existing lint in touched test files (e.g. `UP037` at `tests/api/test_feed_hub.py:12`, `tests/api/test_commit_states.py` unformatted) must not be promised clean; `pixi run dr-tidy true`, not `check_only=true`.

## Done
- Plan 1 (exact server state) implemented from `docs/superpowers/plans/2026-09-19-exact-server-state.md`, every step run, all bite checks run (including the 3 of Task 4 never run before) and reversed by hand, merged into `engine-migration` at `1cd75f9`. `K-30`, `K-31` closed; `K-33` logged. Reported to the owner.
- B's spec, `architecture/` promotions (`d086ff7`); sub-project A complete.

## Known Issues, Not Yet Fixed
- `K-33` (open, `BACKLOG.md`): staged branch of `POST /model/validate` (`src/data_rover/api/routes/validation.py` ~:200-211) never calls `session.invalidate_derived_caches()` after its rollback, as `POST /commits/preview` does. One-line fix, outside plan 1/2 scope.
- `K-29`, `K-32`, `C-20` open in `BACKLOG-ENGINE.md` (`K-32` is B's plan 3).
- Pre-existing, not logged: `UP037` at `tests/api/test_feed_hub.py:12`; `tests/api/test_commit_states.py` not ruff-formatted; stale docstrings at `src/data_rover/api/routes/commits.py` ~:770 and ~:901 (say apply-CR replaces the model; it is a dry run); server `_decode_v2` accepts a line holding two documents, the engine refuses (no fixture).
- No CI; `tests/golden/test_fixtures_current.py` in `core-test` is the only fixture drift guard.

## Deferred — Do Not Do
- **Building or test-running the implementation while writing plans 2–6** — declined by the owner (cheap probes only).
- **Landing a scratch build as the implementation**, **replacing a plan with a short decisions document** — declined by the owner (plan 1 era; the "plan ships as a plan" stance stands).
- Everything in "Deferred — Do Not Do" of `~/.claude/handoffs/data-rover-py-2026-09-19-1207.md` (neighborhoods, hybrid warm-up, resumable transitions, optimizing the `ord` re-sort, feed events from `/model/ops` and `/model/undo`, and the older list).

## Plan
1. Verify state (three git commands above). Read spec §2, §9, §10 and plan 1's header + "After this plan".
2. Map the code plan 2 touches: every caller of `hydration.write_snapshot` (periodic job, evict, rebind-forced, baseline `persist_baseline`, importer, clone), `Snapshot` model + the store key naming, `decode_snapshot`'s v2 path, how `Commit` rows are read (`content.py`), and where `GET /metamodel` is served. Cheap probes allowed (e.g. encode cost of a v2 snapshot at M from `benchmarks/large.model.json`, if the spec's synchronous-write-under-`write_mutex` fallback needs a number before approval).
3. Raise design questions the spec leaves open to the owner, one multiple-choice at a time, recommended option first, before fixing them in the plan.
4. Write `docs/superpowers/plans/2026-09-19-snapshots-and-replica-routes.md` (or today's date) in plan 1's format: header, Global Constraints, File Structure, tasks with TDD order, test cases named with what they assert, signatures, the tricky mechanisms spelled out (tail completeness rules, "newest v2 snapshot reachable by a complete tail ≤ 1,000 revs", synchronous head write under `write_mutex`, `Content-Length` without `Content-Encoding`), doc/backlog edits per §10, and an "After this plan" for plan 3 — without full code and without building it.
5. Self-review the plan, then present it to the owner for approval. Ask how plan 2 should be executed (inline vs subagent-driven) and whether commits are pre-approved.

## Open Questions
- None blocking the start. Design questions surfaced while planning go to the owner one at a time.
