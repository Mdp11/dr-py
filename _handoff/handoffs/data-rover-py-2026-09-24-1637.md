# Handoff: write plan 2 of sub-project C (validation core)

## Mission
Write plan 2 of 8 of sub-project C (Evaluation) for `/home/mdp/workspace/data-rover-py`: the validation core. That is spec §3 minus rules: port `core/validation` (six validators, pipeline, dirty expansion), the engine's live `IssueStore` with a resumable background sweep and incremental revalidation inside every transition, origins by a rewind probe, the `issues` surface (`getModelIssues`, `validateModel`, `previewCommit`'s model half) and the sweep gate. Use superpowers:writing-plans, working from the approved spec. Done = a plan file at `docs/superpowers/plans/2026-09-2x-eval-validation-core.md`, written in plan 1's shape and approved by the owner. Building it is a later `/build-plan` run.

## Orient First
1. `docs/superpowers/specs/2026-09-24-evaluation-design.md`: the binding spec.
   - §3 "Validation, issues, rules": everything except `### Rules`, which is plan 3.
   - §2 "Shadow": issues compare as a multiset, only once the sweep is complete; `previewCommit` is compared WITH staged ops.
   - §7: the plan-2 families `validation_kinds`, `validation_dirty`, `validation_steps`, and the sweep benchmark gate.
   - §8: the plan-2 freeze row.
   - §9: the ADs to add (one live issue store, origins by rewind probe; `issues_version` on `changed`).
   - §11: item 2 is this plan's scope.
2. `docs/superpowers/plans/2026-09-24-eval-artifacts-navigation-search.md`: plan 1, built. Copy its shape: "What planning found" (probe-verified facts), Decisions, Global Constraints, Review Focus, Mechanisms, Tasks.
3. `CLAUDE.md`: the map, commands, and cross-cutting rules.
4. `architecture/README.md`, then `architecture/program.md` (MR-1…MR-4; the MR-3 freeze text was corrected at f7c7136), `architecture/contracts.md` (CT-4, CT-5), `architecture/decisions.md` (AD-8, AD-23, AD-30, AD-31), `architecture/conventions.md`.
5. `src/data_rover/core/README.md` (validation pipeline) and `src/data_rover/core/validation/`: the oracle. Also `src/data_rover/api/validation_sweep.py` and the preview's conformance half in the commits/preview route.
6. `engine/README.md`: the scheduler (restartable background kind; this plan adds a resumable one), transitions, `src/evaluate/` and `EVALUATIONS`, `src/artifacts/`.
7. `frontend/src/lib/engine/README.md` (surfaces, seam, shadow, the artifact follower) and `frontend/README.md` (required before touching `frontend/src/lib/state/`).
8. `BACKLOG-ENGINE.md`: K-49…K-57 are open items from plan 1; R-3 is the freeze list.

Confirm state: `git status --short && git branch --show-current && git log --oneline -3`.
Tests: `pixi run dr-test`. Lint: `pixi run dr-tidy true`, `pixi run engine-check`, `pixi run frontend-check`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` at `f7c7136`, clean. Plan 1 was fast-forwarded into it and `feat/eval-navigation` deleted. Nothing is pushed and `engine-migration` has no upstream.
- Tests (2026-09-24 ~16:30, at f7c7136): `dr-test` green. Core 2,571 passed / 34 deselected; frontend 2,954 in 283 files; engine 1,089 in 70 files; sandbox 14.
- e2e (as of bcae61e): 62 passed, 2 failed. The failures are pre-existing stale selectors, filed as T-8 and T-9 in `BACKLOG.md`. No `[shadow]` lines.
- `docs/` is git-ignored. The spec and plans live there; never `git add -f`.
- Next free ids: `AD-32`, `K-58`, `C-23`. Also `T-10` in `BACKLOG.md`.
- Engine bench data from 2026-09-22 exists (`pixi run engine-bench`).

## Standing Constraints
- The owner's C decisions are in spec "Decisions" 1–8. Binding here:
  - one live issue store in the engine over the working copy (sweep, incremental per transition, origins on demand);
  - strict enforcement and the server's `validation_error_count` stay server-side until F;
  - the artifact/view half of the commit preview stays a server call until F;
  - a staged `metamodel.rebind` sends the whole preview to the server until plan 7.
- Plan 1's rulings that carry over:
  - the artifact methods are `now` context methods, not model-lane transitions (the spec §1 text is updated);
  - the fallback mark is `fallback: 'script' | 'pattern'`, not `source: 'server'`;
  - new surfaces default to `server`, sit outside the staging force (`READ_SURFACES`), and flip in their plan's last task;
  - a 501 from the engine routes the call to the server, never a guessed answer (CT-7).
- MR-3 freeze, after the correction at f7c7136:
  - `core/model`, `core/metamodel` and the model-op applier stay frozen.
  - `core/navigation`, `core/search`, `api/search.py` and the two route functions stay frozen until tables and exports (plans 4–5) default to the engine.
  - `core/table/resolve.py` is frozen from plan 1.
  - Plan 2 adds `core/validation` minus `rules/`, `api/validation_sweep.py` and the preview's conformance half.
  - A bug lands on both sides with a fixture. The Python core is the oracle: fix the engine, never a fixture.
- Owner's execution preferences:
  - Plans are "direction with specifics": interfaces, test cases, mechanisms, no full code. Probe facts with throwaway Python probes and list them with how they were checked.
  - `/build-plan` runs subagent-driven development with agent types `implementer` / `critical-implementer` / `task-reviewer` / `branch-reviewer` / `chores`. Tag each task in the plan with its agent.
  - Commits: ask the owner before the build starts; one commit per task; ask before fast-forwarding `engine-migration`; never push; never touch `main`.
- Lessons from plan 1 for the new plan:
  - Spell out the shell's startup and commit windows (first load, commit refresh, failed fetch). Plan 1 needed three fix rounds there.
  - Say how a same-run read sees a staged change.
  - Say what reads the engine answers before its data has loaded (gate to the server, as plan 1's `loaded()` gate does).
  - Validate every field pydantic validates (D5: loud 422, never silent 200).
  - Check any warm-up or whole-list parse against the 16 ms chunk budget (CN-3).

## Done
- Plan 1 of C is built, reviewed and merged into `engine-migration` (4fd6cf6..f7c7136, 15 commits):
  - value helpers `jsStr` / `toNumber` / `pyFloatOf` / `pyReprValue`;
  - `translatePyRegex` (ok / invalid / unsupported; its `fullmatch` mode is ready for pattern facets);
  - criteria and `searchModel`;
  - `ArtifactSet` with `setArtifacts` / `putArtifacts` / `setStagedArtifacts`;
  - navigation and `evaluateNavigation`;
  - `GET /artifacts/payloads`;
  - the shell artifact follower (`frontend/src/lib/engine/artifacts.ts`) and the staged mirror;
  - the `navigation` and `criteria` surfaces, defaulting to the engine (navigation is gated on the follower's first load);
  - `route()`'s 501 fallback;
  - AD-30 and AD-31, CT-4 and CT-5.5 updated; C-20 closed; C-22 logged.
- The plan-1 SDD workspace has been deleted. Git history is the record.

## Known Issues, Not Yet Fixed
- Open from plan 1, all in `BACKLOG-ENGINE.md`:
  - K-49: a peer's new navigation named before its payload lands gives 422.
  - K-50: a staged artifact edit can miss a read made in the same run; the D10 hold is possibly redundant.
  - K-51: `setArtifacts` goes over CN-3 (whole-list exact parse), and every open loads payloads twice.
  - K-52: superseded navigation previews are not aborted.
  - K-53: no timeout on the follower's fetch.
  - K-54: the Advanced Search pattern fallback is unmarked.
  - K-55: the criteria warm-up can freeze the worker.
  - K-56: `sendArtifacts` swallows refusals.
  - K-57: missing `to_number` fixture rows.
- Doc slips left after the plan-1 final review (unfixed, no behaviour impact):
  - `BACKLOG-ENGINE.md`: K-55 cites `criteria.ts:124-125` (the warm-up is `:258-259`); K-53's path should be `frontend/src/lib/engine/artifacts.ts`; the new K-items say "Suggested fix:" where older ones say "Fix direction:".
  - AD-31 (`architecture/decisions.md` ~:277) understates the startup window when both loads fail: it stays open until the next feed snapshot.
  - `architecture/program.md` ~:113-121 and `BACKLOG-ENGINE.md` ~:16-23:
    - the MR-3 text ends bug parity at plans 4–5, where MR-3's rule says "until F";
    - the reason given ("still read them") is too broad for `api/search.py` and the route functions, which stay frozen as the 501 fallback;
    - "plans 4–5" and "plan 1" don't name C.
  - The `engine/src/navigation/route.ts:95-100` docstring lists pattern translation before the script refusal; the code refuses the script first.
- Other open items, outside C: K-29, K-32, K-35, K-36, K-38, K-41, K-42, K-45…K-48, C-21.

## Deferred — Do Not Do
- Rules (compile, reach, evaluation, `POST /rules/parse`, rules in `/artifacts/payloads`) are plan 3, not plan 2 (spec §11). The owner decided they reach the engine as server-parsed JSON.
- Scripts in the browser are sub-project D. `previewTransform`, `fetchScriptErrors` and `runSnippet` stay server calls (spec non-goals).
- The owner put server-side strict enforcement and `validation_error_count` in F.
- `POST /model/validate`'s `inline` branch, `POST /model/save` and `GET /exports/run-by-name` are spec non-goals (E).
- No caching of navigation or search results between pages; the server keeps none.
- The JWT-leeway flake fix is the owner's separate call, on its own branch.

## Plan
1. Confirm state (Orient First commands). Read spec §3 (minus Rules), §2 Shadow, and the §7/§8/§11 rows for plan 2, then plan 1's plan file for shape.
2. Invoke superpowers:writing-plans for "C plan 2: validation core" from the spec. Probe the Python oracle while planning (`pixi run -e core-dev`, `PYTHONPATH=src`):
   - validator messages and `pyRepr` formatting;
   - `DirtyCollector` expansion per mutation kind;
   - the `Scope` walk order;
   - `validate_global`;
   - the preview's conformance half and `attributable_issues`;
   - `/model/status` progress fields;
   - how the frontend's `scheduleIssuesRefetch` and open-progress refetch are triggered.

   List each probed fact with how it was checked.
3. Decide in the plan, with reasons:
   - the scheduler's resumable background kind;
   - the `issues_version` field on `changed`;
   - the origin probe's cache key `(rev, staged_version, artifacts_version)`. `artifacts_version` does not exist yet (plan 1 omitted it by ruling R7), so plan 2 adds it or keys without it until plan 3;
   - the `issues` surface's gate before the sweep completes (plan-1 lesson);
   - whether K-50/K-51 are folded in or left.

   Record the goldens `validation_kinds`, `validation_dirty` and `validation_steps` via `tests/golden/model_steps.py`. Plan the sweep benchmark at M with a report to the owner before optimizing.
4. Tag each task `implementer` or `critical-implementer`. Fill in the baseline (the counts above, re-run). Get the owner's approval.
5. Then ask whether to start `/build-plan` on a new branch cut from `engine-migration` (e.g. `feat/eval-validation`), and whether commits are pre-approved.

## Open Questions
- Should the doc slips under Known Issues be fixed as a small `chore` commit on `engine-migration` before plan 2 starts, or folded into plan 2's first task?
- Should any of K-50…K-57 be pulled into plan 2? K-50's staged-edit ordering and K-51's `setArtifacts` budget touch the same transition and scheduling paths the sweep uses.
