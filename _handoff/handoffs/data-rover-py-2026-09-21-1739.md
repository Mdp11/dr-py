# Handoff: Data Rover client-engine program — BUILD plan 3 of sub-project B ("Engine service")

## Mission
Data Rover (`/home/mdp/workspace/data-rover-py`) is moving computation into the browser against a full model replica (TypeScript engine in `engine/`), with the Python core as the oracle. Sub-project B has six plans; plans 1 and 2 are built and merged into `engine-migration`. Plan 3 — the engine service: CT-4 dispatcher, scheduler, the index build / digest check / big sorts in steps, the five read surfaces — was WRITTEN and APPROVED by the owner on 2026-09-21. This session BUILDS it: eleven tasks, one commit each, on `feat/engine-service`, inline with `superpowers:executing-plans`. "Done" = all eleven tasks landed, every suite and linter green, the branch fast-forwarded into `engine-migration` with the owner's go-ahead, and the result reported to the owner.

## Orient First
1. `docs/superpowers/plans/2026-09-19-engine-service.md` (git-ignored, 534 lines) — THE plan. Read it whole before Task 1: "What planning found" (:15, 21 checked facts), Decisions (:41), Global Constraints (:65), File Structure (:85), Mechanisms M1–M11 (:142), Tasks 1–11 (:192–:483), Known limits (:512), After this plan (:524). It holds no code on purpose: you write the tests and the code; its "see it fail" expectations are reasoned, not observed.
2. `docs/superpowers/specs/2026-09-19-replica-and-frontend-seam-design.md` — §3 (~:130) and §4 (~:176) are the scope, §9 (~:296) the tests, §10 (~:317) the doc edits. Where the plan and the spec differ, the plan wins and says why (facts 6, 11, 12; D3, D4, D5, D7, D8, D11, D12).
3. `CLAUDE.md`, "Engine package" — the engine's modules and the golden-fixture pipeline as they stand.
4. `architecture/contracts.md` (CT-2…CT-5, CT-7), `architecture/constraints.md` (CN-3), `architecture/system.md` rule 4, `architecture/decisions.md` (AD-13, AD-23; AD-26 is this plan's to add), `architecture/conventions.md`, `BACKLOG-ENGINE.md` (`K-32`).
5. Code the tasks build on: `engine/src/working/working-copy.ts`, `engine/src/snapshot/open.ts`, `engine/src/model/{model,indexes,root-order,naming}.ts`, `engine/src/ops/apply.ts` (`errorDetail`, `applyPatch`), `engine/test/golden/model-steps.ts`, `engine/test/working/{helpers,invariants.test,random-ops}.ts`, `tests/golden/{driver,model_steps}.py`, and the oracle routes `src/data_rover/api/routes/read.py` + `routes/elements.py::get_element` (FROZEN — read, never edit).

Confirm state: `git status --short` (expect clean), `git branch --show-current` (`engine-migration`), `git log --oneline -1` (`e5688f9 Update BACKLOG-ENGINE`).
Commands: `pixi run dr-test` (core + frontend + engine); `pixi run engine-test`; `pixi run engine-check`; `pixi run engine-tidy`; `pixi run golden-fixtures`; `pixi run -e core-dev pytest tests/golden -q`; `pixi run engine-bench` (its input `benchmarks/large.snapshot.v2` already exists); check-only lint `pixi run dr-tidy true`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` at `e5688f9`. `feat/engine-service` does NOT exist yet — Task 1 Step 1 cuts it. `feat/replica-routes` and `feat/exact-server-state` still exist (merged, not deleted). `main` is untouched.
- Uncommitted: clean (as of 2026-09-21 17:39). The plan lives under the git-ignored `docs/`.
- Tests: NOT re-run this session. Last known (2026-09-19 ~17:05, on `18058bb`; `e5688f9` changed `BACKLOG-ENGINE.md` only): core 2,551 passed / 34 deselected; frontend 2,493 in 252 files; engine 272 in 39 files; `dr-tidy true` clean; `golden-fixtures` unchanged. Run `pixi run dr-test` once before Task 1 to have a baseline of your own.
- Toolchain seen today: Node v22.22.3 (Unicode 17.0), Python 3.14.5 (Unicode 16.0.0). `engine/node_modules` is installed.
- No servers, no background jobs. No Docker in this WSL distro, no Postgres on :5432 (nothing in this plan needs either).
- The planning probes were throwaway scripts in a session scratchpad that may be gone; their numbers are in the plan's facts. Do not look for them and do not re-create them in the repo.

## Standing Constraints
- **The plan is approved as written, D1–D16 included.** D1–D3 are the owner's own answers (slice both big sorts; a transition waits behind earlier reads; deltas cross into the engine as JSON text). D4–D16 were shown to the owner with the plan and approved with it. Do not relitigate them; if execution shows one is wrong, stop and say so.
- **Commits are NOT pre-approved.** "Approved" covered the plan. Ask the owner before Task 1 whether commits are pre-approved for this plan's execution (the plan's Task 1 Step 1), and ask again before the final fast-forward merge.
- **The owner maintains two logs; keep both.** A new finding that needs a decision or a fix goes into `BACKLOG-ENGINE.md` (ids unique across it and `BACKLOG.md`; next free `K-37`, `C-22`; this plan uses `AD-26`, then `AD-27` is free). Anything that loses you time gets appended to `~/code/papercuts.md` as `date · symptom · fix · project`; check that file first when tooling fails mysteriously.
- **The owner answers design questions fastest as ONE multiple-choice question at a time**, recommended option first.
- **Freeze rule MR-3:** `core/model` (with `naming.py`), `core/metamodel`, the model-op applier, `routes/read.py`'s route functions and helpers, and `routes/elements.py::get_element` are frozen. The Python core is the oracle: fix the engine, never a fixture. Fixtures and `engine/src/value/lower-tables.ts` change only via `pixi run golden-fixtures`.
- **Engine `src/` rules:** no DOM, no Node built-in, no timer, no clock, erasable syntax only, `.ts` import specifiers, no `any` in an exported signature; the service gets `now`, `yieldToHost`, `inflate` injected. Tests import through `engine/src/index.ts` only.
- **Performance stance (owner):** build what is written and report numbers; do not optimize. A longest step over 16 ms in Task 11's bench is a finding to report, not to tune. The browser benchmark of plan 5 is the judge.
- **Git conventions (RC-11):** subjects are one imperative capitalized sentence, no prefix, no period (the plan names each task's subject); the message ends with the session's `Co-Authored-By` line. Never `git add -f` `docs/` or `benchmarks/`. `architecture/`, `CLAUDE.md` and the backlogs change in the same commit as their code (RC-10) — every task has a docs step for that.
- `dr-tidy` lints neither `tests/` nor `scripts/`: run ruff check and format by hand on Python files you add or edit. Pre-existing unformatted test files are not yours to fix.
- **Shell habit that cost time today:** a `cd` inside a compound Bash command moves the session's working directory for later calls. Use absolute paths, or start every command with `cd /home/mdp/workspace/data-rover-py &&`.
- **The spec's §4 table shows the 404 text unstripped** (`No element with id 'x'`); the server really answers `No element with id 'x` (fact 11). The route is the oracle; the plan's D11 governs.
- **From plan 2, still true:** a tail delta is exactly `feed.commit_event`'s dict; `head_rev` is the session's `model_rev`; the descriptor's `state_digest` is the digest AT the snapshot's `rev`; the descriptor's `url` is a path; the blob route serves gzip bytes with no `Content-Encoding`. The commit RESPONSE names its revision `model_rev`, the feed and the tail `rev` (fact 15).

## Done
- Plan 1 (exact server state) and plan 2 (snapshots and replica routes): built, merged into `engine-migration` at `18058bb`.
- The backlog edits left open by the previous handoff (`K-35`, `K-36`, `C-21`, `R-3` status): committed by the owner as `e5688f9`. That open question is closed.
- Plan 3: surveyed, probed, three design questions answered by the owner, written, self-reviewed against spec §3, §4, §9, §10, and approved (2026-09-21). Nothing of it is built.

## Known Issues, Not Yet Fixed
- `K-32` (`BACKLOG-ENGINE.md`): this plan's — Task 11 rewrites it in place as the `ord` re-sort watch item.
- `K-29`, `K-35`, `K-36`, `C-20`, `C-21` open in `BACKLOG-ENGINE.md`; `K-33`, `K-34` open in `BACKLOG.md`. None is this plan's to fix.
- There is no CI. `tests/golden/test_fixtures_current.py` is the only guard against fixture drift (after Task 1 it also guards the generated `lower-tables.ts`).
- Diagnosed while planning, handled BY the plan, listed so you do not rediscover them: the 2,000-line parse batch peaks at 36 ms at M (→ 500, D12); the roots sort is one 24 ms step (→ `sortedInSlices`, D1); a `Map` iterator re-reads every entity after `byOrd`'s clear-and-refill (→ D2, D7); Node's `toLowerCase()` differs from Python 3.14's `str.lower()` at 28 code points and `trim()` from `strip()` at 6 (→ Task 1).

## Deferred — Do Not Do
- Options the owner declined on 2026-09-21: leaving the two big sorts native and watching them, or slicing the roots sort only (chose: slice both); letting a transition run mid-scan and restarting the scan, or strict FIFO with nothing interleaving (chose: the transition waits, short reads interleave); deltas as parsed objects, or as transferred bytes (chose: JSON text).
- Caching search results between pages (plan D16; spec non-goal "optimizing anything that meets its budget").
- Tuning step sizes, the slice target or the `ord` re-sort past what the plan states; optimizing anything the bench shows (owner: report, do not optimize).
- Coalescing in place (plan fact 18: it leaves a state no replay reproduces).
- Touching `frontend/`, `sandbox/` or `src/data_rover/`: plan 3 changes `engine/`, `tests/golden/`, and docs only. The shell, the sandbox, surface switches and the forked store are plans 4–6.
- Building or test-running plans 4–6 while writing them (owner: cheap probes only) — relevant when this session hands off to plan 4's writing.
- Still standing from earlier handoffs: optimizing `_decode_v2` or the v2 encode (`K-34`: watch only); reading the descriptor from the blob header or a single JSON `header` column; landing a scratch build as an implementation, or replacing a plan with a short decisions document; refactoring `create_commit`'s scope computation onto `replica.scope_of_ops` (C-12); and everything under "Deferred — Do Not Do" in `~/.claude/handoffs/data-rover-py-2026-09-19-1207.md` (neighborhoods, hybrid warm-up, resumable transitions, feed events from `/model/ops` and `/model/undo`, and the older list).

## Plan
1. Verify state with the three git commands. Invoke `superpowers:executing-plans`. Read the plan whole, then Orient First items 2–5 as far as Task 1 needs. Run `pixi run dr-test` for your own baseline.
2. Ask the owner ONE question: are commits pre-approved for this plan's execution? Then `git switch engine-migration && git switch -c feat/engine-service`.
3. Execute Tasks 1–11 in order, each as the plan's steps say: write the failing tests, see exactly the expected ones fail (any OTHER red test is a finding to report, not to silence), implement, see them pass, lint, docs in the same commit, commit under the plan's subject. Tasks: (1) `pyLower` / `pyStrip` + generated tables; (2) steps, `sortedInSlices`, the index build and the digest check in steps; (3) a snapshot open that yields, 500-line batches; (4) working copy: coalescing, `adoptStaged`, own on a duplicate, `stagedVersion`, `structural`, `stagedDiff`; (5) reads I + the recorder's `read` / `view` / `drop_view` steps + `read_pages`; (6) the containment tree + `read_tree`; (7) fuzzy search + `read_search`; (8) the scheduler; (9) the service: envelope, replica life, events, `readDeltaText`, `AD-26`, CT-4; (10) staging, reads and context through the service; (11) bench rows, `K-32` → watch item, `program.md`, final verification.
4. After Task 11 Step 4 (every suite, every linter, fixtures unmoved), commit `Mark the engine service built`, then ask the owner for the go-ahead and `git switch engine-migration && git merge --ff-only feat/engine-service`.
5. Report to the owner: what differed from the plan's expectations, the bench numbers (open against 3 s and against 2.30 s before; heap against 400 MB; the longest step of the index build, the digest check and the search), and any finding logged to `BACKLOG-ENGINE.md`.
6. Then hand off for the next session, which WRITES plan 4 (sandbox and shell, spec §5–§6) in this plan's form; the plan's "After this plan" (:524) lists what it inherits.

## Open Questions
- Are commits pre-approved for this plan's execution, or does the owner want to approve each task's commit? Ask before Task 1. The final fast-forward merge needs its own go-ahead either way.
