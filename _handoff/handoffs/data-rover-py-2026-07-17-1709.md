# Handoff: Code-execution feature — begin M1 backend implementation

## Mission
Build the code-execution feature (users write/run Python snippets in-browser against the MBSE model) in `/home/mdp/workspace/data-rover-py`. M0 spike is done (GO verdict, merged to `main`) and the M1-backend task-level plan is written. Next arc = **execute the M1 backend plan** (subagent-driven, the user's standing preference from M0) to ship standalone snippets server-side end-to-end (artifact kind + lint + WASM runner + `dr` facade + run/lint/cancel routes). "Done" for this arc = all 12 M1-backend tasks implemented, `pixi run test-core && pixi run tidy` green, and the `integration`-marked real-WASM test passing.

## Orient First
1. `docs/superpowers/plans/2026-07-17-code-execution-m1-backend.md` — THE plan to execute, 12 tasks, TDD, real signatures. Read fully before starting.
2. `docs/superpowers/specs/2026-07-17-code-execution-design.md` — approved design spec (gitignored). Authoritative for detail; §3/§4/§5/§7/§8/§9/§10 map to M1 tasks.
3. `spikes/code_exec/FINDINGS.md` — M0 results + the 4 implementation notes the M1 runner must copy (memory-cap error mapping, epoch trap_code, determinism recipe, module-cache numbers). `spikes/code_exec/host.py` + `s04..s08` are the reference mechanics for `WasmScriptRunner`.
4. `CLAUDE.md` — pixi-only toolchain, architecture map, Python 3.14-runtime/3.10-typing-floor gotcha.
5. Key existing code the plan modifies: `src/data_rover/api/db_models.py:259-267` (ArtifactKind), `src/data_rover/api/routes/artifacts.py:47-186` (adapters + create/update), `src/data_rover/api/schemas.py:217-270` (OPS_ADAPTER wire format) + `:657-674` (artifact schemas), `src/data_rover/api/authz.py:45-64` (read-only POST allowlist), `src/data_rover/api/main.py:165-235` (create_app + lifespan + router registration), `tests/api/conftest.py` (fixtures: AUTH_HEADERS/papi/seed_default_project; pins DATA_ROVER_DEV_SEED=false).

Confirm state: `git branch --show-current`, `git status --short`, `pixi run test-core`, `pixi run tidy`. Fetch the guest binary before any WASM task: `bash spikes/code_exec/fetch_python_wasi.sh` (`vendor/` is gitignored).
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main` (M1-backend branch NOT yet created — Task 1 Step 1 creates `feature/code-execution-m1-backend`).
- Uncommitted: **clean** (as of 2026-07-17 17:09).
- HEAD: `2407cc9` = merge of the M0 spike into `main` (11 spike commits `3374d96..b6370ec` + merge commit). `main` is ahead of `origin/main` — **not pushed** (no push instruction given).
- Tests: not re-run this session (only docs + a merge). M0 probes were 8/8 green at spike close; `pixi run test-core` state for the existing suite is unverified this session — run it before starting to establish a baseline.
- Runtime pinned (from M0, now on `main`): wasmtime-py 46.0.1 (`pixi.toml` core-dev pypi-deps), CPython-WASI 3.14.6 (`spikes/code_exec/fetch_python_wasi.sh`, SHA-256 pinned).

## Standing Constraints
- **Execution method = subagent-driven** (`superpowers:subagent-driven-development`): fresh implementer + task review per task, as used for M0. User's standing preference — but re-confirm (see Open Questions; they were mid-answer when this handoff was cut).
- **No Alembic migration for the enum** — VERIFIED this session: `ArtifactRow.kind` compiles to `VARCHAR(12)`, NO CHECK constraint (SQLAlchemy 2.0 `native_enum=False` defaults `create_constraint=False`); `"code_snippet"` is 12 chars, fits. The spec §3 "widen the CHECK" step is MOOT — do not write a migration. (Re-verify via the one-liner in plan Task 1 Step 7 if in doubt.)
- **wasmtime stays out of `core/`** — `core/script/` imports only core + stdlib; only `api/script_runner.py` and `tests/` may import wasmtime. This is the `ScriptRunner` protocol seam (spec §4) and the whole test strategy depends on it.
- **`TrustedRunner` lives under `tests/`, never ships**; boot guard refuses `snippet_runner="trusted"` unless `dev_seed` is on (RCE tripwire). Route tests inject it via `app.dependency_overrides[get_runner]`, NOT via settings — else conftest's `dev_seed=false` trips the guard (plan Task 11 note).
- **Ops use the exact `OPS_ADAPTER` wire format** (`frontend/src/lib/state/ops.ts` is the contract): create/update/delete for elements + relationships; `update_*` uses `properties_patch` (JSON-merge-patch, null deletes a key). Runner records ops, never applies them (dry-run → client staged buffer).
- **Determinism + error-mapping recipes are fixed by M0 findings** — copy exactly (allow_shadowing before define_wasi, shims after, PYTHONHASHSEED=0; timeout=Trap.trap_code==INTERRUPT; memory=nonzero exit+stderr MemoryError not a host Trap). In plan Global Constraints.
- **`docs/` is gitignored** — spec + plans are intentionally uncommitted; do not try to commit them.
- **M1 split backend-first, frontend-later** — frontend planned only after backend API shapes are real/tested (more reliable UI plan). Deliberate.

## Done
- Full brainstorming → spec (2 external-agent review rounds folded in) → M0 plan → M0 execution (subagent-driven, 8/8 probes GO) → spike merged to `main`.
- M1-backend task-level plan written (12 tasks): `docs/superpowers/plans/2026-07-17-code-execution-m1-backend.md`.
- Codebase specifics for M1 gathered and embedded in the plan as real signatures (artifact routes, schemas, ops format, authz, settings, lifespan, core Model/Metamodel/IndexSet API).
- Verified the no-migration finding (VARCHAR(12), no CHECK).

## Known Issues, Not Yet Fixed
None. (`main` unpushed is intentional, not a defect.)

## Deferred — Do Not Do
- **M1 frontend task-level plan** — deferred until the backend lands (user was mid-answer on whether to expand it now; treat as open). CodeMirror editor, console, Stage-ops UI, `createCodeSnippetArtifact`, e2e — scope outline is at the tail of the M1-backend plan.
- **M2 `ScriptColumn`, M3 `ScriptStep`, M4 polish** — milestone-level only; expand each to its own plan when reached (spec §14, M0-plan tail).
- **Spec non-goals** (do NOT add): third-party packages (numpy/pandas), read-your-own-writes in a run, streaming console output, browser Pyodide runtime, `dr.navigate()`, `snippet_runs` DB table (audit is a log line in v1). Spec §1/§9.
- **Subprocess-runner fallback / spec §4 amendment** — moot; in-process WASM works (GIL released, FIFO bridge works). `s02b_subprocess.py` is committed unrun as documented fallback only; do not wire it in.
- **Pushing `main` / opening a PR** — not requested; do not push without the user.

## Plan
1. **Confirm execution method with the user** (they were answering when the session was cut — see Open Questions), then invoke `superpowers:subagent-driven-development` against the M1-backend plan.
2. Execute plan Task 1 first: `git checkout -b feature/code-execution-m1-backend main`, add `ArtifactKind.code_snippet` + widen `ArtifactCreateIn.kind` Literal, TDD per the plan, commit. Then Tasks 2→12 in order (schema → lint → entry-points-on-write → bridge → runner protocol → facade+TrustedRunner → WASM pool → WASM bridge/limits/determinism → runner selection+tripwire → routes → docs).
3. WASM tasks (8, 9, 12) need `bash spikes/code_exec/fetch_python_wasi.sh` first; their tests are `integration`-marked (`pixi run -e core-dev pytest -m integration`).

## Open Questions
- **Execution method**: confirm subagent-driven (recommended, matches M0) vs inline execution for the M1-backend plan. User was answering `/handoff` interrupted this.
- **Expand the M1 frontend plan now or after backend implementation?** My recommendation: after — UI plan is more reliable against real run/lint/cancel response shapes. User to decide.
