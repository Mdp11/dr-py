# Handoff: Data Rover client-engine program — WRITE plan 4 of sub-project B ("Sandbox and shell")

## Mission
Data Rover (`/home/mdp/workspace/data-rover-py`) is moving computation into the browser against a full model replica (TypeScript engine in `engine/`), with the Python core as the oracle. Plans 1–3 of sub-project B are built and on `engine-migration`. The next job is to WRITE plan 4, sandbox and shell (spec §5–§6), in the same form as plan 3. "Done" means the plan file is written under `docs/superpowers/plans/` (git-ignored, never `git add -f`), backed by cheap probes only, and approved by the owner. Don't build it in this session.

## Orient First
1. `docs/superpowers/specs/2026-09-19-replica-and-frontend-seam-design.md` — §5 Sandbox (:201), §6 Shell (:223), §11 Plans (:333: plan 4 = "the replica opens and follows in the background, no surface switched"), §12 Done when (:346), decisions taken with the owner (:39).
2. `docs/superpowers/plans/2026-09-19-engine-service.md` — plan 3, the FORM to copy: What planning found (:15) → Decisions (:41) → Global Constraints (:65) → File Structure (:85) → Mechanisms (:142) → Tasks (:192–:509) → Known limits (:512) → After this plan (:524, what plan 4 inherits: the worker entry's `inflate`/`yieldToHost`/`now`, raw text kept per AD-26, the open and re-bootstrap sequences, the in-process transport of `engine/test/service/helpers.ts`).
3. `CLAUDE.md`, "Engine package" — `src/service/` bullet describes the CT-4 dispatcher and scheduler the shell talks to. Read it instead of the code.
4. `architecture/contracts.md` CT-4 (:84), CT-5 (:136); `architecture/decisions.md` AD-16, AD-26; constraints CN-3, CN-9, CN-14, CN-17 (headers, CSP) in `architecture/`.
5. `frontend/README.md` "State model" + `frontend/src/lib/state/realtime.svelte.ts` (the socket `sync.ts` hooks into); `process-compose.yaml` and `frontend/playwright.config.ts` (both gain the sandbox process). `sandbox/` and `frontend/src/lib/engine/` do not exist yet.
6. `~/.claude/handoffs/data-rover-py-2026-09-19-1207.md` "Deferred — Do Not Do", still standing.
Commands: `pixi run dr-test`; `pixi run dr-tidy true` (check-only, positional arg); `pixi run engine-test`; `pixi run engine-check`; `pixi run golden-fixtures`; `git status --short`. Always start a Bash command with `cd /home/mdp/workspace/data-rover-py &&`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` at `c7bba47 Mark the engine service built` (fast-forwarded from `feat/engine-service` with the owner's go-ahead, 2026-09-21; not pushed). `feat/engine-service` still exists at the same commit.
- Uncommitted: clean.
- Tests (as of 2026-09-21 ~18:45, same content as HEAD): `pixi run dr-test` EXIT 0 (core 2,551 passed / 34 deselected; frontend 2,493 in 252 files; engine 461 in 59 files). `pixi run dr-tidy true` EXIT 0 and `pixi run golden-fixtures` moved no file (as of 2026-09-21 18:55).
- Bench (2026-09-21, Node 22.22.3, medians of 3, `pixi run engine-bench`): open 2,413 ms of 3,000 (2,300 before plan 3); heap 232 of 400 MB; index build in steps 618 ms, longest step 12 ms (8–13 across passes); digest check 211 ms, longest 3.8 ms; search q='a' 328 ms, longest 5.2 ms; q='sensor' 230 ms; stage 45, unstage 38, rebase 13, delta 8.3, `ord` re-sort after a rewind 56 ms.

## Standing Constraints
- The spec's decisions and plans 1–3 as built are settled. Don't relitigate them. MR-3 freeze: `core/model`, `core/metamodel`, the model-op applier, `routes/read.py` route functions and `routes/elements.py::get_element`. The Python core is the oracle, and fixtures and `engine/src/value/lower-tables.ts` change only through `pixi run golden-fixtures`.
- Writing a plan: cheap probes only. No building or test-running of plans 4–6 while writing them (owner). Record probe results as facts in "What planning found", marked *measured* or *estimate*.
- Performance stance (owner): report numbers, do not optimize.
- The owner answers design questions fastest as ONE multiple-choice question at a time, recommended option first.
- RC-11 commit subjects: one imperative capitalized sentence, no prefix, no period, ending with `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`. Never `git add -f` `docs/` or `benchmarks/`.
- Two logs: new findings needing a decision go to `BACKLOG-ENGINE.md` (next free ids `K-37`, `C-22`; next decision id `AD-27`); time-wasters go to `~/code/papercuts.md` (`date · symptom · fix · project`).
- Sandbox origin: `localhost:5174` beside the app on `127.0.0.1:5173`. It is another HOST on purpose, because cookies ignore ports (spec §5). The sandbox is always served as built files, so development runs under the real policy.
- `engine/src` stays free of DOM and WebWorker typings. Those live in `sandbox/` (spec §5).
- `Scheduler.turnStarted()` was removed in plan 3 on purpose (it made the fake host's slice measurement incoherent). Don't re-add it.
- `yieldToHost` must be a macrotask (a `MessageChannel` post or `setTimeout(0)`). A resolved promise is not a yield (plan 3, After this plan).

## Done
- Plan 1 (exact server state), plan 2 (snapshots and replica routes) and plan 3 (engine service, Tasks 1–11) are built and on `engine-migration`. `architecture/program.md` B row says "plans 1–3 built", and `CLAUDE.md` describes the engine service.
- The plan 3 findings were reported to the owner: the Task 3/5/9 differences from the plan's expectations, the bench numbers, and that nothing new was logged beyond the `K-32` rewrite.

## Known Issues, Not Yet Fixed
- Open, none of them plan 4's to fix unless the plan says so: `K-32` (the `ord` re-sort after a rewind, 56–58 ms at M, watched), `K-29` (element and relationship sharing an id: the engine refuses it at `end`, and plan 4's boot fallback serves such a project from the server), `K-35`, `K-36`, `C-20`, `C-21` in `BACKLOG-ENGINE.md`; `K-33`, `K-34` in `BACKLOG.md`.
- Plan 3 known limits that plan 4 must respect (plan 3 :512): a tail body is read as one document in one step; the digest check can be starved by continuous editing; `changed` over-reports.
- `docker` was absent in the WSL distro on 2026-09-19 (no Postgres on :5432). Probes that need the dev stack need Docker Desktop's WSL integration first.

## Deferred — Do Not Do
- Owner-declined 2026-09-21 (plan 3): leaving the two big sorts native or slicing only the roots sort (chose: slice both); letting a transition run mid-scan / strict FIFO (chose: the transition waits, short reads interleave); deltas as parsed objects or transferred bytes (chose: JSON text, AD-26).
- Caching search results between pages (plan 3 D16); tuning step sizes, the slice target, the `ord` re-sort or anything the bench shows; coalescing in place.
- Building or test-running plans 4–6 while writing them (owner: cheap probes only); landing a scratch build as an implementation, or replacing a plan with a short decisions doc.
- Optimizing `_decode_v2` / the v2 encode (`K-34`, watch only); reading the descriptor from the blob header or a single JSON `header` column; refactoring `create_commit`'s scope computation onto `replica.scope_of_ops` (C-12).
- Everything under "Deferred — Do Not Do" in `~/.claude/handoffs/data-rover-py-2026-09-19-1207.md`.

## Plan
1. `cd /home/mdp/workspace/data-rover-py && git status --short && git log --oneline -1`: expect clean, `c7bba47`. Create `feat/sandbox-and-shell` off `engine-migration` for any committed work (the plan file itself is git-ignored).
2. Read the Orient First list, then invoke `superpowers:writing-plans` and write `docs/superpowers/plans/<date>-sandbox-and-shell.md` in plan 3's form, covering spec §5 (`sandbox/`: Vite static site, `page.ts`, `engine-worker.ts`, headers CN-14/CN-17, COOP/COEP, the handshake, pixi tasks under `frontend`, the process-compose and playwright entries) and §6 (`frontend/src/lib/engine/`: `frame.ts`, `client.ts`, `sync.ts`, `cache.ts`, progress slices, boot fallback). Scope: the replica opens and follows in the background, with no surface switched (that is plan 5).
3. Probe cheaply while planning (e.g. `DecompressionStream` availability and behavior in the test env, how the app's dev server can add COOP/COEP, what `realtime.svelte.ts` exposes for commit/rebind/snapshot events, IndexedDB in happy-dom/vitest), and record each result as a fact.
4. Ask the owner design questions one at a time (multiple choice, recommended first) wherever the spec leaves a real choice. Record the answers as the plan's Decisions.
5. Present the plan for approval. Build only after approval, in a later session unless the owner says otherwise.

## Open Questions
None yet. Plan 4's design questions arise while writing it.
