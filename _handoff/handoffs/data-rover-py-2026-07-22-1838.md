# Handoff: Staged elements sidebar section

## Mission
Implement the "Staged elements" sidebar section in `/home/mdp/workspace/data-rover-py` (frontend only): a collapsible section below the containment tree listing every element touched by the staged-ops buffer (new/modified/deleted, badged), with click-to-select into the existing Inspector and a per-row cascade revert. Spec and a complete task-by-task implementation plan are already written and approved. Done = all four plan tasks executed, full frontend test suite + svelte-check + dr-tidy green, work committed.

## Orient First
1. `docs/superpowers/plans/2026-07-22-staged-elements-sidebar-section.md` — the implementation plan; it contains ALL code and test code. Execute it task-by-task.
2. `docs/superpowers/specs/2026-07-22-staged-elements-sidebar-section-design.md` — the approved design, for rationale only.
3. `frontend/src/lib/state/model.svelte.ts:580-700` — staged queue (`_queue`), `queuedTargetId`, `revertOptimistic`, `revertStagedFor`, `getStagedDiff` (the diff the new derivation builds on).
4. `frontend/src/lib/state/snippet-stage.ts` — why snippet ops land in the same staged buffer (context for the feature).
5. `frontend/src/lib/components/Sidebar.svelte` — mount point for the new section.
6. Read CLAUDE.md (pixi toolchain; frontend npm scripts MUST run from inside `frontend/`).

Confirm state: `git status --short` (expect clean), `pixi run -e frontend bash -c 'cd frontend && npm test'`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main` at `4cdc4a2` (clean working tree as of 2026-07-22 18:38)
- Uncommitted: only the two gitignored docs under `docs/superpowers/` (spec + plan)
- Tests: not run this session — plan assumes green baseline; run the suite before starting
- No servers running; no worktree created yet

## Standing Constraints
- User chose **subagent-driven execution** (superpowers:subagent-driven-development): fresh subagent per task, review between tasks. Start a worktree first per superpowers:using-git-worktrees.
- Build on `getStagedDiff()` (`model.svelte.ts:663`) — do NOT re-derive statuses from raw ops; the diff already yields added/modified/deleted with `before` snapshots (deleted-row names come from there).
- Section contents: created + modified + deleted elements. Elements that are endpoints of staged relationship ops count as **modified** (user decision).
- Per-row **revert** buttons on every row (user decision, replaced "deleted rows inert on click" question); cascade is mandatory for created elements — a surviving staged rel referencing a reverted temp id would 422 the commit.
- Section named **"Staged elements"**, shown in BOTH view and no-view modes (user decisions). Mounting in `Sidebar.svelte` after `<ContainmentTree />` satisfies mode-independence by construction.
- A temp element created-then-deleted in the same buffer is a net no-op — omit its row (getStagedDiff reports it as 'deleted' because the delete's journal snapshot captured the optimistic create).
- Never hardcode temp-id strings in tests — use `createTempId()` from `frontend/src/lib/state/ops.ts`.
- `docs/superpowers/` is gitignored by convention — spec/plan stay uncommitted.
- Rejected alternatives (do not revisit): clickable-DiffDrawer-rows-only (doesn't scale to script batches); merging staged rows into the server-paged containment levels (highest risk: paging/windowing/view-overlay/drag).

## Done
- Design brainstormed and approved; spec written: `docs/superpowers/specs/2026-07-22-staged-elements-sidebar-section-design.md`.
- Implementation plan written and self-reviewed: `docs/superpowers/plans/2026-07-22-staged-elements-sidebar-section.md` (4 tasks: staged-rows derivation, cascade revert, StagedSection component + mount + tests, verification sweep). All exports/type names verified against the real codebase (`getStagedDiff`, `getCachedTreeItems`, `seedRelationships`, `clearSelection`, etc. all exist in `state/index.ts`).
- No implementation code written yet.

## Known Issues, Not Yet Fixed
None.

## Deferred — Do Not Do
- Clickable DiffDrawer rows (approach A) — superseded by the sidebar section; possible later follow-up only if the user asks.
- Rows for relationships themselves — spec's out-of-scope list (endpoints appear as Modified instead).
- Drag-and-drop from/into the new section — out of scope per spec.
- Placing staged elements inside the containment hierarchy proper — rejected during brainstorming (risk), do not rebuild.

## Plan
1. Invoke superpowers:using-git-worktrees to create an isolated worktree, then superpowers:subagent-driven-development to execute `docs/superpowers/plans/2026-07-22-staged-elements-sidebar-section.md` starting with Task 1 (create `frontend/src/lib/state/staged-rows.ts` + its test, TDD steps in the plan).
2. Task 2: `revertStagedForElement` in `model.svelte.ts` + tests.
3. Task 3: `StagedSection.svelte` + mount in `Sidebar.svelte` + component tests.
4. Task 4: full suite + `npm run check` + `pixi run dr-tidy`.
5. On completion invoke superpowers:finishing-a-development-branch (merge/PR decision is the user's).

## Open Questions
None.
