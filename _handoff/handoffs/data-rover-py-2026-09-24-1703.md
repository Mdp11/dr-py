# Handoff: build plan 2 of sub-project C (validation core)

## Mission
Build plan 2 of 8 of sub-project C (Evaluation) for `/home/mdp/workspace/data-rover-py`, the validation core. The plan file is `docs/superpowers/plans/2026-09-24-eval-validation-core.md`; the owner has approved it. Run it with `/build-plan` (subagent-driven development; agent types `implementer`, `critical-implementer`, `task-reviewer`, `branch-reviewer` and `chores`; no `model` parameter).

It is done when:
- all 6 tasks are committed on `feat/eval-validation`;
- the branch reviewer's review is clean;
- the owner is offered the fast-forward of `engine-migration`.

## Orient First
1. `docs/superpowers/plans/2026-09-24-eval-validation-core.md`: the plan to build. Read "What planning found", the Decisions D1–D16, the Global Constraints, the Review Focus and the Mechanisms M1–M8 before Task 1.
2. `docs/superpowers/specs/2026-09-24-evaluation-design.md`: the binding spec. Plan 2 covers §3 minus `### Rules`, §2 "Shadow", §7's plan-2 rows and §8's plan-2 freeze row.
3. `CLAUDE.md`: the map, the commands and the cross-cutting rules.
4. `docs/superpowers/plans/2026-09-24-eval-artifacts-navigation-search.md`: plan 1, built. It shows the idioms the plan reuses.
5. `engine/README.md`, `frontend/src/lib/engine/README.md` and `src/data_rover/core/README.md`, for the areas the tasks touch.

Confirm state with `git status --short && git branch --show-current && git log --oneline -3`.
- Tests: `pixi run dr-test`.
- Lint: `pixi run dr-tidy true`, `pixi run engine-check`, `pixi run frontend-check`.

Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `feat/eval-validation`, cut from `engine-migration` at `f7c7136`, with no commits yet. Plan Task 1 Step 1 ("ask, then cut the branch") is therefore already done. Do not re-cut the branch, and do not ask about commits again.
- Uncommitted: clean.
- SDD workspace: `.superpowers/sdd/2026-09-24-eval-validation-core/`. It is git-ignored and holds only `plan-path`, from `scripts/sdd-workspace`. There is no ledger yet, so start the ledger fresh.
- Tests at `f7c7136` (2026-09-24):
  - engine: 1,089 in 70 files, re-run today, all green;
  - core: 2,571 passed / 34 deselected;
  - frontend: 2,954 in 283 files;
  - sandbox: 14;
  - e2e: 62 passed, with 2 pre-existing failures (T-8, T-9).
- The planning traces are in the previous session's scratchpad, `/tmp/claude-1000/-home-mdp-workspace-data-rover-py/2ca83c31-aad0-4bb6-b444-9536f5cb6a82/scratchpad/`, as `trace-python.md`, `trace-engine.md`, `trace-frontend.md` and `probe_*.py`. They may be gone. The plan's "What planning found" carries what matters.
- Engine bench data from 2026-09-22 is in `benchmarks/`, including `large.model.json` and `large.snapshot.v2*`.

## Standing Constraints
- **Commits.** Commits are pre-approved: one per task, on `feat/eval-validation`. Never push, never touch `main`, and ask the owner before fast-forwarding `engine-migration` at the end.
- **Dispatch.** Follow `/build-plan`: each task goes to the agent its plan heading tags (Tasks 1–5 `critical-implementer`, Task 6 `implementer`), each review to `task-reviewer`, and the final review to `branch-reviewer`.
- **Freeze (MR-3).** No file under `src/data_rover/` changes unless D16 fires, i.e. a Python bug found while porting lands on both sides with a fixture. Python changes are test-side only (`tests/golden/`, plus `scripts/issues_large.py`). The Python core is the oracle: fix the engine, never a fixture.
- **Benchmarks.** Report the numbers to the owner before optimizing anything. The only tuning allowed is `SWEEP_STEP` (D4), and only if the longest step at M passes 8 ms.
- **Local files.** `docs/` and `benchmarks/` are git-ignored: never `git add -f`.
- **Next free ids.** `AD-32`, `K-58`, `C-23`, and `T-10` in `BACKLOG.md`. The plan uses AD-32 and C-23.
- **Plan-1 lessons the plan already applies.** Spell out the startup and commit windows (D10, D11, Review Focus 3–4). Say how a same-run read sees a staged change (arrival order; the stage transition splices before the read). Gate reads that come before the data has loaded to the server (D10). The 501 fallback never guesses (D9).

## Done
- The plan was researched, probe-verified, written, self-reviewed and approved by the owner (2026-09-24).
- The branch `feat/eval-validation` is cut.

## Known Issues, Not Yet Fixed
- Open items from plan 1 (K-49…K-57) are in `BACKLOG-ENGINE.md`. Plan 2 deliberately leaves them alone (D14).
- The doc slips left after plan 1 are scheduled in plan Task 6, Steps 4–5; they are not a separate chore.

## Deferred — Do Not Do
- Rules are plan 3: compile, reach, evaluation, `POST /rules/parse`, and rules in the payloads. Plan 2 refuses with 501 `reaches validation rules` instead (D9), deferred by the spec and the owner.
- `artifacts_version` waits for plan 3; plan 2's probe cache key is `(rev, staged_version)` (D6).
- A full-run (`Scope.all`) port is not in plan 2; plan 7 decides for the candidate validation (D1).
- The open journey's progress bar is not driven by the engine sweep (D12).
- K-50 and K-51 are not folded into plan 2 (D14).
- Server-side strict enforcement and `validation_error_count` belong to F. The artifact and view half of the preview stays on the server until F. A staged `metamodel.rebind` sends the whole preview to the server until plan 7. (Owner decisions.)
- The JWT-leeway flake fix is the owner's separate call, on its own branch.

## Plan
1. Invoke `/build-plan docs/superpowers/plans/2026-09-24-eval-validation-core.md`. Resolve the workspace with `bash scripts/sdd-workspace <plan>` from the skill's directory. Create the ledger with first line `# SDD ledger — plan: docs/superpowers/plans/2026-09-24-eval-validation-core.md`, and run the pre-flight conflict scan (a table with a row for each task pair that shares a file or interface, and a row for each task).
2. Task 1 (validators and scoped pipeline). Tell the implementer that Step 1 is done: the branch is cut and commits are pre-approved. Then run Tasks 2–6 in order.
3. Relay the Task 4 and Task 6 benchmark numbers and the `engine-parity-large` result to the owner in the final report.
4. Run the final `branch-reviewer` review over `git merge-base engine-migration HEAD`..HEAD. Report the tasks, every ruling with its cost if wrong, and the verdict. Then use superpowers:finishing-a-development-branch, which asks before fast-forwarding `engine-migration`.

## Open Questions
None. Plan approval and commit pre-approval are both given.
