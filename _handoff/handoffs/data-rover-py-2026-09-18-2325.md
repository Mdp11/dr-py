# Handoff: Data Rover client-engine program — execute plan 4 of sub-project A (the last one)

## Mission
Data Rover (`/home/mdp/workspace/data-rover-py`) is being re-architected so computation runs in the browser against a full model replica (TypeScript engine in `engine/`), with the Python core as the oracle. Plans 1–3 of sub-project A have landed on `engine-migration`; plan 4 (snapshot reader, the engine's own SHA-256 state digest, benchmarks at model M, closing docs) is **written, pre-verified and approved by the user**. This session's job is to **execute it inline with `superpowers:executing-plans`**, on `feat/engine-snapshot`, ending with a fast-forward merge into `engine-migration`. "Done" = the plan's four tasks committed, `pixi run dr-test` + `pixi run dr-tidy` green, the bench says `within budget` twice, the branch merged, and a report of test counts, bench medians and any deviation.

## Orient First
1. `docs/superpowers/plans/2026-09-18-engine-snapshot.md` — THE plan (2,315 lines; `docs/` is git-ignored). Read :1–77 whole (header, Provenance, "What the oracle taught this plan", Global Constraints, File Structure), then each task as you reach it: Task 1 :79, Task 2 :769, Task 3 :1616, Task 4 :2115, "After this plan" :2308.
2. `~/.claude/handoffs/data-rover-py-plan4-extract.py` — writes a task's/step's code blocks from the plan ON DISK into the repo (`python3 <it> <task> [--step N]... [--root DIR] [--dry-run]`). Handles whole files, "replace … with …" and "delete the line" blocks; it does NOT run shell blocks or `Run:` lines. Its docstring is the manual.
3. `CLAUDE.md` — "Engine package (`engine/`)" and the toolchain commands (everything through pixi).
4. `docs/superpowers/specs/2026-09-18-engine-foundation-design.md` §7, §9, §11 — already updated this session to match the plan's findings; read only if a step's intent is unclear.
5. `architecture/contracts.md` (CT-1, CT-3), `constraints.md` (CN-3, CN-5) — only if needed.

Confirm state: `git status --short` (expect clean), `git branch --show-current` (expect `engine-migration`), `git log --oneline -1` (expect `98387a7`). Tests: `pixi run dr-test`; engine only `pixi run engine-test`; lint `pixi run dr-tidy`, engine only `pixi run engine-tidy`; golden `pixi run -e core-dev pytest tests/golden -q`; a focused engine run is `pixi run -e frontend bash -c "cd engine && npx vitest run <path>"`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` at `98387a7` ("Document the engine's op applier and working copy"). `feat/engine-snapshot` does NOT exist yet — Task 1 Step 1 cuts it.
- Uncommitted: clean (verified 2026-09-18 23:25). Nothing of plan 4 is in the repo tree; only git-ignored files changed: the plan and the spec under `docs/`, and `benchmarks/large.snapshot.v2` + `benchmarks/large.snapshot.v2.metamodel.json` (the bench input, already generated — Task 3 Step 3 regenerates it identically, ~8 s).
- Tests on `98387a7` (as of the previous handoff, ~22:00): core pytest 2,429 passed / 34 deselected, frontend vitest 2,492 in 252 files, engine vitest 209 in 34 files. With plan 4 applied in a scratch clone (22:30–23:00): engine 266 in 38 files, core pytest 2,429 / 34 deselected, `tsc`×2 + eslint + prettier + ruff clean at every task boundary. `dr-test`/`dr-tidy` were NOT run end to end on the applied tree (no frontend or `src/` change) — Task 4 Step 1 does that.
- Bench in the verification pass (model M, Node 22.22.3, medians of 3): open 2.30 s of 3 s; same model as one document 3.14 s; heap 231 MB of 400; verify digest 0.2 s; stage/unstage 1,000 ops 44/39 ms; re-sort after rewind 58 ms; rebase 14 ms; delta 8.9 ms. The closing docs of Task 4 quote exactly these.
- Scratch from this session MAY still exist (convenience only, never depend on it): `/tmp/claude-1000/-home-mdp-workspace-data-rover-py/38f73efe-124a-480d-8c51-1dc3433dc404/scratchpad/` — `s1` = verified final tree, `s2` = step-by-step replay, `s4` = tree rebuilt from the plan text alone (all three byte-identical), `replay/*.log` = observed outputs, `gen/` = the plan generator (`build_plan.py`, `template.md`, `edits.py`, `doc_edits.py`, `values.py`), `bite.py` = the nine mutations, `run.sh` = run a command in a scratch copy on the repo's pixi envs.
- `engine/node_modules` installed. No servers running.

## Standing Constraints
- **Execution mode: inline, `superpowers:executing-plans`** — the user's choice again (same as plans 2 and 3; subagent-driven was offered and not taken). Do not re-ask.
- **Do not "improve" blocks while executing.** Every block was built, replayed red→green in a clean clone, and the plan text alone reproduces the verified tree byte for byte. If a step's expected output does not appear, suspect the environment first; a real deviation goes to the user, not into an ad-hoc fix.
- **Apply blocks with the extractor, not by retyping** — a 4-digit `\uXXXX` typed through Write/Edit/Bash gets decoded. No block of plan 4 holds one; `tests/golden/scenarios/snapshot_v2.py` does (its element table, since plan 2), which is why Task 2 Step 1 edits it in three places and never shows it whole. Never rewrite that file whole.
- **Task 2 must be extracted step by step**, because shell steps sit between the blocks: `--step 1`, then Step 2 by hand (`ruff`, `pixi run golden-fixtures`, the red run), then `--step 3 --step 4 --step 5` (run the red/green commands between them as the plan says — extract 3, run, extract 4, run, …), then the `git mv` of Step 6 BY HAND before `--step 6`, then `--step 8`. Extracting Step 6 before the `git mv` leaves the old test file behind.
- **The Python core is the oracle; the MR-3 freeze is in force.** Plan 4 changes no file under `src/`. A failing golden test means the engine is wrong. Fixtures change only through `pixi run golden-fixtures`; exactly one moves in this plan (`engine/fixtures/golden/snapshot_v2.json`). An oracle bug found mid-way is raised with the user.
- **Bench rule (user-facing):** if `pixi run engine-bench` ends with `OVER BUDGET` for `open`, run once more on a quiet machine; if it still does, STOP and ask the user — optimizing is their call. If the `open` or heap medians differ from 2.30 s / 231 MB by more than a quarter, tell the user before committing Task 4's docs rather than editing numbers silently (CN-5: timings drift 1.5–1.9× between sessions).
- **Decisions taken while writing the plan, accepted by the user with the plan** (reasons in the plan's findings 1–11): `TextDecoder` is reached through a typed `globalThis` lookup in `src/snapshot/utf8.ts`, not declared ambiently (a declaration collides with `@types/node`, TS2300, and would with the frontend's DOM lib); `parseLines` refuses a line holding two documents (it would silently drop an entity); the line count is checked before the last batch is parsed (gives a cut text the server's words); `verifyDigest()` folds committed images, not the staged model; a header count past 2^53 is refused as invalid (documented gap); no optimization was done — three >16 ms operations are logged as `K-32` for B.
- `engine/src/`: no DOM, no Node built-ins, erasable syntax only, `.ts` import specifiers, no `Date.now`/`Math.random`/`Intl`. Tests and `engine/bench/` may import `node:*`.
- `dr-tidy` lints neither `tests/` nor `scripts/`: run ruff by hand where the plan says.
- ASCII check: `LC_ALL=C grep -rnP --include='*.py' --include='*.ts' '[^[:ascii:]]' tests/golden engine/test` must show only the `café` line of `engine/test/value/serialize.test.ts`.
- `architecture/` is tracked and changes in the same commit as what it describes (RC-10); `docs/` and `benchmarks/` are git-ignored — never `git add -f`. Commit subjects (RC-11): one imperative sentence, capitalized, no prefix, no trailing period, ending with the session's `Co-Authored-By` line. The four subjects are given in the plan.
- Branching: `feat/engine-snapshot` from `engine-migration`, finish with `git merge --ff-only`. Never touch `main`.
- Bite-check steps (Task 1 Step 10, Task 2 Step 10) break a source on purpose: restore each (`git checkout -- <file>` works once the file is tracked; before the task's commit the new files are untracked, so copy the file to the scratchpad first), re-confirm the full count, never commit a mutation.

## Done
- Plans 1–3 of sub-project A landed (`e8abbf3`..`98387a7`): package, value layer, golden pipeline; Python snapshot v2 + `state_digest.py`, metamodel, store, indexes, mutation boundary; op applier, working copy, invariants.
- Plan 4 written to `docs/superpowers/plans/2026-09-18-engine-snapshot.md`, built in a scratch clone against the real core, replayed step by step in a clean clone (every red failed as stated, every green passed, final tree identical), rebuilt a third time from the plan text alone via the extractor (identical again), nine mutation checks all caught, benchmark run at M. Approved by the user 2026-09-18.
- Spec `docs/superpowers/specs/2026-09-18-engine-foundation-design.md` updated (§1 `TextDecoder` lookup + `engine-bench-data`, §7 reader rules and `verifyDigest`, §9 bench notes and numbers). Do not redo.
- Extractor written: `~/.claude/handoffs/data-rover-py-plan4-extract.py`.
- `benchmarks/large.snapshot.v2` and `.metamodel.json` generated (git-ignored).

## Known Issues, Not Yet Fixed
- **K-29** (logged in `BACKLOG.md`): `src/data_rover/api/routes/_snapshot.py:54-92` keeps `seen_ids` per kind, so the engine's loader refuses what Python accepts. User chose to keep it logged.
- **K-30**, **K-31** (logged; sub-project B's): the server's `_rollback` is not exact (`src/data_rover/api/routes/ops.py:477-487`); a CT-2 delta cannot say "deleted and created again under the same id".
- **K-32** (diagnosed by the bench; Task 4 Step 2 writes the `BACKLOG.md` entry — not in the repo yet): at M, `rebuildIndexes()` 0.52 s, `verifyDigest()` 0.2 s and the re-sort of an entity map after a rewind restored an old `ord` (58 ms vs 2 ms) each outlast one 16 ms chunk. B's.
- **C-20** (logged): comment at `src/data_rover/core/metamodel/schema.py:198` contradicts the code; frozen.
- Documented gaps, no fix planned: `Multiplicity.parse` and non-ASCII digits; a snapshot header count past 2^53 (server takes it, engine refuses it).
- The server's `_decode_v2` loads a line holding two documents without complaint (8 lines / 9 documents under a header promising 6 + 2 load as 6 + 3); the engine refuses. Observed, in the plan's finding 4, not logged in `BACKLOG.md` and no fixture holds it.
- No CI. `tests/golden/test_fixtures_current.py` in `core-test` is the only drift guard.

## Deferred — Do Not Do
- **Optimizing anything in plan 4** (index build, digest check, the `ord` re-sort, `unstage({entity})` scanning, `Touched` over-reporting): the budget is met; the user's standing rule is measure first, optimize only on a missed budget and only after asking. Logged as `K-32` for B instead.
- **Fixing K-29 / K-30 / K-31 in sub-project A**: declined by the user in earlier sessions (K-29 "keep it logged"; K-30/K-31 are B's).
- **Deleting the merged branches** `feat/engine-value-layer`, `feat/engine-store`, `feat/engine-ops`: the user said "Leave them" (2026-09-18). Leave `feat/engine-snapshot` too after the merge.
- **Rust/wasm engine; Python core under Pyodide; orjson; Pyodide memory snapshots; server-side strict-mode verification; switching server writers to snapshot v2 (B); merging into `main`**: each declined or postponed by the user in earlier sessions.
- **`pyEquals`, `isPlainObject`**: postponed by the user to sub-project C.
- **A TypeScript v2 snapshot writer in `engine/src/`**: not needed until a host writes snapshots; the test helper `engine/test/snapshot/text.ts` is deliberately test-only.
- **Starting sub-project B**: out of scope until A is done and the user says so; B begins with its own brainstorm and spec (RC-9).

## Plan
1. Verify state with the three git commands in Orient First. Read the plan's :1–77.
2. Invoke `superpowers:executing-plans` on `docs/superpowers/plans/2026-09-18-engine-snapshot.md` and execute inline, task by task, with the extractor: `python3 ~/.claude/handoffs/data-rover-py-plan4-extract.py 1 --step 2` writes Task 1's two test files, and so on. Begin with Task 1 Step 1: `git switch engine-migration && git switch -c feat/engine-snapshot`.
3. Task 1 (SHA-256, digest, `WorkingCopy` default hash, `verifyDigest`): expect red 6-tests/2-files → 10 pass → 219 → red 53 of 223 → 223 in 36 files; two bite checks (1 and 2 failures of 223); `pixi run engine-tidy`; commit "Give the engine its own SHA-256 and state digest".
4. Task 2 (`openSnapshot`): follow the step-by-step extraction order in Standing Constraints; expect 266 in 38 files; three bite checks (1, 8, 2 failures of 266); ruff + golden pytest + ASCII check; commit "Open a v2 snapshot into a replica at its rev and digest".
5. Task 3 (bench): extractor for Steps 1–2, `pixi run engine-tidy`, `pixi run engine-bench-data`, `pixi run engine-bench` on a quiet machine (~1 min); apply the bench rule; commit "Benchmark the engine at model M".
6. Task 4: `pixi run dr-test` (expect 2,429 / 2,492 / 266) and `pixi run dr-tidy` (no changes); extractor for Step 2's eleven doc edits; commit "Close the engine foundation: snapshot reader, digest and benchmarks documented"; `git switch engine-migration && git merge --ff-only feat/engine-snapshot`.
7. Report to the user: test counts, the bench table's medians next to the plan's, any deviation, and that sub-project A is done. Then write a handoff if the user wants to go on to B.

## Open Questions
None. (Plan approved, execution mode chosen, branches stay.)
