# Handoff: Code Execution M2+M3 — frontend plan (F1 done, F2–F8 + final review remain)

## Mission
Ship M2 (`ScriptColumn` table cells via a snippet's `value(elements)`) and M3 (`ScriptStep` navigation steps via `step(el)`) in /home/mdp/workspace/data-rover-py. The **entire backend plan (B1–B15) is DONE, reviewed, and green.** Remaining work is the **frontend plan** (Tasks F1–F8) executed with superpowers:subagent-driven-development (fresh implementer subagent per task + task review + fix loops), then a final whole-branch review, then superpowers:finishing-a-development-branch. Done = all 8 frontend tasks implemented+reviewed+green (`npm test`/`check`/`lint`/`test:e2e`, `dr-tidy`), final review triaged, branch finished.

## Orient First
1. `.superpowers/sdd/progress.md` — THE durable ledger and source of truth for resume point. Lists every B1–B15 (done) + F1 (done, review-pending) with per-task Minor findings queued for final-review triage, plus frontend-task line ranges and model choices.
2. `docs/superpowers/plans/2026-07-19-code-execution-m2-m3-frontend.md` — frontend plan (Tasks F1–F8, global constraints at line 11, file structure at 27). Exec order: F1→F2→(F3→F4, F5)→F6→F7→F8.
3. `docs/superpowers/specs/2026-07-19-code-execution-m2-m3-design.md` — approved design spec (§7 is the frontend section).
4. `frontend/README.md` — REQUIRED read before touching `frontend/src/lib/state/` (repo rule). State model §129, nav-editor state §171, tests §341, type-check/lint §377.
5. `CLAUDE.md` — toolchain (pixi-only), snippet/M1+M2/M3 architecture (the M2/M3 paragraph is at ~line 153).
6. F1 review inputs (review it FIRST): brief `.superpowers/sdd/fe-task-F1-brief.md`, report `.superpowers/sdd/fe-task-F1-report.md`, diff via `review-package e69b6bb 4c028b2`.

Confirm state: `git status --short && git log --oneline -6`. Backend: `pixi run core-test` (expect ~1141 pass; ONE env-flake `tests/api/test_session_registry.py::test_idle_lists_stale_projects` fails ONLY when machine uptime < ~1000s — ignore it, it's on `main` too). Frontend: `pixi run -e frontend bash -c 'cd frontend && npm run check'` (expect 0 errors) and `... && npm test` (expect ~1072 pass). `pixi run dr-tidy`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `feature/code-execution-m2-m3` (off main @ 3cf94ff = merge-base).
- Uncommitted: clean (as of 2026-07-20 11:13).
- Backend: B1–B15 all done+reviewed+committed (HEAD-of-backend = e69b6bb). `core-test` 1141 pass / 0 fail, `dr-tidy` clean, WASM integration 18/18 — all as of B15 (e69b6bb).
- Frontend: F1 IMPLEMENTED + committed (4c028b2) but **REVIEW NOT YET RUN** (see Plan step 1). `npm run check` 0 errors, `npm test` 1072 pass, lint clean — as of F1 commit.
- Guest WASM binary already fetched at `spikes/code_exec/vendor/` (python.wasm + lib/ + cached .cwasm) — F8 e2e / backend integration tests need it; `bash spikes/code_exec/fetch_python_wasi.sh` is idempotent if missing.

## Standing Constraints
- **SDD loop per task**: extract brief → implementer subagent (model per below) → `review-package BASE HEAD` → reviewer subagent (sonnet) → fix loop for Critical/Important only → ledger line. Minor findings go to the ledger for FINAL-review triage, not per-task fix loops. Skill scripts live at `/home/mdp/.claude/plugins/cache/claude-plugins-official/superpowers/6.1.1/skills/subagent-driven-development/scripts/{task-brief,review-package}`. Always record BASE (the commit before dispatching the implementer) — never use `HEAD~1`.
- **Frontend briefs**: the `task-brief` script only handles NUMERIC tasks, so extract F-tasks by LINE RANGE: `sed -n 'A,Bp' docs/superpowers/plans/2026-07-19-code-execution-m2-m3-frontend.md > .superpowers/sdd/fe-task-FN-brief.md`. Ranges: F2=108-136, F3=139-186, F4=189-203, F5=206-230, F6=233-247, F7=250-263, F8=266-288.
- **Model selection**: implementers on haiku ONLY when the brief has complete verbatim code AND is single-file mechanical (F2 columns.ts factory/labels qualifies). Everything touching .svelte components or state (F3–F8) → sonnet. Reviewers → sonnet. Final whole-branch review → most capable model. ALWAYS specify model explicitly in Agent dispatches.
- **Frontend cmds MUST run from inside `frontend/`**: `pixi run -e frontend bash -c 'cd frontend && npm test|npm run check|npm run lint|npm run format|npm run test:e2e'`. Bare `pixi run -e frontend npm test` fails ("Missing script"). Single vitest file: `... && npx vitest run <path>`.
- **IDE Pyright/TS diagnostics in `<new-diagnostics>` blocks are chronically STALE for this repo** (wrong interpreter/lag) — they falsely report "fastapi/wasmtime could not be resolved", "X not accessed". This happened ~7× this session; the CLI (`core-lint`/`backend-lint`/`npm run check`) was green EVERY time. Verify with the CLI, never trust the diagnostic block.
- **Column identity is by reference** (`frontend/src/lib/table/columns.ts:14-26`): editors emit whole-column patches via `onChange(next)`, never mutate in place.
- Snippet artifacts are `kind: 'code_snippet'`; pickers filter with `entryAvailable('value'|'step', header.entry_points)` from `frontend/src/lib/snippet/entry-stubs.ts`. Inline snippet editor is a plain `CodeEditor` mount over `snippet.definition.code` + component-local debounced lint — do NOT copy NavigationColumnEditor's embedded-draft machinery (deliberate; F3 constraint).
- Commit trailers (every commit): `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01MvNbAUZTCjDCdnmvTKJT9d`.
- Backend degraded-not-failed asymmetry (already implemented): dangling SNIPPET refs never raise (→ error cells / prune-with-warning), dangling NAVIGATION refs still raise. Sessions read-only (`record_ops=False`), memoized by `(code, entry, ids)`.
- `docs/superpowers/` and `.superpowers/` are gitignored — plans/specs/ledger/briefs/reports are never committed.
- No worktree — work directly in the primary checkout (pixi envs, node_modules, wasm binary all live here).

## Done
- **Backend B1–B15: ALL implemented, reviewed-approved, committed.** Per-task commits/findings in `.superpowers/sdd/progress.md`. Highlights of B7–B15 (this session): B7 table eval (e33959c), B8 script-column sort (55fb78e), B9 ScriptStep nav + ChainResult.warnings (82eabf0), B10 concurrency guard + script_eval glue (9b2c3c1), B11 /tables/evaluate wiring (62c0a1d), B12 /tables/export #ERROR markers (1b91161), B13 /navigations/evaluate wiring (8c7a6ae), B14 WASM embedded sessions (27165ce), B15 docs (e69b6bb).
- **F1 implemented + committed (4c028b2)** — types/schemas. NOT yet reviewed (see Plan step 1). Details + scope-bleed flags in the ledger's F1 entry.
- Spec + both plan docs written and user-approved earlier. Old M1 ledger archived at `.superpowers/sdd/progress-m1-backend.md`.

## Known Issues, Not Yet Fixed
- **F1 scope-bleed (unavoidable type fallout from widening shared unions — flag to the F1 reviewer, and F5/F6 must account for it):**
  1. `frontend/src/lib/components/Table/TableGrid.svelte` got a MINIMAL inline error branch (renders `cell.message` in `text-destructive`) because the new `error` TableCell variant broke the grid's exhaustive narrowing. **F5 must REPLACE this with the real `ErrorCell.svelte` component (don't just add alongside).**
  2. `frontend/src/lib/navigation/tree.ts` (`nodeLabel()` + `chainColumns()`) now label a `script` step as `s.comment || 'script'` (matches backend `step_types` convention). F6 may refine the UX.
  3. `table-editor.test.ts` `EMPTY_PAGE` toEqual assertion was changed to `{...EMPTY_PAGE, warnings: undefined}` because `installPage`/`TableData` never carried `warnings` pre-F1. **F5 adds `warnings` to `TableData` — revisit that assertion then.**
  4. ~14 test-fixture files got `warnings: []` added to TablePage/ChainPage literals (mechanical; `table-editor.test.ts` `pageAt()` helper fixed centrally).
- **B14 Minor (final-review triage)**: `_WasmSnippetSession.wall_deadline` (`src/data_rover/api/script_runner.py:1173, :1223`) double-adds `_TIMEOUT_READ_GRACE_S` (once in the computation, again inside `_serve_until`). Only widens a backstop timeout ceiling (deadline_s+2×1.5s vs 1.5s), never the epoch-kill time; brief-inherited, not security-relevant. One-line fix.
- **B8 Minor (triage)**: `_script_sort_atom` docstring (`evaluate.py:563`) missing the brief-mandated collision-edge sentence (landed in inline comments only); elements-kind sort atoms (`evaluate.py:682`) don't dict.fromkeys-dedupe like cells.py:329.
- Other per-task Minor findings (B2–B7) are itemized in the ledger for final-review triage.

## Deferred — Do Not Do
- Real cancellation of embedded runs, per-cell value cache, `call_many` chunked calls, live re-eval push on snippet artifact edit — all explicitly out of scope (user-approved, spec "Out of scope").
- Inline snippet editor does NOT reuse NavigationColumnEditor's embedded-draft machinery — deliberate (F3): inline code is a plain string + component-local debounced lint.
- Do NOT "fix" F1's minimal TableGrid error branch or tree.ts label handling by expanding them beyond type-correctness — F5/F6 own the real UX (see Known Issues).

## Plan
1. **Review F1 FIRST** (it's committed but unreviewed — same pattern this session opened with for B7). Generate `review-package e69b6bb 4c028b2`, dispatch a sonnet reviewer with inputs: brief `.superpowers/sdd/fe-task-F1-brief.md`, report `.superpowers/sdd/fe-task-F1-report.md`, the diff, BASE e69b6bb / HEAD 4c028b2. Attention lens: schemas match the plan (SnippetDefinition/SnippetSource/ScriptColumn placed between ColumnSourceSchema@670 and ColumnSchema union@722; `const ScriptColumnSchema` matching sibling non-exported column schemas; error TableCell variant; `warnings` on TablePage+ChainPage; NavScriptStep in NavStepItem); the 7 new tests in `tables.test.ts` are non-tautological; and the scope-bleed fixes (Known Issues 1–4) are minimal + type-correct, NOT over-built. Run fix loop only for Critical/Important; then mark F1 review-clean in the ledger.
2. Continue the SDD per-task loop for F2–F8 in plan order (F2→F3→F4/F5→F6→F7→F8). F2 = haiku (columns.ts factory + labels, verbatim code). F3–F8 = sonnet. Each: extract brief by line range → implementer → review-package → reviewer → fix loop → ledger line. F8 runs Playwright e2e (needs the fetched guest binary; follow `e2e/snippet-flow.spec.ts`'s runner-availability handling).
3. Final whole-branch review: `review-package $(git merge-base main HEAD) HEAD` (merge-base = 3cf94ff), dispatch on the most capable model with the accumulated Minor-findings list from the ledger for triage; ONE fix subagent for all surviving findings.
4. superpowers:finishing-a-development-branch (merge/PR decision is the user's).

## Open Questions
None.
