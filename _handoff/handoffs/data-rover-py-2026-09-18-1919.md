# Handoff: Data Rover client-engine program — write plan 3 (op applier + working copy)

## Mission
Data Rover (`/home/mdp/workspace/data-rover-py`) is being re-architected so computation runs in the browser against a full model replica (TypeScript engine in `engine/`), with the Python core as the oracle. Plans 1 and 2 of sub-project A have landed on `engine-migration`. This session's job: **write plan 3 (the op applier and the working copy)** with `superpowers:writing-plans`, to the same standard as plan 2 (every code block built and run in a scratch copy against the real Python core before it goes into the plan). "Done" = plan 3 written to `docs/superpowers/plans/`, verified in scratch, and reviewed/approved by the user. Do not execute it in the same session unless the user says so.

## Orient First
1. `docs/superpowers/plans/2026-09-18-engine-store.md:5026` — "After this plan": what plan 3 inherits (applier as one more `do` in the Recorder, `ord` on before-images, `findArrayIndexKey` to export). Then skim :1–90 of that plan for the house style plan 3 must match (provenance paragraph, "What the oracle taught this plan", Global Constraints, File Structure, per-task Files/Interfaces/Steps with observed expected outputs).
2. `docs/superpowers/specs/2026-09-18-engine-foundation-design.md` — the spec; find the sections covering the op applier / working copy (§5–§6 area) and §8 (golden scenarios).
3. `architecture/README.md`, `architecture/contracts.md` (CT-1, CT-3, CT-7 and whatever contract covers ops/deltas), `architecture/program.md` (MR-3 freeze; A's status row), `architecture/conventions.md` (RC-10, RC-11).
4. `src/data_rover/api/routes/ops.py` (`_apply_batch`, the inverse-op collection, rollback) and `src/data_rover/api/schemas.py` (`OpIn` union) — the oracle for plan 3.
5. `tests/golden/model_steps.py` (Recorder, `run_steps`) and `engine/test/golden/model-steps.ts` (`replaySteps`, the `apply` switch) — where the new step kind goes on both sides.
6. `engine/src/model/model.ts`, `engine/src/model/load.ts` (`findArrayIndexKey`), `engine/src/model/indexes.ts` — the store plan 3 builds on.
7. `CLAUDE.md` — "Engine package (`engine/`)" and toolchain commands.

Confirm state: `git status --short` (expect clean), `git branch --show-current` (expect `engine-migration`), `git log --oneline -1` (expect `fe9cf18`). Tests: `pixi run dr-test`; lint: `pixi run dr-tidy`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` at `fe9cf18`. `feat/engine-store` still exists (fully merged, ff); delete it only if the user asks.
- Uncommitted: clean.
- Tests (2026-09-18 ~19:15, on `feat/engine-store` at the same tree): `pixi run dr-test` green — core pytest 2,429 passed / 34 deselected, frontend vitest 2,492 (252 files), engine vitest 135 (25 files). `pixi run dr-tidy` exit 0, no file changes.
- `engine/node_modules` installed. No servers running.
- Plan-2 helper `~/.claude/handoffs/data-rover-py-plan2-extract.py` is plan-2-specific (hardcoded path and partial edits); reuse its approach (write files from the plan on disk) for plan 3, not the script as-is.

## Standing Constraints
- **The Python core is the oracle; MR-3 freeze is in force** for `core/model`, `core/metamodel` and the model-op applier (`routes/ops.py` apply path): no behaviour change. A failing golden test means the engine is wrong. Fixtures only via `pixi run golden-fixtures`, never hand-edited. An oracle bug found mid-port is raised with the user, not fixed on one side.
- **Plans are written just-in-time and pre-verified**: build every file in a scratch copy of `HEAD` against the real core, replay the tasks in a second clean copy (red → green at each step), then generate the plan's code blocks from those files — plan 2 did this and executed with zero surprises; keep the standard.
- **Unicode-escape trap (tooling):** 4-digit `\uXXXX` typed into Write/Edit/Bash is decoded into literal characters (8-digit `\U…` and `\u{…}` survive). Files with escapes must be generated from disk (scripts), never retyped. Plan's ASCII check: `LC_ALL=C grep -rnP '[^[:ascii:]]' tests/golden engine/test` → only the `café` line of `engine/test/value/serialize.test.ts`.
- **Do not relitigate plan 2's nine findings** (plan 2 :15–27), notably: engine never mints ids; ancestor's property definition wins on a name clash (mirrors code, logged C-20); loader stricter than Python for cross-kind duplicate ids and array-index keys at any depth; `restoreElement/restoreRelationship(…, ord)` put a record back in its old place, which the oracle cannot show (it always appends) — plan 3's rewind paths are covered by unit tests/invariants, not fixtures.
- **Server writers keep emitting snapshot v1.** `encode_snapshot_v2` is called only by tests and the `snapshot_v2` scenario until sub-project B.
- **Engine source rules:** no DOM/Node in `engine/src` (`lib: ["ES2023"]`, `types: []`), erasable syntax only, `.ts` specifiers, no `any` in exported signatures, no `Date.now`/`Math.random`/`Intl` in `src/`; observable order sorts with `cmpCodePoint`; data-named properties via `getProp`/`Object.hasOwn`.
- **Use pixi's Node/Python only.** Format the engine with `pixi run engine-tidy`. `dr-tidy` does not lint `tests/` — run `ruff format/check tests/golden` by hand.
- **`architecture/` is tracked; `docs/` is git-ignored — never `git add -f`.** Architecture edits land in the same commit as the code (RC-10).
- **Git (RC-11):** subject one imperative sentence, capitalized, no prefix, no period; end with the session's `Co-Authored-By` line.
- **Branching:** each plan on its own `feat/…` branch cut from `engine-migration`, fast-forwarded back into it; never merge into `main` (user's choice).
- **Inline execution** was the user's choice for plan 2 (subagent-driven was offered and declined); ask again for plan 3.
- **Performance is plan 4's job** (budget: ≈ 0.4 s of CN-3's 3 s left at model M). Do not optimize in plan 3. Benchmark hygiene (CN-5): compare timings only within one pass.

## Done
- Plan 1 (package, value layer, golden pipeline): `e8abbf3`..`1b31a31`.
- Plan 2 (snapshot v2 + state digest, metamodel, record-graph store, indexes, mutation boundary, bulk load): `e978826`..`fe9cf18`, ten commits incl. the formatter pass and `spikes/client_engine/`; every step matched the plan's observed outputs (incl. the "fixtures bite" check: 13 tests / 6 files fail with the roots line disabled).
- `BACKLOG.md` updated: R-3 text, `K-29` (BACKLOG.md:1180), `C-20` (BACKLOG.md:1214). `architecture/program.md` A row = "plans 1–2 of 4 landed". `CLAUDE.md` documents `src/metamodel/`, `src/model/`, `src/debug/`, the step fixtures, and `encode_snapshot_v2`.

## Known Issues, Not Yet Fixed
- **K-29** (diagnosed): `src/data_rover/api/routes/_snapshot.py:54-92` keeps `seen_ids` per kind, so the Python bulk loader accepts an element and a relationship sharing an id; the engine loader refuses it. Logged only; `_snapshot.py` is outside the freeze, fix needs user go-ahead.
- **C-20** (diagnosed): `src/data_rover/core/metamodel/schema.py:198` `_effective_props` comment contradicts code (ancestor definition wins). Frozen under MR-3; log only.
- `Multiplicity.parse`: Python `int()` reads non-ASCII digits, the engine refuses them (documented gap, no fixture).
- No CI; `tests/golden/test_fixtures_current.py` in `core-test` is the only drift enforcement.

## Deferred — Do Not Do
- **Rust/wasm engine** — user chose TypeScript after the cost table; revisit only with profiling evidence.
- **Python core under Pyodide / orjson in Pyodide / Pyodide memory snapshots** — benchmarked dead ends (CN-22).
- **Server-side strict-mode verification / periodic headless audit** — user chose client-enforced.
- **Merging into `main`** — user asked only for the `engine-migration` integration branch.
- **`pyEquals`, `isPlainObject`, `engine-bench`, ambient `TextDecoder`** — cut from plan 1 as YAGNI; belong to C / plan 4.
- **Optimizing load/rebuild (per-element `pyKey` text ≈ 0.23 s; deep array-index scan in `readProps`)** — deferred to plan 4.
- **Writing plan 4 now** — just-in-time after plan 3 lands (user's process choice).
- **Switching server writers to snapshot v2** — sub-project B.

## Plan
1. Verify state (three git commands above). Invoke `superpowers:writing-plans`. Read plan 2's "After this plan" (:5026) and :1–90, then the spec sections for the op applier / working copy and `routes/ops.py`.
2. Scope plan 3 from the spec: the engine-side op applier mirroring `routes/ops.py::_apply_batch` (op shapes of `schemas.OpIn`'s model ops, inverse ops, mid-batch rollback, `id` hints, `id_map`), the working copy (staged ops over the replica, rewind with `ord`-preserving restores), refusal of array-index keys in staged values (export `findArrayIndexKey` from `engine/src/model/load.ts`). Raise any scope ambiguity with the user before building.
3. Build it in a scratch copy of `HEAD` against the real core: extend `tests/golden/model_steps.py`'s Recorder with an op-batch `do` run through `_apply_batch`, extend `engine/test/golden/model-steps.ts`'s `apply` switch, add scenarios + generated fixtures, implement, keep `tsc`×2/eslint/prettier/ruff/mypy/pyright clean at every task boundary.
4. Replay task by task in a second clean copy; generate the plan's code blocks from the verified files; write a matching extractor for the unicode trap.
5. Write the plan to `docs/superpowers/plans/2026-09-18-engine-ops.md` (or dated per the day), present a summary + open questions to the user, and get approval before execution.

## Open Questions
- Execution mode for plan 3 (inline again vs subagent-driven) — ask when the plan is approved.
- Whether to fix K-29 now (outside the freeze, small) or keep it logged — user's call.
