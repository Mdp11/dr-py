# Handoff: K-25 (`GET /model/relationships` is unpaged) — spike, design, plan, execute, close the program

## Mission
Finish the large-model performance program in `/home/mdp/workspace/data-rover-py`
(spec: `docs/superpowers/specs/2026-08-26-large-model-performance-program.md`, § "Program").
K-20, K-6, K-21, K-22, K-23 and **K-24** are merged and pushed. The next session owns **K-25**,
the LAST item (`BACKLOG.md:1095`): `routes/relationships.py:11-27` materializes every
relationship (~240k at scale 320, ~400k on the owner's largest model) into pydantic
`RelationshipOut`s with no `limit`/`offset`; no app caller — page it or delete it.
"Done" = a § "K-25 design" section appended to the spec (Problem with a measurement /
Design / Non-goals — the page-vs-delete decision is the design's), a plan in the K-24 plan's
shape, executed on a worktree branch, merged into `main` with a merge commit, pushed, and a
closing handoff that records the program as complete (all seven items) with pointers to every
design section and BACKLOG entry.

## Orient First
1. `docs/superpowers/specs/2026-08-26-large-model-performance-program.md` — § "Program"
   item 7 (`:45`) is K-25; § "K-24 design" (`:365-448`) is the freshest design-section SHAPE.
2. `src/data_rover/api/routes/relationships.py:11-27` — **the target.** Note the BACKLOG
   entry claims the `source_id`/`target_id` filters are "already served by
   `IndexSet.outgoing_ids`/`incoming_ids`" — the route does NOT use them; it list-scans
   `model.relationships.values()` three times. Verify before repeating the claim.
3. `src/data_rover/api/routes/elements.py` — the PAGED element list to mirror (its
   `limit`/`offset` contract and page-out schema); `src/data_rover/api/authz.py`
   (read-only GETs need no allowlist entry).
4. `frontend/src/lib/api/relationships.ts:20,39,47` — the only client; establish it is
   test-only (`grep -rn "api/relationships" frontend/src` printed no importer at 16:50) and
   decide whether it goes with the route.
5. `tests/api/` — find the route's tests (`grep -rln "model/relationships" tests/`).
6. `docs/superpowers/plans/2026-08-27-untyped-navigation-scope.md` — the K-24 plan, the SHAPE
   to match (header / Global Constraints / cross-task precondition table / literal code and
   test bodies / measurement task with stop conditions / docs / verify+integrate / handoff).
7. `CLAUDE.md` for repo conventions (the K-24 "Scope row sources" bullet is new, `:196`).

Confirm state: `git status --short`; `git branch --show-current`; `git log --oneline -5 main`;
`pixi run core-test`; `pixi run frontend-test`; `pixi run dr-tidy true`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- **The main checkout is NOT on `main`** (as of 16:51): it is on `feat/script-column-inputs`
  (another session's branch, `3e78aa5`) with that session's UNCOMMITTED work —
  `src/data_rover/core/table/cells.py` modified, `src/data_rover/core/table/script_inputs.py`
  and `tests/table/test_script_inputs.py` untracked. **Do not checkout, stash, or touch it.**
  Work in a fresh worktree off `main`; merge via a worktree or plumbing (`git commit-tree`
  + `git update-ref refs/heads/main`) if the main checkout is still occupied when you finish —
  the K-24 merge tree equalled the branch tree, so the branch's test run covered it.
- `main` = `origin/main` = `ea566a7` (K-24 merge, pushed 16:45). Tests on that exact tree:
  `core-test` **2288 passed / 31 deselected**, `frontend-test` **2349 passed / 243 files**,
  `dr-tidy true` exit 0 (all as of 16:30).
- Worktrees on disk owned by OTHER sessions — leave alone:
  `.claude/worktrees/feat-metamodel-diagram-editor`. The K-24 worktree is removed and its
  branch deleted. Stale local branches `perf/large-model-overhaul`,
  `perf/script-column-overhaul`, `worktree-perf+script-column-overhaul` are not mine — leave.
- Fixture: the scale-320 model is at
  `/tmp/claude-1000/-home-mdp-workspace-data-rover-py/4cabd46f-abde-4fbc-becd-17dee88a1256/scratchpad/prod.model.json`
  (320,640 el / 238,720 rel, 145 MB; existed at 16:20). If gone, regenerate with
  `pixi run -e core-dev python examples/generate_large_model.py --scale 320 --out $SCRATCH/prod.model.json`
  (~1 min) into the session scratchpad — **never into the repo**.
- Alembic head `0013`. K-24 added no migration, setting, or API contract change.

## Standing Constraints
- **K-24 (new, load-bearing):** `_scope_ids` returns the ASCENDING-ID list — never insertion
  order (decided against, spec § "K-24 design" Non-goals: visible row-order change for
  non-insertion-ordered ids, breaks the "SORTED element ids" paging contract, buys 24 → 7 ms).
  The untyped branch must never go back through a `set` (hash order is what made the sort
  O(n log n)); the criteria walk iterates a `list(model.elements.values())` SNAPSHOT because
  readers hold no `write_mutex` (`api/serialize.py:86-92` precedent — a live-dict walk under
  Python-level work raises `RuntimeError` on a peer create); the no-criteria
  `sorted(model.elements)` is one C-level call and needs none. Matchers are plain loops;
  `core/search/criteria.py` untouched.
- **K-23:** `_trigrams_of` entry ⇔ indexed (`()` included); hooks never index a builder-owned
  element while `search_ready` is False; `on_property_changed`'s diff is exact only for plain
  strings with no list-valued `name` key — every other case falls back to the whole-element
  path; `verify_consistent` is the oracle.
- **K-22:** `element_order` maintained by the two element hooks only; order-only semantics.
  **K-21:** bytes-sniffing snapshot reader; `iter_model_json` stays the save contract.
  **K-20:** no search index on transient models; `keep_search=True` only at the four rebind
  sites. **K-6:** `entity_states` NULL = reconstruct.
- `Metamodel` immutable after load; `_Caches` reset by `model_copy`. Per-entity validator
  hooks O(entity); nothing O(model) may move to the mutation boundary.
- `docs/` is gitignored — spec and plans never commit. pixi for everything; `dr-tidy` must
  pass (ruff + mypy + pyright; pyright covers `tests/` — no `# type: ignore`). `ruff.toml`
  enables only defaults + `UP`: **no import ordering, no `ANN` rules** (a `# noqa: ANN001`
  is dead — do not add one). Comments/docstrings: present tense, invariants only, no
  spec/plan/phase references, no history narration.
- Integration = merge commit into `main` + push (standing owner policy, `BACKLOG.md`
  Process/infra). **Re-check `git log origin/main` immediately before merging**; another
  session commits to `main` directly and the main checkout may be occupied (see State).
- **Worktree harness guard:** inside a worktree the harness refuses compound Bash (`&&`,
  `;`, loops, heredocs, `$(...)`, pipes). Plain single commands, Write/Edit, or
  `bash <scratchpad script>`. `EnterWorktree` names the branch `worktree-<name>` —
  `git branch -m <real-name>` right after; `ExitWorktree` later REPORTS the stale creation
  name, so read `git branch --list` before merging. `pixi run frontend-install` in a fresh
  worktree before `dr-tidy`. The SDD `sdd-workspace` script resolves to the worktree's own
  `.superpowers/sdd/<plan>/` once inside it — create the ledger there, not in the main checkout.
- SDD model choices (worked on K-6 → K-24): **haiku** for transcription tasks carrying
  literal code and single-file docs edits, **sonnet** for measurement/logic tasks, per-task
  reviews and fix waves, the most capable model for the final whole-branch review only. Run
  suites in the background while a review is pending — never alongside a timing measurement.
- A measurement task has no diff: its report's stop-condition table is the review gate.

## Done
- **K-24 fully delivered, merged (`ea566a7`) and pushed** (4 commits): `78e2038` untyped
  branch walks `model.elements` + empty-criteria shortcut · `bf65638` matchers as plain
  loops · `aeee192` CLAUDE.md bullet + BACKLOG (K-24 done) · `8cbeaf5` final-review fix
  (list snapshot on the criteria walk; CLAUDE.md "navigations pay per page"; dead noqa).
- **Measured at scale 320** (final tree): untyped no-criteria **465 → 26.2 ms**, untyped +
  one `exists` criterion **762 → 321.1 ms**, typed `Person` (51,200 el) 45 → 16.4 ms,
  shuffled-insertion-order degraded case 184.6 ms (today's set-ordered sort was 165 + 48 ms
  set build); 0 of 40 scope shapes differ from the set-based derivation on the ordered and
  the shuffled model.
- Spec § "K-24 design" (`:365-448`) and the plan
  `docs/superpowers/plans/2026-08-27-untyped-navigation-scope.md` are on disk (gitignored).
- BACKLOG K-24 marked `done (2026-08-27, perf/untyped-navigation-scope)` with the Task 3
  numbers (criteria path recorded as 307.4 ms, measured before the list-snapshot fix; the
  merge commit body carries the final 321.1 — a ~10 ms snapshot cost inside run noise).

## Known Issues, Not Yet Fixed
- **`routes/relationships.py:19-25` filters by list scan**, not by `IndexSet.outgoing_ids`/
  `incoming_ids` as the BACKLOG entry claims — three full passes over ~400k relationships
  plus a pydantic conversion per row, per request. This IS K-25.
- The scope matcher's own cost (~0.8 µs per criterion per element in
  `_match_nav_criterion` → `match_element`, shared with `/model/search`) — recorded in the
  K-24 non-goals; nobody owns it.
- The ~11 µs per-write floor and the applier's ~15 µs per op; `_rekey`'s
  `_frozen(properties)` for keyless types (~8 µs); `on_element_deleted`'s redundant
  `_trigrams_of` write-then-pop (cosmetic).
- `_scope_ids`' new docstring sentence uses double backticks where the file uses single
  (cosmetic; parked in the K-24 final review).
- Unowned, carried forward: `element_order` ~16 MiB resident per hydrated session;
  `ENTITY_STATES_MAX` is an entity-count cap (`api/commit_states.py:36`); snapshot blob GC
  (`content.py:229-233`); four tests call `schedule_periodic_snapshot` without
  `write_mutex`; `snapshot_job` row ahead of `models.model_rev` after `touch_model()`;
  `scripts/bench.py:208` pyright note; the validation sweep's other ~33 ms per element chunk.

## Deferred — Do Not Do
- **Returning insertion order from `_scope_ids`** — decided against (spec K-24 non-goals),
  not deferred. **A maintained sorted-id index** (`SortedList` of every id) and **a per-rev
  cache of the untyped id list** — declined: the sort is already O(n) on a presorted input
  and `TableOrderCache` bounds tables to once per commit.
- **A criterion-shaped fast path in the shared matcher** — different item; `/model/search`
  must stay byte-identical.
- **Updating BACKLOG's K-24 numbers to the post-fix 321.1 ms** — ruled inside noise; the
  merge commit carries the final figures.
- Everything K-23/K-22/K-21/K-20/K-6 deferred: a bulk `restore_element(..., properties=...)`,
  diffing refs/rekey/roots per property, the `zip` trigram idiom, per-field trigram storage,
  bypassing hooks under `search_ready=False`, folding the sequence number into
  `uniq_groups`, a sequence field on `Element`, the per-sweep position-map hoist, zstd, an
  `encoding` column, serialize-outside-the-mutex, posting-set shrinking, `entity_states`
  backfill, journal-based `GET /commits/{rev}/model`.

## Plan
1. Read the spec's § "Program" item 7 and `BACKLOG.md:1095`, then `routes/relationships.py`,
   `routes/elements.py`'s paging contract, `frontend/src/lib/api/relationships.ts` and its
   tests; grep `frontend/src` and `scripts/` for any caller of `/model/relationships`
   (none was found at 16:50 — confirm) and find the route's API tests.
2. **Spike at scale 320** in the session scratchpad: time `GET /model/relationships` unfiltered
   and with `source_id`/`target_id`/`type` through the FastAPI test client on a session
   holding the fixture (`build_model_from_dicts` + `SessionRegistry`, or the
   `tests/api/conftest.py` fixtures), and the same filters served by `IndexSet.outgoing_ids`/
   `incoming_ids`/`elements_by_type`-style lookups; count the pydantic conversion cost
   separately.
3. Decide **page-vs-delete** and append § "K-25 design" (Problem with the spike table /
   Design / Non-goals) to the spec. Record: whether a paged route keeps the filters (cheap
   via the adjacency index) or the route goes together with its test-only client; the
   frontend never lists relationships globally.
4. Write `docs/superpowers/plans/<date>-relationships-paging.md` (or `-removal.md`) with
   `superpowers:writing-plans` in the K-24 plan's shape.
5. Execute with `superpowers:subagent-driven-development` on a worktree branch
   `perf/relationships-paging`; pre-flight conflict scan as a table; verify every quoted
   anchor against the tree before dispatching Task 1.
6. Verify (`dr-tidy`, both suites), measure, final whole-branch review on the most capable
   model, merge commit + push (mind the occupied main checkout — see State), remove the
   worktree — then the closing handoff recording the program complete.

## Open Questions
None blocking. The page-vs-delete choice is the design section's to make and record; if
the frontend client is deleted with the route, say so explicitly in the plan's docs task so
`frontend/README.md`'s API surface list stays accurate.
