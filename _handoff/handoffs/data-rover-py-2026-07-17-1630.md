# Handoff: Code-execution feature — decide next steps after M0 GO verdict

## Mission
The M0 go/no-go spike for the code-execution feature (users write/run Python snippets in-browser against the MBSE model) in `/home/mdp/workspace/data-rover-py` is **complete with a GO verdict**. You wrote the design spec and the M0 plan in an earlier session; this handoff hands you the finished, reviewed spike so you can drive the **next-step decisions with the user**: (1) whether to accept GO and expand M1 into a task-level plan, and (2) what to do with the spike branch. "Done" for this arc = user has accepted (or amended) the verdict, and you have either produced the M1 plan or recorded why not, plus resolved branch disposition. **Do NOT start M1 planning until the user explicitly accepts the verdict** (their standing instruction from the plan).

## Orient First
1. `spikes/code_exec/FINDINGS.md` — the committed spike record: 8-criteria results table, per-criterion evidence sections, the **GO verdict**, clean-re-run transcript, and the M1 implementation notes. Read this first and in full; it is the deliverable.
2. `docs/superpowers/specs/2026-07-17-code-execution-design.md` — the approved design spec (gitignored). §4 = architecture, §12 = spike criteria, §14 = milestones. Authoritative for M1+ detail.
3. `docs/superpowers/plans/2026-07-17-code-execution-m0-spike.md` — the executed M0 plan; its tail (lines ~950–976) holds the **M1–M4 milestone-level outline** you'd expand from for M1.
4. `.superpowers/sdd/progress.md` — the full task-by-task ledger of the spike execution (commit ranges, per-task findings, review outcomes, the final-review result). Your recovery map.
5. `CLAUDE.md` — pixi-only toolchain, architecture map, conventions. `spikes/code_exec/host.py` — the shared WASM host helper the probes built (reference for M1's `WasmScriptRunner`).

Confirm state: `git branch --show-current`, `git status --short`, `git log --oneline f2b6b6f..HEAD`. Re-run any probe with `pixi run -e core-dev python spikes/code_exec/<probe>.py` (guest binary must be fetched first: `bash spikes/code_exec/fetch_python_wasi.sh` — `vendor/` is gitignored).
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `feature/code-execution-spike` (11 commits, `3374d96..b6370ec`, off `main` at `f2b6b6f`). **Not merged.**
- Uncommitted: clean (as of 2026-07-17 16:30).
- Tests: N/A — spike has no pytest; verification is the 8 probes. Full clean re-run passed 8/8 EXIT=0 (Task 10, and again spot-checked in final review). Spike code is ruff-clean via `pixi run -e core-dev ruff check spikes/code_exec/*.py` (NOTE: `pixi run lint-core` does NOT cover `spikes/` — cwd=src/data_rover/core).
- Runtime pinned: wasmtime-py **46.0.1** (added to `pixi.toml` core-dev pypi-deps), CPython-WASI **3.14.6** (`brettcannon/cpython-wasi-build`, SHA-256 pinned in `fetch_python_wasi.sh`). Host Python 3.14.5.

## Standing Constraints
- **Verdict = GO: in-process WASM runner as specced.** Interactive FIFO stdio works (p50 0.34 ms) and wasmtime-py releases the GIL (ratio ~1.0) — the two probes GO hinged on. No subprocess fallback needed; **no spec §4 amendment**. Do not relitigate the architecture.
- **M1 planning is gated on explicit user acceptance of the verdict** (plan Task 10 + user's original instruction). Present, then wait.
- M1–M4 were deliberately kept milestone-level, not task-level (user's instruction); expand M1 to task-level only after acceptance.
- Spike code is throwaway-but-committed, print-and-measure, exempt from mypy/pyright (outside `src/`); only ruff applies. `vendor/` (guest binary) is gitignored; only the hash-pinned fetch script is committed. `TrustedRunner` must live under `tests/`, never ship (RCE tripwire) — an M1 concern.
- `docs/` is gitignored — spec and plan are intentionally uncommitted; do not try to commit them.
- Execution method used: `superpowers:subagent-driven-development` (fresh implementer + task review per task, final whole-branch review on opus). All 10 tasks + final review came back clean.

## Done
- All 10 M0 tasks executed, each task-reviewed clean; final whole-branch review (opus) = **GO verdict supported**, no open Critical/Important findings.
- 8 probes written and passing (s01 boot, s02 FIFO bridge, s03 GIL, s04 epoch, s05 memory-cap **with counterfactual**, s06 determinism, s07 warm-pool, s08 50k bench). One Important review finding (s05 proved cap blocks a huge alloc but not the enforcement boundary) was **closed** by adding a counterfactual (same alloc succeeds uncapped, fails at 256 MB cap — commit `b6370ec`).
- FINDINGS.md fully filled: table, evidence, verdict, clean-re-run transcript, M1 implementation notes (memory-cap maps via exit-code+stderr not Trap; epoch → `trap_code==INTERRUPT`; determinism recipe; page-size 500 validated).

## Known Issues, Not Yet Fixed
- None blocking. Minor spike-code nits logged in `.superpowers/sdd/progress.md` for M1 reference only (e.g. s02 doesn't assert guest rc; s07 fds close by refcount; s08 unbatched sample unasserted) — all triaged as leave-as-is for throwaway spike code, none affect the verdict.
- **For the M1 memory-cap test (not the spike):** prove the enforcement *boundary* (an alloc between interpreter-base and the cap is blocked), not just "a huge alloc dies" — the final reviewer's note.

## Deferred — Do Not Do
- **M1–M4 task-level planning** — until the user accepts the verdict. (User instruction.)
- **Subprocess-runner fallback, spec §4 amendment** — moot; in-process works. `s02b_subprocess.py` was committed unrun as documented fallback only; do not wire it in.
- **Third-party packages (numpy/pandas), read-your-own-writes, streaming console output, browser Pyodide runtime, `dr.navigate()`, `snippet_runs` DB table** — all explicit spec non-goals (§1, §9). Do not add.
- **Merging the spike to main** — not decided; it's the user's call (see Open Questions). Do not merge/PR without the user.

## Plan
1. Present the GO verdict to the user (already done in the prior session, but re-surface if they haven't responded) and get an explicit **accept / amend** decision. Do not proceed to step 2 until they accept.
2. On acceptance: expand **M1** (spec §14 M1 + plan's M1 outline) into a task-level plan via `superpowers:writing-plans`, folding in the FINDINGS.md M1 implementation notes (error mapping, determinism recipe, `WasmScriptRunner` from `host.py`, page-size 500). M1 = artifact kind + payload adapter + CodeMirror editor tab + lint endpoint + console run/cancel with op recording + "Stage ops" integration.
3. Resolve branch disposition (see Open Questions) — keep-as-branch / open PR / merge spike scaffolding to main.

## Open Questions
- **Does the user accept the GO verdict** as-is, or want any amendment before M1 planning?
- **Branch disposition** for `feature/code-execution-spike` (11 committed spike commits, not merged): keep as-is on the branch, open a PR for the record, or merge the committed spike scaffolding to `main` as reference? Their call — do not act unilaterally.
