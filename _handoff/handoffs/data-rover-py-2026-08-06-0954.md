# Handoff: Artefacts Phase 1 frontend rewire — execute the plan

## Mission
Execute the reviewed implementation plan `docs/superpowers/plans/2026-08-06-artefacts-phase-1-frontend-rewire.md` in `/home/mdp/workspace/data-rover-py`, using **superpowers:subagent-driven-development**, on a fresh branch `feat/artefacts-phase-1-frontend` off `main`. The slice moves artifact editing (navigations, tables, code snippets) off legacy `POST/PUT/DELETE /artifacts` onto the lock→edit→commit flow: `art:<id>` lease on editor open, Save stages an artifact op, `POST /commits` lands mixed batches. "Done" = all 12 tasks executed with per-task review, a whole-branch review + fix wave, all suites green, merged to `main` (ask the user before merging).

## Orient First
1. `docs/superpowers/plans/2026-08-06-artefacts-phase-1-frontend-rewire.md` — **THE plan.** Read it fully: its Decisions section (8 numbered, user-visible choices), Global Constraints, and the Backend-wire-contract reference block are binding on every task brief. Note its own warning: **sample code is illustrative** — executors must re-read target regions and follow intent over transcription (the backend plan's snippets had defects in 6 of 9 tasks).
2. `CLAUDE.md:136` — the "Artifact ops (artefacts revamp Phase 1)" paragraph: the accurate summary of the backend this frontend speaks to, including the Lease rule (lease, not OCC, is the concurrency control).
3. `frontend/README.md` — client architecture (staged-commit flow, editor stores, feed). Required before touching `frontend/src/lib/state/`.
4. `docs/superpowers/specs/2026-07-29-artefacts-revamp-design.md:52-69` — locking + Phase 1 sections of the approved design (gitignored, never commit it).
5. Frontend ground truth the plan was verified against (line refs are as-of `5316210`): `frontend/src/lib/state/ops.ts` (75 lines, no artifact ops yet), `checkout.svelte.ts` (registry :58, `ensureCheckout` :147, `commitStaged` :270, `lockedResourcesNeededBy` :300, `_discardWith` :341), `edit-gate.ts`, `artifacts.svelte.ts`, `navigation-editor.svelte.ts` (`ensureDraft` :466, `saveDraft` :665), `table-editor.svelte.ts` (:1006/:1050/:1370), `snippet-editor.svelte.ts` (:239/:330), `components/DiffDrawer.svelte` (:85 total, :401 commit gate), `realtime.svelte.ts` (:95-139), `api/types.ts` (:203 LockTargetIn, :284 CommitResponse, :358 ArtifactHeader — TDZ ordering matters).

Confirm state: `git status --short`, `git log --oneline -3`, and the frontend suite: `pixi run -e frontend bash -c 'cd frontend && npm test'` (must be inside `frontend/` — bare `pixi run -e frontend npm test` fails). Backend untouched-check later: `pixi run core-test`.

Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main` at `5316210`, clean tree, not pushed. **No feature branch exists yet** — create `feat/artefacts-phase-1-frontend` off `main` as the first act.
- Tests: frontend vitest **1525 passed / 169 files** (run 2026-08-06 09:51, at `5316210`). Backend `pixi run core-test` = 1576 passed (verified previous session at the identical tree). Playwright e2e NOT run this session.
- Housekeeping from the last handoff is done: merged branch deleted, SDD scratch removed.
- The user has seen the plan summary and chose subagent-driven execution (this handoff's mission). They have NOT line-reviewed the plan document itself; surface anything that smells wrong rather than transcribing it.

## Standing Constraints
- **The plan's Decisions 1-8 are settled** (save=stage into the shared commit batch; frontend abandons legacy write routes while backend keeps them; no `artifact_rev` on ops; temp-id tabs re-key only at commit; token partition so model-only commits keep open editors' leases; never send an empty commit — backend `routes/commits.py:520` orphans lock_tokens; no proactive peer-conflict marking; HistoryDrawer stays on client-side reconstruction). Reasons are written next to each in the plan — do not relitigate without the user.
- **Frontend-only slice.** Any backend change that seems needed is a scope change — stop and surface it.
- **Task execution order is 1 → 2 → 3 → 4 → 6 → 7 → 8 → 5 → 9 → 10 → 11 → 12** — the plan lists Task 5 (sidebar library) before the editors but its own ordering note defers it until after Tasks 6-8 (the editors' create helpers die when the editors stop calling them). Put the order in the task briefs.
- **Coalescing is correctness, not tidiness**: the backend 422s `[create tmp_x, update tmp_x]` (update ids are not resolved through id_map), so the staging store MUST fold update-over-create into the create (plan Task 3).
- **Registry canonicalization is load-bearing**: lease grants come back as `art:<id>` but lock targets are sent with bare id + `type:"artifact"`; without `canonicalResource` in `alreadyHeld` (plan Task 4a) every editor re-open acquires a duplicate lease.
- Elements keep BARE lock resource ids — badges and registry keys for elements must not grow prefixes.
- `types.ts` zod schemas are `const`s: `ArtifactHeaderSchema` must MOVE above `CommitResponseSchema` before being referenced (TDZ at module init).
- One git commit per task (each task's final step); dense invariant-comment style preserved in touched files.
- Execution discipline that worked for the backend slice and should repeat: fresh subagent per task, task review after every task, fix round for every Critical/Important, then one whole-branch review with a single fix wave.
- `pixi` only, no global node; frontend npm commands must `cd frontend` first.

## Done
- Plan written, self-reviewed, and saved (this was the whole previous session's deliverable). Self-review fixes already applied in place: `assertNoNameClash` fully specified in Task 6, Tasks 7/8 converted from "mirror Task 6" prose to explicit per-edit checklists.
- Frontend vitest baseline confirmed green at `5316210` (see State).
- All backend/wire facts in the plan's reference block were verified against real code this session (schemas.py, routes/commits.py, routes/artifacts.py, locking.py, feed.py, artifact_ops.py) — trust the plan's contract block over memory.

## Known Issues, Not Yet Fixed
All backend, all deliberately deferred, all bearing on the plan (details in the plan + previous handoff):
- Empty `POST /commits` with `lock_tokens` orphans leases until TTL (`src/data_rover/api/routes/commits.py:~520`) — the plan guards client-side (Task 4 guard + Task 9 total).
- No OCC on the artifact op path (`UpdateArtifactOp.artifact_rev` optional, never sent) — by design this slice.
- `locking.py:~238` `peer_leases` ignores mode; `verify_held` ignores intent — inert for this slice.
- `clone_project` drops artifacts (Phase 3 fixes); root-level `View.artifacts` dropped on round-trip (Phase 2 fixes).

## Deferred — Do Not Do
- **Backend retirement of legacy `PUT/DELETE/POST /artifacts` routes** — plan Decision 2 keeps them alive this slice (frontend goes grep-clean instead); retirement is a later backend cleanup.
- **HistoryDrawer onto `GET /commits/{rev}/diff`** — plan Decision 8; separate follow-up slice.
- **Proactive draft-conflict marking from feed artifact events** — plan Decision 7 (leases make it near-unreachable; commit 409 is the backstop).
- **Artifact OCC, portable CR doc extension, real CR workflow, revert-across-artifact-commits, Phase 2-4 plans upfront** — all carried from the previous handoff; unchanged.

## Plan
1. Invoke **superpowers:subagent-driven-development**. Create `feat/artefacts-phase-1-frontend` off `main` (use superpowers:using-git-worktrees if isolation is wanted).
2. Execute the plan task-by-task in the order 1,2,3,4,6,7,8,5,9,10,11,12 — one subagent per task, each brief carrying: the task's full text, the plan's Decisions + Global Constraints + wire-contract block, the "sample code is illustrative — re-read targets first" warning, and the relevant Standing Constraints above. Task review after each; fix rounds for Critical/Important findings.
3. Whole-branch review (superpowers:requesting-code-review) + one fix wave. Full gates: frontend vitest + `npm run check` + `npm run lint`, `pixi run core-test` (proves backend untouched), Playwright e2e (Task 12; `rm -f /tmp/data-rover-e2e.db` first; WASM specs need `bash spikes/code_exec/fetch_python_wasi.sh`).
4. Ask the user before merging to `main` (superpowers:finishing-a-development-branch). After merge: update the `CLAUDE.md` frontend notes if the review says so, then the next slice is the **Phase 2 (view) plan**, written only after this lands — carry this handoff's Deferred section forward.

## Open Questions
None. (The plan flags three executor-level judgment calls in its Self-Review — `markEditorLockDenied` placement, DiffDrawer tab relabel, exact snippet payload literal — decide them in-task, no user input needed.)
