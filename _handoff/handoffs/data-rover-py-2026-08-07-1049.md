# Handoff: Artefacts Phase 2 — View as First-Class Content (Backend)

## Mission
Execute the implementation plan at
`/home/mdp/workspace/data-rover-py/docs/superpowers/plans/2026-08-07-artefacts-phase-2-view-backend.md`
via **superpowers:subagent-driven-development** (user's explicit choice — fresh subagent
per task, review between tasks). Project: `/home/mdp/workspace/data-rover-py`. Done =
all 11 tasks committed on a feature branch, checkpoints green (`pixi run core-test`,
`backend-lint`, `core-lint`), then merge per superpowers:finishing-a-development-branch.

## Orient First
1. `docs/superpowers/plans/2026-08-07-artefacts-phase-2-view-backend.md` — THE plan; 11 tasks with full code, tests, checkpoints. Execute it task-by-task.
2. `docs/superpowers/specs/2026-07-29-artefacts-revamp-design.md` — the approved four-phase spec; Phase 2 section + locking section are the authority behind the plan.
3. `CLAUDE.md` — toolchain (everything through pixi), test conventions, the Phase 1 artifact-ops paragraph the new work extends.
4. `src/data_rover/api/artifact_ops.py` — the Phase 1 pattern the view applier mirrors (full-state inverses, split_ops chokepoint, restore mode).
5. `src/data_rover/api/routes/commits.py` — commit flow the plan extends (staleness branches, lock verify, mixed-batch atomicity, `_batch_touched_ids`/`_affected_ids`).
6. `docs/superpowers/plans/2026-07-29-artefacts-phase-1-artifact-platform-backend.md` — house style precedent for task execution (same structure, same guardrail protocol).

Confirm state: `git status --short` (expect clean), `git log --oneline -3` (expect head `e8c5639`), `pixi run core-test` if you want a baseline (last known: 1576 passed, as of the Phase 1 merge).

Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main`, clean, head `e8c5639` (Phase 1 frontend merge). No feature branch created yet.
- Tests: core 1576 passed, frontend vitest 1697/1697, Playwright 41/41 — all as of the Phase 1 merge (previous session). Not re-run in this session.
- The plan + this handoff are the only artifacts of this session; no code was touched. `docs/superpowers/{plans,specs}/` are gitignored, so the plan never shows in git status.
- Alembic head is `0008`; the plan's Task 3 adds `0009_view_rev`.

## Standing Constraints
- **Backend only.** Frontend untouched; a separate Phase 2 frontend-rewire plan comes after (mirrors the Phase 1 two-plan split). The plan's op field names / lock type `"folder"` / scope `"view"` are the wire contract that plan will mirror into `ops.ts` — do not rename mid-implementation.
- **Op semantics were derived from a frontend survey this session** (an Explore agent read `view-ops.ts`, `view-tree.ts`, `checkout.svelte.ts`, `ops.ts`): element order in a folder is user-meaningful → placement ops carry `index`; sibling-folder order renders alphabetically → `index` still carried/concretized purely for byte-identical inverses; one element ↔ at most one folder; artifact refs may sit in many folders (once per folder); the client deliberately sends no per-op OCC precondition — the lease is the concurrency control.
- Element placements never target the root: "at root" = unplaced, so move-to-root is `remove_element`. Artifacts have a real root list (`View.artifacts`); root membership locks as `folder:root` (`VIEW_ROOT_ID = "root"`).
- Folder-id migration is lazy (heal at hydration/PUT/import, `bump_rev=False` — normalization is not an edit); the ONLY Alembic migration is `view_rev`.
- `split_ops` becomes a 3-tuple in Task 4; pyright is deliberately used to enumerate every call site. Two TEMPORARY guards land in Task 4 (commits 422, `_batch_touched_ids` no-op branch) and are removed in Tasks 7/9 — the Task 11 sweep greps for `TEMPORARY (plan Task`.
- Revert refuses ranges containing view ops (409), same deliberate boundary as Phase 1's artifact refusal.
- Guardrail protocol in the plan header: TDD per step, checkpoints at Tasks 3/6/7/10/11 run `pixi run core-test`; never batch two tasks in one commit; commit messages end with the Co-Authored-By line.

## Done
- Spec read; Phase 2 plan written, self-reviewed (spec coverage, placeholder scan, type consistency — fixed a broken parametrized test, the MoveFolderOp cycle-check draft, and moved the `_batch_touched_ids` temporary branch from Task 6 to Task 4 where pyright first demands it).
- Frontend survey completed and folded into the plan's constraints (no frontend work done).

## Known Issues, Not Yet Fixed
- `DELETE /view` clears `session.view` but leaves `ViewRow`, so eviction resurrects the view (`src/data_rover/api/routes/view.py:54`). Pre-existing, unused by the frontend — listed in the plan's out-of-scope; do not fix in passing.

## Deferred — Do Not Do
- Frontend rewire (staged view ops, folder-id DnD, lease-at-drag-start, retiring `view-diff.ts`) — deferred by me to a follow-up plan per the Phase 1 precedent; user approved by choosing to execute this backend plan.
- View-scoped feed events for the legacy PUT; renaming the view itself (no `View.name` op); multiple named views; revert across view ops — all explicitly out-of-scope in the plan/spec.
- Phase 2 frontend carried-forward UX gaps from the Phase 1 session (denied-checkout editor still typeable; Save-as-on-denied-tab alternative) — belong to the frontend plan, not this one.

## Plan
1. Invoke `superpowers:using-git-worktrees` (or create branch `feat/artefacts-phase-2-view-backend` from `e8c5639`) — the plan expects isolated feature work.
2. Invoke `superpowers:subagent-driven-development` and execute the plan task-by-task, Task 1 first (core folder identity: `Folder.id`, `core/view/ids.py`, `tests/view/test_folder_ids.py` — full code is in the plan).
3. Review each task's diff between subagents; at checkpoints (3/6/7/10/11) run `pixi run core-test` + lints and stop on any unrelated failure.
4. After Task 11: `superpowers:finishing-a-development-branch` (Phase 1 precedent: merge to main, delete branch).

## Open Questions
None.
