# Handoff: Snippet-docs arc — SDD execution from Task 1

## Mission
Execute the snippet API documentation plan in `/home/mdp/workspace/data-rover-py` via `superpowers:subagent-driven-development` (user chose subagent-driven). The feature: autogenerated in-app docs for the snippet `dr` facade — docstrings in `facade_src.py` as single source of truth, AST extraction, `GET /snippets/docs` endpoint, docs panel, CM6 autocomplete + hover. "Done" = all 6 plan tasks implemented + task-reviewed, final whole-branch review (opus) clean, full gates green (`pixi run tidy`, `test-core`, frontend `npm test`/`check`/`lint`, e2e), then `superpowers:finishing-a-development-branch` (user decides merge).

## Orient First
1. `docs/superpowers/plans/2026-07-18-snippet-docs.md` — THE plan; 6 tasks, complete code per task; Global Constraints + self-review notes at the tail (judgment calls implementers must not "fix").
2. `docs/superpowers/specs/2026-07-18-snippet-docs-design.md` — approved spec; U1–U4 are user decisions (in-app audience; content = facade + project vocabulary + limits; autocomplete NOW; Approach A mechanism). Do not relitigate.
3. `src/data_rover/core/script/facade_src.py` — the facade Task 1 adds docstrings to (currently NO per-member docstrings). NOTE: `dr.create`/`dr.connect` return temp-id STRINGS (lines 183/196), not Elements — spec/plan already corrected for this.
4. `.superpowers/sdd/progress.md` — ledger. Tail = completed M1-frontend arc (merged); START A NEW SECTION "Snippet Docs" for this arc.
5. CLAUDE.md + `frontend/README.md` — repo mandates (pixi-only, cd-frontend npm rule, state architecture).

Confirm state: `git branch --show-current` (expect `main`), `git status --short` (expect clean), `git log --oneline -3` (HEAD `66168e2` = M1 frontend merge). Baseline as of 2026-07-18 ~14:10: frontend 127 files/1026 tests green on merged main; `test-core` 1067 passed/15 deselected.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main`, clean, HEAD `66168e2` (M1 frontend arc merged --no-ff and its branch deleted). The new arc branch `feature/snippet-docs` does NOT exist yet — plan Task 1 Step 1 creates it off main.
- Tests: green on merged main (frontend 1026, test-core 1067) as of merge verification 2026-07-18 ~14:10.
- main is 32 commits ahead of origin/main, unpushed (never push without the user).
- WASM guest binary fetched at `spikes/code_exec/vendor/python.wasm` (docs e2e doesn't need it; existing run e2e does).
- User's dev Postgres: host port 5432 is owned by ANOTHER project's container (`local_dev_postgres`); this project's `data-rover-pg` currently has no published host port. User was given stop-or-remap options; unresolved, irrelevant to test suites (hermetic) but relevant if manually booting the backend.

## Standing Constraints
- **Execution method**: subagent-driven-development. Models per plan tail: T1/T2/T4/T5/T6 sonnet implementers, T3 haiku, all task reviewers sonnet, final whole-branch review opus. Always name the model in dispatches.
- **File handoffs**: briefs via `scripts/task-brief PLAN N` (skill dir `~/.claude/plugins/cache/claude-plugins-official/superpowers/6.1.1/skills/subagent-driven-development/`), diffs via `scripts/review-package BASE HEAD` (record BASE before dispatch, never HEAD~1), reports to `.superpowers/sdd/task-N-report.md`. Fix loops → SAME implementer via SendMessage; re-review → SAME reviewer.
- **Frontend npm ONLY via** `pixi run -e frontend bash -c 'cd frontend && npm ...'`; never check gate exit codes through pipes (masks status).
- **Plan judgment calls — do not let implementers/reviewers "fix" them** (recorded in plan self-review): concrete-types-only in `vocabFromMetamodel`; heuristic `Element.` completions need a typed partial or explicit trigger; `dr.connect("` gets NO type completion (first arg is a relationship type); `__getitem__` undocumented (covered under `Element.get`).
- **Tripwire is the point**: `get_facade_docs()` must raise on any undocumented public member — a reviewer calling it too strict is wrong by spec.
- **Endpoint runner-independent**: docs served even when runner is None (503 for /run). Frontend degrades silently (no toasts) on docs-fetch failure.
- **Backend snippet run/lint/cancel contract frozen** — this arc only ADDS `GET /snippets/docs`.
- **Only doc-model completions**: `autocompletion({override: [source]})`; if config-merge with basicSetup's default autocompletion complains, keep only the configured instance (fallback documented in plan T5 Step 6).
- IDE diagnostics about missing modules for new test files are known TS-server noise; `npm run check` is the gate.
- Commit trailer: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. docs/ is gitignored — spec+plan stay uncommitted.
- Prior-arc precedent (T4 adjudication, in ledger): plan-mandated defects may be fixed without user round-trip ONLY when the fix fulfills the plan's stated intent; otherwise ask.

## Done
- M1 code-execution FRONTEND arc: all 9 tasks + final opus review (1 Critical + 1 Important found and fixed in `04badf3`, re-review = merge Yes), merged to main `66168e2` (--no-ff), feature branch deleted, suites re-verified on merged result. Full record: `.superpowers/sdd/progress.md`.
- Snippet-docs feature: brainstormed (U1–U4 user-decided), spec written + approved, plan written + self-reviewed. NO implementation yet — zero code written for this arc.

## Known Issues, Not Yet Fixed
- Pre-existing e2e environment flake: `{"detail":"invalid session"}` hits a different spec per full-suite run (~32/33). Not this repo arc's regression; flagged in T9 report of prior arc.
- M2-triaged Minors from the M1-frontend final review (ledger tail): cross-project store-reset gap; stageError persistence; `'missing'`/`update_relationship` stage coverage; e2e dialog handlers in tests 1–2; onReloadModel reset asymmetry; sequential stage prefetch. Do NOT fix in this arc.

## Deferred — Do Not Do
- Property-key completion in `el.set("…")`, docs search, insert-on-click, panel resize persistence, relationship-type completion for `dr.connect(` — spec Out-of-scope, user-approved.
- Replacing the script README with generated output — it stays, gains one canonical-pointer line (plan T6).
- M2/M3/M4 of the parent code-execution roadmap (embedding UIs, real cancel, polish) — milestone-gated by user.
- Pushing main or any branch / opening PRs — never requested.

## Plan
1. Invoke `superpowers:subagent-driven-development`. Start a "Snippet Docs" section in `.superpowers/sdd/progress.md` (plan path, spec path, branch, baseline counts).
2. Task 1 (facade docstrings + `core/script/docs.py` + tripwire tests; creates branch `feature/snippet-docs` off main): `scripts/task-brief docs/superpowers/plans/2026-07-18-snippet-docs.md 1` → sonnet implementer → `review-package` → sonnet reviewer → ledger line. Record BASE (= HEAD of main, `66168e2`) before dispatch.
3. Tasks 2–6 same loop (T2 sonnet, T3 haiku, T4 sonnet, T5 sonnet, T6 sonnet incl. e2e + `pixi run tidy`).
4. Final whole-branch review: `scripts/review-package $(git merge-base main HEAD) HEAD` → opus via requesting-code-review's code-reviewer.md; hand it the arc's deferred-Minors list; ONE fix subagent if findings; re-review by same reviewer.
5. `superpowers:finishing-a-development-branch` — user decides (prior two arcs merged --no-ff to main, no push).

## Open Questions
None.
