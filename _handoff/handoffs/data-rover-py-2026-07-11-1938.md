# Handoff: Stage 2 (Table system) — frontend plan, 8/12 done, finish FE Tasks 9-12 + whole-branch review

## Mission
Complete **Stage 2 — Table system** for the data-rover MBSE engine at
`/home/mdp/workspace/data-rover-py` via subagent-driven development. The **backend plan
(12 tasks) is COMPLETE + green**; the **frontend plan is 8/12 done** on branch
`feat/stage2-tables`. This session must: execute **FE Tasks 9-12**, then run ONE
whole-branch review (backend + frontend), dispatch ONE fix wave, then finish the branch.
"Done" = both plans' tasks implemented, all suites green, svelte-check 0 errors, lint clean
(no NEW prettier failures), whole-branch review clean, branch integrated.

## Orient First
Read, in order:
1. `~/.claude/handoffs/data-rover-py-2026-07-11-1503.md` — the PRIOR handoff (backend done,
   frontend plan to execute). Its Standing Constraints + Deferred sections still bind.
2. `.superpowers/sdd/progress.md` — the **live ledger** (gitignored). The "Slice: Stage 2"
   section has per-task detail for all 12 backend + FE Tasks 1-8 (commit SHAs, reviews,
   every Minor deferred to final review). **Trust this + `git log` over memory.**
3. `docs/superpowers/plans/2026-07-10-stage-2-tables-frontend.md` — the 12-task FE plan.
   FE Tasks 9, 10, 11, 12 remain. Read their sections + the Self-Review notes (bottom).
4. `docs/superpowers/specs/2026-07-10-stage-2-table-system-design.md` — Stage 2 design.
5. `CLAUDE.md` — toolchain (pixi), architecture, gotchas. `frontend/README.md` before
   touching `frontend/src/lib/state/`.
6. Re-invoke `superpowers:subagent-driven-development` — it defines the per-task loop.
   Helper scripts at `/home/mdp/.claude/plugins/cache/claude-plugins-official/superpowers/6.1.1/skills/subagent-driven-development/scripts/`
   (`task-brief PLAN N`, `review-package BASE HEAD`). Run from repo root.

Confirm state:
- FE unit: `pixi run -e frontend bash -c 'cd frontend && npm test'`
- FE types: `pixi run -e frontend bash -c 'cd frontend && npm run check'`
- FE e2e: `pixi run -e frontend bash -c 'cd frontend && npm run test:e2e'`
- Backend (should still be green): `pixi run -e core-dev pytest tests/ -q` ; `pixi run lint-core && pixi run lint-backend`
- `git status --short && git branch --show-current && git log --oneline -12`

Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `feat/stage2-tables` (merge-base with main = `8383a0d`).
- HEAD: `85fea25` (FE Task 8). Backend commits: `d02452b..7b7dfc7`. FE commits: `387e633..85fea25` (10 commits incl. 2 fix commits `afcae4b`, `01698d3`).
- Uncommitted: **clean** (as of 2026-07-11-1938).
- FE unit tests: **795 passed** (107 files), svelte-check **0 errors/0 warnings**, lint clean, as of FE Task 8 (2026-07-11 ~1935). FE e2e NOT run since baseline (deferred to FE Task 11).
- FE green baseline at session start: 754 unit / check 0.
- Backend: green at handoff start (936 passed / 2 deselected [opt-in GCS integration test], lint clean). NOT re-run this session (no backend files touched).
- gitignored, NOT committed, NOT pushed: `.superpowers/` (ledger + task briefs/reports) and `docs/` (spec+plans). Back up `docs/` if the user values it (it exists only locally).

## Standing Constraints
- **Execution = subagent-driven.** One sonnet implementer + one sonnet reviewer per task.
  Sonnet has been the right tier for every FE task. Fix-loop Critical/Important; Minors → ledger.
- **Commit trailer THIS slice** (verbatim): `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- **FE npm scripts MUST run via** `pixi run -e frontend bash -c 'cd frontend && <cmd>'` (bare form fails "Missing script").
- **Two pre-existing prettier failures** on `npm run lint` (`ProjectCard.test.ts`, `UsersTab.test.ts`) — DO NOT fix, DO NOT add new ones.
- **svelte-check (`npm run check`) is the authoritative type gate.** The harness's ambient
  LSP throws FALSE `Cannot find module '$lib/...'`, `typeof import("*.svelte")`, `resetModelStore
  does not exist`, spy `not assignable to 'default'/'never'`, and `'err' is unknown` diagnostics
  on every FE task — the `$lib` alias + `.svelte.ts` runes modules only resolve through
  svelte-kit sync. ALWAYS re-run `npm run check` to confirm 0 errors; ignore those diagnostics.
- **`@testing-library/svelte` is NOT a dependency.** FE component tests use the repo's
  Svelte-5 mount/unmount/flushSync convention (see `frontend/src/lib/components/Table/__tests__/TableGrid.test.ts`).
  The plan's snippets say `@testing-library/svelte` — adapt to mount/unmount.
- **Frontend uses TABS** (prettier). Plan snippets use spaces — match repo tab style.
- `untrack()` idiom for register/unregister-in-`$effect` cards (see `PathCard.svelte`).
- **Cell editing reuses the Inspector path EXACTLY** (`editLock`→`emit`→DiffDrawer→POST /commits).
  Never a new mutation surface. Already implemented in FE Task 6.
- **Backend wire schemas the FE mirrors** (verify against source): `src/data_rover/core/table/schema.py`,
  `src/data_rover/api/schemas.py` (TableCellOut is a FLAT all-optional model; the FE's
  discriminated-union `TableCellSchema` parses it because zod strips null extras — confirmed),
  `src/data_rover/api/routes/tables.py`. `View.artifacts` root field exists (backend Task 11).
- **Reuse boundaries discovered this session (load-bearing for FE Task 9):**
  - `ScopeEditor.svelte` IS a clean controlled component (`{scope,onChange}`); ScopeRows ≡ NavScope.
  - `NavigationNode.svelte` is STORE-COUPLED (reads `getDraft(tabId)` from navigation-editor) —
    NOT a controlled component. Do NOT embed it in table editors. Nav sources use a saved-nav
    REF picker over `getArtifactHeaders().filter(kind==='navigation')` → `{navigation:{ref}}`.
    Inline nav-definition-in-a-table = Stage-2.1 deferral (already taken in FE Task 7).
  - `onCommitEvent(cb)` in `state/realtime.svelte.ts` is the feed seam (fires for local+peer
    commits); table-editor registers via it at module load (one-way import, no cycle).
  - `openArtifactTab(kind, opts)` (workspace, + `$lib/state` barrel) is the tab opener;
    `openNavigationTab` is a thin wrapper. `bindTabToArtifact` derives prefix from tab kind.
- **FE Task 10 (root artifacts):** placement must NOT go through `findFolderByPath(view, [])`
  (detached virtual root, silently drops writes). Add explicit `path.length===0` branch in
  `view-ops.ts`. `apiFetchRaw` already applies CSRF (used for xlsx blob download).
- **Locked design (do not relitigate):** row identity = tuple of bindings; RowStart sentinel;
  present:false wire flag; columns.ts pure module; sort empties-last both directions; server-side xlsx.

## Done
- **Backend plan: ALL 12 tasks complete, reviewed, green** (commits d02452b..7b7dfc7). Ledger has detail.
- **FE Tasks 1-8 complete, each reviewed APPROVED, green** (commits 387e633..85fea25):
  - T1 (387e633): table API types + client (`api/types.ts`, `api/tables.ts`). Threaded corrections:
    NO CriterionSchema (criteria=z.array(unknown)), schema_version default=1, reuse TreeItemSchema/NavigationDefinitionSchema.
  - T2 (2788062): pure `lib/table/columns.ts` (add/remove/move/rename/setWidth/setMode/columnLabel/ColumnInUseError; moveColumn ColumnRef remap + forward-ref reject).
  - T3 (ed965ca + fix afcae4b): `state/table-editor.svelte.ts` store (draft lifecycle/paging/sort/save/conflict/export). Fix: moveTabState re-issues load on save-while-loading race.
  - T4 (40bc1bb): workspace tabs `kind: navigation|table`; `openArtifactTab`; Workspace.svelte kind dispatch; minimal TableView stub (replaced in T5).
  - T5 (f23a36d): read-only `TableView` + `TableGrid` + 4 cell components (windowed rows, resize-on-release).
  - T6 (60107fd + fix 01698d3): inline ValueCell editing via Inspector path; columnName threaded TableView→TableGrid→ValueCell. Fix: restored exact `cell.present` read-only gate.
  - T7 (d79880e): ColumnManager + RowSourceEditor + NavigationColumnEditor + mount in TableView.
  - T8 (85fea25): `createTableArtifact`; sidebar Tables section (SECTIONS config); `handleTableModelRevChanged` feed refetch via onCommitEvent.
- 12 FE TaskCreate todos exist (IDs 1-12); IDs 1-8 marked completed. IDs 9-12 pending.

## Known Issues, Not Yet Fixed
None blocking. Accumulated **Minors** (backend + FE) are deferred to the whole-branch review's
fix-wave triage — the FULL list is in `.superpowers/sdd/progress.md` (search "Minor for final review"
and the backend "OUTSTANDING (B)" line). Notable FE ones to feed the final reviewer:
- FE T1: backend numeric bounds (chain_index/index ge=0, cell_cap ge=1, columns max_length=50) not mirrored as zod constraints.
- FE T2: no bounds-checking on index/from/to (TypeError not domain error on stale index); untested successful-forward-move + addColumn arg-clone aliasing; columns.ts:61 dead dangling-source branch.
- FE T6: `columnNameFor` recomputes `getTableDraft` per cell (matches existing `widthFor` pattern).
- FE T7: moveColumn forward-ref throw path untested; NavigationColumnEditor setSourceKind no guard for 0-prior-columns (UI-unreachable).
- FE T8: module-level `onCommitEvent` registration has no init/dispose lifecycle (breaks if a future `resetRealtime()` call path is added — consider `initTableEditorFeedTap()` from startup); handler test only exercises 1 open tab; no dedicated Tables-section component test.
- Backend: `_base_slots_from_keys` derivation duplicated 3× (factor a helper); evaluate.py:329 `isinstance(v,list)` vs cells.py `(list,tuple)`; several coverage gaps (ChainRows build_rows, binding-col-over-budget sort, mixed-datatype `_property_is_numeric`, rule-4 expand-property static, vacuous `all()` in a Task-2 nav test); Task-3 ElementColumn-sources-EXPAND-PropertyColumn static hazard; pre-existing `def client` fixture copy-pasted across 26 tests/api files.

## Deferred — Do Not Do
- **expand_property_values ValueError→422 on heterogeneous-scope expand** (core/table/cells.py):
  USER DECIDED (2026-07-11) to KEEP the 422. Graceful per-row degrade is Stage-2.1. Do NOT add it. (Finding A CLOSED.)
- **Inline navigation-definition editing inside a table row-source/column** — Stage-2.1. Only the
  saved-navigation REF picker is offered (FE Task 7 decision, sanctioned by the plan). Do not build it.
- **Stage 2 non-goals** (spec, user-approved cuts): per-datatype column formats (only header+width_px
  persist); CSV export (xlsx only); bulk cell edit / relationship editing from tables / editing derived
  columns; column virtualization; cursor pagination. Do NOT add back.
- Stage 3 (Block diagrams) — separate later stage. Stage 1 deferred follow-ups — not part of Stage 2.

## Plan
1. Re-invoke `superpowers:subagent-driven-development`; re-read this Orient First + the ledger.
2. **FE Task 9 (base 85fea25): open-navigation-as-table + table tree node.** `task-brief <plan> 9`.
   Adds `navigationAsTableDefinition({artifactId, definition})` to `lib/table/columns.ts` (chains
   row_source: `{ref}` if saved else `{definition}`; one element column per chain step, `chain_index`
   0..n, headers from step relationship types — reuse `chainColumns` from `navigation/tree.ts`).
   NavigationBuilder gets an "Open as table" button (reuse `isRunnable`); TreeRow.svelte dispatches
   open by `artifactHeader.kind` (table icon vs Route). Verify the plan's zod/type snippets against
   real types first (NavScope start shape — the plan's `{kind:'row'}` filler is invalid, use a real
   NavScope; FE Tasks 2 & 9's own tests hit this — see ledger). Dispatch sonnet impl + sonnet reviewer.
3. **FE Task 10 (root-level artifact placement, view ops):** explicit `path.length===0` branch in
   `view-ops.ts` (NOT findFolderByPath(view,[])); add `View.artifacts` to FE types (backend field exists);
   render root artifacts in the view tree. Dispatch impl + reviewer.
4. **FE Task 11 (e2e table flow):** `frontend/e2e/table.spec.ts` — nav-as-table → add column → edit+commit
   → save+reopen. Then FULL e2e regression (`npm run test:e2e`). The e2e harness boots backend+dev server itself.
5. **FE Task 12 (integration sweep):** full unit suite + check + lint (no NEW prettier failures) + commit fixups.
6. **ONE whole-branch review** (most-capable model, `review-package $(git merge-base main HEAD) HEAD`)
   covering BOTH plans; feed it the FULL Minors list from the ledger to triage. Dispatch ONE fix subagent
   with the complete findings list (not one-per-finding).
7. `superpowers:finishing-a-development-branch` — ask user merge vs PR.

First concrete action: re-invoke the SDD skill, append a "session resumed" line to the ledger,
then `task-brief docs/superpowers/plans/2026-07-10-stage-2-tables-frontend.md 9` and dispatch a
sonnet implementer (brief path + the reuse-boundary + real-NavScope-start notes above + report path).

## Open Questions
- None blocking. At branch-finish, ask the user merge vs PR (per finishing-a-development-branch),
  and whether to push `feat/stage2-tables` to origin (prior slices merged to main but main was NOT
  pushed to origin as of the 1503 handoff — confirm current origin state with `git log origin/main..main`).
- The gitignored `docs/` spec+plans exist only locally — offer to back them up if the user values them.
