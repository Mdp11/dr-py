# Handoff: Exporter v2 Phase 1 execution

## Mission
Execute Phase 1 of the Exporter v2 feature in `/home/mdp/workspace/data-rover-py`: rename the `custom_export` artifact kind to `exporter` end-to-end, restructure its payload (`output` options + per-entry `folder`), and land the template engine, folder paths, zip-filename template, bare output mode, and manifest — plus F-11 and F-10. Done = all 9 tasks of the plan committed on `feat/exporter-v2-phase1` with `dr-tidy` clean and `dr-test` green.

## Orient First
1. `docs/superpowers/plans/2026-08-19-exporter-v2-phase1.md` — THE plan; execute it task-by-task with superpowers:subagent-driven-development (the user chose subagent-driven).
2. `docs/superpowers/specs/2026-08-19-custom-export-v2-design.md` — the approved spec the plan implements (§16 Phase 1 only).
3. `CLAUDE.md` — toolchain (pixi only), architecture, conventions.
4. `src/data_rover/core/table/custom_export.py` + `src/data_rover/api/routes/exports.py` — the two files most of the work reshapes.
5. `BACKLOG.md` §2 (P-15/P-16) and §5 (F-10/F-11) — the items this closes.

Confirm state: `git status --short` (expect clean), `pixi run core-test`, `pixi run frontend-test`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main` at `77a12da` — the plan's Task 1 Step 1 creates `feat/exporter-v2-phase1`.
- Uncommitted: clean (the spec + plan live under gitignored `docs/superpowers/`).
- Tests: not re-run this session; baseline per BACKLOG.md was core 1835 passed / frontend 2078 passed (as of 2026-08-16).
- No code has been written for this feature yet — this session produced only the spec and the plan.

## Standing Constraints
- **Clean slate**: the owner waived backwards compatibility and will wipe the DB — no migrations beyond the trivial data-UPDATE Alembic `0012`, no old-payload byte-identity tests, no legacy windows. Zip *determinism* (same rev → same bytes) is kept: it's a consumer feature, not compat.
- **Kind value `"exporter"`, UI label `Exporter`**; the persisted workspace tab-id prefix stays **`exp:`** — renaming it would churn `unsaved.ts`/`artifact-lock-denied.ts`/localStorage for nothing.
- **No CHECK constraint exists** on `project_artifacts.kind` (`native_enum=False` → bare VARCHAR(32)); the spec's §2.11 assumed one — the plan's data-only 0012 is the corrected form. Do not edit 0011's semantics (already applied).
- **Strict-422-at-export, never-block-Save** for every naming/contract setting (unknown `${...}` tokens, bad folder, bare-with-many-files) — machine consumers over silent degradation; approved in the design dialogue.
- **Manifest has no wall-clock field** — `model_rev` is the identity; a timestamp would break `build_zip`'s pinned-timestamp determinism.
- **RENDER ONLY holds**: nothing in this phase may touch evaluation, row order, or script cache keys.
- `sanitize_stem` moves from `split.py` to the new `naming.py` (with docstring verbatim), re-exported from `split.py` — the only cycle-free direction (plan Task 3 Step 3 explains).
- Never push to origin; never commit `docs/superpowers/`; commits end with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Frontend Task 8's test steps sketch names/testids/assertions but tell the executor to copy the boilerplate from the existing test files — the real harness there is the accurate template; don't invent it.

## Done
- Approved spec written: `docs/superpowers/specs/2026-08-19-custom-export-v2-design.md` (scope, all strictness calls, the rename §2.11, transform-on-tables §8, phases §16).
- Implementation plan written and self-reviewed: `docs/superpowers/plans/2026-08-19-exporter-v2-phase1.md` (9 tasks, TDD steps, exact code).
- A codebase survey established every rename site (46 files), the ArtifactKind/registry/Alembic shapes, and frontend state/test inventory — its load-bearing findings are already baked into the plan; no need to re-survey.

## Known Issues, Not Yet Fixed
None (for this feature). Pre-existing repo-wide items live in `BACKLOG.md` — notably flaky tests T-4 (`test_projects_wizard.py` ~1/30 even in isolation; `test_search_index.py` ~0.8%): re-run and move on, don't debug.

## Deferred — Do Not Do
- **Spec phases 2–5** (CSV/JSONL + `json_doc`; draft runs + run-by-name + picker P-15.1; transform hook incl. `TableDefinition.transform`; bundle-draft export) — deferred by the plan itself; each gets its own plan after Phase 1 ships. Do not start them from this plan.
- **Brainstorm-deferred features** (owner declined for now, re-ask first): per-entry row-filter overrides / run-time parameters (breaks RENDER ONLY), delta exports (R-2/K-6 territory), non-table entries, whole-export snippet hook, arbitrary-text transform output — spec §2 "Explicitly deferred".
- **F-10 as a code change** — owner-approved resolution is a spec amendment only (plan Task 9 Step 1); do not add Save-blocking for templates.
- **localStorage tab migration for the kind rename** — deliberately skipped: DB wipe orphans stored tabs anyway (per-project keys, dead project ids).

## Plan
1. Invoke superpowers:subagent-driven-development and execute `docs/superpowers/plans/2026-08-19-exporter-v2-phase1.md` task-by-task, starting with Task 1 (backend rename sweep; its Step 1 creates the branch).
2. Review each subagent's diff between tasks; run the task's stated test commands before moving on.
3. After Task 9 (backlog + `dr-tidy` + `dr-test`), report results; branch integration (merge to main) is a user decision — use superpowers:finishing-a-development-branch.
4. Remind the owner to wipe the DB before manual testing (kind value changed; 0012 covers a non-wiped dev DB but the wipe is their stated intent).

## Open Questions
None.
