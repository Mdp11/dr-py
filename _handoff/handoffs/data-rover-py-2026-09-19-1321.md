# Handoff: Data Rover client-engine program — implement plan 1 of sub-project B ("Exact server state")

## Mission
Data Rover (`/home/mdp/workspace/data-rover-py`) is moving computation into the browser against a full model replica (TypeScript engine in `engine/`), with the Python core as the oracle. Sub-project B (replica and frontend seam) is built as six plans. **Plan 1 is written and shipped; this session's job is to IMPLEMENT it** — apply it task by task in the real repo, run every step's command, verify, commit with the owner's go-ahead. "Done" = Task 5 complete: `feat/exact-server-state` fast-forwarded into `engine-migration`, `dr-test` and `dr-tidy` green, fixtures current. After that, plan 2 gets WRITTEN — as a plan, not as a build (see Standing Constraints).

## Orient First
1. `docs/superpowers/plans/2026-09-19-exact-server-state.md` (git-ignored, 5,699 lines) — the plan. Read its header, **Provenance** (what was and was not observed), "What building it taught this plan" (12 design choices), Global Constraints and File Structure before Task 1. Tasks start at lines 94, 1018, 2498, 3911, 5660.
2. `docs/superpowers/plans/2026-09-19-exact-server-state.apply.py` — applies the plan's blocks mechanically instead of retyping them: `pixi run -e core-dev python <that file> <the plan> . <task> [--from "Step 5"] [--upto "Step 6"]`. It refuses a block that does not occur exactly once. Verified 13:21: all 152 blocks of Tasks 1–4 apply to a clean clone of `d086ff7` and give the scratch build's tree byte for byte (generated fixtures aside).
3. `docs/superpowers/specs/2026-09-19-replica-and-frontend-seam-design.md` (git-ignored) — B's approved spec; §1 is plan 1's scope, §9 its tests, §10 its doc edits.
4. `architecture/contracts.md` (CT-2, CT-3, CT-5.2 — plan 1 edits all three areas), `architecture/program.md` (B, MR-3), `architecture/conventions.md`, `BACKLOG-ENGINE.md` (`K-30`, `K-31` close in this plan).
5. `engine/src/ops/rewind.ts` — the operation Task 2 ports to `src/data_rover/api/routes/ops.py::_rollback`.
6. `CLAUDE.md` — commands and what the code does today.
7. `~/.claude/handoffs/data-rover-py-2026-09-19-1207.md` — the previous handoff: its Standing Constraints and its "Deferred — Do Not Do" list still stand except where this file says otherwise. `~/.claude/handoffs/data-rover-py-2026-09-19-1207-server-map.md` — a server code map (line numbers as of `0dd5c3d`).

Confirm state: `git status --short` (expect clean), `git branch --show-current` (expect `engine-migration`), `git log --oneline -1` (expect `d086ff7`). Tests: `pixi run dr-test`; Python only `pixi run core-test`; engine `pixi run engine-test [file]`; one frontend file `pixi run frontend-test <path under frontend/>`; lint `pixi run dr-tidy`; fixtures `pixi run golden-fixtures`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` at `d086ff7`; `main` untouched. `feat/exact-server-state` does NOT exist yet — Task 1 Step 1 cuts it.
- Uncommitted: clean (verified 2026-09-19 13:21). The plan and its applier live under git-ignored `docs/`.
- Tests: NOT run in the real repo this session. In a scratch clone of `d086ff7` (2026-09-19 ~12:55): baseline 2,429 Python passed / 34 deselected and 266 engine tests; with plan 1 fully built, 2,465 Python / 34 deselected, 272 engine tests in 39 files, ruff + mypy + pyright + eslint + `tsc` + prettier clean, fixtures current.
- A scratch build of plan 1 exists under `/tmp/claude-1000/-home-mdp-workspace-data-rover-py/8b06ad4d-f8d7-4a65-bd49-b6017104d344/scratchpad/` (`build/`, branch `tasks`, commits T1–T4). It is in `/tmp`, may be gone, and is NOT the deliverable: do not copy from it, implement from the plan.
- `benchmarks/large.model.json` and `benchmarks/large.snapshot.v2` exist (git-ignored). `engine/node_modules` installed. No servers running, no background jobs.

## Standing Constraints
- **Planning is planning (owner, 2026-09-19) — REPLACES the old "plans are pre-verified: built in a scratch clone, replayed, bite-checked" rule.** From plan 2 on, a plan gives implementation direction, specifics included (interfaces, signatures, test cases, order, the tricky mechanism spelled out) — but NOT full code of everything, NOT a build of the implementation, and NO running of the implementation's tests. Reason: the old rule turned "write plan 1" into "implement plan 1", cost hours before the owner saw anything, and inverted the approval gate (decisions were made in code before they were approved). Where `superpowers:writing-plans` demands complete code in every step, the owner's instruction wins.
- **Plan 1 is the exception and ships as it is** (owner's call): it carries full code because the build already existed. The owner wants an implementer to actually implement it — apply, run, verify, judge — not to land the scratch build.
- **Plan 1's 12 design choices were made by the planning session, not reviewed one by one by the owner.** They are accepted as shipped. If one looks wrong during implementation, raise it — do not silently change it, do not silently keep it.
- **Tasks 3 and 4 of plan 1 were never replayed step by step** (the owner stopped the verification on purpose). Their "see it fail" expectations are reasoned, the full-suite counts inside Tasks 2–3 are sums (2,450 and 2,453), and Task 4's three bite checks were never run. Task 1 whole and Task 2 Steps 1–8 were observed. If an expected result does not appear, trust the run, read the step's intent, report it.
- **Measured at M, reported to the owner, no objection raised:** the order repair after a rollback that re-inserted something costs ≈ 70 ms (element dict) / ≈ 106 ms (both dicts); a rollback of 1,000 updates 5.7 ms; the first digest after hydration / `set_model` / `touch_model` is one 286 ms pass under `write_mutex`. **Do not optimize any of it; if a number looks wrong, ask first** (standing rule across A and B).
- `Model.settle_order()` REPLACES the entity dicts, never refills them — read routes iterate them without `write_mutex`. Never keep a reference to `model.elements` / `model.relationships` across it.
- Bite checks inside a task edit files that hold the task's uncommitted work: reverse them by hand, never with `git checkout -- <file>`.
- Still in force from the previous handoff: MR-3 freeze (`K-30` / `K-31` land on both sides with a fixture, one commit each); the Python core is the oracle, fixtures change only via `pixi run golden-fixtures`; `engine/src/` has no DOM, no Node built-ins, erasable syntax, `.ts` specifiers, no `Date.now` / `Math.random` / `Intl`; `dr-tidy` lints neither `tests/`, `scripts/` nor `alembic/` — run ruff on those by hand; RC-11 git (work on `feat/<topic>` from `engine-migration`, `git merge --ff-only`, never touch `main`; subjects one imperative capitalized sentence, no prefix, no period; message ends with the session's `Co-Authored-By` line); **commit only when the user asks**; never `git add -f` `docs/` or `benchmarks/`.
- Ids: plan 1's Task 2 logs `K-33` in `BACKLOG.md`, so afterwards next K is `K-34`; next C is `C-21`; next AD is `AD-26`.
- The owner answers design questions fastest as one multiple-choice question at a time, recommended option first.

## Done
- Plan 1 written: `docs/superpowers/plans/2026-09-19-exact-server-state.md`, with its block applier beside it. Block application checked against a clean clone (see Orient First 2).
- The spec's pre-approval measurement (order repair at M) taken and reported, plus the digest's full-pass cost.
- B's spec and the `architecture/` promotions (`d086ff7`) — unchanged from the previous handoff. Sub-project A complete.

## Known Issues, Not Yet Fixed
- `K-33` (logged by plan 1 Task 2, not fixed): the staged branch of `POST /model/validate` (`src/data_rover/api/routes/validation.py` ~:200-211) applies, validates and rolls back under `write_mutex` but never calls `session.invalidate_derived_caches()` as `POST /commits/preview` does — read in the code this session. One-line fix; left out of plan 1 because it is outside spec §1.
- `K-29`, `K-32`, `C-20` — open in `BACKLOG-ENGINE.md`. `K-30` and `K-31` close with plan 1.
- Stale docstrings at `src/data_rover/api/routes/commits.py` ~:770 and ~:901 say apply-CR replaces the model; it is a dry run. Not logged.
- Server `_decode_v2` accepts a line holding two documents; the engine refuses. Not logged, no fixture.
- No CI; `tests/golden/test_fixtures_current.py` in `core-test` is the only drift guard.

## Deferred — Do Not Do
- **Finishing the step-by-step replay of plan 1's Tasks 3–4 in a scratch clone** — proposed by the planning session, stopped by the owner: that verification is the implementer's, done for real in the repo.
- **Landing the scratch build's commits as the implementation** — proposed, declined by the owner.
- **Replacing the full plan with a short decisions document** — proposed, declined: the plan ships as it is.
- **Building or test-running the implementation while writing plans 2–6** — declined by the owner (see Standing Constraints).
- Everything in the "Deferred — Do Not Do" section of `~/.claude/handoffs/data-rover-py-2026-09-19-1207.md` still stands (neighborhoods, hybrid warm-up, resumable transitions, optimizing the `ord` re-sort, feed events from `/model/ops` and `/model/undo`, and the older list).

## Plan
1. Verify state (three git commands above). Read the plan's header through File Structure, then its 12 findings.
2. Ask the owner the execution mode (see Open Questions) and get the go-ahead to commit on `feat/exact-server-state`.
3. Execute Tasks 1–4 in order with the chosen sub-skill (`superpowers:subagent-driven-development` or `superpowers:executing-plans`): apply each step's blocks with the applier (`--from` / `--upto` follow the TDD order — tests first, see them fail, then the implementation), run every command the step names, compare with its Expected, do the bite checks and reverse them by hand, commit per task.
4. Task 5: `pixi run dr-test`, `pixi run dr-tidy check_only=true`, `pixi run golden-fixtures` with a clean `git status`, then `git switch engine-migration && git merge --ff-only feat/exact-server-state`.
5. Then write plan 2 (v2 snapshot writers, `Snapshot.format`, descriptor / blob / tail routes, `X-Metamodel-Id`; spec §2; what it inherits is in the plan's "After this plan") — **as a plan under the new constraint**, and get it approved before anything is built.

## Open Questions
- Execution mode for plan 1: subagent-driven (a fresh subagent per task, review between tasks) or inline `superpowers:executing-plans` (as A's plans 2–4 were run)?
- Does the owner want to go through plan 1's 12 design choices before implementation starts, or only hear about one if it goes wrong?
- For plans 2–6: may planning still include a cheap probe of a fact the plan rests on (for example a measurement a spec demands before approval), or is that the implementer's too? The planning session proposed it; the owner has not answered.
