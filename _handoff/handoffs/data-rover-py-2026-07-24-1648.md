# Handoff: Inspector inspection history (back/forward + long-press dropdown)

## Mission
Implement back/forward navigation over the trail of elements the user inspects in the
Inspector panel of `/home/mdp/workspace/data-rover-py` (SvelteKit frontend), with a
long-press/right-click dropdown per arrow listing up to 10 entries (Name + Stereotype +
ellipsized id). Spec and plan are approved; "done" = all 5 plan tasks executed with green
tests, `npm run check`, and `dr-tidy`, on a feature branch ready for review.

## Orient First
1. `docs/superpowers/plans/2026-07-24-inspector-history.md` — THE implementation plan; 5 TDD tasks with complete code. Execute this.
2. `docs/superpowers/specs/2026-07-24-inspector-history-design.md` — approved design rationale behind the plan.
3. `CLAUDE.md` — pixi toolchain rules; frontend npm commands MUST run from inside `frontend/`.
4. `frontend/src/lib/state/selection.svelte.ts` — the 31-line choke point the capture hook lands in.
5. `frontend/src/lib/components/Inspector.svelte` — panel getting the `<HistoryNav />` cluster.
6. `frontend/src/lib/state/model.svelte.ts:367-376` — the `applyDelta` id-remap block Task 2 extends.

Confirm state: `git status --short` (expect clean), `pixi run -e frontend bash -c 'cd frontend && npm test'`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main` at `5079f1a` — feature branch NOT yet created (plan says create `feat/inspector-history` at execution start).
- Uncommitted: clean. Spec + plan files exist but live in gitignored `docs/superpowers/` dirs (deliberate repo convention — do not try to commit them).
- Tests: not run this session (no code written yet).
- No servers running; nothing else to resume.

## Standing Constraints
- Execution mode: user chose **subagent-driven** — use superpowers:subagent-driven-development (fresh subagent per task, review between tasks).
- Capture at `select()` (approach A) — user-approved over explicit `visitElement()` wrapper and Inspector `$effect`; guarantees all current/future navigation paths push.
- Elements only; `select(null)` and relationship selections never push — user decision.
- Per-direction dropdown stacks (browser semantics), not a unified recent list — user decision.
- In-memory only, reset on project open; no localStorage — user decision (stale-snapshot avoidance).
- Module named `inspection-history.svelte.ts` — `history.svelte.ts` is taken (commit-history browser).
- `remapVisitIds` (not the spec's `remapIds`) — barrel-safe name; must run BEFORE `applyDelta`'s selection re-point so the re-point dedups instead of duplicating an entry.
- Plain `<button>` + `buttonVariants`, not the `Button` component — `use:longpress` is a Svelte action, actions only attach to DOM elements.
- bits-ui trigger: spread `{...props}` first, then override `onclick`/`onpointerdown`/`onkeydown` after, so click navigates and only longpress/contextmenu opens the controlled menu.
- Dropdown rows resolve in the open handler, never in `$derived` — `noteResolved` mutates `$state`; doing it in a derived throws `state_unsafe_mutation`.
- Selection ↔ inspection-history ES-module cycle is accepted (call-time only); fallback is callback inversion — do not eagerly restructure.
- Component-test precedent for menus: `frontend/src/lib/components/Table/__tests__/TableGrid.test.ts:560-573` queries `[role="menuitem"]` under happy-dom.

## Done
- Brainstorming (approach + 3 user Q&As), approved design, spec written and self-reviewed.
- Implementation plan written and self-reviewed (spec coverage, placeholders, type consistency all checked).
- No implementation code exists yet — Tasks 1-5 are all outstanding.

## Known Issues, Not Yet Fixed
None.

## Deferred — Do Not Do
- Keyboard shortcuts (Alt+←/→) — cut as YAGNI during design; user approved the cut.
- localStorage persistence of the trail — user explicitly chose in-memory only.
- Relationship visits in history — user explicitly chose elements only.
- Pruning deleted elements from the stack — design keeps them navigable (lands on the Inspector's existing "Selection not found" state).
- Backend involvement of any kind — spec scopes this feature entirely client-side.

## Plan
1. Invoke superpowers:subagent-driven-development for `docs/superpowers/plans/2026-07-24-inspector-history.md`; first create the `feat/inspector-history` branch (superpowers:using-git-worktrees).
2. Task 1: state module `inspection-history.svelte.ts` + unit tests.
3. Task 2: `select()` hook, `applyDelta` remap, project-open reset, barrel exports + tests.
4. Task 3: `longpress` action + unit tests.
5. Task 4: `HistoryNav.svelte` + Inspector mount + component tests.
6. Task 5: README line, full suite, `npm run check`, `pixi run dr-tidy`, final commit; then superpowers:finishing-a-development-branch.

## Open Questions
None.
