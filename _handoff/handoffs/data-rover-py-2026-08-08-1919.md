# Handoff: Execute artefacts Phase 3 (import/export closure) via subagent-driven development

## Mission
Implement the artefacts-revamp Phase 3 backend slice in `/home/mdp/workspace/data-rover-py`
by executing the ready, user-approved plan
`docs/superpowers/plans/2026-08-08-artefacts-phase-3-import-export.md` (8 TDD tasks)
via **superpowers:subagent-driven-development** — the user chose that mode explicitly.
"Done" = all 8 tasks implemented and committed on
`feature/artefacts-phase-3-import-export`, final whole-branch review on the most capable
model, ONE fix wave, merge `--no-ff` to local `main`, delete branch. Push only if asked.

## Orient First
1. `CLAUDE.md` — read fully (architecture, invariants, toolchain; current).
2. `docs/superpowers/plans/2026-08-08-artefacts-phase-3-import-export.md` — THE plan;
   every task carries its own test code, signatures verified against `707f6d6`.
3. `docs/superpowers/specs/2026-08-08-artefacts-phase-3-import-export-design.md` — the
   approved spec behind the plan (error table + brainstorm decisions).
4. `src/data_rover/api/artifact_kinds.py` — registry seams the slice consumes
   (`extract_refs`/`rewrite_refs` generic walk; contract tests pin per-kind behavior).
5. `src/data_rover/api/artifact_ops.py:384-395` — temp-id → uuid + `_resolve_json`
   resolution the import commit leans on; `:172` `_validated_payload` (runs
   `derive_metadata`, and `_check_clash` makes the confirm race a 422, never IntegrityError).
6. `src/data_rover/api/routes/commits.py:585` — `create_commit` signature the confirm
   route calls directly.

Confirm state:
```
git status --short                  # expect clean
git branch --show-current           # expect main (branch not yet created — Task 1 Step 1)
git log --oneline -1                # expect 707f6d6
pixi run core-test                  # backend pytest
```
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main`, clean tree, head `707f6d6` == `origin/main`. Feature branch NOT yet
  created (plan Task 1 Step 1 creates `feature/artefacts-phase-3-import-export`).
- Tests: not run this session (no code changed — session was brainstorm + spec + plan
  only). Last known good: 2026-08-08 ~18:05, backend 1638 passed / 26 deselected,
  frontend 1763 passed, on this exact tree.
- Alembic head `0009`; this slice needs NO migration (bundle is wire/blob-level only).
- Spec + plan files written this session (both gitignored, per repo convention — do not
  try to commit them).

## Standing Constraints
- **Phase 3 chosen over Phase 4** by the user (2026-08-08 brainstorm); Phase 4
  (mm lease + diff) remains open and independent.
- **Backend-only slice** — user decision; export-preview/import-confirm UI is a separate
  follow-up slice (Phase 2 precedent).
- **No view ops on import** — user explicitly reversed the initial place-at-root
  recommendation mid-brainstorm: imported artifacts appear ONLY in the flat artifacts
  list; bundle format carries no placement data. Do not "helpfully" place them.
- **Stateless import protocol** — client re-sends the bundle at confirm; no server-side
  staging (bundles are KB-scale). Stateful/token staging was considered and rejected.
- **Clash = (kind, name)** matching DB `uq_artifact_project_kind_name`; payload-aware
  proposal (reuse iff identical after `derive_metadata` normalization + ref rewrite
  through the tentative reuse map); copy gets deduped name "Name (2)".
- **Malformed envelope → 422** not 400 (pydantic body validation, API-wide convention —
  spec was amended to say so).
- **Confirm race → applier's `_ClashTracker` 422 → confirm maps to 409 + fresh plan**;
  IntegrityError is unreachable (spec amended; don't add IntegrityError handling).
- **`/artifacts/import/plan` is deliberately NOT in `_READ_ONLY_POST_SUFFIXES`**
  (planning is part of the write flow); export + export/preview ARE (viewers export).
- **Clone copies every row verbatim** (unregistered `diagram` kinds ride along; only
  import-from-outside filters kinds) via `content.list_artifacts`, not a closure walk.
  Unknown-enum kinds cannot be rows and are skipped at baseline import.
- **Never send an empty commit batch** — confirm returns `rev: null` no-op instead.
- **No OCC on artifact ops; import creates need no leases** (fresh-id rule).
- **The real frontend lint gate is `cd frontend && pixi run -e frontend npm run lint`**
  (moot this slice — frontend untouched).
- **Process precedent (5th session running):** fresh implementer subagent per task, task
  review, scoped re-reviews; final whole-branch review on the most capable model; ONE
  fix wave (re-review each round); merge `--no-ff`; push only on ask.
- Plan signatures were verified against the tree, but two test stubs (`...` in plan
  Tasks 3/5: viewer-seeding, feed-scope assertion) intentionally tell the implementer to
  copy idioms from `tests/api/test_authz.py` / `test_commits_artifact_ops.py` rather
  than inventing fixture code — implementers must read those files.

## Done
- Brainstorm (superpowers:brainstorming, full checklist): phase choice, scope, clash
  rules, view-placement decision, protocol choice — all user-confirmed.
- Spec written, self-reviewed, amended (422 envelope; 422-not-IntegrityError race),
  **user-approved**: `docs/superpowers/specs/2026-08-08-artefacts-phase-3-import-export-design.md`.
- Plan written + self-reviewed (spec-coverage gap fixed: evict-survival pin added as
  Task 7 Step 3b): `docs/superpowers/plans/2026-08-08-artefacts-phase-3-import-export.md`.
- User chose subagent-driven execution.
- NO implementation code written yet — the tree is untouched at `707f6d6`.

## Known Issues, Not Yet Fixed
(All pre-existing, carried from prior handoffs; none block this slice.)
- Stale cross-ref comments in `routes/ops.py` ~:707/:714 ("create_commit's own
  `created_view`") — fix on next `ops.py` touch.
- `commits.py` ~:688 orphan short comment line; b/b3 preamble bare-`created_view`
  mentions — cosmetic, reflow on next touch.
- `routes/locks.py::expand_targets` degrades vs cold `session.view` (~:84) —
  false-409-only, deferred.
- `view → realtime → artifacts → view` frontend import cycle worked around by
  `setTimeout(…,0)` in `view.svelte.ts`.
- Pre-existing flakes: e2e auth/session family (`frontend/e2e/helpers/auth.ts:33`);
  unit `tests/model/test_search_index.py::test_string_properties_indexed_non_strings_ignored`
  (~0.8%) — re-run and move on.
- Older Phase-2 review-ledger minors (`_seed_view` conftest duplication ×4 files,
  excluded-pool transient injection, discard banner on in-SPA project switch, client
  `TEMP_ID_PREFIX` guard) remain open.

## Deferred — Do Not Do
- **Phase 4 (mm lease + structural metamodel diff)** — user picked Phase 3 first; do not
  start Phase 4 work in this slice.
- **Frontend for Phase 3** (export preview / import confirm dialogs) — separate slice.
- **Bundle folder placements / auto-placement in view** — user declined both.
- **Stateful staged import** — rejected approach.
- **No OCC on view/artifact ops; no per-entry view revert; no revert-across-view-ops
  UI; no multiple named views** — long-standing declines.
- **Do not migrate `routes/ops.py::undo` onto `_CommitUnwind`** — different op_log
  contract; belongs to the legacy-path migration slice.
- **Do not resurrect `PUT /view/snapshot` / `DELETE /view`.**
- **HistoryDrawer consuming `GET /commits/{rev}/diff`** — separate unclaimed slice.
- **`_seed_view` conftest consolidation** — only when next touching those test files.

## Plan
1. Invoke **superpowers:subagent-driven-development** with the plan file
   `docs/superpowers/plans/2026-08-08-artefacts-phase-3-import-export.md`. First
   concrete action: dispatch the Task 1 implementer (branch creation is that task's
   Step 1). Fresh subagent per task; review after each task per the skill.
2. After Task 8's gates pass: final whole-branch review on the most capable model
   (superpowers:requesting-code-review), ONE fix wave with scoped re-reviews per round.
3. Merge `--no-ff` into local `main`, delete the feature branch. Push only if asked.

## Open Questions
None.
