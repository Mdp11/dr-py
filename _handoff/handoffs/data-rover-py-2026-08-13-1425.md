# Handoff: Metamodel diagram editor (P-9) — execute the plan

## Mission
Implement the metamodel diagram editor for `/home/mdp/workspace/data-rover-py`: an
editable UML class-diagram view added to the existing metamodel tab (topology on a
Svelte Flow canvas, attributes in a selection-bound form panel), sharing the YAML
draft/lint/preview/rebind/`mm`-lease lifecycle, with comment-preserving YAML writeback
and shared server-side node positions. Done = all 14 tasks of the plan executed, each
committed, `pixi run dr-tidy && pixi run dr-test` green, BACKLOG P-9 marked done.

## Orient First
1. `docs/superpowers/plans/2026-08-13-metamodel-diagram-editor.md` — THE plan; 14
   TDD tasks with code, in dependency order. This session executes it.
2. `docs/superpowers/specs/2026-08-13-metamodel-diagram-editor-design.md` — the
   approved spec the plan argues from (decisions table up top).
3. `CLAUDE.md` — toolchain (everything via pixi), architecture, conventions.
4. `frontend/src/lib/state/metamodel-editor.svelte.ts` — the lifecycle module the
   diagram composes; do not restructure it (plan Global Constraints).
5. `frontend/README.md` §"Live metamodel editing" — existing tab architecture.

Confirm state: `git status --short` (expect clean), `pixi run core-test`,
`pixi run frontend-test`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main` at `d52826c` (spec + plan live in gitignored `docs/superpowers/`,
  so `git status` is clean — the two files exist on disk, not in git).
- Uncommitted: clean.
- Tests: not run this session; last known baseline (BACKLOG header, 2026-08-12):
  core 1739 passed, frontend 1863 passed, dr-tidy clean.
- Execution mode chosen by user: **subagent-driven** (superpowers:subagent-driven-development),
  fresh subagent per task, review between tasks, on a worktree branch
  `feat/metamodel-diagram-editor` (create via superpowers:using-git-worktrees).

## Standing Constraints
- YAML string draft stays canonical; diagram edits go Document-mutation →
  `serializeDraft` → `editMetamodelBuffer`. Chosen (approach A) so lease/draft/lint/
  dirty logic needs zero changes; B (Document-canonical) and C (server-side edit
  routes) were explicitly rejected.
- Comments must survive diagram edits — `yaml` npm Document API, stringify with
  `lineWidth: 0`. This was an explicit owner choice over "re-serialize and warn".
- Layout (`GET/PUT /metamodel/layout`, `metamodel_layouts` table) is presentation:
  NO `mm` lease, NO commit journal entry, last-write-wins — owner approved this
  exception explicitly. Editors+ may PUT; buffer editing itself is owner-only
  (existing `isEditBlocked()` checks `getRole() !== 'owner'` — the spec was
  corrected to match this after reading the code).
- Rename key-deferral: a draft rename must NOT rewrite shared layout keys until
  rebind succeeds (peers render baseline names); plan Task 9 carries the mechanism.
- Strict UML notation + app-native styling (sage glass, Outfit names, gold
  {id}/«enumeration», italic+dashed abstract) — mockups the owner approved are in
  `.superpowers/brainstorm/25414-1786618301/content/` (canvas-style-v2.html is the
  approved one).
- Auto-arrange is a permanent toolbar button (owner requirement), elkjs `layered`.
- Full coverage: everything UI-editable including the `out:`/`in:` key-DSL builder.
- Do NOT commit anything under `docs/superpowers/` or `.superpowers/` (repo
  convention, BACKLOG §10); `main` is deliberately ahead of origin — never push.
- Plan's known risk: exact `yaml`-package node-manipulation calls are where reality
  most likely diverges from the plan's code; Task 1-4 tests are authoritative —
  adjust implementation, not tests.

## Done
- Brainstorm complete (visual companion used; server already stopped).
- Spec written, self-reviewed, owner-approved, and corrected for the owner-only
  editing gate: `docs/superpowers/specs/2026-08-13-metamodel-diagram-editor-design.md`.
- Plan written and self-reviewed: `docs/superpowers/plans/2026-08-13-metamodel-diagram-editor.md`.
- No implementation has started. No code, no deps installed, no migration.

## Known Issues, Not Yet Fixed
None (for this feature — repo-wide known issues live in `BACKLOG.md`).

## Deferred — Do Not Do
- Spec §8 non-goals, all owner-ratified: no live peer canvas sync / feed events for
  layout; one layout per project (no named diagrams — adjacent P-8 is separate); no
  instance-level diagrams (that's P-7); no canvas image/SVG export; no comment
  authoring from the diagram.
- Committing the spec/plan docs — declined because `docs/superpowers/` is
  gitignored by convention.

## Plan
1. Invoke superpowers:subagent-driven-development; create the worktree/branch
   `feat/metamodel-diagram-editor` via superpowers:using-git-worktrees.
2. Execute plan Task 1 (deps `yaml`+`elkjs`, `yaml-edit.ts` parse/serialize core,
   tests in `frontend/src/lib/metamodel/__tests__/yaml-edit.test.ts`) with a fresh
   subagent; review; commit.
3. Continue task-by-task through Task 14 (backend layout table/routes are Task 5;
   canvas Tasks 7-13; integration + docs Task 14), reviewing between tasks.
4. Finish with `pixi run dr-tidy && pixi run dr-test`, then
   superpowers:finishing-a-development-branch.

## Open Questions
None — design and execution mode are settled; begin at Plan step 1.
