# Handoff: data-rover-py — Phase 4 execution (metamodel lease + structural diff)

## Mission
`/home/mdp/workspace/data-rover-py` — reflective MBSE metamodel engine (Python core +
FastAPI + SvelteKit). Phase 4 of the artefacts-revamp (metamodel `mm` lease + structural
metamodel diff) was brainstormed, spec'd and planned this session; the user approved both
documents and chose **subagent-driven execution**. "Done" = execute the 8-task plan via
`superpowers:subagent-driven-development` (LEAN variant, see Standing Constraints),
review, then `--no-ff` merge into local `main` and delete the branch.

## Orient First
1. `docs/superpowers/plans/2026-08-10-metamodel-lease-structural-diff.md` — THE plan to
   execute; 8 tasks with full code and test snippets. Read it end to end.
2. `docs/superpowers/specs/2026-08-10-metamodel-lease-structural-diff-design.md` — the
   approved spec behind it (user-approved, includes three late amendments: quiet-predicate
   fix, no source/target attribute diff, owner-gated lease acquisition).
3. `CLAUDE.md` — pixi toolchain (no global python/node), conventions.
4. `src/data_rover/api/locking.py:56` — `METAMODEL_RESOURCE = "mm"`, `peer_leases` at
   `:245`; `src/data_rover/api/routes/metamodel_swap.py` — the rebind flow Task 3 extends.
5. `frontend/src/lib/state/checkout.svelte.ts` — lock registry/heartbeat Tasks 6-7 build on;
   `frontend/README.md` mandatory before touching `frontend/src/lib/state/`.

Confirm state:
```
git status --short          # clean (as of writing)
git branch --show-current   # main
git log --oneline -1        # bc34821
```
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main`, clean tree, head `bc34821` (2026-08-10 21:46). No Phase 4 code written yet.
- `main` is ~30 commits ahead of `origin/main` — deliberate, never push.
- Last known gates (NOT re-run this session — no code changed): core-test ~1691 passed;
  frontend vitest 1825 passed; lints clean (all as of the previous session's merge).
- Spec + plan live under gitignored `docs/superpowers/` — deliberately uncommitted.

## Standing Constraints
- **TOKEN BUDGET IS A HARD CONSTRAINT** (user, previous session: "Too many agents!…").
  Execution agreed this session: **one implementer subagent per task, sequential, NO
  parallel fan-out, NO reviewer subagents — the main session reviews each task inline.**
  ~8 subagents total. Ask before spending more.
- Work on branch `feat/metamodel-lease-structural-diff` off `main`; `--no-ff` merge when
  done, delete branch, never push.
- **Honor-don't-require lease semantics** (user chose): rebind/upload/clear proceed when
  `mm` is free or caller-held, 409 `{"detail": "metamodel locked", "holder_email": …}` on
  peer hold. No lock token on the request; no server-side release on success (client
  surface releases its own lease). Do not "upgrade" to token verification.
- **Drawer-held lease from review step, owner-gated** (user chose): only owners acquire
  (editors' read-only diff must not lock the owner out); acquire BEFORE firing the diff.
- **Typed nested diff tree** (user chose over flat records); adds/removes carry full
  definitions; raw-document diff (no inheritance flattening); rename = remove+add;
  `source`/`target` NOT diffed as attributes (they mirror `mappings[0]` — mappings diff is
  authoritative); wire keys `from`/`to` via pydantic alias.
- **History surface backend-only** (user chose): commit-diff API renders `metamodel`
  section; NO HistoryDrawer UI.
- **Phase 5 forward constraint** (user): next phase is live metamodel editing (raw YAML +
  UI-assisted) which DROPS SwapMetamodelDrawer — hence the reusable
  `metamodel-lease.svelte.ts` module + standalone `MetamodelStructuralDiff.svelte`
  component and minimal drawer styling. Keep lease/diff logic out of drawer internals.
- **Quiet predicate must exclude `mm`** (`hasModelLocks` in
  `frontend/src/lib/state/realtime.svelte.ts:98`): mirrors backend `is_model_resource`;
  counting it would let the drawer's own lease disable its own Rebind button. The existing
  `quiet.test.ts` `'mm'` case gets FLIPPED — that's intended, not a regression.
- Task 5's required `structural` field on `MetamodelDiffSchema` will break existing
  SwapMetamodelDrawer test mocks — fixing those mocks is expected fallout (plan Task 5
  step 4), not scope creep.
- The Phase 1–3 backend contract is frozen: ADD fields only.
- Svelte 5 dialog rules (paid for over two sessions): local `$state` + `bind:open` +
  `onOpenChange`; async flows need generation guards (metamodel-lease module has one).
- Frontend files TAB-indented (run prettier before committing); Python 4-space.
  Conventional commits, NO attribution/co-author trailers.
- Frontend lint gate: `pixi run -e frontend bash -c 'cd frontend && npm run lint && npm run check'`;
  single vitest file: `… npm test -- <path>`. Python: `pixi run core-lint` AND
  `pixi run backend-lint` must pass (ruff+mypy+pyright).
- A plan's illustrative snippets do not outrank its behavior contract (ruled repeatedly).
- Beware Edit writing control bytes into .svelte files (git diff shows `Bin` → hunt NULs).

## Done
- Phase 4 brainstorm (4 axes user-decided), spec written + approved, plan written +
  self-reviewed (Tasks 2/4 test code firmed against real fixtures in
  `tests/api/test_metamodel_diff.py` / `tests/api/test_commit_diff.py`).
- Recon baked into the plan: `mm` namespace already plumbed for acquisition
  (`routes/locks.py` canonicalizes `type:"metamodel"`, `checkout.svelte.ts:160` maps it);
  nothing honors/verifies it yet; `POST /metamodel/diff` has no structural section;
  `CommitDiffOut` has `is_rebind` + metamodel FKs but renders nothing.
- Phases 1–3 + cleanup wave: merged (previous sessions).

## Known Issues, Not Yet Fixed
- Abandoned wizard uploads not aborted (`apiUpload`/`createProject` take no AbortSignal) —
  parked follow-up from previous session.
- Backend minors from Phase 3 (inert `ack_errors` in 422→409 remap; "Imported 1 artifacts"
  pluralization; unbounded bundle sizes; `_landable_artifacts` reason-string mismatch).
- Pre-existing flakes: e2e auth family (`frontend/e2e/helpers/auth.ts:33`);
  `tests/model/test_search_index.py::test_string_properties_indexed_non_strings_ignored` (~0.8%).

## Deferred — Do Not Do
- **Reviewer subagents / parallel fan-out** — user's token constraint; reviews are inline
  by the main session unless the user opts in.
- Rename detection in the differ; rebind revert (Phase 8); HistoryDrawer consuming
  `GET /commits/{rev}/diff` (parked again this session); Redis locks (Phase 7); e2e tests.
- Live metamodel editing — that's Phase 5, this plan only keeps its seams clean.
- Hard-verify (token-required) rebind — user explicitly chose honor-don't-require.
- Drawer-held lease from drawer OPEN — user chose review-step acquisition.
- Frontend commit-diff typing — no client-side commit-diff schema exists; nothing to extend.
- AbortSignal plumbing; `bindableOpen` helper; shared registered-kind selector;
  auto-placement of imported artifacts; stateful import; pushing to origin; committing
  anything under `docs/superpowers/`.

## Plan
1. **Invoke `superpowers:subagent-driven-development`** and execute
   `docs/superpowers/plans/2026-08-10-metamodel-lease-structural-diff.md` in the LEAN
   variant: create branch `feat/metamodel-lease-structural-diff`, then one fresh
   implementer subagent per task (1→8, sequential), main session reviews each task's diff
   + test run inline before dispatching the next. No reviewer agents.
2. After Task 8's gates pass: one inline final review of the whole branch diff; fix wave
   only if needed (ask user before any agent-heavy review round).
3. `superpowers:finishing-a-development-branch`: `--no-ff` merge into local `main`,
   delete branch, do not push.

## Open Questions
- None blocking. (If a task's fixture assumptions surprise the implementer, the plan's
  Tasks 2/4 carry adapt-notes; behavior contract wins over snippets.)
