# Handoff: Data Rover client-engine program — start sub-project B (replica and frontend seam)

## Mission
Data Rover (`/home/mdp/workspace/data-rover-py`) is being re-architected so computation runs in the browser against a full model replica (TypeScript engine in `engine/`), with the Python core as the oracle. Sub-project A (engine foundation) is **done and merged** into `engine-migration`. The next part is **sub-project B: replica and frontend seam** — scope and "done when" at `architecture/program.md:38-45`. B starts, per RC-9, with a **brainstorm → spec → plan** (`superpowers:brainstorming` first); no code before the user approves a spec and a plan. "Done" for this session's slice is at least an approved B spec under `docs/superpowers/specs/`.

## Orient First
1. `architecture/README.md` — entry point and rules of the architecture docs (one fact, one home; IDs stable; two-letter prefixes).
2. `architecture/program.md` — status table (:7-19), B's scope/depends/done-when (:38-45), migration rules MR-n (:90), non-goals (:110).
3. `architecture/contracts.md` — CT-1 (snapshot v2), CT-2 (delta: `applied`/`duplicate`/`gap`), CT-3 (digest, checked in the background), CT-5 (working copy); `K-30` cited at ~:110, `K-31` at ~:62.
4. `architecture/system.md` — rule 4 (16 ms chunks) and the worker/sandbox shape B builds.
5. `architecture/constraints.md` (CN-3 budgets at model M, CN-5 timing drift) and `architecture/decisions.md`.
6. `BACKLOG-ENGINE.md` — the program's own backlog (new this session): `R-3`, `K-29`–`K-32`, `C-20`. B inherits `K-30`, `K-31`, `K-32`.
7. `CLAUDE.md` — "Engine package (`engine/`)" section describes what A built; the backend sections describe the server B's shell talks to.
8. `frontend/README.md` — "State model" section; B turns `frontend/src/lib/state/model.svelte.ts` into a view over the engine. Read before touching `frontend/src/lib/state/`.
9. `docs/superpowers/specs/2026-09-18-engine-foundation-design.md` — A's spec (git-ignored); the template/precedent for B's spec, and §9 holds the bench notes.

Confirm state: `git status --short` (expect clean), `git branch --show-current` (expect `engine-migration`), `git log --oneline -1` (expect `0dd5c3d`). Tests: `pixi run dr-test`; engine only `pixi run engine-test`; lint `pixi run dr-tidy`; bench `pixi run engine-bench` (input already in `benchmarks/`).
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` at `0dd5c3d` ("Give the client-engine program its own backlog file"). `main` untouched.
- Uncommitted: clean (verified 2026-09-19 10:58).
- Tests (as of 2026-09-19 ~01:40, on `690845f`; `0dd5c3d` is docs-only): core pytest 2,429 passed / 34 deselected; frontend vitest 2,492 in 252 files; engine vitest 266 in 38 files; `dr-tidy` clean.
- Bench (2026-09-19, Node 22.22.3, medians of 3): open snapshot 2,286 ms of 3,000; same model as one document 3,183 ms; heap 231 MB of 400; verifyDigest 199 ms; stage/unstage 1,000 ops 43/38 ms; re-sort after rewind 53 ms; rebase 13 ms; delta 8.3 ms.
- `benchmarks/large.snapshot.v2` + `.metamodel.json` exist (git-ignored). `engine/node_modules` installed. No servers running.
- Branches `feat/engine-value-layer`, `feat/engine-store`, `feat/engine-ops`, `feat/engine-snapshot` all merged, kept on purpose.

## Standing Constraints
- **Design before code (RC-9):** brainstorm → spec → plan → build; specs/plans under git-ignored `docs/superpowers/`. Decisions that outlive B are promoted into `architecture/`, open items into `BACKLOG-ENGINE.md`, current behaviour into `CLAUDE.md`, in the same commit as the code (RC-10).
- **Two backlogs, split by program (user's choice 2026-09-19):** client-engine program items go in `BACKLOG-ENGINE.md`, the app's items in `BACKLOG.md`. IDs are unique across both files — a new item takes the next free number of its letter in either (next K is `K-33`, next C is `C-21`).
- **The Python core is the oracle; MR-3 freeze** on `core/model`, `core/metamodel` and the model-op applier (`routes/ops.py`). A fix there lands on both sides with a golden fixture; fixtures change only via `pixi run golden-fixtures`; never edit a fixture by hand.
- **Measure first; optimize only on a missed budget, and only after asking the user** (standing rule across A).
- **Execution mode for plans has been inline `superpowers:executing-plans`** for plans 2–4 of A (subagent-driven was offered and not taken). Ask again for B only if the user hasn't said.
- **Plans are pre-verified before approval:** A's plans were built in a scratch clone against the real core, replayed step by step, and bite-checked; blocks were applied with an extractor from the plan on disk (a 4-digit `\uXXXX` typed through tools gets decoded). Repeat that practice for B's plans unless the user says otherwise.
- `engine/src/`: no DOM, no Node built-ins, erasable syntax only, `.ts` import specifiers, no `Date.now`/`Math.random`/`Intl`. Tests and `engine/bench/` may import `node:*`.
- `dr-tidy` lints neither `tests/` nor `scripts/`: run ruff on them by hand.
- Commit subjects (RC-11): one imperative sentence, capitalized, no prefix, no trailing period; message ends with the session's `Co-Authored-By` line. Branch `feat/<topic>` from `engine-migration`, finish with `git merge --ff-only`. Never touch `main`. `docs/` and `benchmarks/` are git-ignored — never `git add -f`.
- Commit only when the user asks (they approved committing the backlog split directly on `engine-migration`).

## Done
- Sub-project A complete: plans 1–4 landed `e8abbf3`..`690845f` — value layer, golden pipeline, snapshot v2 + digest (Python), metamodel, store, indexes, op applier, working copy, SHA-256, `openSnapshot`, `verifyDigest`, benchmark at M, closing docs (`CLAUDE.md`, `architecture/program.md` status "done", `decisions.md`, `README.md`).
- `K-32` logged; `BACKLOG-ENGINE.md` created and `R-3`, `K-29`–`K-32`, `C-20` moved there with pointers left in `BACKLOG.md`; `architecture/` references updated (`0dd5c3d`).

## Known Issues, Not Yet Fixed
All in `BACKLOG-ENGINE.md`:
- **K-29** — `src/data_rover/api/routes/_snapshot.py` keeps `seen_ids` per kind; engine refuses an element/relationship sharing an id that Python accepts. User: keep it logged.
- **K-30** — server `_rollback` (`src/data_rover/api/routes/ops.py` ~:477-487) not exact: `rev` drifts, restored entities move last; every `/commits/preview` does this. Becomes a divergence bug once the server serves the digest/v2 — **B must address it**.
- **K-31** — a CT-2 delta cannot say "deleted and re-created under the same id"; **B's** (delta shape change).
- **K-32** — `rebuildIndexes()` 0.52 s, `verifyDigest()` 0.2 s, re-sort after rewind ~55 ms each exceed one 16 ms chunk; B's worker must slice the first two.
- **C-20** — `src/data_rover/core/metamodel/schema.py:198` comment contradicts code; frozen.
- Server `_decode_v2` accepts a line holding two documents; the engine refuses (plan 4 finding 4). Not logged, no fixture.
- No CI; `tests/golden/test_fixtures_current.py` in `core-test` is the only drift guard.

## Deferred — Do Not Do
- **Optimizing A's hot spots** (index build, digest check, `ord` re-sort, `unstage({entity})` scanning, `Touched` over-reporting) outside a missed budget — user's rule; logged as K-32 instead.
- **Fixing K-29 in isolation** — user: "keep it logged".
- **Deleting merged branches** (`feat/engine-*`) — user: "Leave them".
- **Rust/wasm engine; Python core under Pyodide; orjson; Pyodide memory snapshots; server-side strict-mode verification; merging into `main`** — each declined or postponed by the user in earlier sessions.
- **`pyEquals`, `isPlainObject`** — postponed by the user to sub-project C.
- **A TypeScript v2 snapshot writer in `engine/src/`** — not needed until a host writes snapshots; `engine/test/snapshot/text.ts` stays test-only.

## Plan
1. Verify state (three git commands above). Read `architecture/README.md`, then `program.md` B section, `contracts.md`, `system.md`, `constraints.md`, and `BACKLOG-ENGINE.md`.
2. Invoke `superpowers:brainstorming` for sub-project B. Explore with the user: scope split into plans (A used four), the sandbox page + engine worker boundary, the shell (snapshot descriptor route, tail route, feed buffering, IndexedDB cache, re-bootstrap), `prev_rev` + digest on the current server and switching server snapshot writers to v2 (touches `snapshot_codec.py`, and K-30/K-31 — which fixes are in B's first plan), the engine client behind `frontend/src/lib/api`, and which read surfaces move first (element pages, tree, search, neighborhoods, summary).
3. Write B's spec to `docs/superpowers/specs/2026-09-19-<topic>-design.md`, get user approval, then promote any lasting decisions into `architecture/`.
4. Write B's first plan with `superpowers:writing-plans`, pre-verify it in a scratch clone as A's plans were, get approval, then execute.

## Open Questions
- **K-32's framing:** its title says three operations exceed 16 ms, but staging/unstaging a 1,000-op batch (43/38 ms) also does; the entry lumps them under "everything else stays under 50 ms". Does the 16 ms rule apply to a single user-sized batch? Raised with the user 2026-09-19, not answered — settle during B's brainstorm (and fix the entry's wording if needed).
- Execution mode for B's plans (inline vs subagent-driven) — ask when the first plan is approved.
