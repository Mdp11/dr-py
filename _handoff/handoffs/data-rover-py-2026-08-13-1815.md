# Handoff: SDD execution of P-13/P-14 — Task 7 review, then Tasks 8-13

## Mission
Execute the approved 13-task plan for P-13 (per-element JSON split export, zipped, `${name}` filename template) and P-14 (new `custom_export` artifact kind bundling many tables' exports with per-entry presentation overrides) in `/home/mdp/workspace/data-rover-py/.claude/worktrees/feature+table-export-artefacts`. **Tasks 1-7 (the entire backend) are implemented and committed**; Task 7 alone still needs its task review. Done = all 13 tasks executed with their reviews clean, `pixi run core-test` + `pixi run frontend-test` + `pixi run dr-tidy` green, BACKLOG P-13/P-14/C-6 marked done.

**Work in the existing worktree** — the session cwd is already it. Do NOT work in the main checkout; another session is using it for the metamodel edit UI feature. If you need to re-enter: `EnterWorktree {path: "/home/mdp/workspace/data-rover-py/.claude/worktrees/feature+table-export-artefacts"}` (it no-ops if you're already there).

## Orient First
1. `.superpowers/sdd/2026-08-13-table-export-split-and-custom-export/progress.md` — **THE LEDGER. Read this first.** It carries the pre-flight conflict scan, all 8 rulings (R1-R8), every task's completion line with its commit range, and the deferred-minor list the final review must triage.
2. `docs/superpowers/plans/2026-08-13-table-export-split-and-custom-export.md` — the plan. Tasks 8-12 are frontend (lines ~1434-2150), Task 13 is docs+gates (~2154-2180). Do NOT paste whole tasks into subagent prompts — use `scripts/task-brief` (below).
3. `docs/superpowers/specs/2026-08-13-table-export-split-and-custom-export-design.md` — the spec the plan argues from; §2 is the decision table, §5 is the frontend design. **The spec is the binding authority when the plan conflicts with it.**
4. `frontend/README.md` — required before touching `frontend/src/lib/state/`; Tasks 11-12 mirror the snippet-editor pattern documented there.
5. `CLAUDE.md` — pixi-only toolchain, RENDER ONLY export boundary, test layout.
6. `src/data_rover/api/table_export_engine.py` + `src/data_rover/api/routes/exports.py` — what the backend half actually landed as.

Confirm state:
```sh
git -C . status --short              # expect clean
git -C . log --oneline -1            # expect 089478b
pixi run core-test                   # ~1791 passed (as of 2026-08-13 ~18:00, per Task 7's report)
pixi run frontend-test               # 1890 passed (as of ~14:55 — RE-RUN, it is the Task 8 baseline)
```
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `worktree-feature+table-export-artefacts`, 8 commits ahead of `main` (`d52826c`).
- Uncommitted: clean (the SDD workspace and `docs/superpowers/` are gitignored).
- Commits so far: `db8b3cc` (T1 json_split field) → `d74af4b` (T2 split.py) → `f39d696` (T3 custom_export.py) → `9f7367e` (T4 engine extraction) → `ea5b188` (T5 split zip) → `f1dacd7` (T6 kind registration) → `efef243` (T6 fix) → `089478b` (T7 /exports/run).
- Tests: `core-test` 1791 passed, `backend-lint` clean (as of Task 7's report, ~18:00). Frontend untouched so far — its 1890-passing baseline is from ~14:55 and should be re-confirmed before Task 8.
- Alembic: head is now `0010` (`alembic/versions/0010_artifact_kind_width.py`, widens `project_artifacts.kind` VARCHAR(12)→(32)).
- Env: `pixi install -e core-dev -e frontend` + `npm install` already done in this worktree.

## Standing Constraints
- **Process**: this is superpowers:subagent-driven-development. Fresh implementer subagent per task → task review (spec + quality) → fix loop (max 5 rounds, resume the same implementer for rounds 1-3) → ledger line → next task. Never fix findings yourself in the controller session. Scripts live at `/home/mdp/.claude/plugins/cache/claude-plugins-official/superpowers/6.3.0/skills/subagent-driven-development/scripts/` — `task-brief PLAN N` (writes the brief file), `review-package PLAN BASE HEAD` (writes the diff file). Hand subagents FILE PATHS, never pasted diffs.
- **Never run `ruff format` on anything under `tests/`.** `pixi.toml` scopes the formatter to `src/data_rover/core` (`core-tidy`, :130-131) and `src/data_rover/api` (`backend-tidy`, :157-158) only. `tests/` was never format-clean and reformatting it produced ~550 lines of churn that had to be reverted in Task 6 (ruling R7). DO run `ruff check` on tests, and DO run `ruff format` on `src/` files.
- **RENDER ONLY** (spec §3.4): evaluation (`build_rows_ex`/`order_rows`/`iter_export_rows`/script context) always gets the ORIGINAL `TableDefinition`; override/presentation copies feed only layout/render. Cell values, row order and script cache keys must stay independent of export presentation.
- **Strict template**: a `${name}`-less filename template is 422 at export and blocked in the dialog — user chose reject over normalize. It is the ONE strict export setting; everything else drift-normalizes.
- **Full override set per entry**, initialized by copying the table's settings at add time; unmentioned columns get DEFAULTS, never the table's own current settings. The two config sets must not bleed either direction.
- **`docs/superpowers/` is gitignored — never `git add` it** (BACKLOG §10 convention, overrides the skill's commit steps).
- Commit messages: imperative, `feat(frontend):` / `refactor(frontend):` style, referencing P-13/P-14/C-6.
- Frontend icon for the new kind: `FolderOutput` from `@lucide/svelte` (verified present in this worktree's node_modules).
- Model selection that worked: `haiku` for transcription-style tasks where the brief carries complete code (T1-T3), `sonnet` for integration tasks (T5-T7) and for reviews, `opus` for the one high-risk review (T4's 47KB refactor diff). Always pass `model` explicitly.

## Done
- Pre-flight conflict scan (in the ledger) with 8 rulings recorded.
- **Tasks 1-6: complete, reviewed, clean.** T2 took 1 fix round (type-signature revert), T6 took 2 (migration coverage test, then reverting the test-format churn).
- **Task 7: implemented and committed (`089478b`), full backend gate green — but its task review was never dispatched** (interrupted). The review package is already built at `.superpowers/sdd/.../review-efef243..089478b.diff`.
- Task 7 disclosed one plan-authorized omission: the optional aggregate-202 route test was dropped (the `CountingRunner` transplant exceeded the plan's ~30-line budget; plan Task 7 Step 4 explicitly permits this).
- Briefs already generated on disk for Tasks 1-7. Tasks 8-13 need `task-brief` run.

## Known Issues, Not Yet Fixed
All are deferred MINORS recorded in the ledger's Execution section — none blocks progress, and the final whole-branch review is supposed to triage them:
- `tests/table/test_split.py:26` carries `# type: ignore[arg-type]` for list invariance; annotating the locals would remove the need.
- `src/data_rover/api/table_export_engine.py:125-155` and `routes/tables.py:476-535` hold two adapted copies of the ~30-line export-mechanics docstring — a drift surface.
- `src/data_rover/api/db_models.py:117-119` — `ArtifactKind`'s docstring claims "all four mega-plan kinds" and "VARCHAR+CHECK"; there are now six kinds and there is no CHECK constraint. Pre-existing, deliberately left alone.
- `core/table/split.py` `render_filenames` re-runs `validate_template` internally, so a split request validates twice. Harmless/idempotent.
- `tests/table/test_schema.py` — `test_json_split_accepts_any_template_at_schema_level` asserts only `.enabled`, not that the template round-tripped unmodified.

## Deferred — Do Not Do
- Navigation exports, xlsx split, sparse/inheriting overrides, background export jobs, stable column ids — all explicitly rejected during brainstorming (spec §7). Do not add them.
- The aggregate-202 route test for `/exports/run` — dropped by the implementer under the plan's own authorization; the engine's 202 path is already pinned by Task 4/5's suites. Do not spend a session rebuilding the fake-runner scaffolding for it unless the Task 7 reviewer rules it load-bearing.
- Do NOT fix `db_models.py`'s stale `ArtifactKind` docstring inside a task — the controller told Task 6's implementer to leave it; it belongs to the final review's triage.

## Plan
1. **Dispatch the Task 7 task review** (model: `sonnet`) using `task-reviewer-prompt.md`, with brief `.superpowers/sdd/2026-08-13-table-export-split-and-custom-export/task-7-brief.md`, report `task-7-report.md`, and diff `review-efef243..089478b.diff`. Global constraints to hand it: the id-in-body decision (`authz._READ_ONLY_POST_SUFFIXES` matches fixed suffixes, so the route must be viewer-callable — spec §4.3 governs, §5.2's `POST /exports/{id}` is stale shorthand); the loud-422 dangling-ref stance; RENDER ONLY; the aggregate-202 `"computing"`-wins-over-`"failed"` rule; the zip layout (single-file entries at root, `archive=True` entries under a folder, `_2` dedupe in entry order, headers OR-ed). Run its fix loop if it returns findings, then ledger `Task 7: complete` and mark todo #7 done.
2. **Tasks 8-12 (frontend), one at a time**: `task-brief` → dispatch implementer → `review-package` → task review → ledger. Task 8 is a zero-behavior C-6 refactor (extract `$lib/artifacts/kinds.ts`, rewire 9 sites) — cheap model. Tasks 11-12 are the heavy ones (state module mirroring `snippet-editor.svelte.ts`, then components incl. extracting `ExportSettingsPanel` out of `ExportDialog` with `ExportDialog.test.ts` passing UNMODIFIED as the regression harness) — use `sonnet`.
3. **Task 13**: docs (BACKLOG P-13/P-14 + C-6 done, CLAUDE.md's "Table export formats" bullet + artifact-kind list, frontend/README.md's `exp:` tab prefix) then the full gates `core-test` / `frontend-test` / `dr-tidy`. Expect `dr-tidy` to reformat `src/data_rover/core/table/custom_export.py` (Task 3 left it unformatted); that hunk riding in the docs commit is acceptable (ruling R8).
4. **Final whole-branch review** on the most capable model, per superpowers:requesting-code-review, pointed at the ledger's deferred-minor list. `review-package PLAN $(git merge-base main HEAD) HEAD`. ONE fix dispatch for its findings, one scoped re-review, then delete the SDD workspace.
5. **superpowers:finishing-a-development-branch** — and note the user has NOT yet decided whether this merges to `main`. Ask then.

## Open Questions
- Merge/integration is still undecided (step 5) — the user has not said whether this branch merges to `main`, opens a PR, or stays put.
- Nothing else: all design decisions are settled in the spec and the ledger's rulings.
