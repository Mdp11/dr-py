# Handoff: Custom validation rules (P-12) — execute the implementation plan

## Mission
Implement backlog item P-12: user-defined declarative validation rules (YAML) for the MBSE engine at `/home/mdp/workspace/data-rover-py`. The design spec and the 13-task implementation plan are both written and approved by the owner. Done = all 13 plan tasks executed on branch `feat/validation-rules`, `dr-tidy` clean, `dr-test` passing, final code review run.

## Orient First
1. `docs/superpowers/plans/2026-08-24-custom-validation-rules.md` — THE plan to execute; its "Key pre-verified facts" section carries the reconnaissance so you don't re-explore.
2. `docs/superpowers/specs/2026-08-24-custom-validation-rules-design.md` — the approved design the plan argues from (pinned language semantics in §2 are contractual).
3. `CLAUDE.md` — repo conventions, pixi commands, architecture (read the "Check-out/commit + locking" and "Backend session" sections).
4. `BACKLOG.md` P-12 entry (~line 204) — the originating item.
5. `src/data_rover/core/validation/pipeline.py` — the pipeline the new validator joins.

Confirm state: `git status --short`, `pixi run core-test`, `pixi run frontend-test`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main` at `0084304` — clean, nothing uncommitted. The feature branch `feat/validation-rules` does NOT exist yet (plan Task 1 Step 1 creates it).
- Tests: not run this session; baseline per BACKLOG §0 was green on main.
- Spec + plan files exist on disk (both under gitignored `docs/superpowers/` — never commit them).
- No implementation code written yet. Execution has not started.

## Standing Constraints
- **Declarative YAML rules, evaluated natively in the pipeline — NOT Python-snippet rules.** Chosen by the owner after a full pros/cons pass: snippet rules would need a second async evaluation subsystem (sweep + read-set capture + caching) and eventual-consistency in the validation UI. Do not reopen.
- **Rules live in a new `validation_rules` artifact kind** (owner picked this over metamodel-embedded or project-singleton storage): YAML text stored verbatim in the payload (`{schema_version, yaml}`) so author comments survive.
- **Rule issues are ALWAYS `IssueCategory.CONFORMANCE`** — P-12's pre-made decision; a user rule must never block a commit. No STRUCTURAL opt-in, permanently.
- **Freshness = reach-aware dirty expansion (spec §6), full sweep as healing backstop** — owner approved option A over sweep-only or full-revalidate-per-commit.
- **`default_pipeline()` stays rule-free**; rules enter only via `api/rules.py::session_pipeline(session)`. `tests/validation/test_check_names.py` must keep passing unchanged.
- **Never cache a pipeline/validator on the Session** — validators carry mutable `MetamodelMemo` caches (thread contract in `pipeline.py:172-177`). Cache only the immutable `CompiledRules`, swap atomically, build validators per run.
- **Drift (rule referencing unknown schema names) is save-OK, skip-whole-rule at compile, surfaced via `rules_status` on `GET /model/issues`** — never as issues (issues require an entity owner; ownerless issues can't be incrementally cleaned).
- **Equality semantics pinned**: plain Python `==` with a bool/int guard (`_eq` in the plan) — spec §2.2's "no coercion" examples, not strict type-identity.
- **`POST /rules/lint` is NOT in `authz._READ_ONLY_POST_SUFFIXES`** (viewers 403), mirroring `/metamodel/lint`'s documented stance.
- **Adding the `ArtifactKind` enum member needs NO Alembic migration** — column is VARCHAR(32), no CHECK constraint (verified against alembic 0008/0011/0012).
- Execution mode: **subagent-driven** (owner chose superpowers:subagent-driven-development — fresh subagent per task, review between tasks).
- Repo-wide: everything runs through pixi; `docs/superpowers/` is gitignored by convention — write specs/plans there but never `git add` them.

## Done
- Brainstorm (architectural path) complete; all design decisions approved by the owner.
- Design spec written + self-reviewed: `docs/superpowers/specs/2026-08-24-custom-validation-rules-design.md`.
- Codebase reconnaissance done by 4 explore agents; distilled into the plan's "Key pre-verified facts" (session/hydration hook points, commit-splice sites in `routes/commits.py`, metamodel/model core APIs, frontend kind-registration site list).
- Implementation plan written + self-reviewed: `docs/superpowers/plans/2026-08-24-custom-validation-rules.md` (13 tasks, TDD, exact signatures).

## Known Issues, Not Yet Fixed
- Pre-existing, relevant while wiring Task 8: `change_request.py` holds no `write_mutex` anywhere, and `_ensure_validation_seeded` (`routes/ops.py:449`) takes no lock of its own — the plan follows the existing benign-race stance (immutable-swap `compiled_rules`) rather than fixing this; do not "fix" it in passing.
- `tests/api/test_projects_wizard.py::test_wizard_create_reports_skipped_artifacts` flakes ~1/30 (BACKLOG T-4) — re-run, don't chase.

## Deferred — Do Not Do
- `else` branches on rules; rules on relationship types; property tests on the relationship itself; value-vs-value joins; snippet-backed rules; per-element muting — all recorded as future directions in spec §11 by the assistant with owner approval of the spec.
- e2e tests for the new surfaces — joins the BACKLOG T-7 list, explicitly out of plan scope.
- STRUCTURAL user rules — permanently out (owner-endorsed spec §5).
- F-12 (`ensure*Draft` close race): the new rules editor deliberately inherits the buggy family pattern; the family-wide fix is a separate backlog item. Do not fix locally in Task 11.

## Plan
1. Invoke `superpowers:subagent-driven-development` and execute `docs/superpowers/plans/2026-08-24-custom-validation-rules.md` task-by-task (Task 1 creates `feat/validation-rules` and the core rule schema; each task is TDD with its own commit).
2. Review each subagent's result between tasks (two-stage review per the skill).
3. After Task 13 (docs + `dr-tidy` + `dr-test`), run `superpowers:requesting-code-review` on the branch.
4. Update BACKLOG.md P-12 → done in the close-out commit (already a plan step).

## Open Questions
None.
