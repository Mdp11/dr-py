# Handoff: Exporter v2 Phase 2 — Formats & Shape

## Mission
Implement Exporter v2 **Phase 2** (CSV + JSONL export formats on both export routes, and `JsonDocumentOptions` document shaping for exporter entries) in `/home/mdp/workspace/data-rover-py`, by executing the ready-made implementation plan with the **superpowers:subagent-driven-development** skill (user chose subagent-driven execution explicitly). Done = all 11 plan tasks complete, `pixi run dr-tidy` + `pixi run dr-test` + `pixi run frontend-check` green, branch merged per the finishing-a-development-branch skill's menu.

## Orient First
1. `docs/superpowers/plans/2026-08-20-exporter-v2-phase2-formats-and-shape.md` — THE plan; 11 tasks with full code, tests, and commit messages. Execute it task-by-task; it is the single source of truth for this work.
2. `docs/superpowers/specs/2026-08-19-custom-export-v2-design.md` §6–§7, §13, §15 — the approved spec the plan argues from (gitignored, exists on disk).
3. `CLAUDE.md` — toolchain (everything through pixi), architecture, docstring conventions.
4. `src/data_rover/api/table_export_engine.py` — the shared engine both routes call; Task 6 modifies its format tail.
5. `src/data_rover/core/table/exporter.py` and `src/data_rover/core/table/json_export.py` — the two core modules most tasks touch.

Confirm state: `git status --short` (expect clean), `git log --oneline -1` (expect `09c8a06`), `pixi run core-test` if you want a fresh baseline.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main` (create `feat/exporter-v2-phase2` off it before Task 1 — the plan's Global Constraints say so)
- Uncommitted: clean (the plan + spec files are under gitignored `docs/superpowers/`)
- Tests: `pixi run dr-test` green — core 1877 passed, frontend 2186 passed (as of earlier today at commit `09c8a06`, before any Phase 2 work; no code has been written yet)
- No servers running; docker services stopped. Nothing pushed to origin this session (the `09c8a06` merge is local-only).

## Standing Constraints
- **Plan decisions are settled — do not redesign.** Key ones and their reasons:
  - Cell display text extracted to `core/table/cell_text.py` (Task 1) — spec §6 mandates extraction from the xlsx writer so CSV/xlsx cannot drift.
  - Object-shape document keys derived inside `render_json_ex` from the bucket's first row's **cells**, never the rendered doc — a hidden `key_column` must work (spec §7: "it is data, not presentation"), and reusing `render_json`'s bucketing avoids a second grouping implementation that would drift.
  - `json_doc` is an **engine parameter**, never a `TableDefinition` field — spec §6 says document shaping stays exporter-entry-only; RENDER ONLY boundary.
  - `MEDIA_TYPES` lives in `table_export_engine.py`, shared by both routes — one map to extend per format.
  - Frontend: a `jsonFamily` derived (`json | jsonl`) replaces every `format === 'json'` gate in `ExportSettingsPanel` — JSONL renders through the JSON document path, CSV through the xlsx layout path.
  - JSONL media type: `application/x-ndjson`; CSV: `text/csv; charset=utf-8`, **no BOM** (machine consumers, spec §6).
- **Two flagged spec interpretations** (deliberate, argued in the plan's self-review — keep unless the user overrules): (1) `json_doc` shaping applies per split file; (2) `json_doc` on xlsx/csv is tolerated-and-ignored (only `transform` — Phase 4 — is a format-mismatch 422 per §13).
- Never block Save with new strictness — all new 422s fire at export time, naming the entry (spec §13). No-migration guarantee: default payloads export byte-identically.
- All commands via pixi (`pixi run -e core-dev pytest …`, `pixi run frontend-test`); bare `npx`/`python` fail (no global node/python; system node too old for eslint).
- Commit messages end with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` (session rule).
- TDD per task: red test first, then implement — the plan's steps encode this; don't collapse them.

## Done
- Phase 2 plan written and self-reviewed (spec coverage, type consistency across tasks) — do not rewrite it.
- Earlier this session, unrelated and merged to `main` at `09c8a06`: exporter add-table-picker empty-state fix + e2e spec (`frontend/e2e/exporter.spec.ts`). Do not touch.
- Confirmed context: only Exporter v2 Phase 1 is shipped; Phases 3–5 (automation surface, transform hook, bundle-draft export) are specced but NOT part of this work.

## Known Issues, Not Yet Fixed
None.

## Deferred — Do Not Do
- **Phase 3–5 features** (searchable P-15.1 picker, draft runs, run-by-name, `transform` hook, bundle-draft export) — user chose Phase 2 only; the phase ordering is the spec owner's (§16).
- **Previewing the object shape in the JSON preview panel** — plan Task 10 explicitly scopes it out; `POST /tables/json-preview` keeps rendering the array shape.
- **Pushing `main` to origin** — user has not asked; the `09c8a06` merge stays local unless they say otherwise.
- **An `on_error:"fail"` end-to-end API test** — plan Task 7 deliberately covers the policy at core/engine level instead (fixture model can't cheaply produce an error cell); a comment in the test file records this.

## Plan
1. Announce and invoke the **superpowers:subagent-driven-development** skill for `docs/superpowers/plans/2026-08-20-exporter-v2-phase2-formats-and-shape.md`; create branch `feat/exporter-v2-phase2` off `main`.
2. Dispatch Task 1 (extract `cell_text` into core) to a fresh subagent; review its diff and test run before moving on.
3. Continue through Tasks 2–11 in order (each has its own commit); Tasks 2–4 are independent core modules and may be parallelizable per the skill's judgment, Tasks 5→6→7 and 8→9→10 are sequential.
4. After Task 11's full sweep (`dr-tidy`, `dr-test`, `frontend-check` all green), run the **superpowers:finishing-a-development-branch** skill: present the merge menu for `feat/exporter-v2-phase2` → base `main` and wait for the user's choice.

## Open Questions
None — the plan is approved for execution (user: "1 but /handoff"). The two flagged spec interpretations above only need raising if a reviewer objects mid-execution.
