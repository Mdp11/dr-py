# Handoff: Data Rover client-engine program — execute plan 3 (op applier + working copy), inline

## Mission
Data Rover (`/home/mdp/workspace/data-rover-py`) is being re-architected so computation runs in the browser against a full model replica (TypeScript engine in `engine/`), with the Python core as the oracle. Plans 1–2 of sub-project A have landed on `engine-migration`; **plan 3 is written, pre-verified and approved by the user (2026-09-18), execution mode chosen: INLINE** (`superpowers:executing-plans`). This session's job: execute `docs/superpowers/plans/2026-09-18-engine-ops.md` task by task on `feat/engine-ops`, ending with the fast-forward back into `engine-migration`. "Done" = all 6 tasks committed, `pixi run dr-test` + `pixi run dr-tidy` green, branch ff-merged, user told. Do NOT start plan 4.

## Orient First
1. `docs/superpowers/plans/2026-09-18-engine-ops.md` — the plan. Read :1–87 fully (Goal, Provenance, **"What the oracle taught this plan"** :15 — 12 findings, Global Constraints :32, File Structure), then each task as you reach it (Task 1 :88, 2 :924, 3 :1165, 4 :2427, 5 :3559, 6 :3855, "After this plan" :3992).
2. `~/.claude/handoffs/data-rover-py-plan3-extract.py` — writes a task's/step's code blocks from the plan ON DISK into the repo: `python3 ~/.claude/handoffs/data-rover-py-plan3-extract.py <task> --step N [--step M] [--dry-run]` (default root = the real repo). It handles both block kinds (whole file; "In `path`, replace: … with: …", old text must occur exactly once). Use it instead of retyping; it was the tool the verified replay used.
3. `CLAUDE.md` — "Engine package (`engine/`)" and the toolchain commands (everything through pixi).
4. `architecture/contracts.md` (CT-2, CT-5), `architecture/program.md` (MR-3 freeze, A's status row) — Tasks 4 and 6 edit them.
5. Only if something surprises you: `src/data_rover/api/routes/ops.py:143-516` (the oracle applier), `tests/golden/model_steps.py`, `engine/test/golden/model-steps.ts`, `engine/src/model/model.ts`.

Confirm state: `git status --short` (expect clean), `git branch --show-current` (expect `engine-migration`), `git log --oneline -1` (expect `fe9cf18`). Tests: `pixi run dr-test`; engine only: `pixi run engine-test`; lint: `pixi run dr-tidy`, engine only `pixi run engine-tidy`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` at `fe9cf18`. `feat/engine-ops` does NOT exist yet (Task 1 Step 1 cuts it). `feat/engine-store` and `feat/engine-value-layer` still exist, fully merged; delete only if the user asks.
- Uncommitted: clean (`docs/` is git-ignored, so the plan and the updated spec do not show).
- Tests (2026-09-18): on `HEAD` — engine vitest 135 in 25 files (20:08), frontend vitest 2,492 in 252 files (20:50). With plan 3 applied in a scratch clone — engine 209 in 34 files, core pytest 2,429 passed / 34 deselected, `tsc`×2 + eslint + prettier + ruff(`tests/golden`) clean at every task boundary (20:30–20:40). `pixi run dr-test`/`dr-tidy` were NOT run end-to-end on the applied tree (no `src/` or frontend file changes); Task 6 Step 1 does that.
- Per-task engine test counts the replay observed: 135 → 140 (T2) → 160 (T3) → 177 (T4) → 209 (T5). Red-step observations are written into the plan's "Expected:" lines.
- `engine/node_modules` installed. No servers running.
- Scratch copies from the writing session may still exist under `/tmp/claude-1000/-home-mdp-workspace-data-rover-py/8b8f12dc-c5b2-4ffe-9685-89bfcd63c66c/scratchpad/` (`s1` = final tree, `s2` = replay with the 6 commits, `gen/` = plan generator). Convenience only — the plan on disk is the source of truth; do not depend on them.
- The spec `docs/superpowers/specs/2026-09-18-engine-foundation-design.md` (§4, §5, §6, §8) was already updated to match the plan's findings. Nothing to do there.

## Standing Constraints
- **Execute inline with `superpowers:executing-plans`** — user's choice for plan 3 (same as plan 2); subagent-driven was offered and declined.
- **The Python core is the oracle; MR-3 freeze is in force** (`core/model`, `core/metamodel`, `routes/ops.py` apply path): plan 3 changes NO file under `src/`. A failing golden test means the engine is wrong. Fixtures only via `pixi run golden-fixtures`, never hand-edited. An oracle bug found mid-execution is raised with the user, not fixed on one side.
- **User decisions (2026-09-18), do not relitigate:** (1) engine rollback is EXACT and image-based (`rewind` from first-touch before-images: props, `rev`, `ord`) — the oracle's `_rollback` drifts `rev` and order, so fixtures run each batch on a deep copy of the oracle model and keep it only on success; (2) `applyDelta` treats an entity arriving under another type/other ends as remove + append (matches server order for apply-CR rewires); the same-shape re-create gap is logged, not solved; (3) K-29 stays logged, not fixed in plan 3.
- **If a step's expected output does not appear, suspect the environment before the code** — every block was run red→green in a clean clone and the final tree matched byte for byte. Do not "improve" blocks while executing; deviations go to the user.
- **Unicode-escape trap (tooling):** 4-digit `\uXXXX` typed into Write/Edit/Bash gets decoded. Plan 3's sources deliberately hold none (Python uses `\UXXXXXXXX`, TS uses `\u{…}`), but still prefer the extractor. ASCII check after Tasks 3–5: `LC_ALL=C grep -rnP '[^[:ascii:]]' tests/golden engine/test` → only the `café` line of `engine/test/value/serialize.test.ts`.
- **Use pixi's Node/Python only.** `pixi run engine-tidy` formats+lints the engine. `dr-tidy` does not lint `tests/` — run `pixi run -e core-dev ruff format tests/golden && pixi run -e core-dev ruff check tests/golden` by hand (Task 1 Step 7).
- **`architecture/` is tracked; `docs/` is git-ignored — never `git add -f`.** Architecture edits land in the same commit as the code (RC-10): `contracts.md` with Task 4, `program.md` with Task 6.
- **Git (RC-11):** subject one imperative sentence, capitalized, no prefix, no period — the plan gives each subject verbatim; end every message with the session's `Co-Authored-By` line.
- **Branching:** work on `feat/engine-ops` cut from `engine-migration`; finish with `git merge --ff-only` into `engine-migration` (Task 6 Step 3). Never merge into `main`.
- **Bite-check steps (T3 S6, T4 S5, T5 S3) temporarily break source files** — restore each before moving on and confirm the full count passes again; never commit a mutation.
- **Performance is plan 4's job.** Do not optimize (known accepted costs: a rewind restoring an old `ord` re-sorts the entity map once; `unstage({entity})` scans every staged op).

## Done
- Plan 1 (`e8abbf3`..`1b31a31`) and plan 2 (`e978826`..`fe9cf18`) landed on `engine-migration`.
- Plan 3 written, verified (scratch build → clean replay → identical tree; 9 mutation checks all caught), spec updated, approved by the user. Extractor written.
- Findings verified against the real core: rollback drift (digest `d2499a403aeabae2` → `6b1333ccfa35d720` after a refused batch, no commit; `/commits/preview` uses the same `_rollback`); apply-CR rewires reuse the id (`change_request_ops.py:147-149`) and move the relationship last; `_error_detail` quote-stripping (`No element with id 'ghost`).

## Known Issues, Not Yet Fixed
- **K-30** (diagnosed, to be LOGGED by Task 6, fix belongs to sub-project B): `src/data_rover/api/routes/ops.py:477-487` `_rollback` is not exact — refused batches and every `POST /commits/preview` leave `rev` +2 on updated entities and move deleted-then-restored entities last. Breaks CT-3 digests once the server serves them. Frozen under MR-3.
- **K-31** (diagnosed, to be LOGGED by Task 6, B's): a CT-2 delta cannot express "deleted and created again under the same id"; same-shape re-creates keep their place in a replica while the server moves them last; the `(id, rev)` digest cannot see it (AD-12's "fully detect" has this exception).
- **K-29** (diagnosed, logged): `src/data_rover/api/routes/_snapshot.py:54-92` per-kind `seen_ids`; engine loader refuses what Python accepts. User chose to keep it logged.
- **C-20** (diagnosed, logged): `src/data_rover/core/metamodel/schema.py:198` comment contradicts code; frozen.
- `Multiplicity.parse`: Python `int()` reads non-ASCII digits, the engine refuses them (documented gap, no fixture).
- No CI; `tests/golden/test_fixtures_current.py` in `core-test` is the only drift enforcement.

## Deferred — Do Not Do
- **Fixing K-29 in plan 3** — offered as a Task 0, user declined ("keep it logged").
- **Mirroring the oracle's inexact rollback in the engine / amending CT-2 with a `recreated_*` field now / always keeping a re-created record's place** — the three alternatives the user declined when choosing the decisions above.
- **Pure-TypeScript SHA-256 in plan 3** — belongs to plan 4; `WorkingCopy` takes `entityHash` as a required option until then.
- **Writing or starting plan 4** — just-in-time after plan 3 lands (user's process choice).
- **Rust/wasm engine; Python core under Pyodide / orjson / Pyodide memory snapshots; server-side strict-mode verification; merging into `main`; switching server writers to snapshot v2 (sub-project B); `pyEquals`, `isPlainObject`, `engine-bench`, ambient `TextDecoder`; optimizing load/rebuild** — all carried over from earlier handoffs, each declined or postponed by the user.

## Plan
1. Verify state (three git commands above). Invoke `superpowers:executing-plans` and read the plan's header through Global Constraints (:1–87).
2. **Task 1** (plan :88): `git switch engine-migration && git switch -c feat/engine-ops`; `python3 ~/.claude/handoffs/data-rover-py-plan3-extract.py 1 --step 2 --step 3`; `pixi run -e core-dev pytest tests/golden -q` → expect the stale-fixtures failure; `pixi run golden-fixtures`; `git status --short engine/fixtures` → exactly three new `ops_*.json`; pytest → 1 passed; do the Step 6 read-check; ruff; commit "Record op batches through the server's applier".
3. **Task 2** (:924): extract `--step 1` → `pixi run engine-test` red (5 failed, `model.insertElement is not a function`) → extract `--step 3` → 140 green → `pixi run engine-tidy` → commit.
4. **Task 3** (:1165): extract steps 1–2 → red (20 failed | 140 passed) → extract step 4 → 160 green → two bite checks (restore after each) → tidy + ASCII check → commit.
5. **Task 4** (:2427): extract step 1 → red (17 failed, `WorkingCopy is not a constructor`) → extract step 3 → 177 green → bite check → extract step 6 (contracts.md) → tidy → commit with `architecture/contracts.md`.
6. **Task 5** (:3559): extract step 1 → 209 green at once → two bite checks → tidy → commit.
7. **Task 6** (:3855): `pixi run dr-test` (2,429/34 deselected; 2,492; 209 in 34 files) and `pixi run dr-tidy` (no changes); extract step 2 (CLAUDE.md, program.md, BACKLOG.md); commit; `git switch engine-migration && git merge --ff-only feat/engine-ops`.
8. Report to the user: what landed, test counts, anything that deviated from the plan's expected outputs. Offer (do not start) plan 4.

## Open Questions
- None blocking. After plan 3 lands: whether to delete the merged `feat/engine-*` branches, and when to write plan 4 — both the user's call.
