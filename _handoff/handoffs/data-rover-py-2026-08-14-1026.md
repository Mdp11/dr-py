# Handoff: SDD execution of P-13/P-14 — paused at Task 12 of 13

## Mission
Execute the approved 13-task plan for P-13 (per-element JSON split export, zipped, `${name}` filename template) and P-14 (new `custom_export` artifact kind bundling many tables' exports with per-entry presentation overrides) in `/home/mdp/workspace/data-rover-py/.claude/worktrees/feature+table-export-artefacts`. **Tasks 1-11 are complete, committed, and reviewed clean.** Remaining: Task 12 (components), Task 13 (docs + gates), the final whole-branch review, and the merge decision. Done = all 13 tasks with clean reviews, `pixi run core-test` + `pixi run frontend-test` + `pixi run dr-tidy` green, BACKLOG P-13/P-14/C-6 marked done.

**Work in the existing worktree.** Do NOT work in the main checkout — another session is using it for the metamodel diagram editor. Re-enter with `EnterWorktree {path: "/home/mdp/workspace/data-rover-py/.claude/worktrees/feature+table-export-artefacts"}` (no-ops if already there). Note: this session's cwd silently reverted to the main checkout once after a subagent was killed — **check `pwd` before any git command.**

## Orient First
1. `.superpowers/sdd/2026-08-13-table-export-split-and-custom-export/progress.md` — **THE LEDGER. Read first.** Pre-flight conflict scan, all 16 rulings (R1-R16), every task's completion line with commit range, and the deferred-minor list the final review must triage.
2. `.superpowers/sdd/2026-08-13-table-export-split-and-custom-export/task-12-brief.md` — the next task, already generated. `task-13-brief.md` too.
3. `docs/superpowers/plans/2026-08-13-table-export-split-and-custom-export.md` — the plan. Do NOT paste whole tasks into subagent prompts; use `scripts/task-brief`.
4. `docs/superpowers/specs/2026-08-13-table-export-split-and-custom-export-design.md` — **binding authority when the plan conflicts with it.** §2 decision table, §5 frontend design.
5. `frontend/README.md` — required before touching `frontend/src/lib/state/`.
6. `CLAUDE.md` — pixi-only toolchain, RENDER ONLY export boundary, test layout.

Confirm state:
```sh
pwd                                  # MUST be the worktree, not /home/mdp/workspace/data-rover-py
git log --oneline -1                 # expect e0e507c
git status --short                   # expect the 4 partial-work entries listed under State
pixi run frontend-test               # expect 1928 passed (as of 2026-08-13 ~20:00)
pixi run frontend-check              # expect 0 errors 0 warnings
```
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `worktree-feature+table-export-artefacts`, 18 commits ahead of `main`. Merge base `d52826c`.
- HEAD: `e0e507c`.
- **Uncommitted (deliberately kept — user's explicit choice):** partial Task 12 work.
  - `M frontend/src/lib/components/Table/ExportDialog.svelte` (−365 lines)
  - `M frontend/src/lib/state/table-editor.svelte.ts` (−46 lines)
  - `?? frontend/src/lib/components/Export/` (`ExportSettingsPanel.svelte` ~14KB, empty `__tests__/`)
  - `?? frontend/src/lib/util/export-download.ts`
  This is Task 12 Step 1 (panel extraction) plus the Step 3 retry-loop extraction, **unverified** — the agent was stopped before running `ExportDialog.test.ts`. Do not assume it is green.
- Tests: frontend 1928 passed, `frontend-check` clean (as of 2026-08-13 ~20:00, at `e0e507c` with a CLEAN tree — not with the partial work applied). Core: 1791 passed as of Task 7.
- **Alembic: our `0010_artifact_kind_width.py` COLLIDES with `0010_metamodel_layouts.py`, which is already on `main`.** See Known Issues.
- Env: `pixi install -e core-dev -e frontend` + `npm install` already done in this worktree.
- IDE noise to ignore: pyright/TS "Import could not be resolved" and `.svelte.ts` "module has no exported member" diagnostics are tooling artifacts (the IDE isn't using the pixi env / resolves `.svelte.ts` via the ambient `*.svelte` declaration). `frontend-check` and `backend-lint` are the truth. Verified repeatedly this session.

## Standing Constraints
- **Process**: superpowers:subagent-driven-development. Fresh implementer per task → task review (spec + quality) → fix loop (max 5 rounds; rounds 1-3 resume the same implementer) → ledger line → next task. **Never fix findings yourself in the controller session.** Scripts at `/home/mdp/.claude/plugins/cache/claude-plugins-official/superpowers/6.3.0/skills/subagent-driven-development/scripts/` — `task-brief PLAN N`, `review-package PLAN BASE HEAD`. Hand subagents FILE PATHS, never pasted diffs.
- **Never run `ruff format` on anything under `tests/`** — `pixi.toml` scopes the formatter to `src/data_rover/core` and `src/data_rover/api` only; reformatting tests produced ~550 lines of churn that had to be reverted (R7). `ruff check` on tests is fine.
- **RENDER ONLY** (spec §3.4): evaluation (`build_rows_ex`/`order_rows`/`iter_export_rows`/script context) always gets the ORIGINAL `TableDefinition`; override copies feed only layout/render.
- **Strict template**: a `${name}`-less filename template is 422 at export and blocks Save/confirm in the dialogs. It is the ONE strict export setting; everything else drift-normalizes. Do not add validation elsewhere.
- **Copy-at-add**: an entry copies the table's settings at add time and the two are independent thereafter, both directions. Option objects are CLONED, not aliased (R15/R16 chain) — do not reintroduce shared references.
- **`docs/superpowers/` and `.superpowers/` are gitignored — never `git add` them.**
- Commit messages: imperative, `feat(frontend):` / `fix(frontend):` style, referencing P-13/P-14/C-6.
- Model selection that worked: `haiku` for transcription tasks where the brief carries complete code and for small scoped re-reviews; `sonnet` for integration tasks and most reviews; `opus` for the one high-risk review (Task 11's 45KB diff — it caught the Important that sonnet-tier reviews of adjacent tasks had missed). **Always pass `model` explicitly.**
- Subagents can call TaskUpdate and will mark the controller's todos complete prematurely — re-assert status after a dispatch returns.

## Done
- **Tasks 1-7 (entire backend)**: complete, reviewed clean. Task 7 took 1 fix round (3 test defects: a vacuous authz test, zero coverage on the aggregate-202 arithmetic, zero coverage on `_dedupe`).
- **Task 8** (C-6 shared artifact-kind registry): complete, 2 fix rounds. `commits 3bbb914..b0fab18`.
- **Task 9** (P-13 frontend split dialog): complete, clean first pass. `27870b6`.
- **Task 10** (custom_export client types/helpers/API): complete, clean first pass. `c62de85`.
- **Task 11** (custom-export state module + kind wiring): complete after 3 fix rounds. `c62de85..e0e507c`.
- Briefs generated on disk for Tasks 1-13. Review packages for every completed task.

## Known Issues, Not Yet Fixed
- **Alembic revision collision (diagnosed, verified via `git ls-tree main`).** Our `alembic/versions/0010_artifact_kind_width.py` declares `revision = "0010"`, `down_revision = "0009"`. `main` already carries a different `0010_metamodel_layouts.py` (from the metamodel-diagram-editor work, landed after our branch point). Both revise 0009 → merging yields two Alembic heads and `pixi run db-upgrade` fails with "Multiple head revisions are present". **The test suite structurally cannot catch this**: API tests use `create_all` on SQLite, and our one real `alembic upgrade head` test runs against our branch's single-head history in isolation. Fix per R16: renumber OURS to `0011`/`down_revision = "0010"` (filename, `revision`, `down_revision`, docstring header), and verify with an actual `alembic upgrade head` against a MERGED history — inspection does not fail when it is wrong. Carried into Task 13.
- Deferred minors from Tasks 1-11 are enumerated in the ledger's Execution section — the final whole-branch review is supposed to triage them. Notable ones: two adapted copies of the ~30-line export-mechanics docstring (`table_export_engine.py:125-155` and `routes/tables.py:476-535`); `db_models.py:117-119`'s `ArtifactKind` docstring claims "all four mega-plan kinds" and "VARCHAR+CHECK" when there are now six kinds and no CHECK (pre-existing, deliberately untouched).

## Deferred — Do Not Do
- Navigation exports, xlsx split, sparse/inheriting overrides, background export jobs, stable column ids — all explicitly rejected during brainstorming (spec §7). Do not add them.
- The aggregate-202 route test for `/exports/run` — dropped under the plan's own authorization; replaced by R10's pure-helper unit tests. Do not rebuild the `CountingRunner` scaffolding.
- Do NOT fix `db_models.py`'s stale `ArtifactKind` docstring inside a task — it belongs to the final review's triage.
- Do NOT swap `DiffDrawer.svelte`'s `ARTIFACT_KIND_LABEL` values for the shared `KIND_LABEL` (R11) — they are inline prose ("code snippet") rendered inside a sentence, not title-case display labels ("Snippet"). Only the TYPE is shared.
- Do NOT clone `row_source` and the other structural fields in `applyEntryOverrides`'s `...defn` spread — judged sound and in-scope-excluded by the Task 11 round-3 re-review: that function is display-only and those fields never enter the persisted `ExportEntry`.
- The user stopped Task 12's implementer **twice** and then chose to pause the plan. Do not relaunch a Task 12 implementer without explicit instruction.

## Plan
1. **Decide Task 12's shape with the user before dispatching** (they paused specifically here). Options previously offered: relaunch whole from the partial work; split it so the `ExportSettingsPanel` extraction commits as its own reviewed step before the new components; or the user takes it themselves.
2. **When Task 12 does run**, its first gate is: `pixi run frontend-test -- src/lib/components/Table/__tests__/ExportDialog.test.ts` green with that file **byte-unmodified** (`git diff --stat` shows no change to it). It is the extraction's regression harness — fix the extraction, never the test. Three items carried from Task 11's review are part of Task 12 (recorded as R14): make `Workspace.svelte`'s and `TreeRow.svelte`'s bare `else` an explicit `{:else if tab.kind === 'navigation'}` (today a custom_export tab renders NavigationBuilder and leaks its draft + `art:` lease on close), and add `retryCustomExportLock` to the state module + barrel + banner (authorized; every sibling editor has one, and without it a lock-denied tab can never regain editability in-session).
3. **Task 13**: docs (BACKLOG P-13/P-14 + C-6 done, CLAUDE.md's "Table export formats" bullet + artifact-kind list, frontend/README.md's `exp:` tab prefix), **the R16 Alembic renumber**, then the full gates. Expect `dr-tidy` to reformat `src/data_rover/core/table/custom_export.py` (Task 3 left it unformatted); that hunk riding in the docs commit is acceptable (R8).
4. **Final whole-branch review** on the most capable model, per superpowers:requesting-code-review, pointed at the ledger's deferred-minor list. `review-package PLAN $(git merge-base main HEAD) HEAD`. ONE fix dispatch, one scoped re-review, then delete the SDD workspace.
5. **superpowers:finishing-a-development-branch.**

## Open Questions
- **Task 12's shape** — the user paused rather than choosing among relaunch / split / self-implement. Ask before dispatching.
- **Merge/integration is still undecided.** The user has never said whether this branch merges to `main`, opens a PR, or stays put. The Alembic collision (R16) makes this more pointed than it was: the branch is no longer cleanly behind `main`.
- Nothing else — all design decisions are settled in the spec and the ledger's 16 rulings.
