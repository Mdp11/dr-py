# Handoff: Table & panel polish pack (session 5, items 7–11)

## Mission
Implement five small table/panel improvements in `/home/mdp/workspace/data-rover-py`: (7) Cancel/Save semantics on the table settings dialog, (8) clone-column button, (9) collapsible snippet editors in column/step editing, (10) optional row-number column (grid + export), (11) Excel export rewritten on xlsxwriter with autofit/borders/header filters. Spec and plan are written and approved; "done" = all 9 plan tasks executed, full test matrix green (`core-test`, frontend `npm test` + `npm run check`, `dr-tidy`).

## Orient First
1. `docs/superpowers/plans/2026-07-24-table-panel-polish-pack.md` — THE implementation plan; 9 bite-sized TDD tasks with complete code. Execute it task-by-task.
2. `docs/superpowers/specs/2026-07-24-table-panel-polish-pack-design.md` — the approved design behind the plan (read for rationale only).
3. `CLAUDE.md` — toolchain rules (pixi-only, frontend commands must `cd frontend`).
4. `frontend/src/lib/table/columns.ts:1-26` — copy-on-write / no-structuredClone invariants every frontend task leans on.
5. `frontend/src/lib/state/table-editor.svelte.ts:280-364` — the suspend/resume staged-edits machinery Task 1 extends.

Commands to confirm state: `git status --short` (expect clean), `pixi run core-test`, `pixi run -e frontend bash -c 'cd frontend && npm test'`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main` at `c763702` (merge of feat/or-criteria-groups)
- Uncommitted: clean (as of 2026-07-24 13:13; spec + plan files exist but `docs/superpowers/{plans,specs}/` is gitignored by project convention)
- Tests: not run this session — assumed green at `c763702`
- No implementation work has started. No servers running.

## Standing Constraints
- **User chose "Subagent-Driven" execution** — use superpowers:subagent-driven-development: fresh subagent per plan task, review between tasks.
- **xlsxwriter over openpyxl for export (user decision)** — user wanted the "existing python function" (`worksheet.autofit(max_width)`); verified in xlsxwriter source that autofit warns-and-noops in `constant_memory` mode, and openpyxl's `bestFit` is ignored by Excel. So: normal in-memory mode, streaming/bounded-memory property of the export is consciously traded away. Do not relitigate.
- **openpyxl stays as a dependency** — the test suite reads workbooks back with it (xlsxwriter is write-only).
- **Definition `width_px` no longer drives the export** — autofit always wins (spec item 11); on-screen widths still use `width_px`.
- **Dialog Cancel scope = ALL edits since open (user decision)** — snapshot-on-open / restore-on-cancel, including sort remaps; X/Escape/overlay = Cancel; button labeled "Save" (user's wording; "Apply" was offered and not taken).
- **Snippet editors collapsed by default (user decision)**, store-backed state (PathCard `_cardCollapsed` precedent — component-local $state resets on remount).
- **Row numbers appear in export too (user decision)**, as a real "#" first column; grid gutter is presentation-only, never a definition column (ColumnRef indices must not shift).
- **No structuredClone on definition objects** — deep copies via JSON round-trip only (`columns.ts` module doc; a leaked `$state` proxy bricks structuredClone).
- Plan follows repo test conventions: mount/flushSync/unmount, NOT @testing-library/svelte; backend tests hermetic via `tests/api/conftest.py`.

## Done
- Brainstorm → approved design → spec written (self-reviewed, user-approved).
- Implementation plan written and self-reviewed (spec coverage, placeholder scan, type-consistency across tasks all checked; four issues found and fixed inline).
- Nothing implemented yet — Task 1 has not started.

## Known Issues, Not Yet Fixed
- Plan Task 2(f): the spec flags that after Cancel, the navigation-editor store's EMBEDDED draft for an inline nav column may still hold edited state. Not verified either way — the plan says verify `NavigationColumnEditor.svelte`'s unmount closes embedded drafts, and covers it in Task 9's manual smoke. This is a check, not a diagnosed bug.
- Plan Task 6 note: if mypy flags `import-untyped` for xlsxwriter, add `# type: ignore[import-untyped]` to the import (xlsxwriter ≥3.2 ships py.typed, so likely unnecessary).

## Deferred — Do Not Do
- **Streaming/low-memory export mode** — removed by design (user accepted the trade for autofit). Do not reintroduce openpyxl write_only or a "small-table fast path".
- **Sample-based autofit** (measure first N rows) — offered, user rejected in favor of real autofit.
- **"Apply" button label** — offered as alternative to "Save"; defaulted to "Save" per user request.
- **Structural remapping of snippet-collapse keys on column reorder** — deliberately accepted as a cosmetic self-healing miss (plan Task 5 store docstring); do not build navigation-editor-style remapping for it.
- **Focus-the-clone after cloning** — spec mentions focusing; plan deliberately skips programmatic focus (clone renders adjacent, visible). Don't add focus plumbing unless the user asks.

## Plan
1. Invoke superpowers:subagent-driven-development and execute `docs/superpowers/plans/2026-07-24-table-panel-polish-pack.md` Task 1 (store-level snapshot & revert, `frontend/src/lib/state/table-editor.svelte.ts`) with a fresh subagent; review its diff before proceeding.
2. Tasks 2–8 in order (each independent-ish, but 1→2, 3→4, 6→7→8 are ordered pairs; commit per task with the message in the task's final step).
3. Task 9 final sweep: `pixi run core-test`, frontend `npm test` + `npm run check`, `pixi run dr-tidy`, optional manual smoke (includes the Task 2f inline-nav Cancel check).
4. Afterwards: superpowers:finishing-a-development-branch (work is currently on `main` — consider branching first per repo git rules).

## Open Questions
None.
