# Handoff: EXECUTE the inline-transform-snippets plan (subagent-driven development)

## Mission
Implement — not redesign — inline (non-ref) `transform(doc)` snippets for table and
exporter JSON export in `/home/mdp/workspace/data-rover-py`. `ExporterEntry.transform` and
`TableDefinition.transform` become `SnippetSource | None` (ref OR inline code), old payloads
load unchanged, export-time strictness is preserved. The design is FINISHED and reviewed;
this session's job is execution via **superpowers:subagent-driven-development**.
Done = all tasks below implemented + reviewed, `pixi run core-test`, `pixi run frontend-test`
and `pixi run dr-tidy` green, docs corrected, work on a branch off `main`.

## Orient First
1. `~/.claude/handoffs/data-rover-py-2026-08-26-1356.md` — **THE PLAN/SPEC.** Read it in
   full first. Its Standing Constraints, Verified Non-Issues, Deferred and Plan sections are
   the binding authority; this file only adds execution mechanics on top.
2. `CLAUDE.md` — the "Table export formats" paragraph is the contract being edited.
3. `frontend/README.md:245-264` — the client-side transform docs to correct; read the whole
   file before touching `frontend/src/lib/state/`.
4. `src/data_rover/core/script/schema.py:29` — `SnippetSource`, the target shape.
5. `src/data_rover/api/routes/tables.py:134` (`_resolve_transform_code`) and `:555` (its
   gate) — the resolver to generalize and the first of the two `is not None` gate bugs.
6. `src/data_rover/api/routes/exports.py:276` — the second gate bug; `:481` — manifest write.
7. `src/data_rover/api/table_export_engine.py:102` — `TransformHost`, gets the LRU bound.
8. `tests/api/_script_fakes.py` — the fake `ScriptRunner`/session seam. Every new test uses
   it; do NOT reach for the real wasm sandbox (`tests/api/test_snippets_wasm.py` is
   `integration`-marked and needs a vendored binary).
9. Existing tests that map 1:1 onto the tasks below: `tests/table/test_exporter.py`,
   `tests/table/test_schema.py`, `tests/api/test_table_export_transform.py`,
   `tests/api/test_exports_transform.py`, `tests/api/test_transform_host.py`,
   `tests/api/test_export_manifest.py`, `tests/api/test_artifact_kinds.py`.

Confirm state:
```sh
git status --short && git branch --show-current
pixi run core-test
pixi run frontend-test
pixi run dr-tidy
```
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main`, HEAD `17024a3` (merge commit), working tree **clean** (verified
  2026-08-26 14:45).
- Tests: NOT run — no code has been touched across the two design sessions. Run them once
  before starting so a pre-existing failure is not misattributed to this work.
- No servers, migrations, or env vars in play. API tests are hermetic (in-memory SQLite).
- Toolchain is pixi-only; there is no global `python`/`node`. `pixi run dr-tidy` runs
  ruff + mypy + pyright + frontend lint — **all three Python checkers must pass.**

## Standing Constraints
- **The spec is the prior handoff, not this file.** Every design decision, and the reason
  behind it, lives in `~/.claude/handoffs/data-rover-py-2026-08-26-1356.md`. Do not
  re-derive, re-argue or "improve" those decisions — its Deferred section exists precisely
  to stop that.
- **Approval is given.** Open Question 1 of the design doc ("approval to implement") is
  CLOSED — the user asked for execution to be handed off. Do not stop to ask for a go-ahead.
- **Isolated workspace, never `main`.** Repo convention is branch-before-commit. Use
  superpowers:using-git-worktrees at setup. `main` is currently checked out.
- **TDD.** The design doc's step 9 enumerates the test list per task; write tests first.
- **Comment style is CLAUDE.md's**: concise, present-tense, invariants/contracts only — no
  spec/plan/phase references, no historical narration. Several files being edited carry
  long "why" docstrings; match that register, and REVERSE the ones asserting ref-only rather
  than leaving them contradicting the code.
- **Fake runner only.** New tests exercise `TransformHost`/export routes through
  `tests/api/_script_fakes.py`. Booting the real sandbox in a unit test is out of scope.
- **Do not bump `schema_version` or `MANIFEST_VERSION`** — the no-migration guarantee is a
  load-bearing part of the design.

## Done
- Design brainstormed, presented, and then reviewed a second time against the real seams.
- Three decisions ratified by the user during the review: bound `TransformHost._sessions`
  with an LRU cap; the UI wrapper contributes only an add/remove affordance (no three-way
  select); accept-and-document the viewer-callable inline-execution surface (no per-user cap).
- The plan file was rewritten to fold in the review: corrected gates, `disabled` prop,
  message shape, LRU bound, zod refine, expanded test list, plus new
  "Verified Non-Issues" and amended "Deferred" sections.
- **No code has been written. Zero files in the repo have been modified.**

## Known Issues, Not Yet Fixed
Two latent sharp edges this change makes likely, both assigned to tasks below:
- `src/data_rover/api/table_export_engine.py:120` — `TransformHost._sessions` is an
  unbounded dict of live guest sessions (Task 4).
- `frontend/src/lib/components/Snippet/SnippetSourceEditor.svelte:27` — no `disabled` prop,
  while `ExporterTab.svelte:393` passes one to the picker it replaces (Task 6).

## Deferred — Do Not Do
Carried forward from the design doc; all were proposed and declined:
- The transform editor inside `EntryLayoutDialog` — user chose collapsed-under-the-row.
- A `none | saved | inline` three-way select — user chose the add/remove affordance.
- A parallel `transform_code: str | None` field beside `TableRef`.
- Any change to the sandbox or to concurrency ACCOUNTING. The LRU bound (Task 4) is the ONE
  ratified exception; no per-user cap on `open_transform_host`.
- Allowing `transform` on `xlsx`/`csv` — the 422 stays.
- A console Test panel for `transform` — no `RunRequest.entry` exists for it.
- A new size bound for inline code in exporters — resolved as unnecessary (3.2 MiB worst case).

## Plan
1. **Setup.** Read the plan file (Orient First #1). Create the worktree
   (superpowers:using-git-worktrees). Run the three confirm-state commands so a baseline
   exists. Then invoke **superpowers:subagent-driven-development** with
   `~/.claude/handoffs/data-rover-py-2026-08-26-1356.md` as the PLAN_FILE.
2. **Pre-flight, controller-owned (design doc step 0).** Determine whether
   `frontend/src/lib/components/Table/ExportDialog.svelte`'s Export ships the staged draft
   definition or `artifact_id`. `EvaluateTableIn` accepts an inline `definition`, so the
   answer decides whether Task 7 also needs a "commit to apply" hint. Ledger the ruling.
3. **Task decomposition** (the design doc's steps mapped to subagent-sized units; the
   dependency order matters — T2 and T3 both touch `routes/exports.py`, so do not run them
   concurrently):
   - **T1 — Core schema.** Design doc step 1. Retype both fields, reverse both docstrings.
     Tests: `tests/table/test_exporter.py`, `tests/table/test_schema.py`.
   - **T2 — Server resolver + the two gates.** Step 2. Depends on T1. The gate fix
     (`is not None` → `is not None and not .is_empty`) is the part the first design got
     wrong; the empty-source tests are the ones that prove it. Decide the 422 message
     strings here, before writing the tests that pin them.
   - **T3 — Manifest marker.** Step 3. Depends on T2 (same file).
   - **T4 — `TransformHost` LRU bound.** Step 4. Independent of T1–T3; safe to run
     alongside T6. Tests in `tests/api/test_transform_host.py`.
   - **T5 — Bundle verification (test-only).** Step 5. Depends on T1. No production code is
     expected to change; the task is to PROVE that in `tests/api/test_artifact_kinds.py`.
   - **T6 — `SnippetSourceEditor`: widen `entry`, gate the test panel, add `disabled`.**
     Step 6. Independent of the backend tasks.
   - **T7 — `TransformSourceEditor` wrapper + wire both consumers + zod.** Steps 7 and 8.
     Depends on T6 and on the step-0 ruling. Includes deleting `TransformPicker.svelte`,
     the `isEmptySnippetSource` predicate for the two format-flip warnings, and the
     `SnippetSourceSchema` both-set refine.
   - **T8 — Docs sweep.** Step 10. Last, so it describes what actually landed.
4. **Final.** Whole-branch review per the skill, then `pixi run core-test`,
   `pixi run frontend-test`, `pixi run dr-tidy` (design doc step 11), then
   superpowers:finishing-a-development-branch.

## Open Questions
None. Approval is given (see Standing Constraints); the design doc's questions 2 and 3 are
resolved in that file, and its question 4 (rollback tolerance) was noted-not-designed-around
by explicit choice.
