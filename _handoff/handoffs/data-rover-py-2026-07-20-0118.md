# Handoff: Code Execution M2+M3 (ScriptColumn + ScriptStep), subagent-driven execution

## Mission
Implement M2 (`ScriptColumn` table columns computed by a snippet's `value(elements)`) and M3 (`ScriptStep` navigation steps via `step(el)`) in /home/mdp/workspace/data-rover-py, per the approved spec, by executing two written plans task-by-task with the superpowers:subagent-driven-development workflow (fresh implementer subagent per task + task review + fix loops + final whole-branch review). Done = all 15 backend tasks and 8 frontend tasks implemented, reviewed, green (`pixi run core-test`, `pixi run dr-tidy`, frontend `npm test`/`check`/e2e), then superpowers:finishing-a-development-branch.

## Orient First
1. `.superpowers/sdd/progress.md` — the durable ledger: which tasks are complete, per-task Minor findings queued for final-review triage. THE source of truth for resume point.
2. `docs/superpowers/plans/2026-07-19-code-execution-m2-m3-backend.md` — backend plan (Tasks 1–15; task-brief extraction works via headings).
3. `docs/superpowers/plans/2026-07-19-code-execution-m2-m3-frontend.md` — frontend plan (Tasks F1–F8, run after backend).
4. `docs/superpowers/specs/2026-07-19-code-execution-m2-m3-design.md` — approved design spec both plans implement.
5. `CLAUDE.md` — toolchain (pixi-only), architecture invariants, snippet/M1 section.
6. `.superpowers/sdd/task-7-brief.md` + `.superpowers/sdd/task-7-report.md` + `.superpowers/sdd/review-c33de6f..e33959c.diff` — Task 7 is implemented/committed but NOT YET REVIEWED; these three files are the pending reviewer's inputs.

Confirm state: `git status --short && git log --oneline -5`; `pixi run core-test`; `pixi run dr-tidy`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `feature/code-execution-m2-m3` (off main @ 3cf94ff, which already contains the merged multi-element `value(elements)` contract — no prerequisite work).
- Uncommitted: clean (as of 2026-07-20 01:18).
- Tests: `pixi run core-test` 1123 passed; `pixi run core-lint` green — both as of Task 7's completion (commit e33959c), core-lint re-verified by controller at HEAD.
- Backend Tasks 1–6 complete AND review-approved (see Done). Task 7 committed (e33959c) — **review not yet run**: the reviewer dispatch failed with "claude-sonnet-5 temporarily unavailable"; retry it first.
- Task briefs/reports/review-packages live in `.superpowers/sdd/` (task-N-brief.md / task-N-report.md / review-BASE..HEAD.diff). Skill scripts: `/home/mdp/.claude/plugins/cache/claude-plugins-official/superpowers/6.1.1/skills/subagent-driven-development/scripts/{task-brief,review-package}`.
- Session todo list mirrors the ledger (B1–B15 as tasks #7–#21); frontend F-task todos not created yet.

## Standing Constraints
- Subagent model selection: implementers on haiku when the brief contains complete verbatim code (B1–B4 pattern), sonnet for multi-file/integration tasks (B5–B7 pattern); task reviewers on sonnet; final whole-branch review on the most capable model. Always specify the model explicitly in Agent dispatches.
- dr-tidy must be green at EVERY commit — that's why commit 3924e68 added inert type-checker placeholder branches; B7 replaced most of them, `_sort_value`'s `(1, "")` ScriptColumn placeholder deliberately remains until Task 8.
- `dr-tidy`/`core-lint` do NOT ruff-check `tests/` (pixi task cwd is src/…) — every implementer must additionally run `pixi run -e core-dev ruff check tests/` (E402 was reintroduced once this way). 26 pre-existing E702 findings in `tests/table/test_build_rows.py` are unrelated noise; ignore.
- IDE Pyright diagnostics appearing in `<new-diagnostics>` blocks are chronically STALE for this repo (wrong interpreter/lag) — verify with `pixi run core-lint` instead of trusting them; this happened four times, CLI was green every time.
- Dangling SNIPPET refs never raise (stay in place as markers → error cells / prune-with-warning); dangling NAVIGATION refs keep raising. Deliberate asymmetry (spec §3/§4, implemented in B6).
- Sessions are keyed by CODE in `ScriptEvalContext` (not per-column, a deliberate superset of the spec text); calls memoized by `(code, entry, ids)` — sound only under determinism; entry points mutating module globals are outside that guarantee (documented in `core/script/embed.py`).
- No worktree: work happens directly in the primary checkout (pixi envs, node_modules, wasm vendor binary all live here; re-setup cost outweighs isolation).
- `docs/superpowers/` and `.superpowers/` are gitignored — plans/specs/ledger are never committed.
- Commit trailers (every commit): `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01MvNbAUZTCjDCdnmvTKJT9d`.
- Guest binary for WASM integration tests (Task 14): `bash spikes/code_exec/fetch_python_wasi.sh` (idempotent; vendor/ gitignored).

## Done
Backend Tasks 1–6 implemented, committed, review-approved (fix loops included):
- B1 protocol layer (3cf94ff..e031cd7), B2 facade serializer (..1c443c1 incl. E402 fix), B3 TrustedRunner sessions (..9339ef0), B4 ScriptEvalContext (..2f2269b), B5 schemas + placeholders + expand-slot fix (..66090e8), B6 resolve passes (..c33de6f). Details + per-task Minor findings: `.superpowers/sdd/progress.md`.
- B7 table evaluation IMPLEMENTED at e33959c (69/69 tests, core-test 1123, controller-verified core-lint green) — awaiting review only.
Also done earlier this session: spec + both plan documents written and user-approved; old M1 ledger archived to `.superpowers/sdd/progress-m1-backend.md`.

## Known Issues, Not Yet Fixed
- None diagnosed in committed code. Minor findings queued for final-review triage are listed per-task in `.superpowers/sdd/progress.md` (e.g. facade `step()` iterates a bare string char-by-char; nested-SetExpression snippet_fetch threading untested; `_TrustedSession` duplicates `run()` logic).
- Task 7's report (`.superpowers/sdd/task-7-report.md`) overwrote a stale same-named file from an old arc — implementer flagged it; old content was from an already-merged task, no action needed.

## Deferred — Do Not Do
- Real cancellation of embedded runs, per-cell value cache, `call_many` chunked calls, live re-eval push on snippet artifact edit — all explicitly out of scope in the spec's "Out of scope" section (user-approved).
- Inline-editor UI does NOT reuse NavigationColumnEditor's embedded-draft machinery — deliberate (frontend plan F3): inline snippet code is a plain string + local debounced lint.
- Do not "fix" `_sort_value`'s ScriptColumn placeholder before Task 8 — Task 8 replaces it wholesale.

## Plan
1. **Re-dispatch the B7 task reviewer** (sonnet was temporarily unavailable; retry sonnet, fall back to opus if still down). The exact reviewer prompt to reuse is the last Agent call attempt in this session — inputs: brief `.superpowers/sdd/task-7-brief.md`, report `.superpowers/sdd/task-7-report.md`, diff `.superpowers/sdd/review-c33de6f..e33959c.diff`, base c33de6f head e33959c; attention lens = the B7 semantics table (cell mapping, expand promotion/readback, keep_empty error retention, chaining, memo economy, `_sort_value` placeholder stays). Run fix loop if findings; then ledger + mark todo #13… (#13 already shows completed in the todo list — the LEDGER governs: B7 is not complete until reviewed).
2. Continue the per-task loop for Tasks 8–15: `task-brief N` → implementer (model per constraints above) → `review-package BASE HEAD` → reviewer → fix loop → ledger line. Task 14 needs the guest binary fetched first.
3. Then frontend plan F1–F8, same loop (frontend commands run from inside `frontend/` via pixi; read `frontend/README.md` before touching `frontend/src/lib/state/`).
4. Final whole-branch review: `review-package $(git merge-base main HEAD) HEAD`, dispatch on the most capable model with the accumulated Minor-findings list from the ledger for triage; ONE fix subagent for all findings.
5. superpowers:finishing-a-development-branch (merge/PR decision is the user's).

## Open Questions
None.
