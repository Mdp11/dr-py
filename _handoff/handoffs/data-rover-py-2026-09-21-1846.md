# Handoff: Data Rover client-engine program — finish BUILD of plan 3 of sub-project B ("Engine service")

## Mission
Data Rover (`/home/mdp/workspace/data-rover-py`) is moving computation into the browser against a full model replica (TypeScript engine in `engine/`), with the Python core as the oracle. Plan 3 of sub-project B — the engine service — is BUILT on branch `feat/engine-service`: Tasks 1–10 committed, Task 11 (bench rows + closing docs) written but NOT committed, and its final verification is half done. "Done" = Task 11 committed as `Mark the engine service built`, the branch fast-forwarded into `engine-migration` with the owner's go-ahead, and the result reported to the owner (see Plan step 5 for what to report).

## Orient First
1. `docs/superpowers/plans/2026-09-19-engine-service.md` (git-ignored) — the plan; Task 11 is at :483–:509, "After this plan" at :524 (what plan 4 inherits).
2. `CLAUDE.md`, "Engine package" — now describes everything built in plan 3 (`src/steps/`, `src/read/`, `src/service/` bullets, bench bullet). Read it instead of the code.
3. `git diff` — Task 11's uncommitted changes: `engine/bench/run.ts` (step rows), `BACKLOG-ENGINE.md` (`K-32` rewritten as the `ord` re-sort watch item, `R-3` "first three built"), `architecture/program.md` (B's status row), `CLAUDE.md` (engine opening paragraph + bench bullet).
4. `architecture/contracts.md` CT-4 / CT-5 and `architecture/decisions.md` AD-26 — already committed in Tasks 4, 9, 10.
Commands: `pixi run dr-test`; `pixi run dr-tidy true` (check-only lint, positional arg); `pixi run golden-fixtures`; `pixi run engine-test`; `pixi run engine-check`; `pixi run engine-bench`; `git status --short`. Always start a Bash command with `cd /home/mdp/workspace/data-rover-py &&` — a bare `cd` in a compound command moves the session's cwd.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `feat/engine-service`, 10 commits ahead of `engine-migration` (`e5688f9`); head `cd8b08a Serve staging and reads through the engine service`.
- Uncommitted (Task 11, intended for its commit): `BACKLOG-ENGINE.md`, `CLAUDE.md`, `architecture/program.md`, `engine/bench/run.ts`.
- Tests (as of 2026-09-21 ~18:45, with the uncommitted Task 11 files in place): `pixi run dr-test` EXIT 0 — core 2,551 passed / 34 deselected (unchanged from baseline); frontend 2,493 in 252 files (unchanged); engine 461 in 59 files (baseline 272 in 39).
- NOT yet run for Task 11: `pixi run dr-tidy true` (the owner interrupted that call to hand off — rerun it), `pixi run golden-fixtures` + `git status` (must show no fixture moved). `engine-tidy`/`engine-check` were clean after the bench edit.
- Bench (one run, 2026-09-21, Node 22.22.3, medians of 3): open 2,413 ms of 3,000 (was 2,300 before the plan; plan expected ~2.4 s); heap 232 of 400 MB; index build in steps 618 ms, longest step 12 ms [13 8.0 12]; digest check in steps 211 ms, longest 3.8 ms; search q='a' 328 ms, longest step 5.2 ms; search q='sensor' 230 ms; stage 45, unstage 38, rebase 13, delta 8.3, re-sort after rewind 56 ms. No longest step over 16 ms.
- Commits for this plan were pre-approved by the owner (2026-09-21). The final ff-merge still needs its own go-ahead.

## Standing Constraints
- The plan is approved as written (D1–D16); don't relitigate. MR-3 freeze: `core/model`, `core/metamodel`, the model-op applier, `routes/read.py` route functions + `routes/elements.py::get_element` — the Python core is the oracle; fixtures and `engine/src/value/lower-tables.ts` change only via `pixi run golden-fixtures`.
- Performance stance (owner): report numbers, do not optimize.
- RC-11 commit subjects: one imperative capitalized sentence, no prefix, no period; end with `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`. Never `git add -f` `docs/` or `benchmarks/`.
- The owner keeps two logs: new findings needing a decision → `BACKLOG-ENGINE.md` (next free ids `K-37`, `C-22`; `AD-27` next decision id); time-wasters → `~/code/papercuts.md` (`date · symptom · fix · project`).
- Owner answers design questions fastest as ONE multiple-choice question at a time, recommended option first.
- `Scheduler.turnStarted()` was added in Task 8 and REMOVED in Task 9: resetting the slice on each message made the fake host's slice measurement incoherent with the scheduler's, and the plan's M5/M6 never had it. Cost: at most one extra yield after a chunk arrives after a gap. Don't re-add it.
- `verifyDigestSteps()` yields `{done: 0, total}` before any work (Task 9 change): the service sends a task's first (done 0) and last progress at once, the rest at most once per slice — that's how a restarted digest check shows "back to zero".
- The service's method table is one `METHODS` map built from kinds (`now`, `later`, `read`, `inspect`, `transition`) plus custom handlers for `adoptStaged`, `applyTail`, `applyDelta` — no per-method branches in the handler; functionally the plan's `method → {lane, kind, run}`.
- `readScans(method, params)` (in `engine/src/read/index.ts`) decides at arrival whether a read is a `scan` job — the job kind must be known at submit time for D2 ordering.

## Done
- Tasks 1–10 of the plan, each committed under the plan's subject (log above): `pyLower`/`pyStrip` + generated tables; steps + `sortedInSlices` + index build/digest check in steps; yielding snapshot open (500-line batches); working copy coalescing/`adoptStaged`/own-on-duplicate/`stagedVersion`/`structural`/`stagedDiff`; reads I (pages, by-id, relationships, summary) + recorder `read`/`view`/`drop_view` steps; containment tree; fuzzy search; scheduler; service (envelope, replica life, events, `readDeltaText`, AD-26, CT-4); staging/reads/context through the service.
- Task 11 Steps 1–3 written (uncommitted): bench rows, bench run, docs (K-32 rewrite, R-3, program.md, CLAUDE.md).
- Task 11 Step 4 partially: `dr-test` green (see State).

## Known Issues, Not Yet Fixed
- Differences from the plan's reasoned expectations, to report to the owner (none needed a code decision):
  - Task 3: the existing `open.test.ts` "reports progress after every batch" asserted 2,000-line batches and had to move to 500 (D12); the plan's "see it fail" didn't list it. The new "refusal through a paused open" test passed before implementation (an ignored `pause` can't change a refusal). "One large chunk pauses ≥ 4 times" needs 5 batches, so that test uses 2,000 elements + 500 relationships, not 2,000 entities.
  - Task 5: the plan expected `readOps` to read `2 ** 60` as `1152921504606846976n`; `JSON.stringify` writes `1152921504606847000`, which is what the server reads — engine right, expectation fixed. I also implemented Task 5 before running its new tests red (the Python fixture was generated first).
  - Task 9 registered `staged` and `unstage` in addition to `getElement` and `stage`: Task 9's own divergence and background tests use them.
  - Task 9: the 1-byte-chunk open of smart-city (~36k chunks) takes ~3.3 s under Node zlib; that test has a 30 s timeout.
  - Task 2: engine suite wall time unchanged by the stepped rebuild (4.35 → 4.52 s before new tests).
  - Bench: `search q='sensor'` is 230 ms vs the planning probe's 128 ms (probe used native `toLowerCase`; `pyLower`'s ASCII check is the likely difference — *not measured*). Index build's longest step 8–13 ms: under 16, over the 8 ms slice target. Report, don't tune.
- `K-32` (now the `ord` re-sort watch item), `K-29`, `K-35`, `K-36`, `C-20`, `C-21` open in `BACKLOG-ENGINE.md`; `K-33`, `K-34` in `BACKLOG.md`. None is this plan's to fix.

## Deferred — Do Not Do
- Owner-declined on 2026-09-21: leaving the two big sorts native or slicing only the roots sort (chose: slice both); letting a transition run mid-scan / strict FIFO (chose: the transition waits, short reads interleave); deltas as parsed objects or transferred bytes (chose: JSON text).
- Caching search results between pages (plan D16); tuning step sizes, slice target, the `ord` re-sort, or anything the bench shows (owner: report, do not optimize); coalescing in place (plan fact 18).
- Touching `frontend/`, `sandbox/` or `src/data_rover/` in plan 3; building or test-running plans 4–6 while writing them (owner: cheap probes only).
- Still standing from earlier handoffs: optimizing `_decode_v2` / the v2 encode (`K-34`, watch only); reading the descriptor from the blob header or a single JSON `header` column; landing a scratch build as an implementation or replacing a plan with a short decisions doc; refactoring `create_commit`'s scope computation onto `replica.scope_of_ops` (C-12); everything under "Deferred — Do Not Do" in `~/.claude/handoffs/data-rover-py-2026-09-19-1207.md`.

## Plan
1. `cd /home/mdp/workspace/data-rover-py && git status --short` — expect exactly the four Task 11 files modified. Review `git diff` briefly.
2. Finish Task 11 Step 4: `pixi run dr-tidy true` (must be clean); `pixi run golden-fixtures` then `git status --short` (no file under `engine/fixtures/golden/` or `engine/src/value/lower-tables.ts` may appear). `dr-test` already passed with these files; rerun only if anything changes.
3. Commit the four files: `git add BACKLOG-ENGINE.md CLAUDE.md architecture/program.md engine/bench/run.ts && git commit` with subject `Mark the engine service built` and the Co-Authored-By line.
4. Ask the owner (one question) for the go-ahead to `git switch engine-migration && git merge --ff-only feat/engine-service`; do it on yes.
5. Report to the owner: the differences listed under Known Issues; the bench numbers (open 2,413 ms vs the 3 s budget and vs 2.30 s before; heap 232 vs 400 MB; longest step of index build 12 ms, digest check 3.8 ms, search 5.2 ms); that no new finding was logged to `BACKLOG-ENGINE.md` beyond the K-32 rewrite.
6. Then hand off for the next session, which WRITES plan 4 (sandbox and shell, spec §5–§6 of `docs/superpowers/specs/2026-09-19-replica-and-frontend-seam-design.md`) in plan 3's form; the plan's "After this plan" (:524) lists what it inherits.

## Open Questions
- The owner's go-ahead for the final fast-forward of `feat/engine-service` into `engine-migration` (ask after the Task 11 commit).
