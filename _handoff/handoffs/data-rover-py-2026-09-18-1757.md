# Handoff: Data Rover client-engine program — write plan 2 (snapshot v2, digest, metamodel, store, indexes)

## Mission
Data Rover (`/home/mdp/workspace/data-rover-py`) is being re-architected so computation runs in the browser against a full model replica (TypeScript engine in `engine/`), with the Python core as the oracle. Plan 1 (package + value layer + golden pipeline) has landed. This session's job: **write plan 2** of sub-project A with `superpowers:writing-plans` — Python snapshot v2 + state digest, metamodel port, record-graph store, indexes, mutation boundary, with their golden scenarios. "Done" = plan written to `docs/superpowers/plans/2026-09-18-engine-store.md` (or similar dated name), pre-verified like plan 1 (every code block actually run in a scratch copy against the real Python core), reviewed with the user. Do NOT execute it unless the user asks.

## Orient First
1. `architecture/README.md` — index + authority rules; then `architecture/conventions.md`, `architecture/contracts.md` CT-1 (snapshot v2, line 15), CT-3 (state digest, line 51), `architecture/program.md` MR-3 freeze rule (line 98).
2. `docs/superpowers/specs/2026-09-18-engine-foundation-design.md` — the spec. Plan 2 = §3 Metamodel (line 110), §4 Store (line 123), the Python half of §7 (line 217), the matching scenarios of §8 (line 231), §10 (line 266).
3. `docs/superpowers/plans/2026-09-18-engine-value-layer.md` — plan 1: the format, level of detail and provenance standard plan 2 must match (complete code in every step, expected output per step, Interfaces blocks).
4. `CLAUDE.md` — its "Engine package (`engine/`)" section describes what landed; toolchain commands.
5. Oracle code the plan must mirror: `src/data_rover/core/metamodel/schema.py` (`Metamodel`, `_Caches`), `src/data_rover/core/model/model.py` (mutation boundary; `set_property` bump at :89-92), `src/data_rover/core/model/indexes.py` (`IndexSet`, `_frozen`), `src/data_rover/core/model/naming.py` (display name), `src/data_rover/api/snapshot_codec.py`, `src/data_rover/api/serialize.py` (`parse_model_json`, `iter_model_json_compact`), `build_model_from_dicts`, `SequentialIdGenerator`.
6. What exists in the engine now: `engine/src/index.ts`, `engine/src/value/*.ts`, `engine/test/golden/load.ts` (tagged decoding, `sameValue`), `tests/golden/driver.py` + `tests/golden/tagged.py`.

Confirm state: `git status --short` (expect only `?? spikes/client_engine/`), `git branch --show-current` (expect `engine-migration`), `git log --oneline -3` (expect `1b31a31` on top). Tests: `pixi run dr-test`; lint: `pixi run dr-tidy` (see Known Issues).
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` (created by the user's request from `main` at `fa121df`; `chore/architecture-docs` and `feat/engine-value-layer` fast-forward merged into it — 10 commits ahead of `main`, no merge commits). `main` itself is untouched. The two source branches still exist.
- Uncommitted: clean except untracked `spikes/client_engine/` (leave it).
- Tests (as of 2026-09-18 ~17:50, on `feat/engine-value-layer` = same tree): `pixi run dr-test` green — core pytest 2414 passed / 34 deselected, frontend vitest 2492, engine vitest 19 (10 files). `python -m tests.golden --check` exit 0.
- `engine/node_modules` installed via `pixi run engine-install` (prettier 3.9.8 there vs 3.8.3 in frontend).
- `benchmarks/large.model.json` (git-ignored, 170,340 elements / 126,820 relationships) exists locally.
- Previous verified scratch copy of plan 1: `/tmp/claude-1000/-home-mdp-workspace-data-rover-py/89e54c0e-858d-4452-a601-3af1d6e59649/scratchpad/` — may vanish; no longer needed.
- No servers running.

## Standing Constraints
- **Engine = TypeScript, headless = Node + Pyodide (AD-2, AD-3).** Cost table showed language moves GCP bill ≤ $5/month; do not relitigate. Escape hatch is wasm kernels behind the engine interface.
- **`architecture/` is tracked source of truth; specs/plans under `docs/` are git-ignored local files** (`.gitignore:22`) — never `git add -f`. Durable decisions made while planning get promoted into `architecture/` in the same change (RC-9, RC-10). IDs are two-letter (`BACKLOG.md` owns one-letter).
- **Sub-project A is four plans, written just-in-time** so each learns from the last: (1) done; (2) this one; (3) op applier + working copy; (4) snapshot reader, engine digest, benchmarks. Plan 2 includes only the *Python* half of §7 (`encode_snapshot_v2`, v2 branch in `decode_snapshot`, new `api/state_digest.py`); the engine's snapshot reader and digest are plan 4. Server writers keep emitting v1 until B.
- **Plans are pre-verified:** plan 1 was built in a scratch package against the real Python core before writing, and its code blocks were generated from those files verbatim (a `make_plan.py` in the old scratch). Do the same for plan 2.
- **MR-3 freeze rule starts with plan 2** for `core/model`, `core/metamodel` and the model-op applier: no behaviour changes there; a bug found during the port lands on both sides with a fixture.
- **The Python core is the oracle.** Failing golden test ⇒ engine is wrong. Never hand-edit fixtures, never loosen scenarios.
- **Golden driver needs no change for model scenarios** — a model scenario is one more module under `tests/golden/scenarios/` returning a JSON-ready document; values a plain JSON reader can't hold go through `tagged.tag`.
- **Fixture files are parser-independent** (floats as IEEE-754 hex, ints as decimal strings, dicts as ordered pairs) so TS tests never depend on the parser under test.
- **Value model (AD-21):** JS `number` ⇔ Python `int`; `bigint` beyond ±(2^53−1); every float is `PyFloat`; `"Infinity"`/`"-Infinity"`/`"NaN"` stay strings. `pyKey` mirrors `_frozen` (not general equality). `pyEquals` cut from A (C's need).
- **Scope cuts for A:** no `core/view` port (`validate_view` → C); engine takes a validated metamodel as JSON (`GET /metamodel` shape), no YAML/`check_metamodel` (AD-22); no trigram index (AD-13); engine never mints real ids (staged creates keyed by `tmp_`; test-only `idFor` mimics `SequentialIdGenerator`).
- **Digest (CT-3):** first 8 bytes of SHA-256 over `utf8(id) ‖ 0x00 ‖ ascii(rev)`, XOR-folded, 16 lower-case hex. CRC32 rejected: linear, XOR fold can't see two same-length ids swapping revs.
- **`set_property` always bumps `rev`, same value included** (`src/data_rover/core/model/model.py:89-92`; a survey agent once claimed otherwise — wrong). `delete_property` of an absent key is a no-op.
- **Canonical-array-index property keys** (`"0"`, `"42"`) can't keep insertion order in a JS object → bulk load rejects them with `SnapshotError` (spec §4).
- **No production data (AD-17):** no migration tooling, no coexistence window. **Strict mode is client-enforced (AD-8).**
- **Unicode-escape trap (tooling):** the Write/Bash tool layer decodes `\uXXXX` in tool-call text into literal characters. Produce escapes via a script (`chr(92) + "u00e9"`) or extract from files on disk; verify with `LC_ALL=C grep -rnP '[^[:ascii:]]' tests/golden engine/test` (only `café` in `engine/test/value/serialize.test.ts` may show). Never put a NUL byte in a markdown file.
- **Use pixi's Node, never system `node`** (system v18 crashes eslint 10). `engine/src`: erasable syntax only, `.ts` specifiers, `lib: ["ES2023"]`, `types: []`; no enums/parameter properties/namespaces; no `Date.now`/`Math.random`/`Intl` in `src/`.
- **Prettier drift:** engine's fresh install is prettier 3.9.8; its output differs slightly from 3.8.3 (collapsed a union in `types.ts`). Generate plan code with the engine's own `pixi run engine-tidy` so blocks match what the executor will get.
- **Git (RC-11):** commit subject = one imperative sentence, capitalized, no prefix, no period; end with the session's Co-Authored-By line. Plan's branch naming convention is `feat/|perf/|chore/<topic>`; the user chose `engine-migration` as the integration branch — cut plan 2's feature branch from it.
- **Benchmark hygiene (CN-5):** timings drift 1.5–1.9× between sessions; compare only within one pass.
- Process: brainstorming/design for A is DONE and approved — go straight to `superpowers:writing-plans`; don't reopen design unless the oracle contradicts the spec (then raise it).

## Done
- Architecture docs (`architecture/*.md`) and A's foundation decisions committed (`cef2a82`, `55965e0`).
- Plan 1 executed inline, all 8 tasks committed (`e8abbf3`..`1b31a31`): `engine/` package, `cmpCodePoint`, `PyFloat`/`pyFloatRepr`, golden pipeline (`tests/golden/`, `pixi run golden-fixtures`, drift guard in `core-test`), `parseExact`/`parseJson`/`parseLines`, `pyDumps`, `pyRepr`, `pyKey`; engine wired into `dr-test`/`dr-tidy`; `CLAUDE.md`, `BACKLOG.md` R-3 and `architecture/program.md` status updated.
- `engine-migration` branch created with both branches merged (fast-forward).

## Known Issues, Not Yet Fixed
- **Pre-existing ruff-format drift in `src/`** (diagnosed): `pixi run dr-tidy` rewrites 8 files — `src/data_rover/api/{content.py,routes/exports.py,routes/snippets.py,serialize.py,table_export_engine.py}`, `src/data_rover/core/table/{cells.py,evaluate.py,virtual_props.py}` — present on `main` already (last src commit `fa121df`). It was reverted out of plan 1's commits. dr-tidy still exits 0. Unresolved: whether to commit it separately (asked, unanswered).
- No CI in the repo; the drift guard `tests/golden/test_fixtures_current.py` inside `core-test` is the only enforcement.
- `architecture/constraints.md` cites untracked `spikes/client_engine/` as raw evidence (key numbers are copied into CN-4).

## Deferred — Do Not Do
- **Rust/wasm engine** — user chose TypeScript after the cost table; revisit only with profiling evidence.
- **Python core under Pyodide as the engine; orjson in Pyodide; Pyodide memory snapshots** — benchmarked dead ends (CN-22).
- **Server-side strict-mode verification / periodic headless audit** — user chose client-enforced (option A).
- **Committing `spikes/client_engine/`** — raised twice, user never asked.
- **Merging into `main`** — user asked only for the `engine-migration` integration branch.
- **`pyEquals`, `isPlainObject`, `engine-bench`, ambient `TextDecoder` declaration** — cut from plan 1 as YAGNI; they belong to C / plan 4.
- **Writing plans 3–4 now** — just-in-time after plan 2 lands.
- **Executing plan 2** in this session — only write it unless the user says otherwise.

## Plan
1. Verify state (commands in Orient First), read the Orient First files, then invoke `superpowers:writing-plans` for plan 2 covering spec §3, §4, Python §7, and their §8 scenarios.
2. Survey the oracle precisely before writing: `Metamodel` cached lookups and their exact names/semantics (effective props override-by-name, `KeySpec` `out:`/`in:` DSL, end constraints with `0..*` filter, cycle tolerance), `Model` method signatures and exact error texts (`ModelError` kind/message), `IndexSet` (uniqueness key composition, references, `roots_order`, `element_order`), `naming.py`, `build_model_from_dicts(strict=False)`, `snapshot_codec` v1 format and CT-1's v2 header.
3. Build it in a scratch dir first (copy `engine/` + `tests/golden/`, symlink `engine/node_modules`): Python `state_digest.py` + snapshot v2 encoder/decoder with pytest; golden scenarios (metamodel caches, every mutation + error text, cascades, indexes after churn incl. unicode/astral root order and restore order, uniqueness with/without keys and bool/int/float collisions, references scalar/list/dangling, smart-city load + index dump + digest, v2 round trip); a Python canonical index dump for the fixtures; TS `Metamodel`, `Model` store, indexes, `debug/dumpIndexes`, `debug/verifyConsistent`, `debug/shuffleAdjacency`. Get everything green (vitest, tsc both projects, eslint, prettier via engine's tooling, ruff/mypy/pyright for any new `src/` code), then generate the plan's code blocks from those files.
4. Decide task granularity like plan 1 (TDD steps, expected output per step, Interfaces per task, one commit per task); likely order: Python digest → snapshot v2 → index-dump helper + scenarios → TS metamodel → TS store/mutation boundary → indexes → smart-city scenario → docs/status.
5. Promote any new durable decision into `architecture/` (RC-9/RC-10); update `architecture/program.md` status only in the plan's final task.
6. Present the plan to the user for review; offer execution mode (they chose inline for plan 1).

## Open Questions
1. Commit the pre-existing `src/` ruff-format drift as a separate change (and on which branch)?
2. Should plan 2's feature branch be cut from `engine-migration` (assumed) and merged back there, rather than into `main`?
3. Should `spikes/client_engine/` be committed as the evidence `constraints.md` cites?
