# Handoff: Model compare / Replace / Create CR / Apply CR — Task 12 (last task)

## Mission
Finish the approved plan that turned Compare and Apply CR into one "server proposes an op batch, client stages it" pipeline in `/home/mdp/workspace/data-rover-py`. **Tasks 1–11 of 12 are done, committed and each reviewed clean.** Only **Task 12 (docs, backlog, full verification)** remains. Done = Task 12's four doc edits applied, `pixi run dr-tidy && pixi run dr-test && pixi run frontend-check` all clean, committed, then the branch merged/PR'd via `superpowers:finishing-a-development-branch`.

## Orient First
1. `.superpowers/sdd/2026-08-25-model-compare-apply-cr/task-12-brief.md` — **your requirements.** Full verbatim text of all 5 steps (the exact replacement prose for CLAUDE.md, frontend/README.md and BACKLOG.md). Follow it literally; it is the only thing you need to do the work.
2. `.superpowers/sdd/2026-08-25-model-compare-apply-cr/progress.md` — the SDD ledger: per-task completion lines, **7 `Ruling:` lines**, and **18 `minor (deferred)` findings** the final whole-branch review must triage.
3. `docs/superpowers/plans/2026-08-25-model-compare-apply-cr.md` — the plan (gitignored, on disk). Task 12 starts at line 3359.
4. `docs/superpowers/specs/2026-08-25-model-compare-apply-cr-design.md` — the approved design the plan argues from.
5. `CLAUDE.md` — repo conventions (pixi-only toolchain). Read before touching anything.

Commands to confirm state: `git status --short` · `git log --oneline -12` · `pixi run frontend-check` · `pixi run dr-test`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `feat/model-compare-apply-cr`, branched from `main` @ `d20bb3a`. 11 commits ahead.
- Uncommitted: **clean** (as of 2026-08-25 20:35).
- HEAD: `64c2edb feat(frontend): Compare… and Apply CR… in the Model menu; drop the compare page and ApplyCrDialog`
- Tests (as of 20:32, at HEAD): `pixi run frontend-test` **2289 passed / 238 files**; `pixi run frontend-check` **0 ERRORS 0 WARNINGS**; `pixi run core-test` **2121 passed** (last run at Task 5's head — re-run it as part of Task 12 Step 4).
- No servers running; no env vars needed (API tests use in-memory SQLite).
- SDD workspace: `.superpowers/sdd/2026-08-25-model-compare-apply-cr/` (git-ignored). Holds every task's brief + report + review diff. **Delete it only after the final whole-branch review is clean.**

## Standing Constraints
- **The environment auto-commits subagent work asynchronously**, minutes after the agent reports, sometimes with a paraphrased message. Every implementer on this plan reported a SHA that did not exist yet. **Never trust a reported SHA** — run `git update-index --really-refresh` then `git log --oneline -1` and read it. Git's stat cache also hides modified files until that refresh (subagents write files with mtimes ~1h in the future).
- **Verify every reported test count.** Implementers inflated theirs repeatedly (11 vs 6, 13 vs 8, 17 vs 12). Re-run the focused suite yourself.
- **Diff the working tree against the previous task's HEAD before committing.** A concurrently-finishing agent once wrote back a stale buffer and silently reverted an earlier task's type fix (`changeRequest.ts`'s `apiFetch<z.infer<…>>`). Reject any hunk outside the task's brief that is not deliberately justified.
- `docs/` and `.superpowers/` are **gitignored** — the spec, plan and ledger are never committed. Do not `git add -f` them.
- Backend is done and reviewed: `/model/compare` and `/model/apply-cr` are **dry runs** that never mutate, journal or validate. `/model/compare` is viewer-allowed (in `authz._READ_ONLY_POST_SUFFIXES`); `/model/apply-cr` stays a write.
- `ops_for_change(cr)` takes **only** the CR (the spec's `(base, final, cr)` was superseded).
- Element retype is a **422**, never delete+create (it would cascade through containment children).
- Task 12 touches **only** `CLAUDE.md`, `frontend/README.md`, `BACKLOG.md`. No code.

## Done
- **Tasks 1–11, all committed, each with a task review returning 0 Critical / 0 Important:**
  - `7e4b607` optional `id` hint on create ops, honored via `Model.restore_*`
  - `503f99e` core `diff_models` + `invert_change_request`
  - `177d4cb` `api/change_request_ops.py` (CR → phase-ordered op batch)
  - `be3bcb9` `/model/apply-cr` becomes a dry-run proposal
  - `87c68bd` `POST /model/compare`
  - `74a6e1e` frontend typed clients (`compareModel`, `proposeCr`)
  - `a375078` `invertChangeRequest` / `crToDiff` / `crPrestate`
  - `d4d3c2e` `stageProposedOps` extracted from snippet staging
  - `cbf34e9` `ModelChangeDialog.svelte` + `ProposalPreview.svelte` (took 2 fix rounds)
  - `e67870b` apply-cr mode tests (12 dialog tests total; no component change needed)
  - `64c2edb` TopBar rewire + deletion of `ApplyCrDialog.svelte`, the `/compare` page, `state/compare.ts`
- The plan's one deliberate open wound is **closed**: `frontend-check` carried exactly 2 `ApplyCrDialog` errors from Task 6 until Task 11's deletions cleared them. It is now 0.

## Known Issues, Not Yet Fixed
- **`tests/api/test_auth.py::test_change_password_happy_path` fails in a full `pixi run core-test` run and passes in isolation.** Pre-existing at the branch point `d20bb3a`, unrelated to this work (auth/cookie provider vs. compare/CR). It did not reproduce on later runs — it is order-dependent. If Step 4's `dr-test` trips on it, that is this flake, not a regression.
- **18 deferred Minor findings** are listed in the ledger (`grep "minor (deferred)" progress.md`). The two worth acting on:
  - `frontend/src/lib/components/__tests__/ModelChangeDialog.test.ts:19-33` — the `$lib/state` mock is an explicit whitelist. This exact shape caused Task 9's fix round 1 (a child component imported a symbol the mock omitted → opaque "testid not rendered"). `importActual` + selective overrides (the pattern `TopBar.test.ts` uses) would fix it.
  - `ModelChangeDialog.test.ts:214` — an `eslint-disable-next-line @typescript-eslint/no-unused-vars` over the `crFile` helper; building the fixture without `complete` instead of destructuring it away removes the need for a permanent suppression.
- Accessibility polish on the dialog: the selected filename (`ModelChangeDialog.svelte:257`) has no `aria-live`, and the error paragraph (`:283-285`) has no `role="alert"`.

## Deferred — Do Not Do
- **e2e coverage for the new dialog** — deliberately out (matches the rules feature's stance); goes on the T-7 backlog list.
- **History diff work (F-9 / K-6)** — adjacent, left out by the user's decision.
- **A retype op / `type_name` on `update_element`** — considered and rejected as YAGNI; retype is a 422.
- **The full CR authoring/review workflow** — out of scope (BACKLOG §10 "Real CR workflow"); this was a UX + staging rework only.
- **Committing `docs/`** — repo gitignores it; earlier specs were never committed.
- **Fixing the pre-existing auth-test flake** — ruled out of scope; it predates the branch.

## Plan
1. Execute `.superpowers/sdd/2026-08-25-model-compare-apply-cr/task-12-brief.md` Steps 1–3: the four `CLAUDE.md` edits, the four `frontend/README.md` edits, the `BACKLOG.md` P-23 rewrite. Use its prose verbatim. Note the README's routes line and "Where to find things" table still mention the deleted `/compare` page — Step 2 items 2 and 3 remove them.
2. Step 4: `pixi run dr-tidy && pixi run dr-test && pixi run frontend-check`. Expect clean; the only tolerated failure is the auth flake above.
3. Step 5: `git add CLAUDE.md frontend/README.md BACKLOG.md && git commit -m "docs: compare / apply-cr proposal flow, id hints, P-23 done"`. Then verify with `git log --oneline -1`.
4. Dispatch the **final whole-branch review** (superpowers:subagent-driven-development's Final Review step) on the most capable model, over `git merge-base main HEAD`..HEAD, pointing it at the ledger's 18 deferred minors so it can triage which must be fixed before merge. Build the package with `.claude/plugins/cache/claude-plugins-official/superpowers/6.3.0/skills/subagent-driven-development/scripts/review-package`.
5. If it returns findings: ONE fix dispatch with the complete list, then ONE scoped re-review. No second wave.
6. Collect every `Ruling:` line from the ledger into the final message under "Rulings I made" (there are 7), then `rm -rf .superpowers/sdd/2026-08-25-model-compare-apply-cr` and invoke `superpowers:finishing-a-development-branch`.

## Open Questions
None.
