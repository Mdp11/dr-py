# Handoff: Lease mirror hardening (backlog B-1 → B-4)

## Mission
Execute the written plan that closes the four small findings from the Redis lock-mirroring
phase in `/home/mdp/workspace/data-rover-py`: B-1 app logging (done), B-2 clamp restored
lease lifetime, B-3 serialize mirror write-throughs, B-4 Redis key prefix. Done means:
all four implemented TDD-style per the plan, each backlog item flipped to `done` in its
own commit, `pixi run core-test` and `pixi run dr-tidy` clean, branch finished via the
superpowers:finishing-a-development-branch skill (local merge to main, no push).

## Orient First
1. `docs/superpowers/plans/2026-08-12-lease-mirror-hardening.md` — the full plan with exact
   code for every step; execution resumes at **Task 2, Step 2**.
2. `src/data_rover/api/lock_mirror.py` — the module B-2/B-3/B-4 modify (seam, clock
   conversion, write-through, restore).
3. `tests/api/test_lock_mirror.py` — Task 2 Step 1's three failing tests are already added
   here (uncommitted); the file's existing `_lease` helper is reused by Task 3's test.
4. `BACKLOG.md` §3 — the four items; B-1 already flipped to `done`.
5. CLAUDE.md — repo conventions (pixi commands, docstring style).

Confirm state: `git status --short` (expect only `M tests/api/test_lock_mirror.py`),
`git log --oneline -2` (expect `750cb34` on `fix/lease-mirror-hardening`),
`pixi run -e core-dev pytest tests/api/test_lock_mirror.py -v -k clamp` (expect FAIL —
that is Task 2 Step 2, the pending red run).
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `fix/lease-mirror-hardening` (created from clean `main` at `7976ef9`)
- Uncommitted: `tests/api/test_lock_mirror.py` — Task 2 Step 1's three new tests
  (`test_to_leases_clamps_remaining_to_max`,
  `test_to_leases_no_clamp_by_default_and_ignores_nonpositive_cap`,
  `test_restore_leases_clamps_to_lock_ttl`), inserted before
  `test_memory_mirror_write_load_and_empty_deletes`. Not yet run (red run pending).
- Tests: `tests/api/test_logging_config.py` + `tests/api/test_lock_mirror.py` = 19 passed
  as of commit `750cb34` (before the Task 2 test edits). Full `core-test` not run this
  session; baseline on main was 1739 passed / 29 deselected.
- Committed this session: `750cb34` — B-1 (`_configure_logging` in
  `src/data_rover/api/main.py`, new `tests/api/test_logging_config.py`, BACKLOG B-1 → done).

## Standing Constraints
- The plan file is authoritative — it was written this session after verifying every
  claim against current code (call sites, `Lease` dataclass shape, `LockTable.seed`
  append semantics, conftest env pins). Follow it exactly; don't re-derive.
- B-2 clamp is a `max_remaining_s` **parameter** on `to_leases`, settings read in
  `restore_leases` — keeps `to_leases` pure (why the fix was parked originally).
  Non-positive cap must mean "no cap": `lock_ttl_seconds` is 0-disableable elsewhere,
  and a 0 cap would otherwise drop every restored lease.
- B-3 uses a new `Session.mirror_mutex` (plain `threading.Lock`), NOT `write_mutex` —
  mirror does network I/O that must never sit inside a route's critical section. Lock
  ordering `mirror_mutex → write_mutex` is safe: all four production call sites of
  `mirror_session_leases` verified (2026-08-12) to run outside `write_mutex`
  (`routes/locks.py:115/130/146`, `routes/commits.py:1112`, `main.py:156`).
- B-3 does NOT fully eliminate mirror phantoms — an outage-cooldown-skipped release can
  still leave one (TTL-bounded). The plan's docstring rewrite says exactly this; don't
  overclaim in docs.
- Logging tests must assert `logger.level`, never `isEnabledFor()` — alembic's
  `fileConfig` in `test_alembic.py` sets `.disabled` on pre-existing loggers
  (order-dependent flake, documented in `tests/api/test_lock_mirror.py`).
- B-4 default prefix is `""` so existing deployments keep reading their unprefixed keys
  across an upgrade.
- Each task flips its BACKLOG.md item to `done` **in the same commit** (BACKLOG.md's own
  rule). Commit messages end with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Working on an in-place branch, not a worktree — deliberate: pixi envs +
  `ensure_guest.sh` activation hook make worktrees expensive here; tree was clean.
- Pyright IDE diagnostics about unresolved `fastapi` imports are environment noise; the
  authoritative check is `pixi run dr-tidy`.

## Done
- Task 0: branch `fix/lease-mirror-hardening` created from main.
- Task 1 (B-1): complete and committed (`750cb34`). `_configure_logging()` added to
  `src/data_rover/api/main.py` (basicConfig + `data_rover` logger at INFO, called first
  in `create_app()`); 3 tests in `tests/api/test_logging_config.py`; BACKLOG B-1 → done.
  Verified: 19 tests passed.
- Task 2 Step 1 only: the three B-2 tests written into `tests/api/test_lock_mirror.py`
  (uncommitted).
- Plan written and self-reviewed: `docs/superpowers/plans/2026-08-12-lease-mirror-hardening.md`.

## Known Issues, Not Yet Fixed
- B-2, B-3, B-4 themselves (diagnosed in BACKLOG.md §3 with the plan's fixes verified
  against code this session).
- Expected-red right now: the 3 new tests in `tests/api/test_lock_mirror.py` fail until
  Task 2 Step 3 lands (`to_leases` has no `max_remaining_s` yet) — Pyright flags the two
  call sites too; both resolve with the implementation.

## Deferred — Do Not Do
- User interrupted execution in the prior session: the remaining tasks were NOT to be
  executed there — but the plan itself stands and this session should execute it
  (that was the user-approved arrangement; work done so far is kept).
- Quick-wins sweep (U-2, F-1, F-3, F-4+U-8) and R-2 (history & revert) — agreed as the
  work AFTER this batch, not part of it.
- Everything in BACKLOG.md §10 — declined by the owner; re-ask before touching.
- No push to `origin` — repo convention (BACKLOG §10), merge locally only.

## Plan
1. Resume plan Task 2 at Step 2: run
   `pixi run -e core-dev pytest tests/api/test_lock_mirror.py -v -k clamp` — confirm the
   3 new tests fail (no `max_remaining_s` param yet).
2. Task 2 Steps 3–6: implement the clamp in `src/data_rover/api/lock_mirror.py`
   (`to_leases` + `restore_leases` + module docstring, exact code in the plan), re-run
   the file green, flip BACKLOG B-2 → done, commit.
3. Task 3 (B-3): TDD per plan — serialization test, `Session.mirror_mutex` field,
   `mirror_session_leases` rewrite, docstring updates, BACKLOG flip, commit.
4. Task 4 (B-4): TDD per plan — `lease_key` prefix, `Settings.redis_key_prefix`,
   `RedisLeaseMirror(key_prefix=)`, `.env.example`, integration test, BACKLOG flip
   (also delete the §11 key-prefix idea bullet), commit.
5. Task 5: `pixi run core-test`, `pixi run dr-tidy`, optional Redis integration run,
   then superpowers:finishing-a-development-branch.

## Open Questions
None.
