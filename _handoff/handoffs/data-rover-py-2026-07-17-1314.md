# Handoff: Code-execution feature — M0 spike execution

## Mission
Implement the code-execution feature (users write/run Python snippets in-browser against the MBSE model) for `/home/mdp/workspace/data-rover-py`. Design is complete and approved; the next step is executing the **M0 go/no-go spike plan** via **subagent-driven execution** (user chose this explicitly). "Done" for this session-arc = all 10 M0 tasks executed, FINDINGS.md verdict written, verdict presented to the user; M1 planning only starts after the user accepts the verdict.

## Orient First
1. `docs/superpowers/plans/2026-07-17-code-execution-m0-spike.md` — the plan to execute, task by task. Read fully.
2. `docs/superpowers/specs/2026-07-17-code-execution-design.md` — the approved spec (authoritative for all design detail; plan cites its sections).
3. `CLAUDE.md` — toolchain (pixi-only), conventions, architecture map.
4. `pixi.toml:1-36` — current deps layout; Task 1 adds `[feature.core-dev.pypi-dependencies] wasmtime = ">=30"`.

Commands to confirm state: `git status --short`, `git branch --show-current`, `pixi run test-core` (not run this session).
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `main`
- Uncommitted: **~34 modified files** (frontend table/navigation UX + backend navigation/table/artifacts code, plus untracked `frontend/e2e/table-ux.spec.ts`). **These are NOT from the code-execution session** — this session only wrote the spec + plan (both under gitignored `docs/`). They look like in-progress table-ux/navigation-property-step work (matches recent commit subjects). Do not commit, revert, or mix them into spike commits.
- Tests: not run this session (no code was written, only docs).
- Task list (session task tracker): all 6 brainstorming-flow tasks completed.

## Standing Constraints
- **Architecture: server-side WASM sandbox** (CPython-WASI under `wasmtime-py`, in the API process) — chosen over browser Pyodide because table columns/nav steps must live inside the server-side evaluators (sort/export/cache/compose); rejected alternatives are recorded in spec §2. Do not relitigate.
- **Writes are dry-run ops only** — snippets record ops in the `ops.ts` wire format; batch goes to the client staged buffer → normal preview/commit. Never direct mutation.
- **Curated `dr` facade only**, pure Python + stdlib allowlist, per-element `value(el)`/`step(el)` call shape — all user-chosen in Q&A; reasons in spec §2 table.
- **`parent=` cut from `dr.create`** — containment-type resolution is ambiguous; users call `dr.connect` explicitly (reviewer amendment).
- **Cache-poisoning guard is load-bearing** (spec §6): truncated/stale evaluations must never populate the row-order cache; fingerprint = SHA-256 of *resolved code* (not `artifact_rev` — inline defs have no rev).
- **`TrustedRunner` lives under `tests/`, never ships**; boot-guard if runner selection becomes a setting (RCE tripwire).
- Spike code style: `spikes/code_exec/` is exempt from mypy/pyright (outside `src/`), keep ruff-clean; guest binary vendored dir gitignored, only hash-pinned fetch script committed.
- `docs/` is gitignored in this repo — spec and plan files are deliberately uncommitted; do not try to commit them.
- Two external-agent reviews already happened (design review + spec review); all their amendments are folded into the spec. Don't re-solicit.

## Done
- Full brainstorming flow: context exploration, 6 clarifying Q&A decisions, 3 architectures compared, design presented in sections and approved.
- Spec written, self-reviewed, amended through two review rounds: `docs/superpowers/specs/2026-07-17-code-execution-design.md`.
- Implementation plan written (M0 task-level with complete probe code; M1–M4 milestone-level by user instruction): `docs/superpowers/plans/2026-07-17-code-execution-m0-spike.md`.
- User chose **subagent-driven execution** for the plan.

## Known Issues, Not Yet Fixed
- Dirty working tree on `main` (see State) predates/parallels this session — unrelated work, provenance unknown to this session. Verified present via `git status` at handoff time.

## Deferred — Do Not Do
- **M1–M4 task-level planning** — user explicitly ordered M0-detailed / M1–M4-milestone-only; expand later plans only after the M0 verdict is accepted by the user.
- **Third-party packages (numpy/pandas), read-your-own-writes, streaming console output, browser Pyodide runtime, `dr.navigate()`** — all explicit spec non-goals (§1); do not add.
- **`snippet_runs` DB audit table** — deliberately a structured log line in v1 (spec §9).
- **Pool ≥ semaphore sizing** — reviewer nit resolved the other way: pool=2 with accepted burst cold-boot cost (spec §4).
- **DB-backed model migration** — discussed as info only ("Ok nvm, that was just for info"); not on any roadmap here.

## Plan
1. Invoke `superpowers:subagent-driven-development` and execute the M0 plan task-by-task (fresh subagent per task, review between tasks). First concrete action: Task 1 — `git checkout -b feature/code-execution-spike main` (safe despite dirty tree: spike files don't overlap the modified paths; uncommitted changes ride along untouched), add the wasmtime pypi dep, scaffold `spikes/code_exec/`, commit only spike files.
2. Tasks 2–9: fetch/pin guest binary, then probes s01–s08 per plan; fill FINDINGS.md row per task; commit per task.
3. Task 10: consolidate FINDINGS.md verdict (GO / GO-subprocess / GO-amendments / NO-GO), apply spec amendments if any, re-run clean probe suite, present verdict to user and **stop for their acceptance**.

## Open Questions
- The dirty tree on `main`: OK to leave as-is while the spike branch carries it along, or does the user want it stashed/committed first? (Spike commits must stage spike files explicitly — plan already does — so it's workable either way; ask only if a spike task would touch an already-modified file.)
