# Handoff: Data Rover client-engine program — EXECUTE plan 2 of sub-project B ("Snapshots and replica routes")

## Mission
Data Rover (`/home/mdp/workspace/data-rover-py`) is moving computation into the browser against a full model replica (TypeScript engine in `engine/`), with the Python core as the oracle. Sub-project B is six plans; plan 1 is built and merged. **Plan 2 is written and APPROVED by the owner (2026-09-19)**; this session EXECUTES it inline with `superpowers:executing-plans`. "Done" = all six tasks of `docs/superpowers/plans/2026-09-19-snapshots-and-replica-routes.md` implemented, one commit per task on `feat/replica-routes`, suites and linters green, fast-forwarded into `engine-migration`, reported to the owner.

## Orient First
1. `docs/superpowers/plans/2026-09-19-snapshots-and-replica-routes.md` (git-ignored) — THE plan. Read "What planning found", "Decisions", "Global Constraints" and "Mechanisms" (M1–M5) before Task 1; every task refers to them.
2. `docs/superpowers/specs/2026-09-19-replica-and-frontend-seam-design.md` §2 (lines ~104-128), §9, §10 — the spec the plan argues from.
3. `architecture/contracts.md` CT-1, CT-2, CT-3; `architecture/constraints.md` CN-11, CN-12.
4. `CLAUDE.md` — commands; "Durable persistence" and "The commit delta (CT-2)" are what the plan's doc steps edit.
5. Code the plan touches: `src/data_rover/api/hydration.py:75` (`write_snapshot`, the one funnel), `src/data_rover/api/snapshot_codec.py`, `src/data_rover/api/content.py:210-227` (`record_snapshot`, `latest_snapshot`), `src/data_rover/api/db_models.py:282` (`Snapshot`), `src/data_rover/api/feed.py:118` (`commit_event` — tail deltas are built through it), `src/data_rover/api/routes/metamodel.py:102` (`GET /metamodel`), `src/data_rover/api/main.py:295-328` (CORS, router mounts), `alembic/versions/0015_commit_state_digest.py` (latest revision; plan's is 0016).
6. Test patterns to reuse: `tests/api/test_commit_delta.py` (fixture, feed helpers), `tests/api/test_alembic.py:144`, `tests/api/test_snapshot_job.py:85` (`DATA_ROVER_SNAPSHOT_EVERY` via setenv), `tests/api/conftest.py` (`commit_create`, `create_folder_via_commit`, `container_lock_target`), `tests/api/test_commits_route.py:109` (`_lock`).

Confirm state: `git status --short` (expect clean), `git branch --show-current` (expect `engine-migration`), `git log --oneline -1` (expect `1cd75f9`).
Tests: `pixi run dr-test`; lint `pixi run dr-tidy true` (positional arg; `check_only=true` is rejected); backend only `pixi run backend-lint`.
Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` at `1cd75f9`. `feat/replica-routes` does NOT exist yet — Task 1 Step 1 cuts it. `main` untouched. `feat/exact-server-state` still exists (merged, not deleted).
- Uncommitted: clean (verified 2026-09-19 16:40). The plan file is under git-ignored `docs/`.
- Tests (as of 2026-09-19 ~14:25 on `1cd75f9`, not re-run since — nothing changed): core 2,465 passed / 34 deselected; frontend 2,493 tests in 252 files; engine 272 in 39 files; `dr-tidy true` clean; fixtures current.
- `benchmarks/large.model.json` (model M) exists, git-ignored. Probe scripts from planning are in the old session's scratchpad; not needed.
- No servers, no background jobs.

## Standing Constraints
- **Commits pre-approved for plan 2 (owner, 2026-09-19):** one commit per task with the subjects the plan gives, and the final `git merge --ff-only feat/replica-routes` into `engine-migration`. Task 1 Step 1 says "ask the owner" — that question is ANSWERED; skip it and just cut the branch.
- **Execute inline** with `superpowers:executing-plans` (owner's choice), not subagent-driven.
- Owner's design answers baked into the plan: D1 typed columns on `Snapshot` (descriptor never touches the blob store); D2 v2 decode cost (2,360 ms vs v1 1,238 ms at M) accepted — log `K-34`, do NOT optimize.
- The plan holds no full code and was never built; its "see it fail" expectations are reasoned. If a step's result differs, trust the run, follow the step's intent, and report it in the hand-back.
- Do not optimize anything; if a number looks wrong, report it.
- JSON-column trap (probed): a Python `None` in `Commit.entity_states` is stored as JSON `null`, not SQL NULL — completeness SQL must test `IS NULL OR CAST(... AS VARCHAR) = 'null'` (plan M2).
- Do not refactor `create_commit`'s scope computation onto `replica.scope_of_ops` (`C-12`: commits.py is not reshaped in passing); the tail-equals-feed test holds them together.
- If the tail-equals-feed test fails on a VALUE (number form, key order), do not normalize in the test — it is a finding; report it.
- Still in force: MR-3 freeze (`core/model`, `core/metamodel`, op applier — plan touches none); fixtures change only via `pixi run golden-fixtures` and must NOT move in this plan; `dr-tidy` lints neither `tests/`, `scripts/` nor `alembic/` — ruff check+format those by hand; pre-existing lint (`UP037` at `tests/api/test_feed_hub.py:12`, `tests/api/test_commit_states.py` unformatted) is not ours to fix; RC-11 git (subjects one imperative capitalized sentence, no prefix, no period; message ends with the session's `Co-Authored-By` line); never `git add -f` `docs/` or `benchmarks/`; `architecture/`, `CLAUDE.md`, backlogs change in the same commit as their code (RC-10).
- Ids: plan uses `K-34`; next free after it `K-35`, `C-21`, `AD-26`.
- The owner answers design questions fastest as ONE multiple-choice question at a time, recommended option first.

## Done
- Plan 1 (exact server state) built and merged at `1cd75f9`.
- Plan 2 written, self-reviewed, approved: `docs/superpowers/plans/2026-09-19-snapshots-and-replica-routes.md`.

## Known Issues, Not Yet Fixed
- `K-33` (open, `BACKLOG.md`): staged branch of `POST /model/validate` (`src/data_rover/api/routes/validation.py` ~:200-211) never calls `session.invalidate_derived_caches()` after rollback. Outside plan 2.
- `K-29`, `K-32`, `C-20` open in `BACKLOG-ENGINE.md` (`K-32` is plan 3's).
- Pre-existing, not logged: stale docstrings at `src/data_rover/api/routes/commits.py` ~:770 and ~:901 (say apply-CR replaces the model); server `_decode_v2` accepts a line holding two documents, the engine refuses (plan 2 lists it under Known limits, still unlogged).
- No CI; `tests/golden/test_fixtures_current.py` is the only fixture drift guard.

## Deferred — Do Not Do
- Optimizing `_decode_v2` / v2 encode — owner chose "accept, log `K-34`" (2026-09-19).
- Reading the descriptor fields from the blob's header line, or a single JSON `header` column — owner chose typed columns (2026-09-19).
- Building/test-running plans 3–6 while writing them — owner (cheap probes only when planning).
- Landing a scratch build as an implementation; replacing a plan with a short decisions document — owner (plan 1 era).
- Everything in "Deferred — Do Not Do" of `~/.claude/handoffs/data-rover-py-2026-09-19-1207.md` (neighborhoods, hybrid warm-up, resumable transitions, optimizing the `ord` re-sort, feed events from `/model/ops` and `/model/undo`, and the older list).

## Plan
1. Verify state (three git commands). Invoke `superpowers:executing-plans`; read the plan's front matter through "Mechanisms".
2. Task 1: `git switch engine-migration && git switch -c feat/replica-routes` (no need to ask), then Steps 2–8 — `Snapshot` columns, migration 0016, `record_snapshot`/`latest_snapshot(format=)`/`get_snapshot`; commit `Record a snapshot's format and header on its row`.
3. Task 2: every writer emits v2 (M1: `write_snapshot` takes `write_mutex` itself); `K-34` into `BACKLOG.md`; fixtures must not move; commit.
4. Task 3: tail (`api/replica.py`, `routes/replica.py`, `content.commit_tail_marks`, M2/M3/M5); CT-2 edit in `architecture/contracts.md`; commit.
5. Task 4: descriptor + blob (M4); commit.
6. Task 5: `X-Metamodel-Id` + CORS `expose_headers`; commit.
7. Task 6: `architecture/program.md` status, full `dr-test` / `dr-tidy true` / `golden-fixtures`, alembic tests, the by-hand Postgres cast check if the dev stack is available (say if skipped), commit `Mark the replica routes built`, `git switch engine-migration && git merge --ff-only feat/replica-routes`.
8. Report to the owner: per task, what ran, anything that differed from the plan's expectations, final test counts. Then plan 3 (engine service, spec §3–§4) is next to be WRITTEN (plan-only, cheap probes allowed).

## Open Questions
None.
