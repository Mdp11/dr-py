# Handoff: Stage 2 (Table system) — backend DONE, execute the frontend plan

## Mission
Implement **Stage 2 — Table system** for the data-rover MBSE engine at
`/home/mdp/workspace/data-rover-py` via subagent-driven development. The **backend
plan is COMPLETE and green** (12/12 tasks, on branch `feat/stage2-tables`). This
session **executes the 12-task frontend plan on the same branch**, then runs ONE
whole-branch review (backend + frontend), then finishes the branch. "Done" = both
plans' tasks implemented, all tests green, lint clean (frontend: svelte-check;
backend already clean), whole-branch review clean, branch integrated.

## Orient First
Read, in order:
1. `~/.claude/handoffs/data-rover-py-2026-07-11-1246.md` — the PRIOR handoff (design
   rationale, locked decisions, non-goals). Its "Standing Constraints" still bind.
2. `.superpowers/sdd/progress.md` — the **live progress ledger** (gitignored). Its
   "Slice: Stage 2 — Table system" section is the authoritative record of every
   backend task: commit SHA, tests, review verdict, and every Minor finding deferred
   to the final review. **Trust this + `git log` over any memory.** Two items are NOT
   yet written to it (the last ledger append was interrupted) — see State below;
   append them first thing.
3. `docs/superpowers/plans/2026-07-10-stage-2-tables-frontend.md` — the 12-task
   frontend plan to execute. TDD, task-by-task. Its self-review notes (bottom) matter.
4. `docs/superpowers/specs/2026-07-10-stage-2-table-system-design.md` — the Stage 2
   design (row/binding model, wire schema, cell kinds). The frontend types mirror the
   backend schemas, which now EXIST on disk — cross-check the frontend zod schemas
   against the real backend I/O schemas (see Standing Constraints).
5. `CLAUDE.md` — toolchain (pixi), architecture, gotchas. `frontend/README.md` before
   touching `frontend/src/lib/state/`.
6. Subagent-driven-development skill — re-invoke it; it defines the per-task loop
   (task-brief → dispatch implementer → review-package → dispatch reviewer → fix loop
   → ledger). The skill's helper scripts live at
   `/home/mdp/.claude/plugins/cache/claude-plugins-official/superpowers/6.1.1/skills/subagent-driven-development/scripts/`
   (`task-brief PLAN N`, `review-package BASE HEAD`, `sdd-workspace`). Run them from the
   repo root (they need git).

Confirm state before relying on anything below:
- Frontend unit: `pixi run -e frontend bash -c 'cd frontend && npm test'`
- Frontend types: `pixi run -e frontend bash -c 'cd frontend && npm run check'`
- Frontend e2e: `pixi run -e frontend bash -c 'cd frontend && npm run test:e2e'`
- Backend (should still be green): `pixi run -e core-dev pytest tests/ -q` ; `pixi run lint-core && pixi run lint-backend`
- `git status --short && git branch --show-current && git log --oneline -14`

Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `feat/stage2-tables` (off `main` @ `8383a0d`).
- HEAD: `7b7dfc7` (backend Task 11). Backend commits: `d02452b..7b7dfc7` (13 commits incl. one fix commit `0a61be9`).
- Uncommitted: **clean** (as of 2026-07-11-1503).
- Backend tests: **936 passed, 2 deselected** (the 2 = opt-in GCS-emulator integration test, expected). lint-core clean (43 files), lint-backend clean (49 files). As of 2026-07-11-1502.
- Frontend: **NOT touched this branch** — identical to `main`, which the prior handoff reported green (unit ~738-754, e2e 26/26). Re-run to establish baseline before starting (so regressions are yours).
- **Two ledger writes were interrupted** and must be re-applied to `.superpowers/sdd/progress.md`:
  1. The USER DECISION below (expand-property = keep 422).
  2. A "Frontend plan starting (base 7b7dfc7)" section header.
- Nothing NOT committed except the gitignored `.superpowers/` (ledger + task briefs/reports) and gitignored `docs/`. Not pushed to origin.

## Standing Constraints
- **Execution mode = subagent-driven** (user chose). One implementer subagent per task
  (model: **sonnet** has been the right tier for every backend task — transcription+judgment;
  keep using sonnet unless a task is trivially mechanical → haiku, or unusually subtle → opus).
  One reviewer subagent per task (sonnet). Fix loop for Critical/Important; Minors → ledger
  for final-review triage.
- **Commit trailer THIS slice** (overrides the Fable trailer earlier slices used):
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- **Frontend npm scripts MUST run via** `pixi run -e frontend bash -c 'cd frontend && <cmd>'`
  (bare `pixi run -e frontend npm test` fails "Missing script" — runs from repo root).
- **Two pre-existing prettier failures** on `npm run lint` (`ProjectCard.test.ts`,
  `UsersTab.test.ts`) — DO NOT fix, DO NOT add new ones.
- `untrack()` idiom for register/unregister-in-`$effect` cards (see `PathCard.svelte`) —
  a literal read-then-write effect infinite-loops.
- **Backend wire schemas the frontend must mirror EXACTLY** (verify against source, don't
  trust the plan's snippets — a few plan snippets had bugs that got fixed during backend impl):
  - Table definition: `src/data_rover/core/table/schema.py` (TableDefinition, RowSource,
    Column unions, RowSlot.chain_index, NavigationSource {ref|definition}).
  - Evaluate I/O: `src/data_rover/api/schemas.py` (EvaluateTableIn, TableSortIn {column,
    direction: asc|desc}, TableColumnOut, TableCellOut {element|value|values|elements},
    TableRowOut, TablePageOut {columns,rows,total,truncated,offset,model_rev}).
  - Route: `src/data_rover/api/routes/tables.py` (`POST /tables/evaluate`, `POST /tables/export`).
    Both are read-only POSTs (viewers allowed). Export returns xlsx bytes +
    `Content-Disposition` + `X-Table-Truncated` header when truncated.
  - `RowStart` sentinel `{"kind":"row"}` in `src/data_rover/core/navigation/schema.py`
    (SCHEMA_VERSION now 3). `POST /navigations/evaluate` accepts optional `row_element_id`.
  - `View.artifacts` root-level list now exists (`src/data_rover/core/view/schema.py`).
- **Cell editing reuses the Inspector path exactly** — `editLock`→`emit`→`DiffDrawer`→
  `POST /commits`. Never a new mutation surface (frontend plan Task 6). The ValueCell needs
  the property NAME to build the patch; the evaluate response's column-out does NOT carry it —
  thread the definition column's `name` from TableView→TableGrid→ValueCell (frontend plan
  Task 6 self-review note).
- **Root-artifact placement must NOT go through `findFolderByPath(view, [])`** — that virtual
  root is detached and silently drops writes. Add an explicit `path.length===0` branch
  (frontend plan Task 10).
- `apiFetchRaw` (src/lib/api/client.ts) already returns raw Response with CSRF applied — use
  it for the xlsx blob download; no client change needed.
- Locked design decisions (row identity = tuple of bindings; RowStart sentinel not the
  id-equals-scope trick; present:false is a wire flag; columns.ts is a separate pure module;
  sort empties-last both directions; server-side xlsx) — see the spec's Decisions table and the
  prior handoff. Do not relitigate.

## Done
- **Backend plan: ALL 12 tasks complete, reviewed, approved, green.** Ledger has per-task
  detail. Highlights: RowStart sentinel + evaluate binding; `core/table/{schema,evaluate,cells,resolve}.py`
  (build_rows with the load-bearing explicit `base_slots`/partial-key invariant — regression test
  `test_column_sourced_from_expand_column_binds_that_rows_element` guards it; per-cell present/editable;
  datatype-aware sort empties-last; the sort layer also correctly special-cases expand columns —
  an implementer bug-catch beyond the plan); `api/table_cache.py` LRU (rev-invalidation +
  truncated now carried through cache after a fix wave); `POST /tables/evaluate` + `/tables/export`
  (openpyxl 3.1.* added to pixi.toml, core stays xlsx-free); `View.artifacts` root field.
- Backend Task 8 had a fix wave (`0a61be9`): (1) cache-hit was dropping the `truncated` flag; (2)
  out-of-range sort.column mislabeled as "unknown artifact" 422. Both fixed + tested + re-reviewed clean.
- 12 backend + 12 frontend TaskCreate todos exist (IDs 1-12 backend all completed). Frontend
  todos NOT yet created — create IDs 13-24 when you start.

## Known Issues, Not Yet Fixed
None blocking. Accumulated **backend Minors** deferred to the final whole-branch review's fix-wave
triage (full list in the ledger's "OUTSTANDING (B)" line). The worth-fixing ones:
- `base_slots = len(keys[0]) - expand_count` derivation is duplicated **3×** (build_rows loop,
  evaluate_cells, order_rows) → factor a shared `_base_slots_from_keys(defn, keys)` helper next to
  `_expand_slot_of` in `src/data_rover/core/table/evaluate.py`.
- `src/data_rover/core/table/evaluate.py` ~line 329: `isinstance(v, list)` vs cells.py's
  `isinstance(v, (list, tuple))` — align (dead-code-safe today, no tuple-valued props exist).
- Coverage gaps: no ChainRows `build_rows` test (base_slots>1 path); no binding-column-over-budget
  sort test; no mixed-datatype `_property_is_numeric` test; rule-4 (expand-property from
  non-single source) static-validation branch untested; the Task-2 nav test uses a vacuous
  `all(chain[0]==root_id)` (add `assert chains`).
- Task-3 latent static hazard: an ElementColumn can statically source an EXPAND PropertyColumn
  (`_source_arity`=(False,True)) → binds a scalar value as an element id at eval time. Deferred;
  decide in final review whether to add a `not producing` guard or leave.
- Pre-existing repo-wide DRY (NOT this feature): `def client` fixture copy-pasted across 26
  `tests/api/*` files — candidate to hoist to conftest, but out of scope.

## Deferred — Do Not Do
- **Graceful per-row degrade for expand-property on heterogeneous scopes.** User decided
  (2026-07-11): **KEEP the plan's 422 behavior.** `expand_property_values` (core/table/cells.py)
  raising ValueError → 422 when a row's type lacks the expand property (or declares it scalar) is
  INTENDED. Do NOT add graceful degradation — it's a Stage-2.1 follow-up. **Append this decision
  to the ledger** (it was interrupted before writing).
- Stage 2 non-goals (spec's Non-goals — user-approved cuts, do NOT add back): per-datatype column
  formats (only header+width_px persist); CSV export (xlsx only); bulk cell edit / relationship
  editing from tables / editing derived columns; column virtualization; cursor pagination.
- Stage 3 (Block diagrams) — separate later stage.
- Stage 1 deferred follow-ups (prior handoff's Deferred list) — not part of Stage 2.

## Plan
1. **Re-invoke `superpowers:subagent-driven-development`** and re-read this handoff's Orient First.
2. **Append to `.superpowers/sdd/progress.md`**: (a) the USER DECISION (expand-property = keep 422,
   finding A closed); (b) a "Frontend plan starting (feat/stage2-tables, base 7b7dfc7)" section.
3. Establish the **frontend green baseline**: run frontend unit + check (+ e2e if time). Record it.
4. Create frontend todos (TaskCreate, 12 tasks mirroring the frontend plan).
5. **Execute the frontend plan task-by-task** (`docs/superpowers/plans/2026-07-10-stage-2-tables-frontend.md`),
   starting with **Task 1 (API types + client for tables)**. For each task: `task-brief <plan> N` →
   dispatch sonnet implementer (brief path + interfaces from earlier tasks + your resolution of any
   sketch cruft + report-file path) → `review-package BASE HEAD` → dispatch sonnet reviewer →
   fix-loop Critical/Important → record in ledger with commit SHA + verdict. Base for FE Task 1 = `7b7dfc7`.
   IMPORTANT for FE Task 1: verify the plan's zod snippets against the REAL backend schemas
   (schema.py / api/schemas.py) — confirm exact exported identifiers (NavigationDefinitionSchema,
   CriterionSchema, TreeItemSchema, apiFetch/apiFetchRaw/ClientConfig) before trusting the snippet.
6. After all 12 frontend tasks: run ONE **whole-branch review** (final code-reviewer on the most
   capable model, `review-package $(git merge-base main HEAD) HEAD`) covering BOTH plans; feed it the
   backend Minors list from the ledger to triage. Dispatch ONE fix subagent with the full findings list.
7. Then `superpowers:finishing-a-development-branch` (merge/PR per user).

First concrete action: re-invoke the SDD skill, append the two ledger items, run the frontend
baseline, then `task-brief docs/superpowers/plans/2026-07-10-stage-2-tables-frontend.md 1` and dispatch.

## Open Questions
- None blocking. The one open decision (expand-property behavior) was answered: keep 422.
- At branch-finish time, ask the user merge vs PR (per finishing-a-development-branch). The
  gitignored `docs/` spec+plans exist only locally — back them up if the user values them (prior
  handoff noted this).
