# Handoff: build plan 3 of sub-project C (Rules)

## Mission
Build plan 3 of 8 of sub-project C (Evaluation), **Rules**, in `/home/mdp/workspace/data-rover-py`. The plan is `docs/superpowers/plans/2026-09-25-eval-rules.md`, approved by the owner on 2026-09-25. It has 7 tasks. Run it with `/build-plan` (subagent-driven).
Done means:
- all 7 tasks are committed on `feat/eval-rules`;
- `pixi run dr-test`, `pixi run dr-tidy true`, `engine-check`, `frontend-check`, `sandbox-check` and `engine-parity-large` are green;
- e2e has no `[shadow]` lines and only the T-8/T-9 failures;
- bench numbers are reported;
- `engine-migration` is fast-forwarded, with the owner's go-ahead.

## Orient First
1. `CLAUDE.md` is the map, commands and cross-cutting rules.
2. `docs/superpowers/plans/2026-09-25-eval-rules.md` is THE plan. Read "What planning found", Decisions D1–D18, Global Constraints, Review Focus and M1–M10 before any task.
3. `docs/superpowers/specs/2026-09-24-evaluation-design.md` §3 `### Rules` (line 268) is the binding spec. The plan's decisions override it where they say so (D2, D6, D10); Task 7 edits the local spec to match.
4. `docs/superpowers/plans/2026-09-24-eval-validation-core.md` is plan 2 (built), which this plan extends: `LiveIssues`, dirty rules, the probe, the bodies.
5. Code the plan changes most:
   - `engine/src/validation/live.ts`, `bodies.ts`, `pipeline.ts`
   - `engine/src/service/service.ts:506-638`
   - `engine/src/artifacts/artifact-set.ts`
   - `engine/src/model/indexes.ts:92-97`, `:256-312`
   - `engine/src/model/model.ts:16-74`
   - `src/data_rover/core/validation/rules/`
   - `src/data_rover/api/routes/rules.py`
   - `frontend/src/lib/engine/artifacts.ts`
   - `frontend/src/lib/state/replica.svelte.ts:231-345`

Confirm state: `git status --short && git branch --show-current && git log --oneline -3`.
- Tests: `pixi run dr-test`.
- Lint: `pixi run dr-tidy true`, `pixi run engine-check`, `pixi run frontend-check`.
- e2e: stop any stale `vite preview` on :5174, then `pixi run sandbox-build`, then `pixi run frontend-test-e2e`.
- Benches: `pixi run engine-bench` (after `engine-bench-data` once), `pixi run engine-bench-browser`, `pixi run engine-parity-large`.

Treat everything below as *as-of* notes, not current fact. Verify before you rely on it.

## State
- Branch: `engine-migration` at `2cca78a`. `feat/eval-rules` does not exist yet: Task 1, Step 1 cuts it. `main` is untouched. Nothing is pushed.
- Uncommitted: clean. The plan file is under `docs/`, which is git-ignored.
- Baseline tests at `2cca78a` (as of 2026-09-25, from the plan; not re-run this session):
  - core: 2,575 passed / 34 deselected;
  - frontend: 3,008 in 283 files;
  - engine: 1,214 in 78 files;
  - sandbox: 14;
  - e2e: 63 passed, 2 failed (T-8, T-9), no `[shadow]` lines;
  - `engine-parity-large`: equal over 7,708 injected issues.
- The planning session's scratchpad traces (`trace-server-rules.md`, `trace-shell-rules.md`, `probe_*.py`) are gone. The plan's "What planning found" holds their conclusions, with `path:line` citations.
- Next free ids (grep before use): `AD-33`, `K-65`, `C-24`, `T-11`, `U-11`. The plan assigns `AD-33`, `K-65` and `K-66`.

## Standing Constraints
- **Commits are PRE-APPROVED** for this build: one commit per task on `feat/eval-rules`, subject as the plan's step says, ending with the session's `Co-Authored-By` line. The fast-forward of `engine-migration` still needs the owner's explicit go-ahead. Never push; never touch `main`.
- **Agent types:** `implementer`, `critical-implementer`, `task-reviewer`, `branch-reviewer` (once, at the end), `chores`. No `model` parameter. The plan tags each task.
- **Owner decisions (2026-09-25), with reasons:**
  - D1: the preview's model half uses the COMMITTED rules. It mirrors the server exactly, so shadow and fallbacks agree. The server's gap is K-65, a later both-sides fix.
  - D2: issue reads WAIT for a rule-set rescan. Answers stay exact, with no flicker and no blocked typing.
  - D3: origins are EXACT across a staged rule set. The ΔPop part is cached per `(rev, rulesVersion)` (M7).
  - D4: K-59, K-60 and K-61 are folded into Task 5, measured before and after.
- **Freeze (MR-3).**
  - `src/data_rover/` changes only in Task 1: `api/routes/rules.py`, `api/routes/artifacts.py`, `api/schemas.py`, `api/README.md`, plus tests.
  - `core/validation/rules/` and `api/rules.py` are frozen from Task 1.
  - A Python bug found while porting lands on both sides with a fixture (D18).
  - The Python core is the oracle: fix the engine, never a fixture.
- **Report benchmark numbers to the owner before any optimization beyond Task 5.**
- `docs/` and `benchmarks/` are git-ignored: never `git add -f`.
- **Carried from plan 2, still true:**
  - strict enforcement and `validation_error_count` stay server-side until F;
  - the artifact and view half of the preview stays a server call;
  - a staged `metamodel.rebind` sends the whole preview to the server.
- **Reviewer findings folded into the plan, not to be undone:**
  - K-60's `rekey` must refresh the cached text before its early return (`indexes.ts:307-312`).
  - K-61 fills `committedOf` from the store in `revalidate` for transitions only. The plan justifies this without claiming the incremental set contains the exact S (not literally true).
  - M2's reader follows pydantic's null handling. Only property tests use key presence.
  - A waiting read re-checks `settled` in its answering transition.
  - The compile memo includes the metamodel identity.
  - A rules artifact arriving with no parse means `unreadable`, answered 501.
  - The FALLBACKS change happens in Task 4, not Task 6.

## Done
- Plan 3 is researched, probe-verified, reviewed adversarially (15 findings, all folded in) and approved.
- Plans 1 and 2 of C are built and merged into `engine-migration`.

## Known Issues, Not Yet Fixed
- C-23: one containment-cycle representative (server's full run) versus per-element reporting (scoped runs).
- K-58: `change_request_dirty_ids` is dead outside tests.
- K-62: a server refetch can overwrite the engine answer.
- K-63: past the 5,000 cap, staged issues are not listed.
- K-64: `migration/legacy.py:332` `_coerce_scalar` fails on a dict or nested-list float.
- T-10: the failure and race paths are untested.
- U-9: the DiffDrawer `Dialog.Content` grid has no `min-width: 0`.
- Test noise: `frontend/src/lib/state/__tests__/replica.svelte.test.ts` logs MSW unhandled `GET /model/issues` in about three tests.
- R-3 is duplicated across `BACKLOG.md` and `BACKLOG-ENGINE.md`.
- K-49…K-57 remain open.
- K-59's backlog text cites `frontend/bench/main.ts:521-532`. The loops are at about `:150-156` now; the plan notes this.

## Deferred — Do Not Do
- U-9's CSS fix: the owner left it recorded, not scheduled (2026-09-25).
- A full-run (`Scope.all`) port: deferred to plan 7 by plan 2 (D1).
- Driving the open journey's progress bar from the engine sweep: rejected in plan 2 (D12).
- K-50 and K-51: not folded into plan 2 or plan 3.
- The Python half of K-60 (the server's uniqueness re-keying): the plan fixes the engine only (D15), since `core/model` is frozen.
- `artifacts_version`: deferred to plan 4 (D10). Plan 3 keys the probe cache on `rulesVersion`.
- A `rules_status.applying` field: dropped in favour of waiting reads (D2).
- The JWT-leeway flake fix: the owner's separate call, on its own branch.

## Plan
1. Run `/build-plan` on `docs/superpowers/plans/2026-09-25-eval-rules.md`. Task 1, Step 1: `git switch engine-migration && git switch -c feat/eval-rules`. Then Task 1 (`critical-implementer`): `POST /rules/parse`, `rules_document()`, the `rules` field on the payloads route, Python tests. After each task, run `task-reviewer`, then commit (pre-approved).
2. Tasks 2–6 in order (all `critical-implementer`); Task 7 (`implementer`).
   - Task 4 Step 5: report `engine-bench` rows and the parity result.
   - Task 5 Steps 1 and 6: report before and after numbers.
   - Task 7 Step 2: report the browser bench.
3. Run `branch-reviewer` once over `feat/eval-rules`.
4. Report to the owner: test counts, benches, parity, any D18 fires. Ask for the go-ahead to fast-forward `engine-migration`.
5. Write the next handoff: plan 4 (tables), which inherits `artifacts_version` and `ruleSources`' layering.

## Open Questions
- The fast-forward of `engine-migration` to `feat/eval-rules`: ask the owner when Task 7 is green.
