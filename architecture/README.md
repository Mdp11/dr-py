# Architecture — source of truth

**Status:** approved 2026-09-18 · sub-project A built ([program.md](program.md)).

These documents define the TARGET architecture of Data Rover — computation in the browser
against a full model replica, a thin server, an on-demand headless host — and the rules for
getting there. They are written for humans and AI agents alike: short, normative,
single-sourced.

| Question | Document | IDs |
|---|---|---|
| What runs where; how data flows | [system.md](system.md) | — |
| Why it is this way; what was rejected | [decisions.md](decisions.md) | `AD-n` |
| Formats and interfaces shared across sub-projects | [contracts.md](contracts.md) | `CT-n` |
| Scale, budgets, cost, platform and security limits | [constraints.md](constraints.md) | `CN-n` |
| What is built in which order; migration rules; status | [program.md](program.md) | `MR-n` |
| How to work in this repository | [conventions.md](conventions.md) | `RC-n` |

## Authority

1. **Where the system is going** — these documents win.
2. **What the code does today** — the code and the co-located READMEs that
   `CLAUDE.md` maps win.
3. A sub-project spec or plan (under `docs/superpowers/`) refines
   these documents and MUST NOT contradict them. If it has to, change the document here
   first, in the same change.

## Editing rules

- One fact, one home. Link to it; never restate it in a second document.
- MUST / MUST NOT / SHOULD carry their RFC 2119 meaning. Everything else is description.
- Every number says what it is: *(measured)*, *(estimate)* or *(budget)*.
- IDs are stable. A reversed decision is marked `superseded by AD-n` and cut to one line;
  contracts and constraints are edited in place — git history owns the past.
- When a sub-project lands, update the status in `program.md` and move what is now current
  behaviour into the co-located README, in the same change.
- Cite IDs in specs, plans, commits and reviews (`per CT-2`, `violates CN-17`).
- The two-letter prefixes are deliberate: `BACKLOG.md` and `BACKLOG-ENGINE.md` own the one-letter ids.

## Glossary

| Term | Meaning |
|---|---|
| **App origin** | The origin serving the UI; holds the login cookie; the only browser context that talks to the server. |
| **Shell** | The app-origin code that fetches snapshots, tails and feed events and forwards them to the engine. |
| **Sandbox origin** | A separate registrable domain serving static files only, under a strict CSP; hosts the engine worker and the script workers. |
| **Engine** | The TypeScript package that holds the replica and performs every evaluation. One codebase, two hosts (browser worker, Node). |
| **Replica** | The engine's full in-memory copy of one project's committed model at a known `rev`. |
| **Staged op** | An uncommitted edit, in the op shapes of `src/data_rover/api/schemas.py`. |
| **Working copy** | The replica with the staged ops applied; what every evaluation reads by default. |
| **Delta** | The state change of one commit: full post-commit entities plus deleted ids (CT-2). |
| **Tail** | The ordered deltas from a given `rev` to head. |
| **State digest** | An order-independent hash over all `(id, rev)` pairs (CT-3). |
| **Head rows** | Postgres rows holding the current state of every entity; the thin server's only model storage. |
| **Partial model** | The subset of head rows a commit needs, loaded into the Python `Model` class for the commit check. |
| **Headless host** | A Node service running the same engine and Pyodide for CI exports. |
| **Oracle** | The existing Python core, whose observable behaviour the engine must reproduce. |
| **Golden fixture** | Inputs plus expected outputs, generated by the oracle, consumed by engine tests. |
| **Surface** | One group of `frontend/src/lib/api` functions that migrates from server to engine as a unit. |
