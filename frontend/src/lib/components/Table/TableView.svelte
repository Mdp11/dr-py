<script lang="ts">
	// Minimal placeholder — Task 5 replaces this with the real table editor
	// (TableView + TableGrid + Cell components). Kept intentionally thin so
	// Task 4 can wire the workspace tab dispatch without depending on the
	// full grid implementation.
	let { tabId }: { tabId: string } = $props();
</script>

<div class="p-4 text-xs text-muted-foreground">Table view stub for {tabId} (Task 5 TODO)</div>
